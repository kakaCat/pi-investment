---
req_id: "{{REQ_ID}}"
title: "{{TITLE}}"
status: brainstorming
owner: "{{WINDOW}}"
category: feature
# sides: frontend,backend        ← 声明后 design/frontend.md、design/backend.md 转为必交
# design_exempt: 文件名=理由      ← 设计文档豁免（空理由 / 未知文件名 = 豁免无效）
---

# 需求说明（{{REQ_ID}}）

> 状态：需求分析（brainstorming） · 窗口 {{WINDOW}} · 立据：{{DATE}}
> 宣布路径：Architectural / Bounded / Spike（三选一，写一句分类理由——人可推翻）

## 背景与动机

（为什么现在要做：用户原话用 > 引用块 / 线上现象 / 数据证据。标注来源与时点。
相关文档/资料链接列在此处；术语非自明时附一行小表或指向术语表。）

## 目标

（每条可证伪。表格化：目标 / 价值 / 衡量指标并排，一眼对齐。）

| 编号 | 目标 | 价值（解决什么问题/对谁的价值） | 衡量指标 | 目标值 |
|---|---|---|---|---|
| G1 |  |  |  |  |
| G2 |  |  |  |  |

## 非目标

- N1 （明确排除项，防范围蔓延）

## 产品定义

（这是什么产品能力、不是什么。边界关系复杂时配一张 **ASCII 范围边界图**
（对应专业 PRD 的 Scope Boundary Diagram）：）

   ┌─────── 本次范围内 ───────┐   ┌─── 范围外 ───┐
   │  组件A    组件B          │   │  组件C       │
   └──────────────────────────┘   └──────────────┘

## 用户与角色（用户分析）

（表格化分析目标用户的特点与痛点——有助于设计与开发理解"为谁做"。）

| 角色 | 特点 | 痛点 | 核心诉求 |
|---|---|---|---|
|  |  |  |  |

## 核心场景

（描述各核心场景下用户的**完整行为路径**——场景层在功能点之前：
先有人怎么用，再拆出什么功能。每个场景用句式：
**（用户 who）在（where、when）的情况下，为了（目标），做了（和某些界面交互），实现了（价值/任务）。**）

- 场景一：（who）在（where/when）下，为了（目标），（行为路径），实现（价值）
- 场景二：…

（场景多或跨阶段时，补一张**用户旅程表**做全景：）

| 阶段 | 用户触点 | 用户行为 | 痛点/情绪 | 机会点（功能转化） |
|---|---|---|---|---|
| 1. |  |  |  |  |
| 2. |  |  |  |  |

## 业务流程

（**可选，When Needed**——需求涉及多环节/多角色协作时写。改动位置讲"点"，
本节讲"面"：业务全流程。泳道用角色×步骤表表达，零渲染依赖；
结构复杂时配 ASCII 字符画（产品架构图同理）。）

| 步骤 | 角色A | 角色B | 系统 |
|---|---|---|---|
| 1 |  |  |  |
| 2 |  |  |  |

## 改动位置

（说清本次改的是流程/链路中的哪个位置（改动环节用【】标出）。
**默认用文字坐标，不强制画图**：

① 链路箭头一行（首选）：
   `入口 → 处理 → 【改动：xxx】 → 出口`

② 环节表（链路复杂、涉及多环节时）：

| 环节 | 本次是否改动 | 说明 |
|---|---|---|
| 上游：… | 否 | |
| 【…】 | 是 | 改什么 |
| 下游：… | 否 | |

③ ASCII 字符画（需要二维布局/结构对照时——零渲染依赖，任何环境可读；
   遵循 svgbob 风格，方框+箭头，可被 svgbob 转成 SVG）：

   +--------+      +------------+
   | 上游   | -->  | 【改动点】 | --> 下游
   +--------+      +------------+

④ mermaid（需要对外发布正式图时可选，依赖渲染器，不必须）。

没有流程语义的改动（改配置/常量/文案）直接写「改动点：文件:行 / 配置项名」，
不硬凑链路图。）

## 改动对比

| 项 | 现状（改前） | 改动后 | 说明 |
|---|---|---|---|
| （流程/接口/字段/行为逐项列出） |  |  |  |

## 功能点

（**功能点清单表**——先列骨架不写细节；逐条编号 FR-x，下游任务卡 requirement_refs
引用它，无人接的条款看板标红。）

| 编号 | 功能点 | 用例角色 | 描述 | 优先级 | 配图 | 备注 |
|---|---|---|---|---|---|---|
| FR-1 |  |  |  | P0 | （原型 prototypes/xxx.html#FR-1 或截图路径，无界面可空） | 新增 |
| FR-2 |  |  |  | P1 |  | 新增 |

（功能点多时按模块分组列多张表：### 模块一 ……；**全局功能**（跨模块共享的，
如登录/权限/通知）在备注列标「全局」并写清共享要求。交互定稿后配图换成正式交互稿路径。）

## 功能点明细

（每个 P0 功能点一组「交互流程 + 规则 + 异常处理」；流程用文字步骤表达即可，
需要图形时才附 mermaid；P1/P2 可只写规则。）

### FR-1 （功能点名）

- 用例角色：（谁来操作这个功能——对应「用户与角色」表里的角色）
- 前置条件：（使用此功能必须满足的条件：登录态/数据准备/权限…）
- 交互流程：1. 触发条件 → 2. 系统动作 → 3. 反馈（文字步骤；分支用文字写明"若…则…"）
- 后置条件：（成功后系统/数据处于什么状态——测试用例的断言锚点）
- 规则与反馈：
- 异常处理：（错误提示 / 无网络 / 边界值——不许只写成功路径）
- 注意事项：（特别约束：性能上限/并发/兼容性…无可空）

## 数据指标（埋点/观测）

（**可选，When Needed**——有数据方面的需求时提前梳理，说明要关注的指标；
与 design 阶段的 observability 设计衔接。）

| 编号 | 模块 | 指标名称 | 事件 | 指标描述 | 现状（如有） | 目标（如有） |
|---|---|---|---|---|---|---|
|  |  |  |  |  |  |  |

## 非功能需求

（**When Needed**——质量属性要求，逐类写或标"无特殊要求"；
验收标准里可执行的条目引用这里的指标值。）

- 性能：（响应时间 / 吞吐 / 并发——给目标值）
- 兼容：（浏览器/版本/存量数据/旧调用方兼容）
- 安全：（密钥/权限/数据边界——投资系统尤其：账户操作边界、凭据不落明文）
- UI 设计：（有界面时写要求，细节归 design/frontend.md 与原型页）
- 其他：（运营/合规/审计留痕…）

## 风险评估

（预估本需求的风险与规避策略——规避策略五选一：减轻 / 预防 / 转移 / 回避 / 接受。）

| 编号 | 风险名称 | 场景描述 | 风险级别 | 如何规避 |
|---|---|---|---|---|
| R-1 |  |  | 高/中/低 |  |

## 边界（不做什么）

（**门禁必填节**：六类型共同。确实不适用某类文档/某条规则时，在此写明理由——
这是正规豁免出口，不要靠删节规避。）

## 验收标准

- [ ] （跑什么命令 / 发起什么请求 / 看到什么算过——可执行，禁"功能正常"空话）

## 迭代计划

（一句话指向：分期交付计划在 decomposition.md（拆分计划）展开；
本节只写"本期做什么、后续期做什么"的意图，不写任务表。
多期时可用 ASCII 时间线：）

   本期 ────────► 下一期 ────────► 后续
  （FR-1~3）     （FR-4~6）      （待定）

## 修订记录

| 日期 | 修改内容 | 修改人 |
|---|---|---|
| {{DATE}} | 初稿 | {{WINDOW}} |
