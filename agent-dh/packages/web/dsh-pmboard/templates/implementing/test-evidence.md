# 测试证据（{{REQ_ID}} · {{DATE}}）

## 环境

（分支 / commit / 依赖版本——没有环境的「跑通了」不可复现。）

## 跑了什么

```
（命令原文）
```

## 结果摘要

（如 `pnpm vitest run → 178 passed / 14 files`——贴关键输出，禁"全部通过"空话）

## 覆盖与对照

（TC-x 是 test-cases.md 用例表的编号——本表同时是用例执行状态的回填入口。）

| 验收标准 | 用例（TC-x） | 对应测试 | 结果 |
|---|---|---|---|
|  | TC-1 |  | ✅/❌ |

## 失败与未跑项

| 项 | 状态 | 处置 |
|---|---|---|
| （失败的用例 / 没跑的部分） | 失败 / 未跑 | （怎么修的 / 为什么可以不跑——不许静默跳过） |

## 故障注入

（门禁/降级路径的故障注入实测记录——没有故障注入的门禁等于没测）
