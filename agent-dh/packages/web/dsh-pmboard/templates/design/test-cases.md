---
requirement_refs: [FR-1]
---

# 测试用例（{{REQ_ID}}）

> **什么时候不交本份**：只改文档/文案/注释，无任何可执行行为变化。
> 测试的执行证据（命令+输出）在实施节点写 test-evidence.md，本文档只写「要测什么」。
> **用例从设计文档推导**：每条用例的「被测对象」必须引用设计文档编号——
> backend（S-x）/ frontend（P-x/C-x）/ interfaces（I-x）/ data-model（T-x）/ use-cases（UC-x）/ migration（M-x）。
> 先读设计再写用例，没有设计落点的用例 = 测空气；设计里没有的编号 = 超范围（回拆分计划对账）。

## 覆盖矩阵 <!-- serves: FR-1 -->

（每条功能点 × 测试类型：哪些层要测，空格 = 该层不测，写理由。）

| 功能点 | 单元 | 集成 | 故障注入 | 回归 |
|---|---|---|---|---|
| FR-1 | ✅ | ✅ | ✅ |  |

## 用例表 <!-- serves: FR-1 -->

| 用例 | 类型 | 被测对象 | 前置条件 | 步骤 | 预期结果 | serves |
|---|---|---|---|---|---|---|
| TC-1 | 正向 | S-1 |  |  |  | FR-1 |
| TC-2 | 反向/边界 | I-1 |  |  |  | FR-1 |

（类型口径：正向=正常路径；反向=非法输入/异常触发；边界=极值/空值/并发临界。
三类都要有——只写正向用例等于只测成功路径。）

## 用例明细 <!-- serves: FR-1 -->

（表格装不下的用例逐个展开：测试数据、断言点。简单用例不必展开，表格一行就够。）

### TC-x 用例名

- **测试数据**：（具体输入值/构造的 fixture——不写"一些数据"，写可复现的具体值）
- **步骤**：1. … 2. … 3. …
- **断言点**：（每条断言 = 一个可判真假的句子：返回码 = 200 / 文件内容逐字节不变 /
  日志含 "template.landed"——"结果正确"不是断言）
- **清理**：（测试产生的副作用怎么还原）

## 故障注入 <!-- serves: FR-1 -->

（门禁/安全默认值/降级路径必须故障注入实测——只测成功路径等于没测。）

| 注入点（挂 S-x/I-x 编号） | 注入方式 | 预期行为 | 实测结果（实施时回填） |
|---|---|---|---|
|  |  |  |  |
