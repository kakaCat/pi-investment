# 数据模型设计（{{REQ_ID}}）

> 每个数据结构必须标注 `serves: FR-x`（缺标注 = 孤儿结构被门禁拦）。
> **什么时候不交本份**：没有新增/修改数据结构（纯逻辑改动）——删本文档。

## 新增/修改的数据结构 `serves: FR-1`

### {{MODEL_NAME}} `serves: FR-1, FR-2`

**用途**：（这个数据结构代表什么领域对象）

**定义**：
```typescript
interface ModelName {
  id: string           // 主键，格式说明
  field: string        // 字段说明
  createdAt: number    // 创建时间（毫秒时间戳）
  updatedAt?: number   // 更新时间（可选）
}
```

**字段说明**：

| 字段 | 类型 | 必填 | 说明 | 约束 |
|---|---|---|---|---|
|  |  |  |  |  |

**索引设计**：

| 索引名 | 字段 | 类型 | 原因 |
|---|---|---|---|
|  |  | 主键/唯一/普通 |  |

**关联关系**：

| 关联到 | 类型 | 外键 | 说明 |
|---|---|---|---|
|  | 1:1 / 1:N / N:M |  |  |
