---
requirement_refs: [FR-1]
---

# 前端设计（{{REQ_ID}}）

> 条件必交：requirement.md front-matter sides 含 frontend 时要求本份；不含则不交。

## 原型页面 <!-- serves: FR-1 -->

（可视化原型：`prototypes/<name>.html`（由 prototype.html 模板生成，每功能点一个锚点区块）；
截图贴本目录。原型讲结构与交互路径，不求视觉精细；定稿后换正式交互稿。）

## 目录与包结构 <!-- serves: FR-1 -->

（文件落在哪个包/目录：新增文件的路径、目录划分理由——
组件树讲逻辑结构，本节讲物理落点。）

```
packages/web/<pkg>/src/
├── components/        （本需求新增：XxxPanel.vue）
├── views/             （既有，不改）
└── api/               （本需求改动：yyy.ts）
```

| 路径 | 内容 | 新增/改动 | 落此处的理由 |
|---|---|---|---|
|  |  |  |  |

## 组件结构 <!-- serves: FR-1 -->

（ASCII 组件树：页面 → 区块 → 组件，标出新增/改动。）

```
页面 PageX
├── 区块A（改动）
│   ├── 组件A1（新增）
│   └── 组件A2
└── 区块B
```

## 页面与组件 <!-- serves: FR-1 -->

（编号口径：**P-x = 页面，C-x = 组件**——拆分计划的覆盖对照按它引用。）

| 编号 | 类型 | 名称 | 职责 | 数据来源 | 新增/改动 |
|---|---|---|---|---|---|
| P-1 | 页面 |  |  |  |  |
| C-1 | 组件 |  |  |  |  |

## 状态与数据流 <!-- serves: FR-1 -->

（数据从哪来、经过哪些状态、到哪去；响应式链路画清楚。）

## 样式与风格 <!-- serves: FR-1 -->

（**优先复用既有设计体系**（组件库/主题 token/CSS 变量），新增样式必须说明理由——
不另立风格。）

| 项 | 取值 | 来源/理由 |
|---|---|---|
| 设计体系 | （遵循哪个组件库/设计规范，如 Element Plus / 宿主 token） |  |
| 布局 | （栅格/间距/对齐口径；关键区块尺寸） |  |
| 色彩与字体 | （用哪些既有 token；新增色值须给理由） |  |
| 响应式 | （断点策略 / 是否适配窄屏，不需要就写不适配+理由） |  |
| 暗黑/主题 | （是否跟随宿主主题切换） |  |
| 样式落点 | （写在哪个文件：scoped style / 公共 css / 内联原型——新增 css 文件须给理由） |  |

（纯内部工具页/原型页可整节从简：一行"沿用宿主默认样式，无新增视觉设计"即可。）

## 交互与异常态 <!-- serves: FR-1 -->

（四态矩阵——不许只画成功路径。）

| 状态 | UI 表现 | 用户可做什么 | 触发条件 |
|---|---|---|---|
| 成功 |  |  |  |
| 加载中 |  |  |  |
| 空态 |  |  |  |
| 报错态 |  |  |  |
