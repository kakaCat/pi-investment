---
requirement_refs: [FR-1]
---

# 接口设计（{{REQ_ID}}）

> **什么时候不交本份**：无新增/变更接口（纯内部实现调整、纯前端展示）。
> 改存量接口必须写**兼容性**（旧调用方怎么办）。

## 接口清单 <!-- serves: FR-1 -->

（编号 **I-x**——拆分计划的覆盖对照按它引用；改本表后回查 decomposition.md 覆盖对照。）

| 编号 | 接口 | 输入 | 输出 | 兼容性 | 错误语义 | serves |
|---|---|---|---|---|---|---|
| I-1 | （谁调用） | （给什么） | （回什么） | （新增/兼容/破坏性） | （失败时怎样） | FR-1 |

## 调用时序 <!-- serves: FR-1 -->

（正常流 + 异常流都画——ASCII 时序图，失败分支不省略。）

```
调用方              服务端
  │  req(params)      │
  │ ────────────────► │ 校验 → 处理
  │ ◄──────────────── │ 200 { result }
  │                   │
  │  req(非法参数)     │
  │ ────────────────► │ 校验失败
  │ ◄──────────────── │ 4xx { error, hint }
```

## 错误语义 <!-- serves: FR-1 -->

| 错误码/情形 | 含义 | 调用方处置建议 |
|---|---|---|
|  |  |  |
