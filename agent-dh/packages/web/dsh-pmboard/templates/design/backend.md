---
requirement_refs: [FR-1]
---

# 后端设计（{{REQ_ID}}）

> 条件必交：requirement.md front-matter sides 含 backend 时要求本份；不含则不交。

## 目录与包结构 <!-- serves: FR-1 -->

（文件落在哪个包/模块：新增文件的路径、分层归属（domain/application/infrastructure），
新文件不随手放——落点即架构声明。）

```
packages/tools/<pkg>/src/
├── application/       （本需求新增：xxx-service.ts）
├── domain/            （本需求改动：yyy.ts）
└── infrastructure/    （既有，不改）
```

| 路径 | 内容 | 新增/改动 | 分层归属与理由 |
|---|---|---|---|
|  |  |  |  |

## 服务与接口实现 <!-- serves: FR-1 -->

（编号 **S-x**——拆分计划的覆盖对照按它引用；构建期脚本/纯工具不进本表，写进目录与包结构即可。）

| 编号 | 服务/模块 | 职责 | 关键逻辑 | 改动文件 | serves |
|---|---|---|---|---|---|
| S-1 |  |  |  |  | FR-1 |

## 处理时序 <!-- serves: FR-1 -->

（ASCII 时序图：入口 → 校验 → 处理 → 存储/下游，标出失败与降级分支。）

```
入口        校验          处理           存储/下游
 │ 请求      │             │               │
 │ ────────► │ 失败→4xx    │               │
 │           │ ──────────► │ 失败→重试/降级 │
 │           │             │ ────────────► │
```

## 数据访问 <!-- serves: FR-1 -->

（读写哪张表/哪个外部源；事务边界；并发冲突怎么解。）

## 任务与调度 <!-- serves: FR-1 --> （When Needed）

（有定时/异步任务才填。）

| 任务 | 触发（cron/事件） | 幂等性 | 失败重试策略 |
|---|---|---|---|
|  |  |  |  |
