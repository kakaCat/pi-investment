---
req_id: "{{REQ_ID}}"
title: "{{TITLE}}"
status: brainstorming
owner: "{{WINDOW}}"
category: bug
---

# 需求说明（{{REQ_ID}}）

> 状态：需求分析（brainstorming） · 窗口 {{WINDOW}} · 立据：{{DATE}}
> 宣布路径：Architectural / Bounded / Spike（三选一，写一句分类理由——人可推翻）

## 背景与动机

（为什么现在要做：用户原话用 > 引用块 / 线上现象 / 数据证据。标注来源与时点。
相关文档/资料链接列在此处；术语非自明时附一行小表或指向术语表。）

## 目标

（每条可证伪。表格化：目标 / 价值 / 衡量指标并排，一眼对齐。）

| 编号 | 目标 | 价值（解决什么问题/对谁的价值） | 衡量指标 | 目标值 |
|---|---|---|---|---|
| G1 |  |  |  |  |
| G2 |  |  |  |  |

## 非目标

- N1 （明确排除项，防范围蔓延）

## 边界（不做什么）

（**门禁必填节**：六类型共同。确实不适用某类文档/某条规则时，在此写明理由——
这是正规豁免出口，不要靠删节规避。）

## 验收标准

- [ ] （跑什么命令 / 发起什么请求 / 看到什么算过——可执行，禁"功能正常"空话）

## 改动位置

（说清本次缺陷位于流程/链路中的哪个位置（缺陷所在环节用【】标出）。
**默认用文字坐标，不强制画图**：

① 链路箭头一行（首选）：
   `入口 → 处理 → 【改动：xxx】 → 出口`

② 环节表（链路复杂、涉及多环节时）：

| 环节 | 本次是否改动 | 说明 |
|---|---|---|
| 上游：… | 否 | |
| 【…】 | 是 | 改什么 |
| 下游：… | 否 | |

③ ASCII 字符画（需要二维布局/结构对照时——零渲染依赖，任何环境可读；
   遵循 svgbob 风格，方框+箭头，可被 svgbob 转成 SVG）：

   +--------+      +------------+
   | 上游   | -->  | 【改动点】 | --> 下游
   +--------+      +------------+

④ mermaid（需要对外发布正式图时可选，依赖渲染器，不必须）。

没有流程语义的改动（改配置/常量/文案）直接写「改动点：文件:行 / 配置项名」，
不硬凑链路图。）

## 改动对比

| 项 | 修复前（缺陷行为） | 修复后（预期行为） | 说明 |
|---|---|---|---|
|  |  |  |  |

## 复现步骤

（环境 / 输入 / 预期 vs 实际；最小重现。先确认是缺陷而不是需求变更。条款前缀 BUG-x。

**证据附件**：日志摘录 / 截图 / 告警链接 / 数据时点——缺陷必须有立据，
不凭记忆与转述报 bug。）

## 根因

（代码级根因：文件:行 / 机制——不是"疏忽"。本节点只定位与划界，不写修复代码。）

## 回归

（将来怎么证明修好了：回归测试必须覆盖这条复现路径。禁止顺手重构。）

## 修订记录

| 日期 | 修改内容 | 修改人 |
|---|---|---|
| {{DATE}} | 初稿 | {{WINDOW}} |
