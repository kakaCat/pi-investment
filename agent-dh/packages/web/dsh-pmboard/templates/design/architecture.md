---
requirement_refs: [FR-1]   # 文档级映射：本设计服务哪些功能点
---

# 架构设计（{{REQ_ID}}）

> 每章标题行必须带 serves 标注（缺标注 = 孤儿章节被门禁拦）。

## 组件结构 <!-- serves: FR-1 -->

（涉及哪些组件/模块，各自职责；附改动文件清单）

## 数据流 <!-- serves: FR-1 -->

（输入 → 处理 → 输出；关键时序）

## 错误处理 <!-- serves: FR-1 -->

（失败路径：降级 / 报错 / 留痕——只测成功路径等于没测）

## 部署与依赖 <!-- serves: FR-1 -->

（新增依赖、配置项、端口；回滚路径）
