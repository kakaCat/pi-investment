---
requirement_refs: [FR-1]   # 文档级映射：本设计服务哪些功能点
---

# 架构设计（{{REQ_ID}}）

> 每章标题行必须带 serves 标注（缺标注 = 孤儿章节被门禁拦）。
> **什么时候不交本份**：单文件小改（无新组件/新依赖/新接口，改动 <50 行）——
> 把「TL;DR + 改动点」并进 requirement.md 的改动位置节即可，不另开本文档。
> 目标与非目标已在 requirement.md 写过，本文档不复述，只引用。

## TL;DR <!-- serves: FR-1 -->

（3 句以内：做什么、怎么做、为什么这么做——评审者 20 秒读完）

## 设计总览 <!-- serves: FR-1 -->

（组件结构：先 ASCII 图后职责表——图讲关系，表讲职责与改动文件。）

```
        ┌──────────┐   调用   ┌──────────┐
        │  组件A   │ ───────► │  组件B   │
        └──────────┘          └────┬─────┘
                                   │ 读写
                              ┌────▼─────┐
                              │ 存储/外部 │
                              └──────────┘
```

| 组件 | 职责（一句话） | 改动文件 |
|---|---|---|
| 组件A |  | path/to/file.ts |

## 数据流 <!-- serves: FR-1 -->

（输入 → 处理 → 输出；关键时序用 ASCII 时序图，**失败分支必须画出**。）

```
调用方        组件A            组件B
  │  请求      │                │
  │ ─────────► │ 校验/处理      │
  │            │ ─────────────► │
  │            │ ◄───────────── │ 结果/错误
  │ ◄───────── │ 失败→降级+留痕 │
```

## 方案对比 <!-- serves: FR-1 -->

（至少 2 个真实备选——只写一个方案 = 没探索过设计空间。
「被否方案 + 否定理由」比「选定方案」更有长期价值：后来人据此不重复踩坑。）

| 方案 | 思路 | 优点 | 缺点 | 结论 |
|---|---|---|---|---|
| A |  |  |  | ✅ 选定 |
| B |  |  |  | ❌ 否定：（理由） |

## 研发规范 <!-- serves: FR-1 -->

（本次涉及的规范：遵循哪些既有规范、新增哪些约定——只写与本次改动相关的，
全项目通用规范给链接不复述。）

| 类别 | 规范/约定 | 来源 | 本次如何遵守 |
|---|---|---|---|
| 代码风格 | （lint/格式化规则） | （链接或文件路径） |  |
| 命名 | （文件/函数/变量命名口径） |  |  |
| 测试 | （单测/冒烟/故障注入要求） |  |  |
| 留痕 | （日志/审计/decision_audit 约定） |  |  |
| 新增约定 | （本次新立的规矩，如"骨架只写需求目录内"） | 本设计 |  |

## 设计模式 <!-- serves: FR-1 --> （When Needed）

（真实用到才写——强行套模式是反模式。没用新模式就如实写"未引入"，
并说明复用了哪个既有模式/惯例。）

| 模式 | 用在哪个组件 | 解决什么问题 | 不用会怎样 |
|---|---|---|---|
|  |  |  |  |

## 错误处理 <!-- serves: FR-1 -->

（失败路径逐条列出：降级 / 报错 / 留痕——只测成功路径等于没测。）

| 失败点 | 检测方式 | 处置（降级/报错） | 留痕（日志/审计） |
|---|---|---|---|
| 组件B 超时 |  |  |  |

## 非功能考量 <!-- serves: FR-1 --> （When Needed）

（性能/兼容/安全/可观测——只写有实际约束的维度，没有就删本节。）

## 部署与依赖 <!-- serves: FR-1 -->

| 新增依赖/配置/端口 | 用途 | 回滚影响 |
|---|---|---|
|  |  |  |

（回滚路径：配置开关？版本回退？写清怎么回到旧状态。）

## 开放问题 <!-- serves: FR-1 --> （When Needed）

（已知未知项，显式请求决策——没有就删本节。）

| 编号 | 问题 | 影响 | 需要谁决策 |
|---|---|---|---|
| OQ-1 |  |  |  |

## 上线与回滚 <!-- serves: FR-1 --> （When Needed）

（跨服务/有灰度需求时必填；单进程插件小改可删本节。）

| 阶段 | 范围 | 验证方式 | 回滚触发条件 |
|---|---|---|---|
| 1 |  |  |  |

## 修订记录

| 版本 | 日期 | 作者 | 变更说明 |
|---|---|---|---|
| v1 | {{DATE}} | {{WINDOW}} | 初稿 |
