# 类型档 · refactor（需求分析）

- [ ] 先盘**依赖与调用方**：列出谁会受影响、对外可观察行为有哪些。
- [ ] 写明不变量：哪些行为必须**行为等价**（输入输出 / 错误 / 副作用 / 时序）。
- [ ] 边界：**一次只改一类东西**（不同时改接口与实现、不同时动多处结构）。
- [ ] 本节点不修 bug、不加功能——顺手发现的问题另立项。

## 条款定义行（G2 门禁可解析，FR-10）

需求文档里的每条功能点/条款必须写成 G2 clause 门禁认得的定义行，否则解析不到、条款接收状态全红：

- [ ] 行首 `**RF-N: 名称**`（可带 `- `/`* ` 列表符）；编号用 RF-1、RF-2…，不要用与批准闸门撞名的 G 系列编号。
- [ ] 根文档必填节（G2 missingCategoryDocs 逐节核对，缺一即拦）：边界、现状、目标结构、行为不变式。

示范（照此独立成行写，门禁即认得；注意是列表项、不是 checklist 复选框）：

- **RF-1: 一句话写清这条做什么**
