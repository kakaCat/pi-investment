# 任务页UI改进 - 测试计划

## 已完成的实施

### 1. 样式增强 ✅
- 添加统计卡片样式（~1.5KB）
- 添加工具栏和筛选器样式（~1.2KB）
- 添加视图切换器样式（~0.8KB）
- 添加任务组增强布局（~2KB）
- 添加进度条可视化样式（~0.8KB）
- 添加响应式设计（~0.5KB）

### 2. 视图渲染 ✅
- 添加 `calculateTaskStats()` - 统计计算
- 添加 `renderTaskStats()` - 统计卡片渲染
- 添加 `renderToolbar()` - 工具栏渲染
- 添加 `renderTaskGroupEnhanced()` - 增强任务组渲染
- 更新 `buildTasksPage()` - 使用增强组件

### 3. 交互逻辑 ✅
- 添加筛选状态变量
- 扩展 `onChange()` - 处理筛选器变化
- 添加 `filter-status` - 状态筛选
- 添加 `filter-blocked` - 阻塞筛选
- 添加 `toggle-section` - 章节折叠
- 添加 `switch-tasks-view` - 视图切换

### 4. 构建验证 ✅
- 客户端构建成功
- 生成 lib/client.js (123KB)
- 无编译错误

## 测试步骤

### 1. 重启 Agent OS
```bash
cd ~/.dsh/profiles/investment
./start.sh 13080
# 或者
cd /Users/yunpeng/pi-investment/agent-dh
pnpm run restart
```

### 2. 访问任务页
1. 打开 http://127.0.0.1:13080
2. 点击左下角的「项目看板」按钮
3. 在看板中点击「任务」链接

### 3. 功能验证清单

#### 统计卡片
- [ ] 显示 6 个统计卡片（全部/进行中/测试/评审/完成/阻塞）
- [ ] 数字显示正确
- [ ] Hover 效果正常（卡片上浮、边框变色）
- [ ] 点击卡片可快速筛选对应状态

#### 筛选工具栏
- [ ] 显示 4 个筛选下拉框（状态/阶段/端侧/需求）
- [ ] 显示排序下拉框
- [ ] 显示 3 个视图切换按钮
- [ ] 筛选器可以正常选择
- [ ] 选择后任务列表立即更新

#### 任务组布局
- [ ] 每个需求显示为独立卡片
- [ ] 标题行显示：ID + 状态徽章 + 标题 + 阻塞标记
- [ ] 进度条正确显示完成百分比
- [ ] 进度条颜色为蓝色渐变
- [ ] "查看需求详情"按钮可点击

#### 章节折叠
- [ ] 甘特图章节可以展开/收起
- [ ] 点击折叠按钮后内容隐藏
- [ ] 再次点击可重新展开

#### 视图切换
- [ ] 点击"分组视图"按钮（默认激活）
- [ ] 点击"列表视图"按钮（待实现）
- [ ] 点击"时间线视图"按钮（待实现）
- [ ] 激活的按钮有高亮样式

#### 响应式设计
- [ ] 在大屏幕上显示正常
- [ ] 缩小浏览器窗口，布局自适应
- [ ] 移动端视图（如果需要）

## 已知限制

1. **列表视图和时间线视图** - 目前只实现了按钮，实际渲染逻辑待实现
2. **筛选逻辑** - 前端状态已设置，但实际过滤逻辑需要在渲染时应用
3. **排序功能** - 状态已保存，但排序逻辑待实现

## 下一步改进

### Phase 2A: 完善筛选逻辑
- 在 `buildTasksPage` 中应用 `taskFilters` 进行实际过滤
- 实现排序逻辑

### Phase 2B: 实现列表视图
- 扁平化任务列表
- 表格样式优化

### Phase 2C: 实现时间线视图
- 以甘特图为主的视图
- 减少其他内容

### Phase 3: 性能优化
- 虚拟滚动（大量任务场景）
- 防抖处理
- 缓存计算结果

## 回滚方案

如果出现问题，可以回滚：

```bash
cd packages/pages/dsh-pmboard
git checkout src/client/view.ts
git checkout src/client/styles.ts
git checkout src/client/board-mount.ts
pnpm run build:client
```

---

**实施完成时间**: 2026-09-14T19:26:04.009Z
**构建状态**: ✅ 成功
**待测试**: ⏳ 等待重启
