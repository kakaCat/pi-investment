# t-665bb5 [FR-5] 立项拒绝粘滞：rejected 落痕 + 弹框前置检查 + 提示词纪律

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
[FR-5] 立项拒绝粘滞：rejected 落痕 + 弹框前置检查 + 提示词纪律

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：implement
- 端侧：backend

## 得到什么结果
npx vitest run packages/web/dsh-pmboard/tests/capture-tool.test.ts 全绿含新 3 用例；构造 30 分钟内拒绝留痕调 reqboard_capture 不弹框返回未立项（单测断言 questions.ask 未被调用）。

## 实施方案（implementation）
新增留痕写（可复用 IsolationTraceFile 模式或注入端口）；CaptureRequirement.ts 改 rejected 分支与前置判定段；capture-tool.test.ts 增 3 用例（写留痕/写失败降级/前置命中不调 ask）。

## 上游产出摘要（dependsSummary）
- （无依赖）

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-21T18:20:35.207Z，窗口 session-9faaac35-2641-473c-a5dc-ea6e6d11efbf）

立项拒绝现在粘滞：用户在四问弹框点"不立项"会落盘留痕（state/capture-rejections.json，ring buffer 50 条原子写），同窗口 30 分钟内再触发 capture 不再弹框直接返回未立项——修复了你现场遇到的"点不立项没结束还继续推进"

### 完成项

- 新增纯逻辑 capture-rejections.ts（ring buffer/TTL 30min/校验）
- ports.ts 增 CaptureRejectionPort，UseCaseDeps 可选注入（缺省=无粘滞，行为不变）
- CaptureRejectionFile 适配器（原子写+串行队列，失败只告警）
- CaptureRequirement rejected 分支落痕 + 弹框前置检查
- index.ts 组合根接线
- CaptureTool 提示词补"超时不得盲目重弹"纪律
- capture-tool.test.ts 增 6 用例，15/15 全绿；layer-boundary 等关联套件 205/206（唯一失败为存量 diag-log.ts 违规，git 实证 HEAD 已红）

### 改动文件

- `packages/web/dsh-pmboard/src/application/internal/capture-rejections.ts`
- `packages/web/dsh-pmboard/src/adapters/CaptureRejectionFile.ts`
- `packages/web/dsh-pmboard/src/application/ports.ts`
- `packages/web/dsh-pmboard/src/application/use-cases/CaptureRequirement.ts`
- `packages/web/dsh-pmboard/src/index.ts`
- `packages/web/dsh-pmboard/src/tools/CaptureTool/prompt.ts`
- `packages/web/dsh-pmboard/tests/capture-tool.test.ts`
- `packages/web/dsh-pmboard/tests/helpers/tool-deps.ts`

### 下一步

t4 FR-4 workspaceRoot API + 客户端绝对路径

---
