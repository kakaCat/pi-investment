# t-054f37 [FR-2] requirementDocPath 消费 docBasePath + 单测

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
[FR-2] requirementDocPath 消费 docBasePath + 单测

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：implement
- 端侧：backend

## 得到什么结果
npx vitest run packages/web/dsh-pmboard/tests/artifact-path.test.ts 全绿，且含新 4 用例；无 docBasePath 老记录输出与现状逐字节一致（单测断言）。

## 实施方案（implementation）
改 src/application/internal/node-input-package.ts 的 requirementDocPath；tests/artifact-path.test.ts 增 4 用例（缺省逐字节一致/含 <REQ>/不含 <REQ> 追加 id/docLinks 优先）。

## 上游产出摘要（dependsSummary）
- （无依赖）

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-21T18:10:32.068Z，窗口 session-9faaac35-2641-473c-a5dc-ea6e6d11efbf）

requirementDocPath 现在消费 docBasePath：Q4 选了非默认文档位置的需求，H2 输入包/阶段详情的文档路径不再断裂；老记录（无 docBasePath）输出逐字节不变

### 完成项

- requirementDocPath 按契约改造（docLinks 优先 → docBasePath 拼接 → 缺省）
- 无 <REQ> 占位符时追加 id 子目录防碰撞
- 新增 tests/requirement-doc-path.test.ts 6 用例全绿
- 关联套件 isolate-node-context 29 + h2-compact 回归全绿（46 passed）

### 改动文件

- `packages/web/dsh-pmboard/src/application/internal/node-input-package.ts`
- `packages/web/dsh-pmboard/tests/requirement-doc-path.test.ts`

### 下一步

t5 立项拒绝粘滞

---
