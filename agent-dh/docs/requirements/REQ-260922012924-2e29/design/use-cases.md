---
requirement_refs: FR-4, FR-5
---

# 用例 · REQ-260922012924-2e29

## UC-1 用户在任意工作区会话打开需求文档 `serves: FR-4`

用户在项目看板点需求文档链接 → 看板用 state.workspaceRoot 拼绝对路径 → 官方右侧栏打开文档。前置：文档已落盘；后置：工作区=dsh-pmboard 等非 agent-dh 工作区的会话同样可开（修复前必现打不开）。异常流：旧服务端无 workspaceRoot 字段 → 维持现状相对解析（显示降级，如实可见）。

## UC-2 用户在立项弹框点"不立项" `serves: FR-5`

用户对四问弹框答"✖️ 不需要立项" → rejected 分支落盘留痕并返回未立项。随后无论同窗口再次触发 capture（含前次调用超时/中断后的重试）→ 前置检查命中 30 分钟内拒绝留痕 → 不再弹框，如实返回"用户已于 HH:MM 选择不立项"。异常流：留痕文件损坏/写失败 → 按无记录处理且不阻断返回（降级，绝不因留痕故障误立项或误拦截）。

## UC-3 立项后上下文压缩 `serves: FR-3`

立项门 G0 确认 → 后置链 H2：requirement.md 未落盘 → skip(doc_not_ready) 留痕不压（保护立项上下文）；需求文档落盘后的后续节点 → 满足自足/idle/配对平衡三纪律则真压缩，H4 只发作答摘要唤醒。回滚：cordis.yml 改回 false + kickstart，30 秒可逆。
