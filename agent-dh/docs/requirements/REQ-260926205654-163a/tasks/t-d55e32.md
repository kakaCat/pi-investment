# t-d55e32 修改 formatTimestamp 函数

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
修改 formatTimestamp 函数

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：implement
- 端侧：fullstack

## 得到什么结果
执行 `cd packages/web/dsh-pmboard && grep -A 10 "function formatTimestamp" src/shared/protocol.ts`，确认：1) 年份变量为 YYYY（4位）；2) 无 ss 变量；3) return 语句格式为 `${YYYY}${MM}${DD}${HH}${mm}`

## 实施方案（implementation）
1. 打开 packages/web/dsh-pmboard/src/shared/protocol.ts
2. 定位 formatTimestamp 函数（约1137-1145行）
3. 修改年份：const yy = date.getFullYear().toString().slice(-2) → const YYYY = date.getFullYear().toString()
4. 删除秒字段：删除 const ss = ... 这一行
5. 修改返回值：return `${yy}${MM}${DD}${HH}${mm}${ss}` → return `${YYYY}${MM}${DD}${HH}${mm}`
6. 修改注释：YYMMDDHHmmss（精确到秒） → YYYYMMDDHHmm（4位年份，精确到分钟）

## 上游产出摘要（dependsSummary）
- （无依赖）

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-26T15:05:22.308Z，窗口 session-944cb001-3359-4203-96cb-924fe55c13a5）

formatTimestamp 函数已修改完成：年份改为4位（YYYY），移除了秒字段，新格式为 YYYYMMDDHHmm（14位）

### 完成项

- 修改年份变量：yy（2位）改为 YYYY（4位）
- 删除秒字段：移除 ss 变量
- 更新返回值：从 ${yy}${MM}${DD}${HH}${mm}${ss} 改为 ${YYYY}${MM}${DD}${HH}${mm}
- 更新函数注释：标注为"YYYYMMDDHHmm（4位年份，精确到分钟）"

### 改动文件

- `packages/web/dsh-pmboard/src/shared/protocol.ts`

### 下一步

验证新需求编号格式

---
