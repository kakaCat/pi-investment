# t-a194e3 搭建分片库骨架与构建期生成器

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 目标
搭建分片库骨架与构建期生成器

## 背景摘要（context）
（待补充）

## 范围
- 阶段：implement
- 端侧：backend

## 验收标准
node scripts/inline-prompt-fragments.mjs 生成 src/domain/prompt/generated/fragments.ts，且其中包含 fragments/ 下全部 md 内容；node scripts/check-prompt-fragments.mjs 退出码为 0；故意改一个 md 而不跑生成器时，check 脚本退出码为 1（故障注入实测通过）。验证命令：npx vitest run tests/prompt-gates.test.ts（在 packages/pages/dsh-pmboard 下）；不通过则本卡不完成。

## 实施方案（implementation）
新增 scripts/inline-prompt-fragments.mjs（递归读 src/domain/prompt/fragments/**/*.md，按路径生成 id，确定性排序输出 TS 常量）；新增 scripts/check-prompt-fragments.mjs（内存重新生成并与磁盘文件比对，差异即 exit 1）；先放一个占位 md 验证管线，内容由 t7/t8 补齐。

## 上游产出摘要（dependsSummary）
- （无依赖）

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-17T14:37:57.505Z，窗口 session-41e7e4cd-4038-476c-9307-d93622a411e2）

搭起分片库骨架与构建期生成/校验管线（父窗口亲手故障注入验证：改 md 不重建 → check exit 1，恢复 → exit 0）。

### 完成项

- 生成器只做整文件拼接（无逐行字符串变换）
- check 脚本比对源与产物，差异即 exit 1
- 6 个节点 default.md + common/iron-rules.md 落位，生成 generated/fragments.ts

### 改动文件

- `packages/pages/dsh-pmboard/scripts/inline-prompt-fragments.mjs`
- `packages/pages/dsh-pmboard/scripts/check-prompt-fragments.mjs`
- `packages/pages/dsh-pmboard/src/domain/prompt/generated/fragments.ts`
- `packages/pages/dsh-pmboard/src/domain/prompt/fragments/common/iron-rules.md`

### 下一步

P1 用 vendor 原文替换 heavy 档内容

---
