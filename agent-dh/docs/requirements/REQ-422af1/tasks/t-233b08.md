# t-233b08 看板展示本次注入信息（只读块）

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 目标
看板展示本次注入信息（只读块）

## 背景摘要（context）
（待补充）

## 范围
- 阶段：implement
- 端侧：frontend

## 验收标准
需求详情出现只读信息块，字段与留痕一致（routeKey / fragmentIds 数量 / charCount / 时间）；无留痕时显示「尚无记录」而非空白或报错；注入 <script> 探针时输出被转义（断言渲染文本包含转义实体、不含可执行标签）；pnpm build:client 通过且 scripts/verify-client-build.mjs 哨兵通过；lib/client.js 的 mtime 新于 src 下最新改动。验证命令：npx vitest run tests/board-injection-info.test.ts（在 packages/pages/dsh-pmboard 下）；不通过则本卡不完成。

## 实施方案（implementation）
在 src/client/ 既有渲染流程新增只读信息块（数据来自服务端新增只读查询接口，读 injection-log ring buffer）；样式沿用既有类名，不新增页面；构建走 pnpm build:client（tsdown + wrap-client.mjs + verify-client-build.mjs）。

## 上游产出摘要（dependsSummary）
- 实现注入留痕（ring buffer + 只读查询）

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-17T15:07:57.074Z，窗口 session-41e7e4cd-4038-476c-9307-d93622a411e2）

看板「本次注入」只读块落地：新增只读路由 + 客户端折叠块（六字段、无留痕显示「尚无记录」、全量 escape），客户端重建且哨兵通过；父窗口已复核 9/9 与泄露事实。

### 完成项

- src/client/injection-info.ts（82 行纯渲染）+ 概览 Tab 折叠块（容器 dsh-pm-injection-info-container）
- src/http/routers/injection.ts 只读路由 GET /dashboard/api/reqboard/injection-log（k 默认 20/上限 200，越界 400，无写操作）
- tests/board-injection-info.test.ts 9/9（父窗口重跑通过）：字段一致、无留痕降级、<script> 探针转义、GET 前后留痕文件字节不变（只读性）、窗口过滤、越界 400
- pnpm build:client + verify-client-build.mjs 哨兵通过（父窗口重跑 exit 0）；新符号故障注入可红
- 父窗口复核：lib/client.js 新于 src（0 个更新的 src）

### 改动文件

- `/Users/yunpeng/pi-investment/agent-dh/packages/pages/dsh-pmboard/src/client/injection-info.ts`
- `/Users/yunpeng/pi-investment/agent-dh/packages/pages/dsh-pmboard/src/http/routers/injection.ts`
- `/Users/yunpeng/pi-investment/agent-dh/packages/pages/dsh-pmboard/src/application/internal/injection-log.ts`
- `/Users/yunpeng/pi-investment/agent-dh/packages/pages/dsh-pmboard/src/client/views/stage-detail.ts`
- `/Users/yunpeng/pi-investment/agent-dh/packages/pages/dsh-pmboard/scripts/verify-client-build.mjs`
- `/Users/yunpeng/pi-investment/agent-dh/packages/pages/dsh-pmboard/lib/client.js`
- `/Users/yunpeng/pi-investment/agent-dh/packages/pages/dsh-pmboard/tests/board-injection-info.test.ts`

### 下一步

待与 t10 一起收口后统一重建 client；另需另立卡处理「提示词分片被打进浏览器包」（+76KB）

---
