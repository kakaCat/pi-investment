# t-86a99d 节点结算点接入隔离（默认关）

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 目标
节点结算点接入隔离（默认关）

## 背景摘要（context）
（待补充）

## 范围
- 阶段：implement
- 端侧：backend

## 验收标准
tests/isolate-node-context.test.ts 中断言：开关 NODE_ISOLATION 关闭（默认）时隔离执行计数为 0，既有全部测试行为与改造前一致；开启时一次真实节点转移后留痕包含一条隔离记录（含 routeKey 与被替换区间）；两种模式下失败都只告警不中断流水线（断言流水线状态仍推进）。验证命令：npx vitest run tests/isolate-node-context.test.ts（在 packages/pages/dsh-pmboard 下）；不通过则本卡不完成。

## 实施方案（implementation）
在 CaptureHook 与状态转移回调处接入 IsolateNodeContext 调用；开关读取复用本包既有配置模式（默认关）；失败路径只记录 + 告警不抛；补充单测：关→0 次调用、开→1 次调用且留痕字段完整。

## 上游产出摘要（dependsSummary）
- 实现节点边界同窗口 surface 替换执行模型

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-17T15:15:41.023Z，窗口 session-41e7e4cd-4038-476c-9307-d93622a411e2）

节点结算点接入隔离（默认关）落地：组合根注入 t9 两个端口；结算只在状态转移登记、在 turn/end 只发信号（回调内零会话写），隔离动作交 setImmediate 异步执行（满足 D-17）；父窗口已复核。

### 完成项

- src/index.ts：nodeIsolationEnabled（config > env NODE_ISOLATION > 默认 false）+ turnBoundaryIdle（投影判不出保守 false）+ 装配 dispatcher
- src/adapters/CaptureHook.ts：onNodeSettled 可选依赖；状态转移只登记（去重键=窗口:节点）、turn/end 只发信号
- src/application/internal/node-settlement.ts（164 行）：switch 关时 return（不调度/不建端口/不触会话）、setImmediate 异步边界、全量 try/catch 只 warn 不冒泡
- isolation-trace.ts（64 行）+ IsolationTraceFile.ts（76 行）：ring buffer cap=200 + 原子写 + 串行队列
- tests/isolate-node-context.test.ts 29/29（父窗口重跑通过）：关态计数全 0 + 故障注入恰好让 2 条关态用例变红（已还原）；开态真实 Session 端到端 replaced=1 且留痕含 routeKey+range+replacementSeq>artifactSeq
- 父窗口复核：守卫在位、tsc 0 错误、四条机械门禁 21/21、新文件均 ≤400 行

### 改动文件

- `/Users/yunpeng/pi-investment/agent-dh/packages/pages/dsh-pmboard/src/index.ts`
- `/Users/yunpeng/pi-investment/agent-dh/packages/pages/dsh-pmboard/src/adapters/CaptureHook.ts`
- `/Users/yunpeng/pi-investment/agent-dh/packages/pages/dsh-pmboard/src/application/internal/node-settlement.ts`
- `/Users/yunpeng/pi-investment/agent-dh/packages/pages/dsh-pmboard/src/application/internal/isolation-trace.ts`
- `/Users/yunpeng/pi-investment/agent-dh/packages/pages/dsh-pmboard/src/adapters/IsolationTraceFile.ts`
- `/Users/yunpeng/pi-investment/agent-dh/packages/pages/dsh-pmboard/tests/isolate-node-context.test.ts`

### 下一步

开态于真实窗口的替换未实测（生产 idle 探针依赖 turnBoundary 投影，保守策略下可能落 skipped）→ 已记入验收材料的已知缺口，开启前需实机观察一次

---
