# t-01322d 回执聚合投递 + 名称批量解析 + 时间格式修复

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
回执聚合投递 + 名称批量解析 + 时间格式修复

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：implement
- 端侧：backend

## 得到什么结果
运行 pytest quantsys-v2/tests/watch/ 通过且断言：同周期 3 条超时→sender 调用 1 次、卡片包含 3 行且每行含归属账户；重跑同周期全部 duplicate、sender 调用 0 次；空组 flush 返回 no-op；回执文案不包含微秒与「| 周期 |」、due 只出现 1 次；名称未命中时返回纯代码+「名称缺失」标注

## 实施方案（implementation）
①receipt_service 加组缓冲与 flush_grouped（空组 no-op，sender 抛错整组 failed）；②watch_sla_job.run_once 循环末尾调 flush；③render_receipt 时间人性化（已超时 XhYm·截止 HH:MM）、period 空 tag 不产「|<due>」；④run_once 开头批量解析名称传入渲染

## 上游产出摘要（dependsSummary）
- 定契约：WatchCardItem 扩展 + StockNameResolver 端口 + render_receipt 签名

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-24T05:11:50.220Z，窗口 session-2bf7941d-3f18-4423-9c83-35f60ea4f5b9）

回执聚合投递落地：巡检一轮的升级/超时回执不再逐条轰炸，收尾时按类型各合成一张卡（超时卡含处置指引，每行带待办#/名称（代码）/归属/已超时人性化时长）；名称一轮一次批量联查 stocks 表注入；文案修掉微秒与「| 周期 |」双印；处置结论回执不参与聚合（为 t5 的即时性留好前提）。

### 完成项

- ReceiptService.begin_group/flush_grouped 聚合投递（FR-8，per-todo 去重不变）
- SLA job 注入 StockNameResolver，一轮一次批量解析（FR-13，无 N+1）
- render_receipt 时间人性化 + 卫生修复（FR-11）
- tests/watch/test_receipt_grouping.py 9 例验收锚点全绿
- 回归 105 例全绿（notification+watch+SLA+wiring）

### 改动文件

- `quantsys-v2/application/services/watch_engine/receipt_service.py`
- `quantsys-v2/adapters/inbound/fastapi_app/watch_sla_job.py`
- `quantsys-v2/application/services/watch_engine/watch_loop_wiring.py`
- `quantsys-v2/tests/watch/test_receipt_grouping.py`
- `quantsys-v2/tests/application/test_sla_job.py`
- `quantsys-v2/tests/notification/test_watch_card_contract.py`

### 下一步

t5 处置结论回执（TodoService.close → ReceiptService.result 三要素）

---
