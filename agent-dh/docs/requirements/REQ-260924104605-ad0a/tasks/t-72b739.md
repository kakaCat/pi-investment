# t-72b739 处置结论回执：close 触发 result + 三要素文案 + 响应填充

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
处置结论回执：close 触发 result + 三要素文案 + 响应填充

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：implement
- 端侧：backend

## 得到什么结果
运行 pytest quantsys-v2/tests/watch/ 通过且断言：close 收敛后 sender 收到的卡片包含结论/原因/next_condition 三要素；terminal=ignored 时文案包含 NEXT 条件原文；重复 close 返回 duplicate 不重复发送；POST /api/watch/todos/{id}/close 响应 receipt 字段非 None；receipt_service=None 时行为与改前一致

## 实施方案（implementation）
①todo_service 构造函数加可选 receipt_service=None（None 不发）；②close 在仓储成功后调 result(updated, terminal)；③render_receipt result 分支三要素文案（名称（代码）/归属/结论动作/原因/NEXT 条件）；④路由装配注入真实 ReceiptService，响应 data.receipt=outcome

## 上游产出摘要（dependsSummary）
- 定契约：WatchCardItem 扩展 + StockNameResolver 端口 + render_receipt 签名

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-24T05:23:42.453Z，窗口 session-2bf7941d-3f18-4423-9c83-35f60ea4f5b9）

处置结论回执落地：你在看板/接口关掉一条盯盘待办后，盯盘群立刻收到一张三要素卡——结论（已处置/忽略+动作）、原因（你填的原文）、后续意见（忽略时的 NEXT 条件原文）；卡片带名称（代码）与归属账户；重复关闭返回 409 不会重复轰炸；不接回执服务时行为与之前完全一致。

### 完成项

- TodoService 注入可选 receipt_service/name_resolver，close_and_receipt 触发 result 回执
- render_receipt result 三要素文案（结论/原因/后续意见，NEXT 原文）
- close 路由 receipt 字段填充真实 outcome
- tests/watch/test_close_receipt.py 7 例 + 路由测试 receipt 非 None 断言
- 回归 212 例通过（1 例 self_heal 失败经基线对照为既有红）

### 改动文件

- `quantsys-v2/application/services/watch_engine/receipt_service.py`
- `quantsys-v2/application/services/watch_engine/todo_service.py`
- `quantsys-v2/adapters/inbound/fastapi_app/routes/watch_todo_async.py`
- `quantsys-v2/tests/watch/test_close_receipt.py`
- `quantsys-v2/tests/api/test_watch_todo_routes.py`

### 下一步

t6 FR-1 回归 + 回滚演练 + 旧卡兼容测试 + 运维备注

---
