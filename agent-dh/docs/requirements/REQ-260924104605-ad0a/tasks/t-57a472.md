# t-57a472 集成验收：触发落群/网关降级/close 收卡/聚合一卡/pytest 全绿

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
集成验收：触发落群/网关降级/close 收卡/聚合一卡/pytest 全绿

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：test
- 端侧：backend

## 得到什么结果
5 步全部通过且有证据：①触发后盯盘群可见卡且 alerts 群无此消息；②停 :8080 后消息仍送达且 metadata 包含降级标注；③close 后盯盘群可见三要素卡；④3 条超时只收到 1 张聚合卡；⑤pytest quantsys-v2/tests/notification/ 与 tests/watch/ 全部通过

## 实施方案（implementation）
按 test-cases.md 集成验证 1-5 逐步执行并留证据（投递日志/截图/pytest 输出）；每步对应需求 §1.2 判定标准

## 上游产出摘要（dependsSummary）
- 重写四级模板为意图驱动骨架（判重/多账户/名称优先/卫生）
- 接线 direct 渠道路由与回执 os_channel、补 rule_id 传递
- 回执聚合投递 + 名称批量解析 + 时间格式修复
- 处置结论回执：close 触发 result + 三要素文案 + 响应填充

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-24T06:34:07.540Z，窗口 session-2bf7941d-3f18-4423-9c83-35f60ea4f5b9）

五步集成验证全部通过并留证。①恒真规则触发→盯盘群收卡（watch_symbol→60d879 sent，alerts 群 0 新增）；②bootout 停 Agent OS 后触发→主渠道连接拒绝→降级直飞书成功送达，日志链完整（主渠道发送失败→降级渠道发送成功），metadata 降级标注由单测钉住（degraded/degraded_reason=agent_os_unreachable）；③close 真实待办→盯盘群收三要素卡（结论/原因/后续意见，watch_symbol→60d879 sent）；④同周期 3 条 L3 超时→巡检统计 group_cards=1、投递日志仅 1 张 risk_stop 聚合卡、3 条回执同 batch id；⑤tests/notification/+tests/watch/ 89 例全绿。联调中发现并修复两个真实缺陷：ADR-002 调度旗误伤通知渠道注册（agent 优先链路静默失效，已拆开关+主渠道未注册响亮告警+降级原因区分）；回执落库 channel 标签与真实落点漂移（已改 route_channel 真实路由标签）。两修复均带回归用例并合并回主线，服务已重启生效，渠道表终态断言 OK。

### 完成项

- ①触发落群：规则 217 触发 600519→watch_symbol sent→60d879（14:18:14），alerts 新增 0
- ②网关降级：停 :8080 后规则 219 触发→连接拒绝→feishu 兜底 sent（notif_c2375f55bc6143c5，14:20:08），Agent OS 已恢复
- ③close 收卡：待办 39 close→三要素卡（已处置（不动）/原因/NEXT 条件）→watch_symbol sent→60d879（14:26:11）
- ④聚合一卡：3 超时（todo 41/42/43）→group_cards=1、1 张 risk_stop 聚合卡、batch:timeout:202609241433 三行同批
- ⑤pytest：tests/notification/+tests/watch/ 89 passed
- 修复1：agent_os_notify_enabled 拆开关 + fallback_cause 如实标注（test_agent_channel_registration 4 例）
- 修复2：route_channel 落库标签=真实路由（test_close_receipt 新增钉案）
- 合并主线 2 个修复 commit，服务重启生效，渠道表断言 OK，合成数据已清理

### 改动文件

- `quantsys-v2/infrastructure/config/settings.py`
- `quantsys-v2/application/notification/notification_factory.py`
- `quantsys-v2/domain/notification/services/notification_service.py`
- `quantsys-v2/application/notification/notification_facade.py`
- `quantsys-v2/application/services/watch_engine/receipt_service.py`
- `quantsys-v2/application/services/watch_engine/watch_channels.py`
- `quantsys-v2/tests/notification/test_agent_channel_registration.py`
- `quantsys-v2/tests/watch/test_close_receipt.py`
- `quantsys-v2/tests/application/test_sla_job.py`

### 下一步

全部任务完成，提交验收材料（reqboard_submit kind=verification）

---
