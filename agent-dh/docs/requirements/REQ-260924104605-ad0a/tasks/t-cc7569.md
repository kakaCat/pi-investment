# t-cc7569 定契约：WatchCardItem 扩展 + StockNameResolver 端口 + render_receipt 签名

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
定契约：WatchCardItem 扩展 + StockNameResolver 端口 + render_receipt 签名

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：implement
- 端侧：backend

## 得到什么结果
运行 pytest quantsys-v2/tests/notification/ 通过：新字段默认值与 display 形态（名称（代码）/缺失降级）用例通过；StockNameResolver 假实现可注入；既有用例结果与改前一致（不红）

## 实施方案（implementation）
①WatchCardItem 加字段（默认 None/空串）；②display 属性改名称优先+「名称缺失」降级；③ports.py 加 StockNameResolver 抽象；④stock_name_resolver.py 实现联查 quant.stocks（异常→全 None）；⑤render_receipt 形参扩展（旧调用兼容默认值）

## 上游产出摘要（dependsSummary）
- （无依赖）

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-24T04:38:47.693Z，窗口 session-2bf7941d-3f18-4423-9c83-35f60ea4f5b9）

契约先行落地：WatchCardItem 增 8 个可选字段（默认 None/空串，旧调用零改动）、display 改「名称（代码）」名称在前且缺失降级不臆造；domain 增 StockNameResolver 批量名称解析端口并落 PG 实现（quant.stocks 只读，异常→全 None）；render_receipt 签名扩展 4 个 keyword-only 参数（t4/t5 的挂点已留好）。

### 完成项

- WatchCardItem 扩展字段 + display 名称优先（FR-13 契约）
- StockNameResolver 端口（ports.py）+ PgStockNameResolver 适配器
- render_receipt 签名扩展（name/close_reason/next_condition/action_kind）
- 契约测试 11 例（tests/notification/test_watch_card_contract.py）
- 2 处旧 display 断言随契约更新；notification 44 例 + SLA/回执 32 例全绿

### 改动文件

- `quantsys-v2/infrastructure/notification/formatters/watch_level_templates.py`
- `quantsys-v2/domain/watch/ports.py`
- `quantsys-v2/adapters/outbound/repositories/stock_name_resolver.py`
- `quantsys-v2/application/services/watch_engine/receipt_service.py`
- `quantsys-v2/tests/notification/test_watch_card_contract.py`
- `quantsys-v2/tests/notification/test_watch_templates.py`

### 下一步

t2 模板骨架重写（depends_on t1 已满足）

---
