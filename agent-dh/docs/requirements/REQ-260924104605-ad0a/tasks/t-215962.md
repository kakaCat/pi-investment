# t-215962 重写四级模板为意图驱动骨架（判重/多账户/名称优先/卫生）

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
重写四级模板为意图驱动骨架（判重/多账户/名称优先/卫生）

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：implement
- 端侧：backend

## 得到什么结果
运行 pytest quantsys-v2/tests/notification/test_watch_templates.py 通过且断言：骨架顺序一致；N=1 时 action 文本出现 1 次；N=3 时卡片包含 1 完整段+2 摘要行（含归属/待办#）；双账户返回两行不合并；无账户显示「通用观察」；渲染结果不包含「规则#-」与「频道：」；P0 包含 @所有人与止损止盈；P3 standalone_push=False 不变

## 实施方案（implementation）
改 watch_level_templates.py 的 render_p0/render_p1/render_p2 与 card_item_from_variables；补 INTENT_META 映射（复用 feishu_formatters 的表）；扩展 tests/notification/test_watch_templates.py

## 上游产出摘要（dependsSummary）
- 定契约：WatchCardItem 扩展 + StockNameResolver 端口 + render_receipt 签名

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-24T04:48:03.987Z，窗口 session-2bf7941d-3f18-4423-9c83-35f60ea4f5b9）

四级模板重写为意图驱动骨架：P0/P1 完整卡按「意图标签→触发→现价→目的→预案→风控→下一步」固定顺序渲染；P1 N=1 判重（action 只出现一次）、N 项首项完整+其余一行摘要（意图 emoji+待办#+归属）；多账户分行不合并；规则号缺失显示「手工」、归属缺失显示「通用观察」；全部渲染无「频道：」外露；INTENT_META 复用旧表单一真相。

### 完成项

- render_p0/p1/p2 骨架重写 + P3 微调（FR-4~7/9/10/12/13）
- card_item_from_variables 填充全部契约字段
- 6 个新验收锚点用例 + 5 处旧断言对齐新契约
- pytest tests/notification/ + SLA 相关 85 例全绿

### 改动文件

- `quantsys-v2/infrastructure/notification/formatters/watch_level_templates.py`
- `quantsys-v2/tests/notification/test_watch_templates.py`
- `quantsys-v2/tests/notification/test_watch_level_channel.py`

### 下一步

t3 路由接线（direct→send_with_fallback、回执 os_channel、rule_id 传递）

---
