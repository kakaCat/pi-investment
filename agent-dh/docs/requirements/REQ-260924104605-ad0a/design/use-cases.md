---
requirement_refs: [FR-1, FR-2, FR-4, FR-6, FR-8, FR-13, FR-14]
---

# 用例：盯盘通知改版（REQ-260924104605-ad0a）

## UC-1 买入信号触达（L1）（serves: FR-1, FR-2, FR-4, FR-13）

中国中免进入买区 → 盯盘群收到 P1 完整卡「🎯 买入跟踪｜中国中免（601888）…
这条提醒为了…预案…风控…处置入口」；alerts 群无此消息。

## UC-2 一票多账户（serves: FR-4, FR-6）

601888 同属 agent_virtual 与 user_main_simulation → 卡内两项/两行，
各带归属账户，互不合并。

## UC-3 超时升级（聚合）（serves: FR-8, FR-13）

一轮巡检 3 条 L3 超时 → 盯盘群收 1 张「⏰ 3 项待办超时未处置」清单卡，
每行「待办# 名称（代码）级别 · 归属 · 已超时 2h14m（截止 10:00）」。

## UC-4 处置闭环（serves: FR-14）

agent 评估中国中免后 close（handled，原因+后续）→ 盯盘群即时收到
「✅ 待办#32 已处置：建仓 / 原因… / 后续：止损 49.20 已挂…」；
你在看板勾选「不动」→ 同样收到回执且必含 NEXT 条件。

## UC-5 网关故障降级（serves: FR-2）

Agent OS 挂掉时触发规则 → 消息降级直飞书到盯盘群对应机器人不可达则落原群兜底，
结果如实标注降级；不丢消息。

## UC-6 回滚（serves: FR-1）

10 行 webhook 改回原值 → 一切消息回原群，5 分钟内可逆。
