# REQ-f0579a 复盘（审计整改）

## 做得好的

- 审计先行：先跑全量拿到 17 失败/10 文件的完整清单再立项，五群失败各自有根因，没有"见一个修一个"的碎修；
- FR-4 排查没有停在症状：output-contract「扫描器可能失效」追到真因是**源代码里正则字面量含裸反引号**，被扫描器当模板串起点吞掉全文 return——修源码（\x60）而不是改扫描器，与门禁"源码保持可扫描"的取向一致；
- FR-5 的 XSS 修复走了完整 bug 纪律：先写红测试复现（2 失败如期）再修；
- 验收走了完整链路：材料提交 → 落章 → 8 项逐项弹框 → 全过归档，没有跳闸。

## 教训（可复用）

- **"构建了"≠"测试过"**：本需求起因就是 e25d6c8b 自称"dist/lib 已构建核验"但没跑测试。发版核验必须含全量测试，构建只是产物存在性；
- **基线归一提交会盖掉在途视图改动**：FR-2 的三处客户端回归全部来自一次 wip 基线归一（f215753f→HEAD）。归一前应先 diff 视图文件，确认没有新渲染被旧版覆盖；
- **两条用户裁定打架时以新裁定为准，并把合并点写进测试注释**：否则下次巡检会把"验收态含 move-req"当新 bug 又改回去（FR-3 已在测试内留注释）；
- **门禁的报警文案要信**："扫描器可能失效"不是误报，它精确指向了扫描盲区。

## 遗留（不阻塞，另行跟踪）

- styles/board.ts 恰好 400 行，尺寸预算用尽，下次加样式即红；
- 审计中发现的包外观察项（profile 目录过时、cordis.yml 飞书 webhook 明文）按需求边界另行处理。