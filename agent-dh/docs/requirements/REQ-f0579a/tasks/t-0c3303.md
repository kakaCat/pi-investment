# t-0c3303 清偿架构门禁债

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
清偿架构门禁债

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：implement
- 端侧：backend

## 得到什么结果
npx vitest run tests/output-contract.test.ts tests/layer-boundary.test.ts tests/tools-dispatch.test.ts tests/message-hygiene.test.ts 全绿

## 实施方案（implementation）
①tests/output-contract.test.ts RESPONSE_SOURCES 补 TaskExecute 与 TaskStatus 映射；②domain/task/TaskStatus.ts 新增 TASK_STATUS_PROGRESS 与 WorkflowRunStatus 常量/类型，TaskExecuteTool/TaskStatusTool/types 改为引用；③TaskExecuteTool timeoutMs 改 LIMITS.timeoutInteractiveMs；④pagination.ts:52 拼接改模板字符串

## 上游产出摘要（dependsSummary）
- （无依赖）

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-20T12:48:00.305Z，窗口 session-dd6cfa29-d8ff-43fe-99d7-371a69442076）

架构门禁债清偿完毕：四个门禁测试文件（output-contract / layer-boundary / tools-dispatch / message-hygiene）36 条用例全绿。变化：①两个新工具的返回体契约补全——reqboard_task_execute 补声明 stages/next_step/error，reqboard_task_status 补声明 workflow/error，线上绑定层不会再因未声明字段拒收回执；②状态判断单点化——domain 新增 isWorkflowRunCompleted，工具壳里三处 status === 字面量/常量比较全部改为调 domain 判定函数；③顺带修出一个扫描器盲区根因：TaskStatusTool 的正则字面量内含裸反引号，被契约静态扫描器误判为模板串起点、吞掉全文 return，改写为 \x60 后扫描恢复（这也解释了"扫描器可能失效"那条红的真正来源）。

### 完成项

- output-contract：TaskExecute/TaskStatus 两工具 RESPONSE_SOURCES 映射已在位（前序进度），补全两工具 output.schema 缺失字段后 18 条全绿
- tools-dispatch：工具壳三处 status === 比较下沉为 domain isWorkflowRunCompleted 调用，门禁转绿
- TaskStatusTool 正则反引号扫描器盲区根因修复（/Run ID.*\x60(...)\x60/）
- layer-boundary / message-hygiene 确认保持绿（前序已完成 LIMITS.timeoutInteractiveMs 与 pagination 模板串）
- tsc --noEmit 绿；全量 vitest 1403/1404，唯一失败 size-budget 属 t-cee913 范围

### 改动文件

- `packages/pages/dsh-pmboard/src/tools/TaskExecuteTool/TaskExecuteTool.ts`
- `packages/pages/dsh-pmboard/src/tools/TaskStatusTool/TaskStatusTool.ts`
- `packages/pages/dsh-pmboard/src/tools/TaskExecuteTool/update-task-card.ts`
- `packages/pages/dsh-pmboard/src/domain/task/TaskStatus.ts`

### 下一步

推进 integrating → testing → in_review；size-budget 失败由 t-cee913 处理

---
