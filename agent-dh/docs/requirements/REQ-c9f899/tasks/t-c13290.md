# t-c13290 升级策略收敛

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 目标
升级策略收敛

## 背景摘要（context）
（待补充）

## 范围
- 阶段：implement
- 端侧：backend

## 验收标准
grep application/services/watch_engine 断言 volume_ratio/multi_rule_confluence/core_zones/触发频率 分支命中数为 0；运行 python -m pytest tests/services/test_escalation_checker.py 通过；断言宪法级不受预算限制

## 实施方案（implementation）
重写 escalation：删除四类判定；保留宪法级与异常波动（metric=price/pct_change，带节流）；支持规则 action_hint 显式声明升级

## 上游产出摘要（dependsSummary）
- 判据 metric 化并建立消费侧契约

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-17T17:01:51.776Z，窗口 session-9b2b54f3-a3f4-4087-84f4-d1038fd1dd98）

升级策略收敛为三条可信路径（规则显式声明 / 宪法级 / 异常波动），删除历史五类（触发频率·量能异常·价格偏差·核心区域·多规则共振），并清理值对象与生产者中的废弃字段。

### 完成项

- escalation_checker.py 重写：只留 ①规则显式声明（action_hint.escalate）②宪法级（exit_stop）③异常波动（|涨跌幅|≥阈值，按 metric 取数）；删掉 5 个旧分支方法
- escalation_coordinator.py：不再收集频率/共振计数（计数能力保留在 StateManager，改供规则自愈 R6 消费）
- domain/watch/models.py：EscalationPolicy 新增 anomaly_change_pct；历史 5 字段标注为「仅兼容 JSONB 解析、不再被消费」；default() 不再预置历史字段
- opportunity_to_watch_rule_service.py：生产者不再把废弃判据字段写进新规则
- metric_contract.py 文档示例更新为新的异常波动路径（去掉 _check_volume_anomaly 引用）
- tests/services/test_escalation_checker.py 重写（9 条）：含 test_legacy_five_paths_are_gone——旧策略全字段齐备下五条路径全部不再生效（收敛证明）
- tests/services/test_metric_contract.py 更新两条用例以匹配 t7 语义（量能路径已删除；契约改由异常波动路径强制）
- 实测：pytest 两个模块 → 20 passed；pytest tests/ -k watch → 271 passed（对照组：HEAD 干净基线为 1 failed/270 passed，本次全绿）
- 残留复核：core_zones/multi_rule_confluence/被删方法名在生产代码仅剩 rule_health_checker 自身的 _check_price_deviation（属规则健康度 STALE 判定，与升级无关）

### 改动文件

- `quantsys-v2/domain/watch/services/escalation_checker.py`
- `quantsys-v2/application/services/watch_engine/escalation_coordinator.py`
- `quantsys-v2/domain/watch/models.py`
- `quantsys-v2/domain/watch/services/metric_contract.py`
- `quantsys-v2/application/services/opportunity_to_watch_rule_service.py`
- `quantsys-v2/tests/services/test_escalation_checker.py`
- `quantsys-v2/tests/services/test_metric_contract.py`

### 下一步

t8（规则自愈）接管「反复触发」语义——计数已在 StateManager，自愈据此抑噪并建修规则待办

---
