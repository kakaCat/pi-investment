# t-665db4 TodoService 与仓储端口

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 目标
TodoService 与仓储端口

## 背景摘要（context）
（待补充）

## 范围
- 阶段：implement
- 端侧：backend

## 验收标准
运行 python -m pytest tests/application/test_todo_service.py 通过；断言 ignored 缺 next_condition 时返回 400 拒绝；断言 L3 的 trade 缺 decision_audit_id 返回 400 拒绝；断言重复关闭返回 409

## 实施方案（implementation）
ports 新增 IWatchTodoRepository；adapters 实现；TodoService 做终态校验与状态迁移；补集成测试

## 上游产出摘要（dependsSummary）
- 新增四张表与两表加列
- 级别判定与账户路由纯函数

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-17T17:42:23.903Z，窗口 session-9b2b54f3-a3f4-4087-84f4-d1038fd1dd98）

TodoService 与仓储端口落地：端口 7 方法 + 领域错误码；create 做账户路由（out_of_scope 拒绝/空账户 defect 照建/autonomy 兜底/flow_state 按级别派生）；close 四条终态校验；HTTP 路由映射 404/409/400。主 agent 独立探针 10/10 通过。

### 完成项

- domain/watch/ports.py：IWatchTodoRepository（create/get/claim/close/promote/list_overdue/list_pending）+ 与 interfaces §4 对应的领域异常（NotFound/AlreadyClosed/InvalidTerminal/MissingAudit/MissingNextCondition/OutOfScope/InvalidAutonomy）——异常放 domain 以免 application→infrastructure 反向依赖，HTTP 映射留路由层
- adapters/outbound/repositories/watch_todo_repository.py（新增）：claim/close/promote 一律 WHERE terminal IS NULL 且 rowcount=0 → None（并发双保险）；list_overdue 命中 (terminal,due_at) 索引
- application/services/watch_engine/todo_service.py（新增）：create 走 route_owner（策略账户抛 OutOfScope 且不落库，绝不把 not_applicable 改写成 remind_only）；flow_state 派生 P0/P1→L3、P2/P3→L1；close 校验顺序 枚举→ignored NEXT→存在性→已终态→L3 动作审计
- adapters/inbound/fastapi_app/routes/watch_todo_async.py（新增）：GET /api/watch/todos、POST /{id}/claim、POST /{id}/close，错误码→HTTP 404/409/400；不注册 main.py（留 t12）
- tests/application/test_todo_service.py（26 例，fake repo）+ tests/api/test_watch_todo_routes.py（20 例，TestClient 自挂 router + 真实库，含 DB CHECK 双保险断言）
- 子代理变异测试：把 L3 审计校验改成 if False → 3 条变红（含 DB CHECK 兜住后路由变 500），改回 46 绿 → 用例有牙
- 主 agent 独立探针（不依赖其测试）10/10 通过：策略账户拒绝、P0→L3、P2→L1、空账户照建且 remind_only、非法 autonomy 拒绝、terminal 非法、ignored 缺 NEXT、重复关闭、L3+trade 缺审计、L3+observe 放行
- 【决策 A】保留任务卡指定的文件名 test_todo_service.py（卡内验收命令就是该文件），不改名；验收口径记录为「显式跑该文件」——因其文件名不含 watch，不会被 -k watch 选中
- 【决策 B】接受其测试库 fixture（用 importlib 读迁移的 DDL 常量在 quant_test 建表）；理由：与迁移 DDL 同源、可测 CHECK；t12 可改为 ORM metadata 建表

### 改动文件

- `quantsys-v2/domain/watch/ports.py`
- `quantsys-v2/adapters/outbound/repositories/watch_todo_repository.py`
- `quantsys-v2/application/services/watch_engine/todo_service.py`
- `quantsys-v2/adapters/inbound/fastapi_app/routes/watch_todo_async.py`
- `quantsys-v2/tests/application/test_todo_service.py`
- `quantsys-v2/tests/api/test_watch_todo_routes.py`

### 下一步

t6（到期巡检）/ t8（规则自愈）/ t11（迁移灰度）现可开工——三者均以本任务的 TodoService/仓储为基；t12 需接线：main.py 注册 todo 路由、close 响应补 receipt（由 t6 的 ReceiptService 产出，当前如实为 null 未伪造）

---
