# t-27088e 级别判定与账户路由纯函数

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 目标
级别判定与账户路由纯函数

## 背景摘要（context）
（待补充）

## 范围
- 阶段：implement
- 端侧：backend

## 验收标准
运行 python -m pytest tests/domain/test_level_router.py 通过；真值表逐行断言一致；断言 agent 账户返回 autonomous、用户账户返回 remind_only、空账户返回数据缺陷标记

## 实施方案（implementation）
domain/watch/services/level_resolver.py 与 router.py 纯函数 + 参数化测试；接入 DispositionEngine 产出 level/owner

## 上游产出摘要（dependsSummary）
- （无依赖）

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-17T17:01:51.758Z，窗口 session-9b2b54f3-a3f4-4087-84f4-d1038fd1dd98）

级别判定与账户路由落成 domain 纯函数：resolve_level（7 条优先级，含冲突用例）+ sla_seconds_for + route_owner（复用 WatchDeliveryPolicy，不造第二份账户映射）。48 用例 + 主 agent 独立探针复核通过。

### 完成项

- domain/watch/services/level_resolver.py（新增）：resolve_level 7 条优先级先命中先返回；LEVEL_SLA_SECONDS（P0=300/P1=1800/P2 当日/P3 无）；宪法级意图复用 disposition.CONSTITUTIONAL_INTENTS 不另立常量
- domain/watch/services/owner_router.py（新增）：route_owner → OwnerRoute(owner_kind/owner_ref/autonomy/is_data_defect/out_of_scope/reason)；账户→agent名/授权/策略账户判定全部复用 WatchDeliveryPolicy（无第二份映射）
- domain/watch/services/__init__.py：纯追加导出（既有导出未动）
- tests/domain/test_level_router.py（新增，48 用例）：级别真值表 24 行 + SLA 映射 + owner 五类事实 + 收口不变式；子代理做变异测试（去掉 has_position 守卫 → 2 failed；空账户 is_data_defect 改 False → 3 failed），证明测试非烟雾
- 主 agent 独立探针复核（不依赖子代理测试文件）：无持仓 exit_stop→P0、无持仓 exit_take_profit→P2、有持仓 exit_reduce→P0、entry→P1、L2 trend_observe→P1、宪法级压观察→P0、五类账户 owner/autonomy 全部与契约一致
- 文件范围复核：子代理仅改声明的 4 个文件（git status 交叉核对）

### 改动文件

- `quantsys-v2/domain/watch/services/level_resolver.py`
- `quantsys-v2/domain/watch/services/owner_router.py`
- `quantsys-v2/domain/watch/services/__init__.py`
- `quantsys-v2/tests/domain/test_level_router.py`

### 下一步

t5（TodoService）接入时注意：策略账户 route_owner 返回 autonomy=not_applicable 且 out_of_scope=True，而 quant.watch_todos.autonomy CHECK 只允许 autonomous/remind_only → TodoService 必须以 out_of_scope 为跳过信号、不得为其建待办（否则被 DB CHECK 拒）

---
## 汇报 2（2026-09-17T17:03:04.452Z，窗口 session-9b2b54f3-a3f4-4087-84f4-d1038fd1dd98）

【主 agent 复核追加】独立探针发现一处需求/实现冲突并已修正：市场/板块 scope 原被「纯观察→P3」压过，导致板块异动进不了 P2 知悉层（与需求 R1 冲突）；已把 scope 判定提到纯观察之前，治理项仍 P3。真值表对应行同步更正。

### 完成项

- 问题发现方式：主 agent 不依赖子代理测试文件，直接用 python 探针打契约（trend_observe + scope=sector 期望 P2、实得 P3）——子代理的测试把自身优先级写成了断言，所以自身全绿而契约不符
- 修正 level_resolver：优先级调整为 宪法级→持仓exit→买卖节点→L2→治理(P3)→市场/板块scope(P2)→纯观察(P3)→兜底(P2)；同步修 docstring 的优先级表与冲突示例
- 更正真值表旧行（观察压过市场 scope：P3→P2）与表内注释；新增 test_scope_outranks_observe_for_market_sector（5 条断言，含治理项不被 scope 拉高）
- 复验：pytest tests/domain/test_level_router.py → 49 passed；pytest tests/ -k watch → 271 passed

### 改动文件

- `quantsys-v2/domain/watch/services/level_resolver.py`
- `quantsys-v2/tests/domain/test_level_router.py`

### 下一步

t5 接入时仍须以 out_of_scope=True 作为策略账户的跳过信号（DB CHECK 只允许 autonomous/remind_only）

---
