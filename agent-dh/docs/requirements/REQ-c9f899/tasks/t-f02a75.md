# t-f02a75 判据 metric 化并建立消费侧契约

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 目标
判据 metric 化并建立消费侧契约

## 背景摘要（context）
（待补充）

## 范围
- 阶段：implement
- 端侧：backend

## 验收标准
运行 python -m pytest tests/services/test_metric_contract.py 通过；断言 price_break 命中的 result.metric == price；误用 metric 时抛出 watch_metric_contract_violation（断言拒绝而非静默）；规则 #162 场景回归断言不含「量能异常」

## 实施方案（implementation）
EvalResult 增加 metric/unit；conditions.py 各 handler 显式产出；EscalationPolicyService/渲染/统计按期望 metric 取数，不匹配即抛错；补故障注入用例

## 上游产出摘要（dependsSummary）
- （无依赖）

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-17T16:59:07.406Z，窗口 session-9b2b54f3-a3f4-4087-84f4-d1038fd1dd98）

判据 metric 化并建立消费侧契约：EvalResult 产出 MetricKind，消费侧读 value 前必须声明期望 metric；删除「量能异常」对任意条件类型的误读路径。11 条契约测试通过，#162 回归（现价不再被当量比）落地。

### 完成项

- domain/watch/models.py：新增 MetricKind 枚举（price/pct_change/pnl_pct/velocity_pct/volume_ratio/indicator_value/event_flag/sector_strength/composite/unknown）+ WatchMetricContractViolation
- domain/watch/services/metric_contract.py（新增）：require_metric（严格，违约抛错）/ metric_matches（宽松，可选路径）/ metric_of（缺失即 UNKNOWN，不猜）
- application/services/watch_engine/conditions.py：EvalResult 增 metric/unit 字段；6 类条件 handler 全部显式声明 metric
- domain/watch/services/escalation_checker.py：量能路径加 metric 守卫——非量能条件直接跳过（不再把现价当量比）；条件声明为 volume_surge 却给出错误 metric 则抛错（真违约）
- application/services/watch_engine/notifier.py：volume_ratio 字段仅在 metric 匹配时填充
- tests/services/test_metric_contract.py（新增，11 条）：各 handler metric 断言、#162 回归、require_metric 抛错、真放量仍能升级
- 实测：pytest tests/services/test_metric_contract.py -q → 11 passed
- 实测：pytest tests/ -k watch -q → 1 failed, 270 passed；同一命令在 HEAD 干净基线工作树 → 1 failed, 270 passed（同一用例 test_e2e_rule_to_trigger_to_notification）→ 零新增失败，该失败为既有 DB 状态依赖问题

### 改动文件

- `quantsys-v2/domain/watch/models.py`
- `quantsys-v2/domain/watch/services/metric_contract.py`
- `quantsys-v2/application/services/watch_engine/conditions.py`
- `quantsys-v2/domain/watch/services/escalation_checker.py`
- `quantsys-v2/application/services/watch_engine/notifier.py`
- `quantsys-v2/tests/services/test_metric_contract.py`

### 下一步

t7（升级策略收敛）依赖本任务，可删除四类自动升级；既有 e2e 用例对 DB 残留敏感，建议在 t12 一并修（去重按 symbol+direction 的跨规则合并语义要用受控数据），已记入验收单风险项

---
