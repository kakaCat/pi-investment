# t-8765b2 迁移脚本、开关与灰度演练

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 目标
迁移脚本、开关与灰度演练

## 背景摘要（context）
（待补充）

## 范围
- 阶段：implement
- 端侧：backend

## 验收标准
运行迁移脚本后断言未收敛积压数返回 0；连续执行两次第二次返回 0 变更；开关回滚演练记录写入 docs/requirements/REQ-c9f899/implementation.md

## 实施方案（implementation）
积压收敛脚本（escalated/pending→todo，rule_overlap→修规则 todo，其余 expired + 理由）；落地开关；测试环境演练回滚

## 上游产出摘要（dependsSummary）
- 新增四张表与两表加列
- TodoService 与仓储端口

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-17T17:48:55.047Z，窗口 session-9b2b54f3-a3f4-4087-84f4-d1038fd1dd98）

旧账收敛脚本（默认 dry-run）+ 开关回滚演练 + 收敛口径修正：真库演练 172 条孤儿积压全部可收敛；测试库 apply 后孤儿=0 且二次运行 0 变更（幂等）。演练 B 抓出并修复了 t10 用例不密封的问题。

### 完成项

- scripts/migrate_watch_backlog.py（新增）：未闭环触发 → 建待办（规则重叠→rule_change / 其余→observe）；悬空 rule_id 与策略账户 → expired 并写明理由；幂等（已有待办跳过）；**默认 dry-run，必须 --apply**；支持 --limit/--json（供 t12 自动化消费）
- 修正并写死收敛口径：**orphaned（未闭环且无待办载体）= 收敛目标 0**；unresolved_total 在待办被收敛前本就 > 0 —— 把 disposition 直接改终态是伪造"已处置"
- tests/migration/test_watch_backlog_convergence.py（新增 4 例）：dry-run 不改库；apply 后 orphaned_scoped=0；二次运行 changed=0（幂等）；策略账户/悬空 rule_id → expired 且有理由；rule_overlap → rule_change 待办（P1/L3）
- 真库 dry-run（不写）：scanned=172 / 可建待办 172 / expired 0 / errors 0 / orphaned=172（与已知积压数一致）
- 回滚演练 A（持久化=true）23+317 passed；C 默认全关 317 passed；B（心跳=false）首轮 4 红 → 定位为 t10 用例依赖环境默认值不密封 → 显式钉住开关后 19 passed
- 演练 C 的 e2e 红已归因：**0 处代码读取 WATCH_TODO_ENABLED/WATCH_SELF_HEAL_ENABLED**，且不带这两个开关时同一条 e2e 同样红 → 与开关无关，属既有状态脆弱用例
- 演练记录 + 测试基础设施欠账（含我自己那个迁移测试的并发脆弱性）写入 docs/requirements/REQ-c9f899/implementation.md §5
- 真实 apply **有意未执行**：新闭环未接线时把 172 条转成待办只会把"无人消费的触发"换成"无人消费的待办"；apply 属 t12 切换窗口第一步，命令与验收口径已就绪

### 改动文件

- `quantsys-v2/scripts/migrate_watch_backlog.py`
- `quantsys-v2/tests/migration/test_watch_backlog_convergence.py`
- `quantsys-v2/tests/jobs/test_heartbeat.py`
- `quantsys-v2/tests/api/test_watch_metrics.py`
- `docs/requirements/REQ-c9f899/implementation.md`

### 下一步

t12：切换窗口第一步 = 真实 apply 收敛 172 条 → 开 WATCH_TODO_ENABLED（当前无读取点，必须先接线）→ 注册 SLA 巡检与心跳巡检（进程外）→ 逐条销 implementation.md §6 的 10 项接线待办

---
