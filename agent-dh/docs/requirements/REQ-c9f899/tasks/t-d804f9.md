# t-d804f9 运行态持久化与启动恢复

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 目标
运行态持久化与启动恢复

## 背景摘要（context）
（待补充）

## 范围
- 阶段：implement
- 端侧：backend

## 验收标准
运行 python -m pytest tests/services/test_runtime_state.py 通过；重启进程后同一规则触发间隔 ≥ cooldown（断言不推送）；写库失败时 tick 不崩溃且返回降级标记

## 实施方案（implementation）
WatchRuntimeStateStore 端口 + 适配器；StateManager 改读 store；变化时批量 upsert；启动时恢复；写失败显式降级并告警

## 上游产出摘要（dependsSummary）
- 新增四张表与两表加列

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-17T17:23:13.314Z，窗口 session-9b2b54f3-a3f4-4087-84f4-d1038fd1dd98）

运行态持久化与启动恢复：闩锁/冷却基准落库并在启动时恢复（开关默认关，关掉行为等价）；写库失败响亮降级不致命。主 agent 独立探针复核通过。

### 完成项

- domain/watch/ports.py：追加 IWatchRuntimeStateStore（load_all/upsert_many/load_meta/save_meta）
- adapters/outbound/repositories/watch_runtime_state_repository.py（新增）：pg_insert + on_conflict_do_update((rule_id,cond_idx))；meta 单行 upsert；失败先 rollback 再 re-raise（SQL 只在适配器层）
- state_manager.py：store 可选注入；restore_from_store()（含 TIMESTAMPTZ aware→naive 归一化——否则 now-last 会 TypeError 打挂 tick）；flush() 只写脏键、无脏键不调 store；写失败 → log + degraded=True + 返回 False（不抛错）
- engine.py：开关 WATCH_RUNTIME_PERSIST_ENABLED（默认 false）；显式注入 store 即启用；run_forever 启动 restore（幂等 flag）；tick 末尾 flush
- tests/services/test_runtime_state.py（新增 23 用例）
- 子代理故障注入（monkeypatch，未改文件）：flush 恒 True → 10 failed；restore 恒 0 → 8 failed；开关恒 False → 1 failed → 用例有牙
- 主 agent 独立探针（不依赖其测试文件）：①恢复后冷却内 should_emit=False ✅ ②冷却窗边界 10s/299s 不推送、400s 可推送 ✅ ③写库失败 flush 返回 False + degraded=True 且不抛异常 ✅ ④无脏键时 store 调用数=0（节流）✅
- 主 agent 复核文件范围：仅声明的 5 个文件；全量 watch 288 passed（271 基线 + t9 新文件 17 条命中 -k watch），剔除后严格 271 零新增失败
- 注：探针首轮一处「失败」经查是我自己预期写错（保留 latched=True 时闩锁优先于冷却，属正确的电平保持语义），非实现缺陷

### 改动文件

- `quantsys-v2/domain/watch/ports.py`
- `quantsys-v2/adapters/outbound/repositories/watch_runtime_state_repository.py`
- `quantsys-v2/application/services/watch_engine/state_manager.py`
- `quantsys-v2/application/services/watch_engine/engine.py`
- `quantsys-v2/tests/services/test_runtime_state.py`

### 下一步

t10（心跳告警）消费 load_meta/save_meta；t12 需在 factory 显式装配 WatchRuntimeStateRepository 并做真实进程重启端到端验证；去重窗/事件窗口/velocity 价格历史跨重启仍待补（本轮卡范围外）

---
