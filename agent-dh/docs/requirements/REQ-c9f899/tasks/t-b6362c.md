# t-b6362c 新增四张表与两表加列

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 目标
新增四张表与两表加列

## 背景摘要（context）
（待补充）

## 范围
- 阶段：implement
- 端侧：backend

## 验收标准
运行 python -m pytest tests/ -k watch 零新增失败；迁移脚本连续执行两次，第二次返回 0 变更；断言 information_schema 中四张新表与新增列存在且约束一致

## 实施方案（implementation）
新增 migrations：watch_todos/watch_runtime_state/watch_rule_changes/watch_receipts；ALTER watch_triggers 加 metric/metric_unit/level/todo_id/suppressed；ALTER watch_rules 加 noise_state/suppress_until/self_heal_count/last_repair_at；ORM 模型同步

## 上游产出摘要（dependsSummary）
- （无依赖）

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-17T16:51:22.213Z，窗口 session-9b2b54f3-a3f4-4087-84f4-d1038fd1dd98）

新增盯盘闭环底座：4 张核心表（watch_todos / watch_runtime_state / watch_rule_changes / watch_receipts）+ 1 张运行态单行 meta（watch_runtime_meta），watch_triggers 加 5 列、watch_rules 加 4 列，6 个索引；迁移幂等（20 变更 → 0 变更），watch 测试零新增失败。

### 完成项

- 新增 migrations/20260918_watch_todo_loop.py（additive + 幂等：CREATE TABLE IF NOT EXISTS / ADD COLUMN IF NOT EXISTS / 变更计数）
- 新增 orm/models/watch_todo.py（5 个模型，含 level/closed_pair/ignored_needs_next/action_needs_audit 三条 CHECK）
- 注册模型到 orm/models/__init__.py 导出
- 踩坑修复：current_date 为 PG 保留字 → 列名改 state_date（迁移事务已回滚，无残留）
- 实测：迁移第 1 次 20 变更、第 2 次 0 变更；information_schema 断言 5 表 + 9 列存在；watch_todos 约束名可见
- 实测：python -m pytest tests/ -k watch -q → 271 passed（零新增失败）

### 改动文件

- `quantsys-v2/infrastructure/persistence/migrations/20260918_watch_todo_loop.py`
- `quantsys-v2/infrastructure/persistence/orm/models/watch_todo.py`
- `quantsys-v2/infrastructure/persistence/orm/models/__init__.py`

### 下一步

t2（判据 metric 化）与 t4（级别/路由纯函数）继续；t3/t5/t11 依赖本任务已完成

---
