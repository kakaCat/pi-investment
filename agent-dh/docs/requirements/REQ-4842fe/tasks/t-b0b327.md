# t-b0b327 domain：stageKind 枚举与子卡映射表

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
domain：stageKind 枚举与子卡映射表

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：implement
- 端侧：backend

## 得到什么结果
pnpm vitest run 中 SubtaskTemplate 用例全绿：8 类映射正确、未映射回退 dev→review、非法 stages（自由文本/空/重复）分别返回明确错误

## 实施方案（implementation）
新增 src/domain/task/SubtaskTemplate.ts（纯数据+纯函数，零 I/O）：StageKind 联合类型、类型→子卡集合映射表（feature/refactor/bug/doc/chore/spike/research/data/ops/review-only，未映射回退 dev→review）、validateExplicitStages（枚举/非空/去重）、buildSubtaskSpecs（含链内依赖与 acceptance 模板）。新增 tests/SubtaskTemplate.spec.ts。

## 上游产出摘要（dependsSummary）
- （无依赖）

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-20T18:24:33.982Z，窗口 session-d41c9696-eb63-4a47-9018-68884d1f1fc8）

交付子卡模板映射表（纯 domain）：16 类 stageKind 受控枚举、11 种卡类型的子卡链映射（未映射回退 dev→review）、显式 stages 逃生舱口校验、子卡规格生成（含链内依赖与可证伪验收模板）。TDD：先写测试跑红，再实现转绿。

### 完成项

- 新增 src/domain/task/SubtaskTemplate.ts（纯数据 + 纯函数，零 I/O、不 import @deepseek-ai）
- 新增 tests/domain/subtask-template.test.ts 共 12 个用例，全部通过（含 1.1 八类映射、1.2 未映射回退、1.3/1.4 逃生舱口校验、链依赖与验收模板锚点）
- 顺序口径按用户裁定落地：review 在测试段之前，无测试段的以 review 收尾
- 修正两处测试假设与模板锚点问题（1.1b 断言与裁定不一致；fix/change 验收模板缺可证伪锚点）
- npx tsc --noEmit 通过；pnpm build:client 重建 lib/client.js 并过 verify-client

### 改动文件

- `packages/pages/dsh-pmboard/src/domain/task/SubtaskTemplate.ts`
- `packages/pages/dsh-pmboard/tests/domain/subtask-template.test.ts`

### 下一步

t2（t-89704e）子卡四态状态机与人工门，或 t3/t4（可并行）

---
