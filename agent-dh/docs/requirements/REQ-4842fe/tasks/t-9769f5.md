# t-9769f5 懒展开：父卡开工同事务落子卡链

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
懒展开：父卡开工同事务落子卡链

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：implement
- 端侧：backend

## 得到什么结果
用例 tests/LazyExpand.spec.ts 全绿：开工后查询返回完整子卡链且 stageKind 顺序与映射表一致；展开与状态变更落在同一 revision；重复开工后子卡数量不变（不产生第二套）

## 实施方案（implementation）
改 application/use-cases/MoveTask.ts：普通卡转 in_progress 时在同一 mutate 内按映射表或显式 stages 落子卡链（首卡继承父卡 dependsOn、链内前序依赖、acceptance 模板），写留痕评论；已有子卡则幂等跳过。

## 上游产出摘要（dependsSummary）
- 数据契约扩展与迁移兼容

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-20T18:33:33.823Z，窗口 session-d41c9696-eb63-4a47-9018-68884d1f1fc8）

父卡一开工，它下面的子卡链就自动出现，而且和「开工」这一步是同一个事务——要么一起成功，要么都没有，不会出现「卡开工了但子卡没落」的半截状态。重复开工不会落出第二套子卡。默认按需求类型决定子卡组合，计划里显式写了阶段就用计划里的；开自动链（autoRun）才会走这套，手动流程还是原来的做法。

### 完成项

- 新增 application/internal/lazy-expand.ts：resolveSubtaskStages（显式 stages 优先 + skipIntegration 跳过联调）+ expandSubtasks（链内依赖成串、链首继承父卡外部依赖、acceptance 模板、幂等）
- MoveTask 在 to=in_progress 的同一 mutate 内展开子卡链并写留痕评论，changed/revision 一致
- 按 autoRun 开关分模：autoRun=true 才懒展开（存量/手动流程保持五段状态机，全量回归 1469 例证明未回归）
- 角色判定 taskRoleIn（子卡 / 有子卡的新式父卡 / 存量卡）+ 新增 PARENT_TRANSITIONS（父卡只走 todo→in_progress→done）
- 协议增 PlanTask.stages 与 TaskRecord.stages（FR-1b 逃生舱口透传），normalizePlanTasks 用 validateExplicitStages 校验
- 新增 tests/lazy-expand.test.ts（9 例），并按仓库 vitest include 约定用 .test.ts 命名（分解决议里的 .spec.ts 不会被收集）

### 改动文件

- `packages/pages/dsh-pmboard/src/application/internal/lazy-expand.ts`
- `packages/pages/dsh-pmboard/src/application/use-cases/MoveTask.ts`
- `packages/pages/dsh-pmboard/src/domain/task/TaskStatus.ts`
- `packages/pages/dsh-pmboard/src/shared/protocol.ts`
- `packages/pages/dsh-pmboard/tests/lazy-expand.test.ts`
- `packages/pages/dsh-pmboard/tests/domain/subtask-status.test.ts`
- `packages/pages/dsh-pmboard/tests/execute-task.test.ts`

### 下一步

t7 AdvanceChain 事件链执行器（单飞锁/幂等/恢复扫描/熔断）

---
