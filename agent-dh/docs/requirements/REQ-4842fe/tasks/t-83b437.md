# t-83b437 并发上限与冲突两级防线

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
并发上限与冲突两级防线

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：implement
- 端侧：backend

## 得到什么结果

同需求第 4 张父卡开工被拒（REQBOARD_PARENT_LIMIT）；两张互不依赖父卡的链并行且都完成后 rollup 正常；跨父卡依赖被拒；拆分期重叠拒绝并列出冲突文件；构造 mtime 冲突则该子卡判失败并暂停
验证：npx vitest run tests/concurrency-limits.test.ts tests/decompose-tools.test.ts → 全绿。

## 实施方案（implementation）
domain/limits.ts 增 MAX_PARALLEL_PARENTS=3；MoveTask 开工校验同需求 in_progress 父卡数；decompose 增冲突拦截；运行期兜底（mtime 跨卡覆盖判失败）；子卡 dependsOn 禁跨父卡。

## 上游产出摘要（dependsSummary）
- AdvanceChain 用例：事件链执行器

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-20T18:39:37.353Z，窗口 session-d41c9696-eb63-4a47-9018-68884d1f1fc8）

同一需求不会一次开太多张父卡（超过 3 张就等一等）；两张互不依赖的父卡可以同时跑各自的分支，都完成后照常进入验收；如果两张并行卡改到同一个文件，拆分阶段就会被挡下来（并列清楚是哪个文件），来不及挡住的运行期会用文件时间戳兜底判断并停链；子卡不能依赖另一张父卡下面的子卡。

### 完成项

- MoveTask 开工校验同需求 in_progress 父卡数 ≤ LIMITS.advanceMaxParallelParents，超限 reject REQBOARD_PARENT_LIMIT
- 新增 application/internal/conflict-check.ts：declaredFiles + findWorkSurfaceConflicts（依赖链上串行的卡不算冲突）+ describeConflicts；Decompose 在落库前拦截并列出冲突文件（REQBOARD_FILE_CONFLICT）
- 新增 application/internal/cross-card.ts：detectCrossCardOverwrite（mtime 落在另一在跑父卡子卡窗口内）；ExecuteTask 命中即判失败 REQBOARD_CROSS_CARD
- protocol checkSubtaskInvariants 增 INV-4（子卡依赖不跨父卡）
- 新增 tests/concurrency-limits.test.ts（7 例，含父卡并行 rollup、拆分冲突、跨卡覆盖）

### 改动文件

- `packages/pages/dsh-pmboard/src/application/internal/conflict-check.ts`
- `packages/pages/dsh-pmboard/src/application/internal/cross-card.ts`
- `packages/pages/dsh-pmboard/src/application/use-cases/Decompose.ts`
- `packages/pages/dsh-pmboard/src/application/use-cases/MoveTask.ts`
- `packages/pages/dsh-pmboard/src/application/use-cases/ExecuteTask.ts`
- `packages/pages/dsh-pmboard/src/shared/protocol.ts`
- `packages/pages/dsh-pmboard/tests/concurrency-limits.test.ts`

### 下一步

t10 工具面（reqboard_task_run）与批准计划接线

---
