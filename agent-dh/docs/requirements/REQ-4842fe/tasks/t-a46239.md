# t-a46239 工具面与批准计划接线（合并拆分确认门）

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
工具面与批准计划接线（合并拆分确认门）

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：implement
- 端侧：backend

## 得到什么结果

批准计划后不再调用任何人工工具即可跑到 accepting；全程不出现「确认拆分清单」弹框；批准弹框文案含自动开跑说明；新工具 schema 冒烟通过；代码中不存在 ctx.tools.workflow 调用
验证：npx vitest run tests/auto-chain-approval.test.ts；根目录 npx vitest run tests/plugin-schema.smoke.test.ts（20/20）；grep -rn "ctx.tools.workflow" packages/pages/dsh-pmboard/src → 仅注释。

## 实施方案（implementation）
新增 tools/AdvanceTool（reqboard_task_run）并注册到 tools/index.ts、src/index.ts 与冒烟测试 PLUGINS；改造批准计划路径：批准即自动 decompose + autoRun=true + 触发首个事件，decomposing>implementing 随批准放行；TaskExecuteTool 下线或改薄壳。

## 上游产出摘要（dependsSummary）
- AdvanceChain 用例：事件链执行器

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-20T18:51:43.448Z，窗口 session-d41c9696-eb63-4a47-9018-68884d1f1fc8）

批准计划现在是这条链上唯一的人工动作：按下它，系统自动拆卡、自动开跑，中途不再弹「确认拆分清单」；同时把新工具 schema 的**真门**补上了——此前 dsh-pmboard 根本不在插件 schema 冒烟名单里，新工具 schema 其实无人编译（属"假绿"），现已是 20/20 实覆盖。

### 完成项

- 新增 tools/AdvanceTool（reqboard_task_run）：一次调用执行当前 ready 的一张子卡，产出回写
- 批准计划⇄拆分确认门合并：批准即自动落章 decomposition + 自动 decompose + autoRun=true + 触发首个推进事件；弹框写明"批准后将自动拆分任务卡并立即开跑，中途不再打断"
- TaskExecuteTool 下线为兼容薄壳：不再依赖被禁用的 ctx.tools.workflow、不再生成脚本；执行入口唯一收敛到 reqboard_task_run
- 补冒烟真门：dsh-pmboard 加入 tests/plugin-schema.smoke.test.ts 的 PLUGINS，并让 stub 支持函数插件形态与可调用 logger → 20/20 通过（13 个 reqboard 工具 schema 全部实编译）
- 验证：pmboard 全量 125 文件 1500 例通过；tsc --noEmit 干净；dist/lib 重建（02:50）且过 bundle 哨兵；代码中无 ctx.tools.workflow 调用（仅 2 处注释提及）

### 改动文件

- `packages/pages/dsh-pmboard/src/tools/AdvanceTool/AdvanceTool.ts`
- `packages/pages/dsh-pmboard/src/tools/AdvanceTool/prompt.ts`
- `packages/pages/dsh-pmboard/src/tools/AdvanceTool/index.ts`
- `packages/pages/dsh-pmboard/src/tools/index.ts`
- `packages/pages/dsh-pmboard/src/index.ts`
- `packages/pages/dsh-pmboard/src/application/use-cases/AskConfirm.ts`
- `packages/pages/dsh-pmboard/src/tools/TaskExecuteTool/TaskExecuteTool.ts`
- `packages/pages/dsh-pmboard/tests/auto-chain-approval.test.ts`
- `packages/pages/dsh-pmboard/tests/apply-wiring.test.ts`
- `packages/pages/dsh-pmboard/tests/tools-dispatch.test.ts`
- `packages/pages/dsh-pmboard/tests/output-contract.test.ts`
- `tests/plugin-schema.smoke.test.ts`

### 下一步

本卡完成。但需求仍有两张 todo 卡：t-3be71b（看板 UI：父子卡展示与 autoRun 控制面）、t-fd25e0（端到端验收与文档更新）——rollup 未满足，需求暂时进不了 accepting。

---
