# t-1819f2 AdvanceChain 用例：事件链执行器

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
AdvanceChain 用例：事件链执行器

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：implement
- 端侧：backend

## 得到什么结果

重复触发同一事件为 noop 且 revision 不变；并发触发被单飞锁挡下（无双重执行）；连续 noop 达阈值触发熔断；杀进程后重启可续跑至完成；autoRun=false 后无新事件、置回 true 并触发一次即续跑
验证：npx vitest run tests/advance-chain.test.ts（幂等/单飞锁/熔断/恢复续跑/autoRun 开关各例全绿）。

## 实施方案（implementation）
新增 application/use-cases/AdvanceChain.ts：事件类型 OPEN_PARENT/RUN_SUBTASK/FINALIZE_PARENT/ROLLUP/PAUSE；每需求单飞锁；幂等选择；启动恢复扫描；停滞熔断；事件日志写 advance-log.md 与台账。

## 上游产出摘要（dependsSummary）
- ExecuteTask 用例：单张子卡闭环
- 懒展开：父卡开工同事务落子卡链

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-20T18:36:16.473Z，窗口 session-d41c9696-eb63-4a47-9018-68884d1f1fc8）

需求批准后不再需要人喊下一步：系统自己判断「现在该干什么」（开哪张父卡、跑哪张子卡、收尾、还是滚进验收），干完一步自动接着干下一步，一路跑到需求进入验收才停。同一件事重复触发不会重复干；有人在跑时别人插不进来；卡住太久会自动停下并标出原因；进程重启后能从中断处接着跑。

### 完成项

- 新增 application/use-cases/AdvanceChain.ts：事件类型 OPEN_PARENT/RUN_SUBTASK/FINALIZE_PARENT/ROLLUP/PAUSE；循环自动触发下一步；事件历史 + advance-log.md 留痕
- 单飞：进程内 inflight Set + 台账 advance.lockAt（stale 15min 可接管）；纯终态重复触发不落盘（revision 不变）
- 幂等选择：新增 application/internal/advance-select.ts 纯函数选择器（状态即事实）
- 停滞熔断：有开放工作但无可推进事件累计 noop，达阈值 PAUSE + autoRun=false + pausedReason=stagnation
- 失败即暂停：子卡失败 → autoRun=false + pausedReason=fail（告警/回弹/返工由 t8 补齐）
- 启动恢复扫描 scanAndResume + 接线 application/internal/startup-scan.ts（index.ts 一行调用）
- SYSTEM_TASK_TRANSITIONS 增 in_progress>done（执行器以 system 身份收尾）；LIMITS 增 advance* 阈值
- 新增 tests/advance-chain.test.ts（7 例），全量 121 文件 1476 例通过

### 改动文件

- `packages/pages/dsh-pmboard/src/application/use-cases/AdvanceChain.ts`
- `packages/pages/dsh-pmboard/src/application/internal/advance-select.ts`
- `packages/pages/dsh-pmboard/src/application/internal/startup-scan.ts`
- `packages/pages/dsh-pmboard/src/domain/task/TaskStatus.ts`
- `packages/pages/dsh-pmboard/src/domain/limits.ts`
- `packages/pages/dsh-pmboard/src/index.ts`
- `packages/pages/dsh-pmboard/tests/advance-chain.test.ts`

### 下一步

t8 失败暂停、告警弹框与返工回上游（子卡回退+attempt+1+revisions、飞书高优告警、三选处置弹框）

---
