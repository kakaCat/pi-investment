# t-89704e domain：子卡四态状态机 + 人工门 + 需求回退转移

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
domain：子卡四态状态机 + 人工门 + 需求回退转移

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：implement
- 端侧：backend

## 得到什么结果

子卡 in_progress→integrating 被拒；子卡失败 in_progress→todo 成功且 attempt+1；done→in_progress 仅人（agent 调用返回 REQBOARD_HUMAN_GATE）；implementing→design 仅人；legacy 卡转移行为不变
验证：cd packages/pages/dsh-pmboard && npx vitest run tests/domain/subtask-status.test.ts tests/gate-catalog.test.ts → 全绿。

## 实施方案（implementation）
改 src/domain/task/TaskStatus.ts：新增 SUBTASK_TRANSITIONS 与 role 参数，子卡禁入 integrating/testing/in_review；HUMAN_ONLY_TASK_TRANSITIONS 增 done>in_progress、done>canceled。改 src/domain/requirement/RequirementStatus.ts：implementing 增加 design 并列入 HUMAN_ONLY_REQ_TRANSITIONS。补单测。

## 上游产出摘要（dependsSummary）
- domain：stageKind 枚举与子卡映射表

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-20T18:26:36.870Z，窗口 session-d41c9696-eb63-4a47-9018-68884d1f1fc8）

子卡有了自己的四态状态机（待办→进行中→完成，失败可退回待办），不会再被塞进「联调/测试/验收」三个中段状态；「完成卡重开」「验收后取消」「实施退回设计重新描述需求」这三类破坏性动作变为只有人能触发，agent 调用会被明确拒绝。存量的老卡走法完全不变。

### 完成项

- TaskStatus.ts 新增 TaskRole(parent/subtask/legacy) 与 SUBTASK_TRANSITIONS：子卡 in_progress→integrating/testing/in_review 一律 invalid_transition
- TaskStatus.ts done 由绝对终态改为 ['in_progress','canceled']，并把 done>in_progress、done>canceled 列入 HUMAN_ONLY_TASK_TRANSITIONS
- canTaskTransition/assertTaskTransition 增 role 入参（缺省 legacy，既有调用点零改动）
- RequirementStatus.ts implementing 增 design 并列入 HUMAN_ONLY_REQ_TRANSITIONS（返工回上游，仅人）
- 新增 tests/domain/subtask-status.test.ts（11 例，覆盖 FR-5/FR-14/FR-15 与 legacy 兼容）
- 顺带修复 t1 遗留：SubtaskTemplate.ts 两处拼接式消息改 fmt（message-hygiene 门禁）；gate-catalog 反向锁区分「回退类」与「流水线确认门」

### 改动文件

- `packages/pages/dsh-pmboard/src/domain/task/TaskStatus.ts`
- `packages/pages/dsh-pmboard/src/domain/requirement/RequirementStatus.ts`
- `packages/pages/dsh-pmboard/src/domain/task/SubtaskTemplate.ts`
- `packages/pages/dsh-pmboard/tests/domain/subtask-status.test.ts`
- `packages/pages/dsh-pmboard/tests/gate-catalog.test.ts`

### 下一步

t3 数据契约扩展（parentId/stageKind/attempt/revisions + autoRun）；子卡失败回退的 attempt+1 落点在该字段与 move/execute 写入侧

---
