# t-b05389 数据契约扩展与迁移兼容

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
数据契约扩展与迁移兼容

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：implement
- 端侧：backend

## 得到什么结果

旧台账（无新字段）可读且看板渲染正常、行为不变；重复触发懒展开子卡数量不变；悬空 parentId 触发 INV-1 校验错误
验证：npx vitest run tests/acceptance-criteria.test.ts（老台账兼容读）；真实台账副本渲染：npx tsx /tmp/render-check.mts（输出 requirements/tasks 计数且零异常，脚本见 docs/requirements/REQ-4842fe/verification.md §3）。

## 实施方案（implementation）
改 src/shared/protocol.ts：TaskRecord 增 parentId/stageKind/attempt/revisions（CardRevision，append-only），RequirementRecord 增 autoRun；补 INV-1/2/3 校验。

## 上游产出摘要（dependsSummary）
- domain：stageKind 枚举与子卡映射表

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-20T18:27:42.643Z，窗口 session-d41c9696-eb63-4a47-9018-68884d1f1fc8）

任务卡下多了一层「子卡」，并且加了三道机械校验：子卡必须挂在一张真实存在的父卡下（否则报错而不是悄悄变孤儿）、子卡的阶段标记与父卡互斥、同一父卡下不会因重复展开而落出两套同样的子卡。新字段全部可选，旧看板数据读出来行为完全不变。

### 完成项

- protocol.ts 新增 CardRevision（append-only 修订记录）与 TaskRecord 的 parentId/stageKind/attempt/revisions 可选字段
- RequirementRecord 新增 autoRun（自动链开关）与 advance 运行状态容器
- 新增 AdvanceEvent/AdvanceRecord/AdvanceState 类型（推进事件留痕契约）
- 新增 taskRoleOf/isSubtask/subtasksOf 角色判定与派生（父卡不存子卡列表，由 parentId 反查）
- 新增 checkSubtaskInvariants/assertSubtaskInvariants（INV-1 悬空父卡 / INV-2 角色字段互斥 / INV-3 幂等展开）
- StageTaskRef 增 parentId/stageKind/attempt 供看板展示
- 新增 tests/subtask-contract.test.ts（8 例）覆盖契约与不变量；typecheck 零错误

### 改动文件

- `packages/pages/dsh-pmboard/src/shared/protocol.ts`
- `packages/pages/dsh-pmboard/tests/subtask-contract.test.ts`

### 下一步

t4 WorkflowRunner 端口 + 引擎 adapter + 脚本契约门禁

---
