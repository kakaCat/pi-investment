# t-1f45e6 在 Hook 注入链路的 5 个关键节点添加诊断日志

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
在 Hook 注入链路的 5 个关键节点添加诊断日志

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：implement
- 端侧：backend

## 得到什么结果

怎么验（可执行）：
1. 命令：cd agent-dh/packages/pages/dsh-pmboard && grep -c "captureDiag" dist/index.mjs → 输出 ≥10（5 节点 + EARLY 均已接入双写）
2. 命令：cat agent-dh/.dsh-data/state/reqboard-capture-diag.log | grep -E "EARLY|NODE-[1-5]" → 每类标记至少 1 条（EARLY/NODE-1/NODE-2/NODE-4/NODE-5 均有实录）
3. 界面路径：打开任意 unbound 窗口发消息 → 上述 diag 文件实时新增 NODE-2/3/4/5 行（tail -f 可见）

## 实施方案（implementation）
在 5 个关键节点添加 debug 日志：节点1(src/index.ts Hook订阅检查)、节点2(src/adapters/CaptureHook.ts 事件到达)、节点3(同文件 pendingCapture填充)、节点4(src/gate-wiring.ts systemPrompt组装)、节点5(src/application/internal/capture-section.ts 提示词生成)。日志包含订阅状态/事件到达/判定结果/windowKey提取/文本长度等关键信息

## 上游产出摘要（dependsSummary）
- （无依赖）

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-21T07:21:49.234Z，窗口 session-49bdb1dd-08e1-4ac1-a28a-daf1db28f99a）

在 Hook 注入链路的 5 个关键节点成功添加诊断日志，每个节点日志包含关键状态信息（触发状态、值、窗口Key等），格式统一且可追溯

### 完成项

- 节点1 (src/index.ts 第306行): Hook 订阅状态检查 - 记录订阅成功/失败与 unsubscribe 类型
- 节点2 (src/adapters/CaptureHook.ts 第227行): user/message 事件到达检查 - 记录事件类型与窗口Key
- 节点3 (src/adapters/CaptureHook.ts 第310行): pendingCapture 填充状态 - 记录 Map 大小、文本长度、是否覆盖
- 节点4 (src/gate-wiring.ts 第110行): systemPrompt 组装时 windowKey 提取 - 记录窗口Key、pending状态、Map大小
- 节点5 (src/application/internal/capture-section.ts 第60-78行): 提示词生成返回 - 分支记录返回空串原因或动态提示词长度

### 改动文件

- `src/index.ts`
- `src/adapters/CaptureHook.ts`
- `src/gate-wiring.ts`
- `src/application/internal/capture-section.ts`

### 下一步

执行诊断测试（T2: t-29e204），在 unbound 窗口发送测试消息，收集日志分析断链位置

---
