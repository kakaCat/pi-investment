# t-20a647 FR-8 验收失败自动回退实施（application）

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
FR-8 验收失败自动回退实施（application）

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：implement
- 端侧：backend

## 得到什么结果
跑 pnpm --filter dsh-pmboard test，tests/verdicts-and-rework.test.ts 的 T-I1/T-I2/T-I3/T-I4 全绿；T-I2 故障注入断言整笔回滚（状态与任务均不变）。

## 实施方案（implementation）
src/application/internal/verdicts.ts：applyVerdicts 出现 failed 时同笔 mutate 内调用 reworkSpecsFor+materializeReworkTask 建返工卡，并用 transitionRequirement 把 accepting→implementing（状态事件+快照结算）；AcceptSheet.ts 调。

## 上游产出摘要（dependsSummary）
- 扩展验收项状态与通过判据（domain）

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
