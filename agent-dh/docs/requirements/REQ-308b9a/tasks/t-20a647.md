# t-20a647 FR-8 验收失败自动回退实施（application）

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
FR-8 验收失败自动回退实施（application）

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：implement
- 端侧：backend

## 得到什么结果
跑 pnpm --filter dsh-pmboard test，tests/verdicts-and-rework.test.ts 的 T-I1/T-I2/T-I3/T-I4 全绿；T-I2 故障注入断言整笔回滚（状态与任务均不变）。

## 实施方案（implementation）
src/application/internal/verdicts.ts：applyVerdicts 出现 failed 时同笔 mutate 内调用 reworkSpecsFor+materializeReworkTask 建返工卡，并用 transitionRequirement 把 accepting→implementing（状态事件+快照结算）；AcceptSheet.ts 调。

## 上游产出摘要（dependsSummary）
- 扩展验收项状态与通过判据（domain）

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-20T02:20:55.884Z，窗口 session-7cfa4169-c6dd-4608-b5c4-fb5af4f69a54）

FR-8 落地：逐项裁决含 failed 时自动回退实施并物化返工卡；同时收敛看板路由的重复裁决实现（原先内联了一份，与用例层漂移）。

### 完成项

- application/internal/verdicts.ts: applyVerdicts 出现 failed → 同笔 mutate 内 materializeReworkFromSheet + transitionRequirement(implementing)，返回真实返工卡
- http/routers/verdicts.ts: 删除内联裁决实现，改为委托 applyVerdicts（消除双实现漂移，t6 一并完成）
- AcceptSheet.ts: 文案改自动回退口径；最终确认判据改为「无 pending」（FR-9/AC-9.3）
- not_verifiable 纳入可裁决状态并强制填原因（路由 + 用例）
- tests: 新增 T-I1（自动回退+建卡）/T-I2（故障注入原子性）/T-I3（留痕）/T-I4（not_verifiable 不回退）；改写受 FR-8 影响的 2 个既有用例

### 改动文件

- `packages/pages/dsh-pmboard/src/application/internal/verdicts.ts`
- `packages/pages/dsh-pmboard/src/http/routers/verdicts.ts`
- `packages/pages/dsh-pmboard/src/application/use-cases/AcceptSheet.ts`
- `packages/pages/dsh-pmboard/tests/verdicts-and-rework.test.ts`
- `packages/pages/dsh-pmboard/tests/accept-sheet-tool.test.ts`
- `packages/pages/dsh-pmboard/lib/client.js`
- `packages/pages/dsh-pmboard/lib/client.cjs`

### 下一步

t3：verification.md 结构化生成 + 9 类文档完整性检查

---
