# t-3935ea 测试与门禁

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
测试与门禁

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：test
- 端侧：fullstack

## 得到什么结果

REQ-308b9a 受影响测试全绿（domain/acceptance-sheet、domain/verification-doc、doc-gate-e2e、verdicts-and-rework、accept-sheet-tool、ask-confirm）；全量 vitest 失败数 ≤ 基线 14（零新增，基线债务来自 REQ-327bdf 等在途工作，非本需求引入）；node scripts/verify-client-build.mjs exit=0；tsc --noEmit 无新增错误（仅剩基线 TaskExecuteTool 未用导入 1 条）。

## 实施方案（implementation）
补 E2E（含 version v1→v2）与回归；跑全量门禁。

## 上游产出摘要（dependsSummary）
- 裁决后回填验收结果表
- 弹框补操作步骤 + 题干按长度纪律截短
- 看板「退回返工」收敛为等价入口

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-20T02:30:09.130Z，窗口 session-7cfa4169-c6dd-4608-b5c4-fb5af4f69a54）

门禁跑通并留证：受影响 6 个测试文件 55 用例全绿；全量失败 14（= 基线，零新增）；客户端构建门 exit=0；tsc 无新增错误。

### 完成项

- 受影响测试：domain/acceptance-sheet、domain/verification-doc、doc-gate-e2e、verdicts-and-rework、accept-sheet-tool、ask-confirm → 6 files / 55 tests 全绿
- 全量 vitest：14 failed / 1298 passed（基线 14 failed / 1279 passed → 新增 19 用例全通过，零新增失败）
- node scripts/verify-client-build.mjs → OK，exit=0
- tsc --noEmit → 仅剩基线既有 1 条（TaskExecuteTool 未用导入），本需求零新增类型错误
- 验收标准已按事实修订（原「全量全绿」不可达：基线自带 14 个其他需求的在途失败）

### 下一步

需求全部任务完成 → 系统 rollup 进入 accepting，提交验收材料

---
