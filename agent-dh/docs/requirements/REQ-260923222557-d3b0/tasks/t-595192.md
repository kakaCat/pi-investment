# t-595192 面板文档行：已交可点、未交占位

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
面板文档行：已交可点、未交占位

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：ui
- 端侧：frontend

## 得到什么结果
TC-6/TC-7 绿；lib/client.js 重建且 verify-client-build 通过；线上打开本需求详情，✅ 行可点击右栏打开。

## 实施方案（implementation）
两处渲染函数修改；tests 增 TC-6/TC-7；pnpm build:client 重建 lib/client.js。验证：用例断言 data-action=open-doc 出现在已交行、占位行文案出现在无文档时。

## 上游产出摘要（dependsSummary）
- （无依赖）

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-23T15:54:25.361Z，窗口 session-959cebfe-094f-4f09-894d-20a5c35d4d62）

文档行不再"看得见点不开"：已交的设计文档整行可点开（走右侧栏），未交的就地显示灰字占位；拆分节点没有拆分计划时也显示一行「未交」，不再整个文档行凭空消失。

### 完成项

- renderDesignInfo（FR-9）：submitted=true 的行改由 docItem() 渲染——整行是按钮，带 data-action="open-doc" + 目标路径 + ✅ 图标，点开走既有右侧栏链路；未交行保持灰字占位（⬜ + 文件名），不带打开动作，一眼能分清「能点」和「还没交」
- 保留 data-submitted="yes/no" 机器标记（docItem 新增可选附加属性位），既有依赖该标记的断言与样式（[data-submitted="no"] 灰字）不失效
- 条件必交〔条件·frontend/backend〕与（已豁免）标注继续随文件名显示，点击目标路径不受影响
- renderDecomposingInfo（FR-9）：无 decompositionDoc 时渲染占位行「⬜ 拆分计划：decomposition.md（未交）」，不再只显示 DAG 让文档行消失；有文档时仍为可点条目
- 测试：node-panel 新增 3 条（TC-6 已交行含 open-doc 且未交行无打开动作 / TC-7 无文档出占位行 / TC-7b 有文档可点且无占位），面板两套件 81/81 绿
- lib/client.js 已重建（285113 字节），verify-client-build 通过；产物 grep 命中新文案「⬜ 拆分计划：decomposition.md（未交）」1 处、「data-submitted」3 处、「未开始」1 处

### 改动文件

- `packages/web/dsh-pmboard/src/client/node-panel.ts`
- `packages/web/dsh-pmboard/tests/node-panel.test.ts`
- `packages/web/dsh-pmboard/lib/client.js`

### 下一步

t6：测试补齐与全量回归（含既有失败归属核验）

---
