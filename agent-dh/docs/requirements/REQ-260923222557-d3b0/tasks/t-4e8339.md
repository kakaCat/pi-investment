# t-4e8339 兼容核验 + 构建部署与线上验收

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
兼容核验 + 构建部署与线上验收

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：merge
- 端侧：fullstack

## 得到什么结果
旧管线文案不变（截图/curl 证据）；线上本需求面板行为符合 FR-8/9/10；弹框 >10min 不超时；R-017 核验结论留痕。

## 实施方案（implementation）
①打开 REQ-47939a 与某 pending 节点核验文案；②构建+门禁；③launchctl kickstart -k 重启；④R-017 noop 核验（只读抽样 regime_position_limit 等不产生委托）；⑤线上逐条过验收清单并留痕。验证：验收清单全过 + 证据写入 verification。

## 上游产出摘要（dependsSummary）
- 测试补齐与全量回归

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-23T15:59:11.018Z，窗口 session-959cebfe-094f-4f09-894d-20a5c35d4d62）

把改动按「线上真实数据 + 真实渲染器」过了一遍：旧需求头部文案没变，本需求的设计文档全部可点；同时抓到一个单测没暴露的真实缺陷——新需求因为有 PlanRecord 也被当成旧管线，头部显示计划文案，已修。剩下最后一步是重启 :13080 让宿主侧改动（弹框超时值/事件提示接线）真正生效，重启会断开本会话。

### 完成项

- ① 旧管线兼容核验（线上接口真实载荷 + 源码渲染器 tsx 直跑，非 mock）：REQ-47939a design 节点 → 「计划已批准」，与改前一致
- ① 附：线上核验抓到真实缺陷并修复——本需求（新管线）design 节点头部曾显示「计划已批准」：判据用了 req.plan 是否存在，而新管线的 PlanRecord 在拆分阶段才产生，导致新需求永远落回计划文案（单测没覆盖到这个组合）。改判据为「design 阶段确有 plan 产物 或 有 plan 但一份设计文档都没交」，本需求现为「设计已确认」；旧需求 REQ-47939a / REQ-6f39b5 经同一份线上载荷复核，文案不变
- ② 构建 + 门禁：pnpm build:client 成功，verify-client-build OK（bundle=285229 字节，关键符号齐全，styles.ts 括号配对）；lib/client.js 266247 字节
- ③ 线上行为核验（线上载荷 + 源码渲染器）：预览「验收」节点状态胶囊 = pending:未开始（FR-10）；本需求 design 节点 5 份设计文档全部渲染为可点条目（data-action=open-doc）、0 条未交占位（FR-9）；拆分节点拆分计划行在位且不显示「未交」占位
- ④ R-017 只读抽样（noop，未产生任何委托）：trading_calendar 2026-09-23=交易日；account_info(agent_brain) 总资产 500550/现金 485920/持仓 1；regime_position_limit=sideways 上限60% 当前2.9% verdict=compliant 无熔断；trade_monitor queued=2（与抽样前一致，无新增）；genome_list 四段齐全 constitution v1 / principles v6 / rules v23 / lessons v11（无缺段无重复）
- ④ 部署链接体检：python3 scripts/relink-profile.py --check → exit 0，3 个条目均为指向仓库的符号链接（非副本），不存在"改源码不生效"的漂移
- ④ FR-5 已部署证据：src/domain/limits.ts 两常量 = 3_600_000，5 个消费方（AskConfirm/Capture/Advance/TaskExecute/AcceptSheet）全部读 LIMITS 无硬编码；TC-8 用例锁定该值与旧值防回潮
- ⑤ 待做（下一步）：launchctl/quick_restart 重启 :13080 后复跑线上核验（宿主侧 limits 与投递接线在进程启动时加载）——重启会中断本会话，需用户刷新页面后继续

### 改动文件

- `packages/web/dsh-pmboard/src/client/stage-panel.ts`
- `packages/web/dsh-pmboard/tests/stage-panel.test.ts`
- `packages/web/dsh-pmboard/lib/client.js`

### 下一步

重启 :13080 → 线上逐条验收（含弹框 >10min 不超时）→ 提交验收材料

---
## 汇报 2（2026-09-23T16:03:22.264Z，窗口 session-959cebfe-094f-4f09-894d-20a5c35d4d62）

重启已完成，线上五条核验在重启后全部复跑通过；顺带把一条账实不符修掉了——需求页原先把 FR-1..FR-10 全标成「未被接收」，根因是覆盖判定读的是拆分文档里的根编号列，而原任务表只有 key/标题/依赖三列。

### 完成项

- ⑤ 重启 + 重启后线上复跑（线上接口真实载荷 + 源码渲染器，2026-09-24 00:0x）：[1] 旧需求 REQ-47939a design 头部=「计划已批准」（兼容不变）[2] 预览验收节点状态胶囊=pending:未开始（FR-10）[3] 本需求 design 头部=「设计已确认」（FR-8）[4] design 节点 5 份设计文档全部可点、0 条未交占位（FR-9）[5] 拆分节点拆分计划行在位且无「未交」占位
- ⑤ 无副作用复核：trade_monitor queued=2 / filled=0，与重启前一致（R-017 noop 抽样与重启均未产生委托）
- 附加修复（账实不符）：decomposition.md 补「RTM 覆盖表（需求条款 ↔ 任务卡）」24 行（一行一条条款，用台账 id + 标题 + 根编号）。修复前 reqboard_status 的 unreceived_clauses=FR-1..FR-10（全红）；修复后 [] ，条款状态 FR-1/2/3/4/6/7=done、FR-5/8/9/10=received（挂在本卡，未完成故未标 done——口径正确）
- 根因说明（防复发）：覆盖判定与三方一致性都从 decomposition.md 读「根编号」列，TaskRecord 不存该绑定（content-trace.ts taskRefsFromDecomposition）；人工补 decompose 时把 binding 传进了工具参数却没有落进文档表格 → 解析器读不到 → 全部标红。已在文档里写明补录原因
- FR-5 弹框 >10min 的端到端实证：现行证据=两常量 3_600_000、5 个消费方均读 LIMITS 无硬编码、TC-8 锁定、重启后进程已加载新值；真正"等 10 分钟再点确认"的实操受本会话单次工具调用 10 分钟上限限制，需用户配合择时验证

### 改动文件

- `docs/requirements/REQ-260923222557-d3b0/decomposition.md`

### 下一步

FR-5 端到端择时验证（或按常量级证据收尾）→ 提交验收材料

---
## 汇报 3（2026-09-23T16:09:56.051Z，窗口 session-959cebfe-094f-4f09-894d-20a5c35d4d62）

把这次上线改动放到真实线上数据上过了一遍：老需求的头部文案一字未变（9 个旧管线需求全走计划文案），本需求详情页 5 份设计文档都能点开，还没走到的节点不再用「已拆分」这类过去式充数，改成「未开始」。弹框超时这一条我说句实话：改成 1 小时的声明值、消费方和门禁都核过了，框架也确实照这个声明计时，但「真的等过 10 分钟」的实测拿不到——本会话一次调用最多 10 分钟，与要跨过的旧值 600 秒刚好撞上；另外发现 PTC 会话里弹框实际等待上限仍被 10 分钟压着，1 小时只在原生工具调用窗口生效，这条需要人拍板或另立需求。

### 完成项

- ① 构建 + 门禁复跑：pnpm build:client 成功、verify-client-build OK（bundle=285229 字节，关键符号齐全）；连跑两次 md5 一致（4c2ed0fc7ebf6bcb28ceb3e89441db9e）→ 产物确定性，且与我上线前那份字节相同，浏览器不会拿到旧包
- ② 部署链路体检：python3 scripts/relink-profile.py --check → exit 0，3 个安装条目全为指向仓库源码的符号链接（无"改源码不生效"漂移）
- ③ 旧管线兼容（FR-8 兼容项）：线上 30 个需求的 design 节点真实载荷喂给真实渲染器 stageHeadSummary，9 个 design 有 plan 记录的需求全走计划文案（REQ-47939a=「计划已批准」，另有 计划待批准/已批准 各若干），新管线 19 个走设计文档文案，取词口径不一致 0 条
- ④ 本需求 FR-8/FR-9：design 节点头部=「设计已确认」；5 份设计文档全部渲染为可点行（data-action=open-doc），未交占位 0 条
- ⑤ FR-9 占位口径：design 阶段需求（REQ-ac5282）预览拆分节点得「⬜ 拆分计划：decomposition.md（未交）」且该行不可点；已交需求该行变为可点行
- ⑥ FR-10：未到达节点状态胶囊=pending「未开始」（ac5282 预览拆分、d3b0 预览验收）；已到达节点保持原词（设计=已设计、拆分=已拆分）
- ⑦ R-017 noop 只读抽样（全程零委托）：trading_calendar 2026-09-24=交易日；account_info(agent_brain) 总资产 500550/现金 485920（占比 97.1%）；regime_position_limit=sideways 上限 60% 当前 2.9% verdict=compliant 熔断未触发（回撤 -0.36% vs 阈值 -8%）；trade_monitor queued=2/filled=0（与抽样前一致）；genome_list 四段齐全 constitution v1/principles v6/rules v23/lessons v11
- ⑧ FR-5 机制级证据：limits.ts timeoutInteractiveMs/timeoutSheetMs=3_600_000；5 个消费方（AskConfirm/Capture/TaskExecute/Advance/AcceptSheet）全部读 LIMITS、零硬编码；TC-8（tests/concurrency-limits.test.ts）锁定新值并禁止旧值回潮；框架 @deepseek-ai/dsh-tool-call-timeout-policy 在 tools/execute 按工具声明的 timeoutMs 武装 deadline，dsh-timeout 允许上限 2^31-1（无 600s 截断）；:13080 进程 2026-09-23T23:59:23 重启（quick_restart，理由已落 state/quick-restart-request.json），晚于 limits.ts 改动时间 23:47:03 → 新值已加载
- ⑨ 测试：stage-panel 54 + node-panel 27 + concurrency-limits 8 + worktree-injection 5 + worktree-events 5 = 99/99 全绿
- ⑩ 未闭环（响亮报出）："弹框跨过旧 600s 仍有效"的端到端实测本会话拿不到——本会话走 PTC(run_code)，run_code 自身预算 default 120000ms/cap 600000ms 覆盖嵌套工具等待；且这意味着 PTC 窗口调用 reqboard_ask_confirm 的实际等待上限仍是 ≤10 分钟，1 小时只在原生工具调用窗口生效。调整 PTC 预算属框架级、会波及所有嵌套工具等待，超出本需求「不影响其他非 reqboard 弹框」的边界 → 交人工裁定/另立需求

### 改动文件

- `packages/web/dsh-pmboard/lib/client.js`

### 下一步

提交验收材料（reqboard_submit kind=verification）交人工审核；FR-5 的运行期「>10 分钟仍有效」需人工在原生工具调用会话中确认，或裁定是否另立需求调整 PTC 预算

---
