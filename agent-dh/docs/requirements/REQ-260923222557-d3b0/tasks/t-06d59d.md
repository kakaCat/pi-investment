# t-06d59d 子任务完成与归档两处事件注入接线

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
子任务完成与归档两处事件注入接线

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：implement
- 端侧：backend

## 得到什么结果

cd packages/web/dsh-pmboard && npx vitest run tests/worktree-injection.test.ts → 5 passed：task_move→done 与 accepting→archived 两条路径各注入一次且文本含对应 git 命令；投递失败（无端口/抛错）仍完成状态转移。

## 实施方案（implementation）
在 CaptureHook 现有转移监听处接 renderWorktreePrompt('task_done'|'archived', …)；沿用里程碑提醒的投递与去重模式；补故障注入用例（投递失败时转移仍成功）。验证：vitest 新增用例绿；模拟转移后注入留痕可见。

## 上游产出摘要（dependsSummary）
- worktree 提示词文本三份（implementing 路由档 + task_done/archived 事件常量）

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-23T15:51:15.947Z，窗口 session-959cebfe-094f-4f09-894d-20a5c35d4d62）

事件型 worktree 提示接线完成：组合根新增 delivery 端口（AgentDeliverer 复用唯一投递实现），子任务完成（MoveTask→done）与需求归档（AcceptSheet→archived）各投递一次提示；投递抛错/未装配都不阻断状态转移。

### 完成项

- ports.ts：UseCaseDeps 增可选 delivery 端口（缺省=不投递）
- index.ts：组合根注入 delivery: deliverer（与 onStagePrompt 同实现）
- 新增 application/internal/worktree-notice.ts：统一投递+吞错降级（失败只留痕）
- MoveTask.ts：changed.status===done 时投递 task_done（含 commit 命令/任务名）
- AcceptSheet.ts：归档后投递 archived（含 merge --no-ff / worktree remove）
- 新增 tests/worktree-injection.test.ts 5 用例：两条路径各投递一次 + 抛错不阻断 + 未装配不阻断 + 非 done 转移安静，全绿
- 相关套件 68 用例：67 passed，1 failed 为预存在项（RandomIdFactory id 格式断言，与本次改动无关，未触碰该文件）

### 改动文件

- `packages/web/dsh-pmboard/src/application/ports.ts`
- `packages/web/dsh-pmboard/src/index.ts`
- `packages/web/dsh-pmboard/src/application/internal/worktree-notice.ts`
- `packages/web/dsh-pmboard/src/application/use-cases/MoveTask.ts`
- `packages/web/dsh-pmboard/src/application/use-cases/AcceptSheet.ts`
- `packages/web/dsh-pmboard/tests/worktree-injection.test.ts`

### 下一步

t4 面板头部修正（FR-8/FR-10）

---
