# t-485233 面板头部：进展胶囊与状态词修正

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
面板头部：进展胶囊与状态词修正

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：ui
- 端侧：frontend

## 得到什么结果

cd packages/web/dsh-pmboard && npx vitest run tests/stage-panel.test.ts tests/node-panel.test.ts → 81 passed（TC-1..TC-5：无 plan 2/5 已交→「设计文档 2/5 已交」；旧 plan→旧文案；交齐已确认→「设计已确认」；pending→「未开始」；current/done 取词不变）；线上核验：curl -s "http://127.0.0.1:13080/dashboard/api/reqboard/requirements/REQ-47939a/stage/design" 的载荷过 stageHeadSummary 得「计划已批准」、本需求得「设计已确认」（或在 :13080 看板打开需求→设计节点看头部胶囊）。

## 实施方案（implementation）
①stage-panel.ts design 分支重写（plan 优先兼容分支 + designDocs 统计）；②node-panel.ts renderHead 增加 state 入参，pending→「未开始」；③tests/stage-panel.test.ts 增 TC-1..TC-5。验证：vitest 相关用例绿 + pnpm build:client 通过。

## 上游产出摘要（dependsSummary）
- （无依赖）

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-23T15:53:27.785Z，窗口 session-959cebfe-094f-4f09-894d-20a5c35d4d62）

面板头部不再自相矛盾：设计节点的进展胶囊按「设计文档交了几份 / 人确认没确认」说人话，未到达的节点一律说「未开始」而不是假装已经做完。

### 完成项

- stageHeadSummary 设计分支重写（FR-8）：有 plan 的存量需求仍按计划状态取词（计划待批准/已批准/被退回，兼容旧管线）；无 plan 时按设计文档清单取词——未交齐说「设计文档 n/N 已交」，交齐未确认说「待确认设计文档」，design 产物全部落章说「设计已确认」，清单缺席说「待提交设计文档」
- 已豁免（design_exempt）的文档不计入分母，避免豁免文件永远凑不齐 n/N
- node-panel renderHead 接收 stageRowState（FR-10）：pending 节点状态词统一显示「未开始」，不再借用该节点的完成态词（如未到达的验收节点不再显示「待验收」）
- 状态胶囊 data-state 与行状态一致（之前恒为 current），CSS 已有的 done=绿 / current=蓝 / pending=灰 三态因此真正生效；skipped 分支行为不变
- 测试：tests/stage-panel.test.ts 设计取词 5 条断言（2/5 已交 / 旧 plan 兼容 / 待确认 / 设计已确认 / 豁免不计分母）；tests/node-panel.test.ts 新增 TC-4 三条（pending→未开始、current/done 取词不变、分类跳过不改词）
- 两个面板套件 78/78 绿；lib/client.js 已重建并通过 verify-client-build（bundle 284935 字节，关键符号齐全）

### 改动文件

- `packages/web/dsh-pmboard/src/client/stage-panel.ts`
- `packages/web/dsh-pmboard/src/client/node-panel.ts`
- `packages/web/dsh-pmboard/tests/stage-panel.test.ts`
- `packages/web/dsh-pmboard/tests/node-panel.test.ts`

### 下一步

t5（FR-9）：已交设计文档行改可点（docItem），拆分节点补未交占位行

---
