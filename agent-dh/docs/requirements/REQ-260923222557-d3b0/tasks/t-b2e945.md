# t-b2e945 worktree 提示词文本三份（implementing 路由档 + task_done/archived 事件常量）

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
worktree 提示词文本三份（implementing 路由档 + task_done/archived 事件常量）

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：implement
- 端侧：backend

## 得到什么结果

cd packages/web/dsh-pmboard && npx vitest run tests/worktree-events.test.ts tests/prompt-baseline.test.ts tests/stage-prompts.test.ts → 全绿：worktree-events 断言事件文本含 `git worktree add .worktrees/REQ-` 与 `git worktree remove`，且 {id}/{task_id}/{task_title} 已替换；prompt-baseline 快照基线含 implementing 档「Worktree 规范」段（快照不一致即失败）。

## 实施方案（implementation）
①写 worktree-events.ts（导出常量与 renderWorktreePrompt(event, ctx)）；②在 fragments/implementing 档加入 worktree 段（无档则新建 light 档）；③node scripts/inline-prompt-fragments.mjs 再生成；④更新 tests/fixtures/stage-prompts-baseline-*.json 基线。验证：resolveStagePrompt(implementing) 返回文本含「git worktree add .worktrees/REQ-」。

## 上游产出摘要（dependsSummary）
- （无依赖）

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-23T15:49:03.750Z，窗口 session-959cebfe-094f-4f09-894d-20a5c35d4d62）

worktree 提示词三份落地：implementing 轻档新增「Worktree 规范」段（创建命令/目录分支约定/提交检查点/归档清理，含不强制说明），事件型两份文本（task_done 提交检查点、archived 合并清理）落在新模块 worktree-events.ts 并做变量替换；生成物重生成、基线刷新、门禁与单测全绿。

### 完成项

- fragments/implementing/light.md 增 Worktree 规范段（收紧至 2498 字符以内，不放过 2500 上限门禁）
- 新建 src/domain/prompt/worktree-events.ts：两份事件文本 + renderWorktreePrompt 变量替换（契约：{id}=含 REQ- 前缀的台账 id，防双前缀）
- domain/prompt/index.ts 导出该模块（供 t3 接线）
- node scripts/inline-prompt-fragments.mjs 重生成 + check-prompt-fragments.mjs 通过（128 分片/71967 bytes，heavy↔vendor 逐字节一致）
- tests/fixtures/stage-prompts-baseline-p1.json 基线刷新（12 键）
- 新增 tests/worktree-events.test.ts 5 用例（变量替换无残骸/命令齐备/不强制口径/implementing 档含创建命令）全绿
- prompt 套件 92/92 通过（prompt-tiers + prompt-baseline + stage-prompts）

### 改动文件

- `packages/web/dsh-pmboard/src/domain/prompt/fragments/implementing/light.md`
- `packages/web/dsh-pmboard/src/domain/prompt/worktree-events.ts`
- `packages/web/dsh-pmboard/src/domain/prompt/index.ts`
- `packages/web/dsh-pmboard/src/domain/prompt/generated/fragments.ts`
- `packages/web/dsh-pmboard/tests/worktree-events.test.ts`
- `packages/web/dsh-pmboard/tests/fixtures/stage-prompts-baseline-p1.json`

### 下一步

t3 事件注入接线（task done / archived 两处投递）

---
