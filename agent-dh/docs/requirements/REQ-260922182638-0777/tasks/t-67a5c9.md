# t-67a5c9 收敛 client 工具回执引用（toolviews 三文件）

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
收敛 client 工具回执引用（toolviews 三文件）

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：implement
- 端侧：frontend

## 得到什么结果
grep -n "SUBMIT_KIND\|KIND_CN" src/client/toolviews/ 无任何命中；pnpm vitest run tests/toolviews-cards.test.ts tests/toolviews-contract.test.ts 全绿；pnpm typecheck 通过

## 实施方案（implementation）
src/client/toolviews/shared.ts 删 L138 SUBMIT_KIND 表；rows/submit.ts L12 与 rows/ask-confirm.ts L9/L14 删 KIND_CN 补丁与 cnLabel(...) ?? kind，统一改 artifactKindLabel(kind)（design/decomposition/task_detail 中文名自动补齐）。

## 上游产出摘要（dependsSummary）
- 新增 artifact-labels 唯一事实源模块与防漂移单测

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-22T13:29:56.660Z，窗口 session-89aa3b25-9e19-48c0-a032-cf932135f88b）

会话流里的工具回执卡片中文名已收敛：提交/确认回执遇到 design、decomposition、task_detail 不再裸显英文枚举（旧 SUBMIT_KIND 只有 4 项、ask-confirm 还有一张 KIND_CN 补丁表，均已删除），统一走 artifactKindLabel。既有契约单测同步改为钉住唯一事实源。

### 完成项

- toolviews/shared.ts：删 SUBMIT_KIND 表
- rows/submit.ts：删 cnLabel(SUBMIT_KIND,...) ?? kind，改 artifactKindLabel(kind)
- rows/ask-confirm.ts：删 KIND_CN 补丁表，统一 artifactKindLabel（kind 缺省「产物」）
- tests/toolviews-contract.test.ts：SUBMIT_KIND 形状断言改为钉 artifactKindLabel 七类映射
- 验收：grep SUBMIT_KIND/KIND_CN 在 toolviews/ 零命中；toolviews-cards+contract 51/51 绿；typecheck exit 0

### 改动文件

- `packages/web/dsh-pmboard/src/client/toolviews/shared.ts`
- `packages/web/dsh-pmboard/src/client/toolviews/rows/submit.ts`
- `packages/web/dsh-pmboard/src/client/toolviews/rows/ask-confirm.ts`
- `packages/web/dsh-pmboard/tests/toolviews-contract.test.ts`

### 下一步

t4 收敛 host 侧 render-summaries.ts

---
