# t-3d33c3 收敛文档区两处引用（artifacts / verification）

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
收敛文档区两处引用（artifacts / verification）

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：implement
- 端侧：frontend

## 得到什么结果
grep -n "ARTIFACT_KIND_LABELS\|ARCHIVE_DOC_KIND_LABELS" src/client/views/ 无任何命中；pnpm vitest run tests/artifact-confirm-board.test.ts tests/verification-sheet.test.ts 全绿；pnpm typecheck 通过

## 实施方案（implementation）
1. src/client/views/artifacts.ts：删 L18 ARTIFACT_KIND_LABELS 表，L78/L107 改 artifactKindLabel(...)，删 ?? kind 兜底。2. src/client/views/verification.ts：DOC_KIND_META 保留 {icon,label} 导出形状，取值改由 KIND_ICONS/KIND_LABELS 供给；删 L189 ARCHIVE_DOC_KIND_LABELS，L175 改 artifactKindLabel(d.kind)（「需求说明」→「需求文档」统一）。

## 上游产出摘要（dependsSummary）
- 新增 artifact-labels 唯一事实源模块与防漂移单测

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-22T13:27:03.091Z，窗口 session-89aa3b25-9e19-48c0-a032-cf932135f88b）

文档区两处本地映射表已收敛：产物 chips 与「确认产物」按钮、归档清单的中文名全部改从唯一事实源取——同一产物在看板各区块从此叫同一个名字；归档清单里的 requirement 由漂移的「需求说明」统一为「需求文档」。DOC_KIND_META 导出形状保留（兼容既有引用），取值改由共享表供给。

### 完成项

- artifacts.ts：删本地 ARTIFACT_KIND_LABELS 表，L78/L107 改 artifactKindLabel(...)
- verification.ts：DOC_KIND_META 保留 {icon,label} 形状、取值由 KIND_LABELS/KIND_ICONS 供给；删归档区本地表，L175 改 artifactKindLabel(d.kind)
- 验收：grep 两旧表名在 src/client/views/ 零命中；artifact-confirm-board + verification-sheet 6/6 绿；typecheck 通过

### 改动文件

- `packages/web/dsh-pmboard/src/client/views/artifacts.ts`
- `packages/web/dsh-pmboard/src/client/views/verification.ts`

### 下一步

t3 收敛 toolviews 三文件

---
