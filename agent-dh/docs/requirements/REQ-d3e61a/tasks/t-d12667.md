# t-d12667 E2E 缺口可见：只有单测不算交完

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 目标
E2E 缺口可见：只有单测不算交完

## 背景摘要（context）
（待补充）

## 范围
- 阶段：test
- 端侧：backend

## 验收标准
只交单元/集成时验收单包含「E2E 覆盖：无（缺口）」；补一条链路 e2e 后返回「有」

## 实施方案（implementation）
实现 checkE2ECoverage：解析测试策略表层级列；接入验收单生成

## 上游产出摘要（dependsSummary）
- 验收项必须能照着验

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-18T14:33:05.322Z，窗口 session-e1fc9bb5-906c-4d20-92fc-cbd5f3e2a512）

E2E 缺口可见：从需求文档测试策略表读 E2E 覆盖读数，验收单**必然出现一项**「E2E 覆盖：有 / 无（缺口）」——只有单元/集成测试从此是可见的验收项，不再靠人记得。

### 完成项

- content-gate-wiring.ts 新增 e2eCoverageOf：读需求文档 → checkE2ECoverage（T-2 已实现的纯函数）；文档缺失/为空 → undefined（读数未知，不制造噪声）
- AcceptanceSheetSpec 新增可选 e2eCoverage：undefined 不追加；false → 「E2E 覆盖：**无（缺口）**」并给出补法；true → 「E2E 覆盖：**有**」
- SubmitVerification 在 mutate 之前读数并传入（读文件异步、mutate 回调同步）
- tests/e2e-coverage.test.ts 7 条：读数三态（缺口/有/未知）+ 验收单可见性三条 + 一条端到端串联（只交单测的需求走完整链路后验收单带缺口项）
- 全量回到基线 6 个失败、tsc 改动面 0 错误、client 产物重建且与备份逐字节一致

### 改动文件

- `packages/pages/dsh-pmboard/src/application/internal/content-gate-wiring.ts`
- `packages/pages/dsh-pmboard/src/domain/workflow/AcceptanceSheetSpec.ts`
- `packages/pages/dsh-pmboard/src/application/use-cases/SubmitVerification.ts`
- `packages/pages/dsh-pmboard/tests/e2e-coverage.test.ts`

### 下一步

继续目标剩余 7 张卡；T-8（三方一致性）落点空闲，可优先。

---
