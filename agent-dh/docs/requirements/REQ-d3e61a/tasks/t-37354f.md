# t-37354f 提示词注入变聪明：难度按需求实质

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 目标
提示词注入变聪明：难度按需求实质

## 背景摘要（context）
（待补充）

## 范围
- 阶段：implement
- 端侧：backend

## 验收标准
"动架构"的需求 → 注入难度判定返回 heavy（非缺省 light）；留痕包含推断依据

## 实施方案（implementation）
难度推断函数（动架构/多子系统/改数据模型 → heavy）+ 写 injection-log；不一致时响亮提示

## 上游产出摘要（dependsSummary）
- 定稿文档标准：六类文档各写什么

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-18T13:51:02.013Z，窗口 session-e1fc9bb5-906c-4d20-92fc-cbd5f3e2a512）

提示词注入变聪明：新增难度推断模块（动架构/改数据模型/新增子系统/跨多子系统/规模大 → heavy），并**真正接上线**——唯一取词入口 resolveStagePrompt 接受 requirement 后按需求实质推断难度，注入点 capture-section 传入需求标题+描述，推断依据随解析结果进注入留痕。

### 完成项

- 新增 src/domain/prompt/difficulty-inference.ts：inferDifficulty（5 类信号 + 证据片段留痕）+ difficultyMismatch（不一致时响亮提示）
- **找到线上现场**：capture-section.ts:138 原为 resolveStagePrompt({ stage, category }) ——未传 difficulty → 静默回落 DEFAULT_DIFFICULTY=light，这就是 REQ-c9f899 被注入轻档提示词的根因
- 接线（4 处，均向后兼容）：types.ts 请求增可选 requirement、结果增 difficultyReasons；index.ts 唯一入口做推断并挂依据；injection-log 自动带走依据；capture-section 注入点传入需求实质
- 显式难度与推断冲突时不静默：显式优先，但依据里写入响亮告警（不断流、可看板核查）
- tests/difficulty-inference.test.ts 16 条：5 类信号、阈值边界（跨子系统需 ≥3 条路径）、回落缺省、4 条端到端断言（含"标题含重构 → heavy + 留痕含动架构"、向后兼容、小需求不误判、冲突响亮）
- 被域层卫生门禁抓到并修正：domain 层禁止字符串拼接，6 处改为 fmt() 抽象（该门禁此前未在失败清单，是本卡新引入的违规）
- 重建 client（exit 0 / 与备份一致）；全量 1044 passed，失败回到基线 6 个；P1 注入基线 15 passed 未被动到（未传 requirement 的路径行为完全不变）

### 改动文件

- `packages/pages/dsh-pmboard/src/domain/prompt/difficulty-inference.ts`
- `packages/pages/dsh-pmboard/tests/difficulty-inference.test.ts`
- `packages/pages/dsh-pmboard/src/domain/prompt/types.ts`
- `packages/pages/dsh-pmboard/src/domain/prompt/index.ts`
- `packages/pages/dsh-pmboard/src/application/internal/injection-log.ts`
- `packages/pages/dsh-pmboard/src/application/internal/capture-section.ts`

### 下一步

T-3/T-4 接线仍等 MoveRequirement.ts 释放；T-16（E2E 覆盖判定）落点亦空闲可做。

---
