# t-0d77a1 验收=三方一致性：做的和说的对得上吗

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 目标
验收=三方一致性：做的和说的对得上吗

## 背景摘要（context）
（待补充）

## 范围
- 阶段：implement
- 端侧：backend

## 验收标准
用 R9 场景回归：需求有编号、无设计、无实施 → 验收单返回「设计缺失/实施缺失」；npx vitest run /Users/yunpeng/pi-investment/agent-dh/packages/pages/dsh-pmboard/tests/consistency.test.ts 通过

## 实施方案（implementation）
实现 ConsistencyRow 比对：需求编号×设计编号×实施+证据，输出 consistent/design_missing/impl_missing/out_of_scope/mismatch

## 上游产出摘要（dependsSummary）
- 证据锚到编号：证据能定位到条款

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-18T14:35:38.987Z，窗口 session-e1fc9bb5-906c-4d20-92fc-cbd5f3e2a512）

T-8 三方一致性完成：实现「做什么 × 怎么做 × 实际做了什么」比对（设计缺失/实施缺失/超范围/悬空引用四类不一致**各报一条**），接进验收单可见项；并解决一个结构性缺口——**任务↔需求编号的绑定原本不落库**（TaskRecord 无该字段），改为随 decomposition.md 的 RTM 覆盖表持久化。

### 完成项

- 发现结构性缺口：TaskRecord 无任务↔需求编号绑定 → R9 那类"没人接"无法被机械发现。修法**不改被占的 protocol.ts**：让 reqboard_decompose 自动生成的 decomposition.md 增加「§1 RTM 覆盖对照表（根编号 ↔ 计划 key ↔ 任务 id）」
- content-gate-wiring.ts 新增：ConsistencyRow/ConsistencyTaskLike 模型 + buildConsistencyRows（四类判定）+ consistencyGaps（人读文案）+ taskRefsFromDecomposition/collectTaskRefs（从 RTM 表读绑定）
- 关键取舍：两类缺失分别判定并**各报一条**——R9 的形态正是"既无设计、也无实施"，只报一条会漏一半信息
- 防误报：没有 RTM 表时**不比对**（避免把"没记录"误报成"实施缺失"）
- 接进 SubmitVerification（mutate 前算）+ AcceptanceSheetSpec（需求级可见项，不改被占的 protocol.ts）
- 修两个真 bug（我自己的测试抓到的）：① 真实任务 id `t-aaa111`（小写+十六进制）不在编号模式里 → 全部漏认；② consistencyGaps 的 mismatch/out_of_scope 分支顺序错，导致悬空引用被误报成"实施缺失"
- tests/consistency.test.ts 10 条：R9 场景回归（同时报设计缺失+实施缺失）、有设计无实施、三方齐全、超范围、悬空引用、camelCase、RTM 表解析、无表不误报、验收单可见性两条
- 全量 1107 passed / 失败回到基线 6 个、tsc 改动面 0 错误、client 产物重建且与备份逐字节一致

### 改动文件

- `packages/pages/dsh-pmboard/src/application/internal/content-gates.ts`
- `packages/pages/dsh-pmboard/src/application/internal/content-gate-wiring.ts`
- `packages/pages/dsh-pmboard/src/application/use-cases/Decompose.ts`
- `packages/pages/dsh-pmboard/src/application/use-cases/SubmitVerification.ts`
- `packages/pages/dsh-pmboard/src/domain/workflow/AcceptanceSheetSpec.ts`
- `packages/pages/dsh-pmboard/tests/consistency.test.ts`

### 下一步

继续目标剩余 6 张卡；T-5/T-6/T-11/T-13/T-14 落点视占用，T-17/T-18 依赖已全部满足。

---
