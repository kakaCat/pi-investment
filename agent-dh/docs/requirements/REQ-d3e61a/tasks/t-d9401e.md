# t-d9401e 标记载体定死：人看得见 + 机器读得懂

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 目标
标记载体定死：人看得见 + 机器读得懂

## 背景摘要（context）
（待补充）

## 范围
- 阶段：implement
- 端侧：backend

## 验收标准
删掉 BASE 某个公共节 → 六类全部返回拒绝；npx vitest run /Users/yunpeng/pi-investment/agent-dh/packages/pages/dsh-pmboard/tests/base-delta.test.ts 通过

## 实施方案（implementation）
落地标注载体规则：逐条状态只存表格行；front-matter 只放文档级索引；门禁按 BASE+DELTA 读取

## 上游产出摘要（dependsSummary）
- 分类文档集：按立项类型要不同文档

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-18T14:48:59.929Z，窗口 session-e1fc9bb5-906c-4d20-92fc-cbd5f3e2a512）

T-14 标记载体定死完成：分类文档集重构成 **BASE + DELTA 两层**（共同骨架只定义一次，DELTA 为类型增量），并落地标记载体规则（逐条状态只存表格行、front-matter 只放文档级索引，附机械探测）。

### 完成项

- BASE+DELTA 两层：COMMON_ROOT_SECTIONS（共同骨架）+ CATEGORY_DELTAS（类型增量）；门禁校验 = BASE + DELTA[类型]。requiredRootSectionsFor 支持注入 base，使"删掉 BASE 会怎样"可测
- **验收要点可证**：tests/base-delta.test.ts 逐条删掉 BASE 的每个公共节 → **六类全部拒绝**；再以 base=[] 反证"拒绝确实来自 BASE"
- BASE 收敛到唯一真正共同的节「边界」（六类无论 PRD/缺陷报告/调研笔记都要回答"不做什么"）；其余全下沉 DELTA，避免把 feature 的节强加给 bug
- 标记载体规则：frontmatterStateViolations() 机械探测"把逐条状态塞进 front-matter"（键里带编号，如 FR1_status / t-aaa111_evidence）——两处都写必然漂移
- 修掉一个**真缺陷**：原实现用**子串**判断节是否存在，而 spike 的必填节「待答问题」**含**「问题」→ BASE 的"问题"会被冒充为已存在。改为**标题精确匹配**（标题 + 可选编号 + 边界符），并在代码里写明踩坑缘由
- 同步迁移 T-13 的测试到新 API；doc-sync 的桩补 BASE 节（无人读其内容）
- 全量 1152 passed / 失败回到基线 6 个、tsc 改动面 0 错误、client 产物重建且与备份逐字节一致

### 改动文件

- `packages/pages/dsh-pmboard/src/application/internal/category-doc-sets.ts`
- `packages/pages/dsh-pmboard/src/application/use-cases/SubmitArtifact.ts`
- `packages/pages/dsh-pmboard/tests/base-delta.test.ts`
- `packages/pages/dsh-pmboard/tests/category-doc-sets.test.ts`
- `packages/pages/dsh-pmboard/tests/doc-sync.test.ts`

### 下一步

剩余 T-17（节点设卡，依赖 T-5 未结/T-13 已满足）与 T-18（依赖 T-17），以及 T-5 的看板尾项（落点被占）。

---
