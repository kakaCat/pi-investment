# t-7d854b 分类文档集：按立项类型要不同文档

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 目标
分类文档集：按立项类型要不同文档

## 背景摘要（context）
（待补充）

## 范围
- 阶段：implement
- 端侧：backend

## 验收标准
六类样例：少交必填文档 → 返回 409 并指出缺失文档名；bug 缺"复现步骤" → 返回拒绝

## 实施方案（implementation）
扩展 CATEGORY_FLOW_PROFILES 为「文档集+门禁+提示词+根前缀」四维；BASE 与 DELTA 两层结构

## 上游产出摘要（dependsSummary）
- 定稿文档标准：六类文档各写什么

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-18T14:45:29.742Z，窗口 session-e1fc9bb5-906c-4d20-92fc-cbd5f3e2a512）

T-13 分类文档集完成：新增 CATEGORY_DOC_SETS（六类各自的必填根文档节 + 必填设计文档），接进计划提交门禁——少交必填文档即拒并点名缺哪份；底线"类型只减少数量、不取消追溯"。用空闲新模块绕开被占的 protocol.ts。

### 完成项

- 新增 application/internal/category-doc-sets.ts（65 行）：六类档案（feature 产品定义/用户/功能点 + 全套设计；bug **复现步骤**/根因/回归；refactor 现状/目标结构/**行为不变式** + 架构/迁移；spike 待答问题/结论；doc 目标读者/大纲；chore 完成判据）
- 接线：submitPlanArtifact 在 mutate 之前校验——root 文档不存在则跳过（还没到可判阶段，不制造噪声）；缺失即 409 + 点名缺哪份（REQBOARD_MISSING_REQUIRED_DOC）
- **设计取舍**：不替换 protocol.ts 的 CATEGORY_FLOW_PROFILES（被占），新增为并列能力；两者消费同一份类型语义，未来可合并。改协议的需求被绕开，能力照样落地
- tests/category-doc-sets.test.ts 11 条：六类齐全、未知类型不拦、**底线守卫**（每类都必须有必填根节）、feature 齐活/缺节/少交设计文档、**bug 缺复现步骤 → 拒绝**、bug 齐活通过且不要 PRD 的用户角色、refactor 要行为不变式、chore 最简
- 夹具迁移 1 处（doc-sync.test.ts）：该文件的 requirement.md 桩靠上一用例残留、design 目录完全没有 → 补最小合法桩（无人读其内容）
- 另修一个**我自己引入的 tsc 错误**（T-9 测试里的无用参数，此前被基线失败掩盖）——现在 tsc 总错误 = 别人的基线 3 个
- 全量 1146 passed / 失败回到基线 6 个、tsc 改动面 0 错误、client 产物重建且与备份逐字节一致

### 改动文件

- `packages/pages/dsh-pmboard/src/application/internal/category-doc-sets.ts`
- `packages/pages/dsh-pmboard/src/application/use-cases/SubmitArtifact.ts`
- `packages/pages/dsh-pmboard/tests/category-doc-sets.test.ts`
- `packages/pages/dsh-pmboard/tests/doc-sync.test.ts`
- `packages/pages/dsh-pmboard/tests/amend-acceptance.test.ts`

### 下一步

T-13 完成后 T-14（标记载体）依赖已满足；T-17 依赖 T-5（in_review 未结）/T-13，T-18 依赖 T-17。

---
