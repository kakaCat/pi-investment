# t-4f2f57 验收项必须能照着验

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 目标
验收项必须能照着验

## 背景摘要（context）
（待补充）

## 范围
- 阶段：implement
- 端侧：backend

## 验收标准
缺"怎么验"的验收项 → 验收单不生成且返回 missing 列表；补齐命令+预期后返回通过

## 实施方案（implementation）
实现 checkAcceptanceKit：校验四件套（验什么/对应编号/怎么验/预期），缺则拒绝生成 sheet

## 上游产出摘要（dependsSummary）
- 验收=三方一致性：做的和说的对得上吗

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-18T14:21:32.387Z，窗口 session-e1fc9bb5-906c-4d20-92fc-cbd5f3e2a512）

【部分交付·执行侧受阻】T-9 规则已落地并通过测试（HOW_TO_VERIFY + checkHowToVerify，7 条单测），但**执行侧接线无法在不破坏既有测试的前提下接入**，已按纪律回退接线、保留规则，等待方案裁决。

### 完成项

- 新增 domain 判据 HOW_TO_VERIFY + checkHowToVerify（验收期门槛：命令 / 可查数据 / 可达界面路径），与计划期的 VERIFIABLE_ANCHOR 分层——后者只要"含断言词"就放行（防卡创作），前者要求"能照着动手"
- tests/acceptance-executable.test.ts 7 条：命令/界面/SQL 三类通过；"确认可用""应该没问题"被拦；**关键分水岭用例**——"注入难度判定返回 heavy" 在计划期合格、在验收期被拦
- 接线尝试并回退 3 轮，找到完整证据链（见下），最终基线零回归（6 个失败 = 其他窗口在途 + 无关文件）

### 改动文件

- `packages/pages/dsh-pmboard/src/domain/task/Acceptability.ts`
- `packages/pages/dsh-pmboard/tests/acceptance-executable.test.ts`

### 下一步

待裁决（见 report 正文与弹框）：(a) 改夹具并同步断言 (b) 先加「任务卡验收标准可修订」通道再接门禁 (c) 只出规则不接执行侧（当前状态）(d) 门禁只对新需求生效。

---
## 汇报 2（2026-09-18T14:26:17.860Z，窗口 session-e1fc9bb5-906c-4d20-92fc-cbd5f3e2a512）

【(b) 已交付，门禁仍待最终裁决】T-9 规则（怎么验判据）+ 修订通道（reqboard_task_move 可选 acceptance 参数 + AmendTaskAcceptance use-case）均已落地并测试；但**启用硬门禁仍会打断 3 个既有测试**，原因已定位清楚，需你最终裁决。

### 完成项

- 规则层：domain/task/Acceptability.ts 新增 HOW_TO_VERIFY + checkHowToVerify（验收期门槛：命令/可查数据/可达界面路径），与计划期 VERIFIABLE_ANCHOR 分层
- 修订通道（用户裁定 b）：新增 application/use-cases/AmendTaskAcceptance.ts + 挂在既有 reqboard_task_move 薄壳的可选 acceptance 参数上——**完全绕开被占用的 MoveTask.ts / ReportTask.ts / src/index.ts**
- 通道语义：修订文本仍须过计划期门槛（弱标准不能换成更弱的）；台账更新 + 卡文档「## 验收标准」段同步（尽力而为）；不传参数则零行为变更
- tests/amend-acceptance.test.ts 7 条（未传零变更 / 弱标准被拒 / 可操作则更新 / 卡文档同步 / 越权被拒）
- tests/acceptance-executable.test.ts 7 条（三类可执行通过；空话被拦；关键分水岭：计划期合格但验收期不合格）

### 改动文件

- `packages/pages/dsh-pmboard/src/domain/task/Acceptability.ts`
- `packages/pages/dsh-pmboard/src/application/use-cases/AmendTaskAcceptance.ts`
- `packages/pages/dsh-pmboard/src/tools/TaskMoveTool/TaskMoveTool.ts`
- `packages/pages/dsh-pmboard/tests/acceptance-executable.test.ts`
- `packages/pages/dsh-pmboard/tests/amend-acceptance.test.ts`

### 下一步

待裁决：是否连那 3 个既有测试一起改（fixture 数据 + 镜像断言），以启用硬门禁。

---
## 汇报 3（2026-09-18T14:32:07.247Z，窗口 session-e1fc9bb5-906c-4d20-92fc-cbd5f3e2a512）

T-9 完成：验收期「怎么验」硬门禁已上线（缺可执行操作 → 验收单不生成并列出缺项），并配套**修订通道**（reqboard_task_move 可选 acceptance）+ 三个测试夹具的透明迁移。

### 完成项

- 规则层：domain/task/Acceptability.ts 新增 HOW_TO_VERIFY + checkHowToVerify——与计划期 VERIFIABLE_ANCHOR 分层（后者含断言词即过，防卡创作；前者要求命令/可查数据/可达界面路径）
- 修订通道（用户裁定 b）：AmendTaskAcceptance use-case + reqboard_task_move 可选 acceptance 参数。**硬门禁必须配修复路径，否则是死锁**——通道绕开被占的 MoveTask.ts/ReportTask.ts/src/index.ts（走薄壳 + 新 use-case）
- 门禁上线（SubmitVerification）：三类分流——① 存量需求（artifacts 空）豁免 ② 过了计划锚点但不可操作 → 硬拦 + 提示用 task_move(acceptance=) 修订 ③ 连锚点都没有 → 只作验收单可见项（不允许静默）
- 验收单新增"不可照着验（历史数据）"可见项（AcceptanceSheetSpec，需求级 source，不动被占的 protocol.ts）
- 测试夹具透明迁移（3 处 + 2 条镜像断言）：把占位验收标准「单测绿」「截图可见」换成可执行标准；代码里写明"本断言验的是任务表正确落库，语义不变，只是值随门禁要求升级"
- 新增两个测试文件共 14 条（acceptance-executable 7 + amend-acceptance 7）；全量回到基线 6 个失败、tsc 改动面 0 错误、client 产物重建且与备份一致
- 过程留痕：本卡经历 3 轮接线-回退（一刀切硬拦打断 10 个测试文件），最终结论已写入经验库——加严门禁前必须先确认存量豁免语义、被拦对象有无修复路径、既有测试是否把被测文本写进断言

### 改动文件

- `packages/pages/dsh-pmboard/src/domain/task/Acceptability.ts`
- `packages/pages/dsh-pmboard/src/application/use-cases/AmendTaskAcceptance.ts`
- `packages/pages/dsh-pmboard/src/tools/TaskMoveTool/TaskMoveTool.ts`
- `packages/pages/dsh-pmboard/src/application/use-cases/SubmitVerification.ts`
- `packages/pages/dsh-pmboard/src/domain/workflow/AcceptanceSheetSpec.ts`
- `packages/pages/dsh-pmboard/tests/acceptance-executable.test.ts`
- `packages/pages/dsh-pmboard/tests/amend-acceptance.test.ts`
- `packages/pages/dsh-pmboard/tests/decompose-tools.test.ts`
- `packages/pages/dsh-pmboard/tests/verification-sheet.test.ts`

### 下一步

继续目标剩余 9 张卡（T-5/T-6 落点仍被占；T-8/T-16 落点空闲，可优先）。

---
