# t-5042c7 拆分门禁：条款没落卡就拦下

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 目标
拆分门禁：条款没落卡就拦下

## 背景摘要（context）
（待补充）

## 范围
- 阶段：implement
- 端侧：backend

## 验收标准
提交"需求有、卡上没有"的拆分 → 返回 409 且 gaps 包含缺失编号；同例标"本轮不做+理由" → 返回通过

## 实施方案（implementation）
实现 checkClauseCoverage 并接入 assertArtifactGates 第三级（唯一改动点），返回 code=requirement_uncovered

## 上游产出摘要（dependsSummary）
- 文档校验骨架：把标准变成能判的代码

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-18T13:54:39.394Z，窗口 session-e1fc9bb5-906c-4d20-92fc-cbd5f3e2a512）

拆分门禁接线完成：新增 content-gate-wiring.ts（取数+组装，读需求文档、从任务对象取 requirement_refs、识别"本轮不做"），接进 reqboard_decompose 的**真实入口** Decompose.ts（放在 mutate 之前，拒绝时零副作用）；GateFailure 扩展 8 个内容闸门错误码；工具参数新增 requirement_refs。

### 完成项

- 定位真正的接线点：验收口径是"提交拆分→409"，入口是 reqboard_decompose（Decompose.ts），不是 reqboard_move —— 且 Decompose.ts 不在另一窗口占用面
- 新增 src/application/internal/content-gate-wiring.ts：requirementRefsOf（同时认 snake/camel，避免写法不一致导致静默漏判）+ assertClauseCoverageGate（三条放行条件：存量需求豁免 / 文档不存在 / 无编号条款）
- content-gates.ts 新增 extractSkippedClauses：识别条款旁的「本轮不做/本轮裁剪/非目标」——拦的是"无记录"，不是"不许多做少做"
- artifact-gates.ts：GateFailure 错误码从 2 个扩到 10 个（补 requirement_uncovered / dangling_reference / orphan_clause / design_orphan / no_e2e_case / acceptance_incomplete / task_card_incomplete / tbd_not_cleared）+ 新增结构化 gaps 字段
- Decompose.ts 接线：放在 deps.repo.mutate 之前，拒绝时不留任何副作用；覆盖 = 任务 requirement_refs 的并集 ∪ 需求侧"本轮不做"
- DecomposeTool 新增 requirement_refs 参数（任务卡声明交付哪些根编号）
- tests/clause-coverage-gate.test.ts 9 条：含 TC-009（需求有 FR-7、卡上没有 → 被拒且 gaps=[FR-7]）、标"本轮不做"→通过、存量豁免、文档缺失不误拦、gaps 是结构化编号清单
- 过程中自测抓到两个真 bug 并修复：① RegExp.exec 返回 null 不是 undefined（同类错误本会话第二次）；② "本轮不做"标记的 2 行前瞻窗口会越过下一条款定义行，导致相邻条款被误染
- 重建 client（exit 0 / 与备份一致）；全量 1053 passed，失败回到基线 6 个

### 改动文件

- `packages/pages/dsh-pmboard/src/application/internal/content-gate-wiring.ts`
- `packages/pages/dsh-pmboard/src/application/internal/content-gates.ts`
- `packages/pages/dsh-pmboard/src/application/internal/artifact-gates.ts`
- `packages/pages/dsh-pmboard/src/application/use-cases/Decompose.ts`
- `packages/pages/dsh-pmboard/src/tools/DecomposeTool/DecomposeTool.ts`
- `packages/pages/dsh-pmboard/tests/clause-coverage-gate.test.ts`

### 下一步

T-4（编号串联接线）落点同样空闲，可继续；T-3 落地后 REQ-d3e61a 自身若重跑 decompose 也会被门禁拦（其条款未被任务 requirement_refs 覆盖）——这是预期行为。

---
