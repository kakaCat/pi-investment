# t-d966a3 全链路映射：设计/用例声明服务哪条

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 目标
全链路映射：设计/用例声明服务哪条

## 背景摘要（context）
（待补充）

## 范围
- 阶段：implement
- 端侧：backend

## 验收标准
设计章节缺 serves → 提交计划返回 409；测试文件缺映射 → 验收单包含"孤儿用例"项

## 实施方案（implementation）
解析 design/*.md 章节头与测试文件头部的 serves 声明，接入第三级门禁

## 上游产出摘要（dependsSummary）
- 编号串联：给一个编号能查到上下游

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-18T14:16:03.481Z，窗口 session-e1fc9bb5-906c-4d20-92fc-cbd5f3e2a512）

全链路映射完成：设计章节缺 serves → 提交计划被拒（design_orphan，复用 T-12 的 checkDesignSectionsHaveServes）；测试文件缺映射 → 验收单**可见地**多出一项「孤儿用例（缺映射）」（需求级项，不动 protocol 的 source 联合）。

### 完成项

- 选定落点：T-5/T-6 的目标文件（client/views/*、QueryStageDetail.ts、ReportTask.ts、MoveTask.ts）**全在另一窗口占用面内**，按纪律改做 T-7（SubmitArtifact/SubmitVerification/AcceptanceSheetSpec 全空闲）
- content-gate-wiring.ts 新增三件：checkDesignServesGate（设计章节缺 serves → design_orphan，点出「文件 → 章节」）、testFileHasServesHeader（只看头部 20 行）、testFilesFromDesign（从「实际文件」列取路径）、collectOrphanTestFiles
- 接进 submitPlanArtifact（mutate 之前，零副作用）：设计章节缺标注即 409
- 接进 submitVerification：孤儿用例在 mutate 前先算（读文件异步、mutate 回调同步），结果作为**需求级验收项**进验收单
- AcceptanceSheetSpec.buildSheet 新增可选 orphanTestFiles → 非空时追加一项，criterion 点名文件；刻意用 requirement 级 source（不扩 protocol 联合，避开被占用的 protocol.ts）
- tests/design-serves-gate.test.ts 13 条：设计门禁四态（缺标注/齐全/存量豁免/无设计目录）、头部 20 行边界、实际文件列解析、孤儿检出、**验收单可见性两条（有孤儿→出现 pending 项；无孤儿→不制造噪声）**
- 过程中自修：测试文件新 describe 未闭合导致 esbuild 报 Unexpected end of file，已补
- 重建 client（exit 0 / 与备份一致）；全量 1076 passed，失败回到基线 6 个；tsc 改动面 0 错误

### 改动文件

- `packages/pages/dsh-pmboard/src/application/internal/content-gate-wiring.ts`
- `packages/pages/dsh-pmboard/src/application/use-cases/SubmitArtifact.ts`
- `packages/pages/dsh-pmboard/src/application/use-cases/SubmitVerification.ts`
- `packages/pages/dsh-pmboard/src/domain/workflow/AcceptanceSheetSpec.ts`
- `packages/pages/dsh-pmboard/tests/design-serves-gate.test.ts`

### 下一步

T-5/T-6 的落点仍被占用；可继续 T-16（落点 SubmitVerification 已在手，且 checkE2ECoverage 已在 T-2 实现）。

---
