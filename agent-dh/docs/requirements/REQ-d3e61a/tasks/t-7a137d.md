# t-7a137d 端到端验证 + 存量豁免回归

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 目标
端到端验证 + 存量豁免回归

## 背景摘要（context）
（待补充）

## 范围
- 阶段：test
- 端侧：backend

## 验收标准
npx vitest run /Users/yunpeng/pi-investment/agent-dh/packages/pages/dsh-pmboard/tests/doc-gate-e2e.test.ts 通过；两条 E2E 全绿，存量需求零误伤

## 实施方案（implementation）
新增 e2e 测试：跑真实入口构建违规/合规样例并断言终态

## 上游产出摘要（dependsSummary）
- 节点设卡：五个节点逐站拦

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-18T14:51:23.758Z，窗口 session-e1fc9bb5-906c-4d20-92fc-cbd5f3e2a512）

T-18 端到端验证完成：两条 E2E 走真实入口跑通，并**当场抓出一个真 bug**（RTM 表的 refs 被覆盖，在"计划携带任务表"这条常见路径上恒显示"未声明接收任何条款"）。

### 完成项

- E2E① 需求 → 拆分（缺条款）→ **被拦** → 补卡 → 通过：走真实 executeDecompose + harness 假 docs；违规样例被拒且**台账零副作用**（拒绝不留残渣），补卡后成功落库，并断言 RTM 覆盖表已生成
- E2E① 附加两条：**等效合法路径**（显式标「本轮不做」→ 同样通过，证明拦的是"无记录"不是"少做"）+ **存量需求零误伤**（artifacts 为空 → 同样样例不被拦）
- E2E② 交付 → 三方一致性验收单：走真实 submitVerification；需求有编号、无设计、无实施（RTM 里 FR-4 无卡）→ 验收单出现「三方一致性」缺口项且点名 FR-4/实施缺失；补齐后**该项消失**（证明是活的，不是常驻噪声）
- **E2E 当场抓到真 bug**：refsByKey 被后写的 planTasks（无 refs）覆盖 a.tasks（有 refs）→ RTM 表恒为"未声明接收任何条款"，而"计划携带任务表"是常见路径 → 改为取并集，代码写明踩坑缘由
- tests/doc-gate-e2e.test.ts 6 条全绿；全量 1165 passed / 失败回到基线 6 个、tsc 改动面 0 错误、client 产物重建且与备份逐字节一致

### 改动文件

- `packages/pages/dsh-pmboard/src/application/use-cases/Decompose.ts`
- `packages/pages/dsh-pmboard/tests/doc-gate-e2e.test.ts`

### 下一步

剩余仅 T-5 的看板渲染尾项（落点 client/views/* 仍被另一窗口占用，未提交）。

---
