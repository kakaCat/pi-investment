# t-8e3513 需求侧标记：需求上直接看出谁接了

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 目标
需求侧标记：需求上直接看出谁接了

## 背景摘要（context）
（待补充）

## 范围
- 阶段：implement
- 端侧：fullstack

## 验收标准
取消某张卡对某条的交付后，该条在 requirement.md 与看板返回「未被接收（红）」状态；npx vitest run /Users/yunpeng/pi-investment/agent-dh/packages/pages/dsh-pmboard/tests/receive-mark.test.ts 通过

## 实施方案（implementation）
接收状态由编号图推导，写入需求文档表格列 + 看板需求详情页渲染（未被接收标红）

## 上游产出摘要（dependsSummary）
- 拆分门禁：条款没落卡就拦下
- 编号串联：给一个编号能查到上下游

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-18T14:40:07.797Z，窗口 session-e1fc9bb5-906c-4d20-92fc-cbd5f3e2a512）

【部分交付·看板面受阻】T-5 需求侧接收标记：四态推导（done/received/skipped/**unreceived 红**）已实现并暴露到 reqboard_status（R9 出事的正是 agent 看不见这一面），9 条测试含验收场景回归；**看板渲染部分**落点（client/views/*、QueryStageDetail.ts）仍被另一窗口占用，未做。期间被尺寸门禁抓到并顺势拆出一个模块。

### 完成项

- 推导层：content-trace.ts（新模块）clauseReceiveStatus 四态 + unreceivedClauses；判据=任务↔编号绑定（RTM 表）× 任务状态 × 是否有证据 × 显式裁剪
- **验收场景回归**：取消某张卡对某条的交付 → 该条回落为「未被接收（红）」（tests/receive-mark.test.ts 专门覆盖取消前/取消后）
- 暴露面：reqboard_status 返回 clause_receive_status（逐条状态）+ unreceived_clauses（红名单）——每次 status 调用都可见，不靠人记得去查；StatusTool schema 同步声明（output-contract 15 passed）
- 被尺寸门禁抓到：content-gate-wiring.ts 447 行 > 宿主单文件 400 行上限 → **按职责拆出 content-trace.ts**（纯分析），wiring 回到 250 行；三层依赖单向：wiring → trace → gates。选拆分而非加白名单例外
- tests/receive-mark.test.ts 9 条：四态各一条 + camelCase + 验收场景组（取消前无红 / 取消后 FR-4 变红 / 红名单是显式输出）
- 全量 1122 passed / 失败回到基线 6 个、tsc 改动面 0 错误、尺寸门禁 5 passed、client 产物重建且与备份逐字节一致

### 改动文件

- `packages/pages/dsh-pmboard/src/application/internal/content-trace.ts`
- `packages/pages/dsh-pmboard/src/application/internal/content-gate-wiring.ts`
- `packages/pages/dsh-pmboard/src/application/internal/content-gates.ts`
- `packages/pages/dsh-pmboard/src/application/query/QueryState.ts`
- `packages/pages/dsh-pmboard/src/tools/StatusTool/StatusTool.ts`
- `packages/pages/dsh-pmboard/tests/receive-mark.test.ts`

### 下一步

缺口：看板需求详情页渲染「未被接收（红）」（client/views/* 被占）。待其释放后补，届时可结单。

---
## 汇报 2（2026-09-18T15:36:10.461Z，窗口 session-e1fc9bb5-906c-4d20-92fc-cbd5f3e2a512）

T-5 需求侧接收标记完成两处缺口：①需求文档面——接收状态回写进 requirement.md（§6 功能点末尾的机器维护表格，红名单点名）；②看板面——需求详情页新增「🏷 条款接收状态」块，未被接收行标红。两处同源（同一份推导），并修掉一个真 bug：已取消的卡此前仍算"交付了这条"。

### 完成项

- 推导层补漏：clauseReceiveStatus 把「台账里存在且已取消」的卡从交付声明中剔除——取消一张卡的交付，该条真的回落为未被接收（此前只按 RTM 引用计数，取消后仍显示已接收）
- 需求文档面：新增 application/internal/requirement-marks-doc.ts（纯函数：渲染 markdown 表格 + 幂等 upsert + 回读），写入 §6 功能点末尾的 <!-- reqboard:marks --> 机器段，§6/§7 锚点定位，找不到锚点退化为文末追加，绝不删改正文
- 回写用例：application/use-cases/SyncRequirementMarks.ts（读 requirement.md + decomposition.md RTM + 台账 → 写回；内容未变不写盘，文档不存在静默跳过）
- 触发点：reqboard_task_move 落库后同步（卡的生命周期变化即更新，对应 FR-3「随卡的生命周期自动更新」）；reqboard_decompose 拆分完成也写一次，让文档面从拆分起就可见；回写失败只降级不阻断推进
- 看板面：client/marks-info.ts（HTML 渲染，未接收行带 is-unreceived 红条+红字，红名单写进摘要行）+ client/styles/marks.ts + 详情页容器 + board-mount 加载器（只挡并发，不做按 id 缓存——否则同一需求重开会永远停在「加载中…」）+ api.fetchRequirementMarks
- 服务端读路径：GET /requirements/:id/marks → handleRequirementMarks → assembleRequirementMarks（client 拿不到判据：条款在 requirement.md、任务↔条款绑定在 decomposition.md 的 RTM 表）
- 测试 tests/marks-surfaces.test.ts（16 条）：推导层取消语义、文档面渲染/插入点/幂等/回读、看板面红类、验收场景（取消前两面无红 → 取消后 requirement.md 与看板同一批条款都标红）；与既有 receive-mark.test.ts 9 条合计覆盖两面
- 层边界：取消过滤下沉到 application（http/ 不写状态字面量），layer-boundary 14 passed
- 全量 1181 passed / 失败回到基线 6 个（acceptance-criteria×2、board-info-fixes、capture-hook、message-hygiene 棘轮、typecheck——均为既有基线）；tsc 改动面 0 错误（总 3 = 基线）
- 客户端产物重建并通过 verify-client-build（bundle=227101 bytes，含 dsh-pm-mk-unreceived 与 /marks 路径）

### 改动文件

- `packages/pages/dsh-pmboard/src/application/internal/requirement-marks-doc.ts`
- `packages/pages/dsh-pmboard/src/application/use-cases/SyncRequirementMarks.ts`
- `packages/pages/dsh-pmboard/src/application/query/QueryRequirementMarks.ts`
- `packages/pages/dsh-pmboard/src/application/internal/content-trace.ts`
- `packages/pages/dsh-pmboard/src/application/use-cases/Decompose.ts`
- `packages/pages/dsh-pmboard/src/client/marks-info.ts`
- `packages/pages/dsh-pmboard/src/client/styles/marks.ts`
- `packages/pages/dsh-pmboard/src/client/styles.ts`
- `packages/pages/dsh-pmboard/src/client/api.ts`
- `packages/pages/dsh-pmboard/src/client/views/stage-detail.ts`
- `packages/pages/dsh-pmboard/src/client/board-mount.ts`
- `packages/pages/dsh-pmboard/src/tools/TaskMoveTool/TaskMoveTool.ts`
- `packages/pages/dsh-pmboard/src/http/routers/stages.ts`
- `packages/pages/dsh-pmboard/src/http/routes.ts`
- `packages/pages/dsh-pmboard/src/shared/protocol.ts`
- `packages/pages/dsh-pmboard/tests/marks-surfaces.test.ts`
- `packages/pages/dsh-pmboard/lib/client.js`

### 下一步

T-5 代码面已闭环；线上 :13080 仍是 13:49 启动的旧进程，新路由 /requirements/:id/marks 需要重启该实例后才在浏览器可见（重启会中断本会话，等用户拍板）。重启后：移动任意一张卡即触发需求文档自动回写。

---
## 汇报 3（2026-09-18T15:58:28.738Z，窗口 session-e1fc9bb5-906c-4d20-92fc-cbd5f3e2a512）

真实数据复核（拿 REQ-d3e61a 自己的台账+文档跑）又抓出两个 bug——都是"单测全绿但真实数据不成立"的那类：①RTM 表的任务列在真实需求里是计划键（T-5），不是台账 id（t-8e3513），取消的卡认不出来 → 红永远红不出来；②FR-3 自己的定义里枚举了四态（含「本轮不做」四个字），被裁剪标记识别器当成"本条已裁剪" → 最该标红的那条变 skipped。两个都修了，并加了回归。

### 完成项

- 计划键解析：taskRefsFromDecomposition 现在同时读「任务标题」列；clauseReceiveStatus 按 id 认不出时用标题**唯一**匹配解析回台账任务 id（认不出就保留原文，宁可不标红也不把外来编号误判成无人接收）。修后真实数据上 16 条状态从"全部 received"变成正确的"已完成（有证据）"，且取消 T-5 → FR-3 = unreceived ✅
- 裁剪标记假阳性：extractSkippedClauses 先去掉括号内内容再判——FR-3 定义里的「（… / 本轮不做 / …）」是概念枚举，不是裁剪声明。修后真实数据上取消 T-5 正确回落为红（此前是 skipped）
- 回归测试：tests/skip-marker.test.ts（3 条：括号内枚举不算裁剪 / 括号外声明仍被认 / 紧随 2 行仍被认）；consistency.test.ts 补"按标题解析回台账 + 取消变红"用例并迁移形状断言（语义不变，只是值随解析通道升级）
- 真实数据探针（tsx 直连 .dsh-data/dsh-reqboard.json + 真文档）：16 条功能点、0 红、状态=已完成（有证据）、承载任务=台账 id；取消 T-5 后 unreceived=[FR-3]
- REQ-d3e61a 自己的 requirement.md 已被回写：§6 功能点末尾出现「条款接收状态」机器段（16 行，含承载任务 id 与红名单小结）
- 全量回到基线：1233 passed / 6 failed（acceptance-criteria×2、board-info-fixes、capture-hook、message-hygiene 棘轮、typecheck）；client 产物重建 verify-client-build OK（227289 bytes）

### 改动文件

- `packages/pages/dsh-pmboard/src/application/internal/content-trace.ts`
- `packages/pages/dsh-pmboard/src/application/internal/content-gates.ts`
- `packages/pages/dsh-pmboard/tests/skip-marker.test.ts`
- `packages/pages/dsh-pmboard/tests/consistency.test.ts`
- `packages/pages/dsh-pmboard/lib/client.js`
- `docs/requirements/REQ-d3e61a/requirement.md`

### 下一步

重启 :13080 让新路由与新产物在浏览器生效（重启会中断本会话）。重启后：打开 REQ-d3e61a 详情页可见「🏷 条款接收状态」；移动任意一张卡会顺带刷新需求文档的机器段。

---
