# t-d332d0 新增苹果风样式分片并接入拼接链

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
新增苹果风样式分片并接入拼接链

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：ui
- 端侧：frontend

## 得到什么结果
pnpm build:client 成功且 verify-client-build 通过；TC-11 全绿（无裸全局选择器）。

## 实施方案（implementation）
按 frontend.md「样式与主题」节取值写样式；styles.ts 追加 NODE_PANEL_CSS；新增 tests/node-panel-styles.test.ts（TC-11 作用域断言）。验证：pnpm build:client（wrap+verify 哨兵）+ vitest。

## 上游产出摘要（dependsSummary）
- 实现节点面板渲染器 node-panel.ts

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-23T07:08:33.690Z，窗口 session-f3978f17-ddb9-4fbc-81b6-7948a6999f4d）

新增苹果风样式分片 styles/node-panel.ts（NODE_PANEL_CSS 215 行）：--dsh-pm-np-* 私有设计令牌、外壳 720px/68vh/10px 圆角/×按钮、面板头 REQ 胶囊+状态胶囊、折叠块、文档列表、设计逐份、DAG/泳道（6 状态道横向滚动）、执行流程三段、验收统计、归档徽标。styles.ts 末尾追加。TC-11 作用域测试 4 用例全绿（除 :root/外壳外每条选择器含 .dsh-pm-np，零裸全局）。

### 完成项

- 新建 src/client/styles/node-panel.ts（NODE_PANEL_CSS）
- styles.ts 拼接链末尾追加 NODE_PANEL_CSS
- tests/node-panel-styles.test.ts TC-11 4 用例全绿

### 改动文件

- `src/client/styles/node-panel.ts`
- `src/client/styles.ts`
- `tests/node-panel-styles.test.ts`

### 下一步

t6 conversation-progress 挂载 + api/isolation 拉取

---
