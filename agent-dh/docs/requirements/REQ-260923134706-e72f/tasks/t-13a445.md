# t-13a445 把新面板挂进会话流程条

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
把新面板挂进会话流程条

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：implement
- 端侧：frontend

## 得到什么结果

怎么验：① `bash agent-dh/scripts/restart-with-build.sh --check` → dist 产物 20/20、依赖 symlink-ok；② 浏览器验收探针 `/Users/yunpeng/anaconda3/bin/python3 packages/web/dsh-pmboard/scripts/.probe/acceptance-260923134706.py` → 报告 packages/web/dsh-pmboard/scripts/.probe/acceptance-report.json 中：gap=8px、rightDelta=0、fixedOverlays=0、关闭按钮存在=true、点面板外关闭=true、tabs=['DAG(默认)','泳道']、点任务节点后右栏内容含 tasks/t-256ac4.md；③ 人工看同目录 a1/a4/a8 截图确认锚定位置与右栏外观。

## 实施方案（implementation）
改 conversation-progress.ts（面板段重写、去 foot、新增留痕拉取 effect）；api.ts 加 fetchIsolationLog。验证：pnpm build + 重启 :13080 后浏览器实测。

## 上游产出摘要（dependsSummary）
- 新增 isolation-log 只读端点并接线
- progress 接口透出 promptDifficulty
- 实现节点面板渲染器 node-panel.ts
- 新增苹果风样式分片并接入拼接链

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-23T07:15:39.777Z，窗口 session-f3978f17-ddb9-4fbc-81b6-7948a6999f4d）

conversation-progress 挂载 node-panel：面板改用 renderNodePanel（注入 overview/标题/难度/注入留痕/隔离留痕，覆盖 FR-1/FR-6/FR-9）；面板壳加 × 关闭按钮（dsh-pm-np-close，取代原「收起」）、去掉底部三按钮（打开项目看板/打开需求看板/收起）与旧面板头；api.ts 新增 fetchIsolationLog（照抄 fetchInjectionInfo 口径）；新增 np-switch-view 委托监听（实施节点 [流程图][泳道] 切换）。client bundle 构建+wrap+verify 全过，新符号齐全。

### 完成项

- conversation-progress.ts 改 renderNodePanel + × + 去底部三按钮/旧头
- api.ts +fetchIsolationLog +IsolationLogResponse
- np-switch-view 委托监听（tab/pane toggle）
- 注入/隔离留痕随面板打开拉取（windowCodeFromSessionId 过滤，失败静默空态）
- client bundle build:client + verify OK，dsh-pm-np-close/np-switch-view/isolation-log 等符号入包
- tsc 全净；我的 5 测试文件 43 用例全绿 + progress-nodes-fallback 2 绿

### 改动文件

- `src/client/conversation-progress.ts`
- `src/client/api.ts`

### 下一步

t7 模板与门禁矛盾修复（FR-10），随后 t8 全量构建+浏览器验收 UC-1/4/5

---
