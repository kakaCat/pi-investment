# 评审报告 · REQ-260923134706-e72f（实施方自评）

> 评审人：实施窗口 session-f3978f17 ｜ 日期：2026-09-23 ｜ **独立审阅：无**（本报告为实施方自评，非第三方评审）

## 一、对照需求条款逐条复核

| 条款 | 承诺（用户视角） | 落地证据 | 结论 |
|---|---|---|---|
| FR-1 | 点节点就地下拉，点外/× 收起 | 探针 gap=8px、rightDelta=0、fixedOverlays=0、点面板外关闭=true | ✅ |
| FR-2 | 头两行：需求胶囊+标题 / 状态胶囊+一句话+时间 | REQ-260923134706-e72f + 标题 / 「实施中」+「7/8 完成 · 剩 1 个 · 进行中 t-9a5797」+「5 小时前」 | ✅ |
| FR-3 | 「ℹ️ 基础信息」默认展开、文档可点 | 六节点均默认 open；文档条目点击走右侧栏（a4/a5 已验证真实打开） | ✅ |
| FR-4 | 各节点基础信息各不相同 | 立项/需求分析/设计/拆分/验收/归档 逐个点开核过（详见 tests/evidence.md §3 第⑥行） | ✅ |
| FR-5 | 实施节点不摆基础信息，直接 [流程图][泳道] | 面板内无「基础信息」；两 tab，默认 DAG 分层视图 | ⚠️ 功能达成，**标签文案偏离**（见二.3） |
| FR-6 | 执行流程 = 规定 vs 实际 | 三段齐；动作 ✅3/⬜1 带出处；隔离留痕 REPLACED·17760 字符·range；片段开真实文件；防漂移单测 10 例绿 | ✅ |
| FR-7 | 泳道 6 列、列内滚动、无裁切 | 6 列等宽 220、overflow-y=auto、越界卡片=[]；窄屏 1000 最右 937<1000 | ✅ |
| FR-8 | 苹果风、最宽 720px、超 68vh 内滚 | cssWidth=720px、radius=10px、白底、overflowY=auto | ✅（观感项待人工目测） |
| FR-9 | 取消底部按钮栏 | 底部按钮命中=[]；右上角 × 存在；点外可关 | ✅ |
| FR-10 | 模板与门禁不再互相矛盾 | tests/template-clause-gate.test.ts 5 例绿（六模板定义行可解析 + feature 模板覆盖策略必填节） | ✅ |

## 二、如实记录（4 条，均已在验收材料中单列）

1. **本卡自身踩到并修掉一条门禁**：新增的 `src/client/node-panel-process.ts` 初版 402 行，超「单文件 ≤400 行」门禁，已裁到 399 行（仅并行化两个小函数，行为不变；相关单测仍绿）。
2. **全量回归仍有 7 个文件红**：其中本卡引入的那条已修；尺寸门禁残留的 `src/index.ts`（429 行）在 **HEAD 上就已 415 行 > 400**，属既有红且不在白名单允许范围内；其余 6 个文件是并行工作线留在同一工作区、**未提交**的改动（地址段注入 `src/domain/template/**`、`diag-log.ts` 越界 import、`board.ts` 删归档栏、design 门禁、RandomIdFactory 格式）。按本需求边界（不改看板其他页面、不对计划外调用方做破坏性改动）**未擅自修改**，逐条出处见 tests/evidence.md §4。
3. **FR-5 文案偏离**：需求文档写「流程图 / 泳道」，实现标签为「DAG / 泳道」（代码注释标注系与拆分节点统一命名、经用户裁定）。功能与默认态符合验收标准，仅可见文案与需求字面不一致，交人裁决是按需求改回「流程图」还是认可「DAG」。
4. **条款接收状态仍全红**：`reqboard_status` 显示 10/10 条款「未被接收」。拆分计划（decomposition.md）的覆盖对照表逐条给了落点（FR-1→t4/t6 等），但落库的任务卡未带 `requirement_refs`，台账因此判「未被接收」。这是拆分落库环节的记账缺口，与本卡交付物无关；建议开一张补记卡或在后续需求里确保 `reqboard_decompose` 带 `requirement_refs`。

## 三、残留风险

- **观感项未经 agent 目测**：本窗口模型不支持图像输入，`a1..a8` 截图只能交人看；「苹果风是否真的高级」这类判断不应用探针数字冒充。
- **实例与产物时序**：:13080 于 20:20:20 重启；其后为过尺寸门禁对 `node-panel-process.ts` 做了一次不改变行为的裁剪并重建产物（20:24），并通过 `restart-with-build.sh --check`（20/20 新鲜）。若要求「浏览器加载的字节 = 最后一次构建产物」，需再重启一次实例 —— 因本窗口自身就跑在该实例内（重启会中断当前会话），故未自行重启，此点交人决定。
- **`index.ts` 既有尺寸超标**会让「全量测试绿」长期不可达，建议单开一张重构卡（把配置/装配段拆出），否则后续每个需求都会被这条既有红挡住。

## 四、结论

本需求自身交付物满足「验收标准（整体）」8 步；三条需人裁决项（全量残留红的处置、FR-5 文案、条款接收状态）已如实列明，不掩饰、不代裁。
