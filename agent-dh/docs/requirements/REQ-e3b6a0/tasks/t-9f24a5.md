# t-9f24a5 后置链框架：H1..H5 + 两相执行 + 幂等/降级

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
后置链框架：H1..H5 + 两相执行 + 幂等/降级

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：implement
- 端侧：backend

## 得到什么结果
npx vitest run tests/gate-post-chain.test.ts（新增）断言链序为 H1→H2(skip)→H3→H4→H5、H2 skip 不阻断 H3、H2/H4 抛错后 H1 落库结果不变；开关关时 Phase B 执行计数为 0

## 实施方案（implementation）
新增 application/gate/{GatePostChain,PendingGate}.ts + handlers/；扩展 node-settlement 触发源；ports.ts 加 GatePostChainPort

## 上游产出摘要（dependsSummary）
- 建闸门领域：GateSpec + GateCatalog，收敛四处重复定义

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-20T03:51:06.740Z，窗口 session-878da638-a076-4266-ae40-70a2390060f2）

后置责任链框架落地：新增 application/gate/{GatePostChain,PendingGate}.ts（H1..H5 契约 + 两相执行 + 幂等/短路/降级 + 执行计数），并把结算点扩展为 Phase B 的唯一触发时机（链与 NODE_ISOLATION 各自独立开关，落实 D1「链先开、压缩后开」）。

### 完成项

- GateSpec 增补 GateAnswer / ConfirmContext（链的输入契约）与 pendingKeyOf（幂等键）
- 新增 application/gate/PendingGate.ts：两相之间的内存瞬态登记表（键=窗口，同窗口只留最新一条，不排队）
- 新增 application/gate/GatePostChain.ts：HANDLER_ORDER 唯一顺序事实源（乱序传入会被纠正）、同名校验去重、skip/degraded 均不阻断、handler 抛错就地转 degraded 并计票、ring 容量 500 的幂等去重、stats 五项计数、onStep 留痕钩子（抛错不中断）
- ports.ts 增 GatePostChainPort；node-settlement 增 GateChainTrigger（结算点跑链；链跑过则跳过遗留隔离路径，避免同一轮压缩两次）
- 新增 tests/gate-post-chain.test.ts 7 用例：顺序与短路、抛错降级不冒泡、幂等去重、开关关时 executed/enqueued 计数为 0、no-pending 与同窗口覆盖、onStep 抛错不中断、同名 handler 只跑一次
- 门禁核验：本包 npx tsc --noEmit 无新增错误（仅剩基线 update-task-card.ts 一条）；全量 npx vitest run 与 pristine HEAD 基线逐条 diff → 新增失败 0（14 vs 基线 15，仍保有上一轮修好的棘轮门）

### 改动文件

- `packages/pages/dsh-pmboard/src/application/gate/GatePostChain.ts`
- `packages/pages/dsh-pmboard/src/application/gate/PendingGate.ts`
- `packages/pages/dsh-pmboard/src/domain/gate/GateSpec.ts`
- `packages/pages/dsh-pmboard/src/application/ports.ts`
- `packages/pages/dsh-pmboard/src/application/internal/node-settlement.ts`
- `packages/pages/dsh-pmboard/tests/gate-post-chain.test.ts`

### 下一步

t4 投递修正（AgentDeliverer + 修 onStagePrompt 的 agents.followup 死调用，顺带救活 30min 催办）；t5 H2 压缩接线；t6 H3 注入 + 难度映射。三者都往本框架注册 handler

---
