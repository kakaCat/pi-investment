# t-3f82e9 H3 注入器 + 难度取词映射（promptDifficulty→light/heavy）

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
H3 注入器 + 难度取词映射（promptDifficulty→light/heavy）

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：implement
- 端侧：backend

## 得到什么结果

① npx vitest run tests/difficulty-mapping.test.ts 全绿（11/11：四档枚举全覆盖→light/light/heavy/heavy、取重不取轻、未声明保持向后兼容、显式 difficulty 优先）；② npx vitest run tests/h3-inject.test.ts 全绿（10/10：G1 确认后注入 design 档且 fragmentIds 不含 brainstorming 档、取词阶段由 ctx.to 决定、INV-6 留痕十字段与取词结果一致、四条 skip/degraded、难度透传 expert→heavy 与 simple→light 与未声明不传）；③ npx vitest run tests/stage-prompts.test.ts 仍全绿（回归）；④ 全量 npx vitest run 与 pristine HEAD 基线 diff 后新增失败为 0

## 实施方案（implementation）
新增 application/gate/handlers/h3-inject.ts；domain/prompt 增加四档→两档映射（冲突取重）+ 单测

## 上游产出摘要（dependsSummary）
- 建闸门领域：GateSpec + GateCatalog，收敛四处重复定义

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-20T04:30:03.250Z，窗口 session-878da638-a076-4266-ae40-70a2390060f2）

H3 注入器与难度取词映射落地：新增 h3-inject handler（取词只走 resolveStagePrompt，时序由 ctx.to 保证、先过 isPromptStage 与 stageEnabledFor，产出写进链交换区并落 INV-6 留痕），并新增 domain/prompt/difficulty-mapping.ts 把立项四档（simple/standard/advanced/expert）真正接进取词两档（冲突取重不取轻）——此前 promptDifficulty 只存不用，expert 的需求照样被注入轻档。

### 完成项

- 新增 src/domain/prompt/difficulty-mapping.ts：difficultyFromDeclaredPrompt（simple/standard→light、advanced/expert→heavy、未声明→undefined 交文本推断）、heavierDifficulty（取重不取轻）、describeDeclaredDifficulty（fmt 化）
- StagePromptRequest 增 declaredDifficulty；resolveStagePrompt 优先级改为「显式 difficulty > 声明与推断取重 > 单一来源 > DEFAULT」，并把映射与取重决策写进 difficultyReasons（既有 110 条 prompt 测试全绿 = 向后兼容）
- 新增 src/application/gate/handlers/h3-inject.ts（永不抛）：四条闸门（not_prompt_stage / no_requirement / stage_disabled / empty_prompt 降级）＋ 产出写 scratch.promptText ＋ 按 INV-6 写注入留痕
- GatePostChain 增 ChainScratch（同一次运行的 handler 间交换区，避免 handler 互相 import 或把中间态塞台账）；runPending 每轮新建并注入
- 抽出 handlers/shared.ts（pickGateRequirement / safeReadDoc / reasonOf），h2-compact 改为复用，消除两处重复实现
- 新增 tests/difficulty-mapping.test.ts 11 用例（含用 shared 的真实枚举遍历，少映射一档即红）与 tests/h3-inject.test.ts 10 用例
- 门禁核验：tsc 无新增错误（仅剩基线 update-task-card.ts 一条）；全量 vitest 与 pristine 基线逐条 diff → 新增失败 0（14 vs 15）；客户端产物重建并过 verify-client
- 自查抓到并修正两处自身违规：difficulty-mapping 初版用中文字面量拼接（domain 禁止，被 message-hygiene 拦）、测试里未使用的类型 import（被 tsc 拦）

### 改动文件

- `packages/pages/dsh-pmboard/src/domain/prompt/difficulty-mapping.ts`
- `packages/pages/dsh-pmboard/src/domain/prompt/types.ts`
- `packages/pages/dsh-pmboard/src/domain/prompt/index.ts`
- `packages/pages/dsh-pmboard/src/application/gate/GatePostChain.ts`
- `packages/pages/dsh-pmboard/src/application/gate/handlers/shared.ts`
- `packages/pages/dsh-pmboard/src/application/gate/handlers/h2-compact.ts`
- `packages/pages/dsh-pmboard/src/application/gate/handlers/h3-inject.ts`
- `packages/pages/dsh-pmboard/tests/difficulty-mapping.test.ts`
- `packages/pages/dsh-pmboard/tests/h3-inject.test.ts`

### 下一步

t7 GateAwareQuestions 装饰器 + 三条 pm 弹框带 gate 声明；同时把 H4（唤醒，读 scratch 按 D5 分流）与 H5（留痕）落地并接进组合根——它们是链能真正"自动推进"的最后一环

---
