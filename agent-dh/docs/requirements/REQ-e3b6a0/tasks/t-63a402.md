# t-63a402 建闸门领域：GateSpec + GateCatalog，收敛四处重复定义

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
建闸门领域：GateSpec + GateCatalog，收敛四处重复定义

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：implement
- 端侧：backend

## 得到什么结果

① grep -rn "const ADVANCE_MAP|const questionByTransition" packages/pages/dsh-pmboard/src 返回 0 命中；② npx vitest run tests/gate-catalog.test.ts 全绿（6/6，含 advanceTargetFor 与 questionCardFor 对旧实现逐条对拍、humanOnly 与 HUMAN_ONLY_REQ_TRANSITIONS 互锁）；③ 全量 npx vitest run 与 pristine HEAD 基线逐条 diff 后新增失败为 0；④ grep -rn "brainstorming>design" src 只剩 RequirementStatus 的人工专属集合（由 ② 锁死一致）与注释

## 实施方案（implementation）
新增 src/domain/gate/{GateSpec,GateCatalog}.ts；ArtifactSpec.ts 改再导出；AskConfirm.ts 删私有 ADVANCE_MAP；support.ts 问题卡由 GateSpec 派生

## 上游产出摘要（dependsSummary）
- （无依赖）

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-20T03:48:18.670Z，窗口 session-878da638-a076-4266-ae40-70a2390060f2）

闸门领域建立：新增 domain/gate/{GateSpec,GateCatalog}.ts 作为五道人工闸门的唯一事实源，把此前散在四处的门知识（AskConfirm 私有 ADVANCE_MAP、ArtifactSpec 的产物门表、RequirementStatus 的人工专属集合、support 的问题卡文案）收敛进来；产物门表与问题卡改为派生/单点，既有调用点零改动。

### 完成项

- 新增 src/domain/gate/GateSpec.ts（GateId/GateChannel/VerdictShape/GateSpec + transitionKeyOf）
- 新增 src/domain/gate/GateCatalog.ts：GATE_CATALOG 五道门（含 humanOnly/autoAdvance/questionCard）+ gateById/gateForTransition/gateIdForTransition/advanceTargetFor/isHumanOnlyTransition + ARTIFACT_CONFIRM_GATES 派生 + questionCardFor
- ArtifactSpec.ts 的产物门表字面量迁出，改为再导出（protocol/MoveRequirement/QueryStageDetail/client 零改动）
- AskConfirm.ts 删私有 ADVANCE_MAP，改调 advanceTargetFor（输出行为不变，由对拍测试锁定）
- support.gateQuestionCard 改为转发 questionCardFor（输出逐字不变，由对拍测试锁定）
- 新增 tests/gate-catalog.test.ts（6 用例）：闸门表五条/id 唯一/G0 无产物门、四道产物门与收敛前字面量逐条等价、humanOnly 与 HUMAN_ONLY_REQ_TRANSITIONS 互锁、advanceTargetFor 与旧 ADVANCE_MAP 逐条等价、questionCardFor 与旧实现逐条等价、转移键查闸门
- 归因验证：在 pristine HEAD 建临时 worktree 跑全量测试得基线 15 条失败；与本树逐条 diff → 我新增失败 0 条，且顺带修好 1 条基线失败（message-hygiene 棘轮门：移走 support.ts 的拼接后 application 计数降到基线以下）
- 自查抓到并修正一处自身违规：questionCardFor 初版用字符串拼接，被 message-hygiene「domain 拼接式消息必须为 0」拦下（domain/gate/GateCatalog.ts 2 处）→ 改用 fmt，输出逐字不变（对拍测试复验 6/6）

### 改动文件

- `packages/pages/dsh-pmboard/src/domain/gate/GateSpec.ts`
- `packages/pages/dsh-pmboard/src/domain/gate/GateCatalog.ts`
- `packages/pages/dsh-pmboard/src/domain/artifact/ArtifactSpec.ts`
- `packages/pages/dsh-pmboard/src/application/use-cases/AskConfirm.ts`
- `packages/pages/dsh-pmboard/src/application/internal/support.ts`
- `packages/pages/dsh-pmboard/tests/gate-catalog.test.ts`

### 下一步

t3 后置链框架（H1..H5 + 两相执行 + 幂等/降级）。注：HUMAN_ONLY 与闸门一致性改为「各存一份 + 测试锁死」，避免 domain/gate ↔ domain/requirement 值环（已写入设计文档口径）

---
