# t-3270ae GateAwareQuestions 装饰器 + 三条 pm 弹框带 gate 声明

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
GateAwareQuestions 装饰器 + 三条 pm 弹框带 gate 声明

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：implement
- 端侧：backend

## 得到什么结果
npx vitest run tests/gate-aware-questions.test.ts（新增）断言：新增一个带 opts.gate 的假弹框用例后链被登记，且 GateAwareQuestions.ts 与 GatePostChain.ts 未被修改；AskConfirm.ts / AcceptSheet.ts 三条路径作答后各触发一次链

## 实施方案（implementation）
新增 adapters/GateAwareQuestions.ts；组合根装配；AskConfirm.ts/AcceptSheet.ts 的 ask() 补 gate；删除旧 autoContinue 桩

## 上游产出摘要（dependsSummary）
- 后置链框架：H1..H5 + 两相执行 + 幂等/降级

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-20T06:27:44.742Z，窗口 session-878da638-a076-4266-ae40-70a2390060f2）

t7 第一段完成（任务仍进行中）：装饰器与 gate 声明落地，旧 autoContinue 桩删除。GateAwareQuestions 包在 UserQuestionPort 外层——委托真实 UI 后按 opts.gate 登记链（Phase A 只登记，verdict 留空由 Phase B 的 H1 回填），三条 pm 弹框路径已带 gate 声明。

### 完成项

- 新增 adapters/GateAwareQuestions.ts：协议零变化（questions/answers 原样进出）+ 带 gate 且确有作答才登记 + 取不到窗口不抛
- ports.ts：UserQuestionPort 的 opts 由 autoContinue 桩改为 gate?: GateId（能力改由装饰器织入）；UserQuestionsAdapter 薄化为纯通道（删 onAnswered/extractWindowKey）
- GateCatalog 增 gateFromStage（按作答前阶段查门）；ConfirmContext.verdict 改可选（Phase A 无权断言结果）
- AskConfirm 按「作答前阶段」查门并声明（G1/G2/G3 自动对应）；AcceptSheet 的逐项裁决与最终归档确认两处均声明 G4
- 新增 tests/gate-aware-questions.test.ts 8 用例：装饰器五态（带 gate 登记/不带不登记/无作答不登记/无窗口不抛/透传）＋「零成本扩展」机械判据（装饰器与链源码不得出现具体入口名）＋三条 pm 路径各触发一次（AskConfirm 1 次 G1；accept_sheet 逐项与最终各 1 次 G4）
- 门禁核验：tsc 无新增错误（仅剩基线一条）；全量 vitest 与 pristine 基线逐条 diff → 新增失败 0（14 vs 15）

### 改动文件

- `packages/pages/dsh-pmboard/src/adapters/GateAwareQuestions.ts`
- `packages/pages/dsh-pmboard/src/adapters/UserQuestionsAdapter.ts`
- `packages/pages/dsh-pmboard/src/application/ports.ts`
- `packages/pages/dsh-pmboard/src/domain/gate/GateCatalog.ts`
- `packages/pages/dsh-pmboard/src/domain/gate/GateSpec.ts`
- `packages/pages/dsh-pmboard/src/application/use-cases/AskConfirm.ts`
- `packages/pages/dsh-pmboard/src/application/use-cases/AcceptSheet.ts`
- `packages/pages/dsh-pmboard/tests/gate-aware-questions.test.ts`

### 下一步

t7 第二段（同任务继续）：落地 H1（以台账刷新 verdict/校验推进）、H4（按 D5 分流组唤醒消息并投递）、H5（链级审计留痕）三个 handler，并在组合根把「装饰器 + 链 + 结算点触发」装配起来——这一步做完链才真正端到端可跑

---
## 汇报 2（2026-09-20T06:31:02.327Z，窗口 session-878da638-a076-4266-ae40-70a2390060f2）

t7 第二段完成：H1/H4/H5 三个 handler 落地并在组合根把「装饰器 + 链（H1..H5）+ turn/end 触发」装配起来——链至此端到端可跑（点弹框肯定项 → 校验推进 → 压缩 → 注入 → 唤醒续跑 → 审计留痕）。

### 完成项

- H1（h1-advance）：Phase B 以**台账实时状态**校验并回填——status===to → verdict=affirmative 且回填 requirementId；否则 verdict=negative；无需求 → negative + skip
- H4（h4-resume）：按 D5 分流组唤醒消息（H2 已压缩 → 只发作答摘要；否则摘要 + 阶段纪律全文），经 AgentDeliverer 投递；非肯定项同样唤醒（带用户意见）；未投递只降级
- H5（h5-audit）：把各步结果写成需求时间线评论（如 `[闸门后置链] G1：h1-advance=ok；h2-compact=skip（doc_not_ready）…`）
- 链交换区扩展：compacted（H2 是否真压缩）+ steps（逐步追加，供 H5 审计）
- H2 增 compactionEnabled（D1：链默认开、压缩默认关）；CaptureHook 增 onTurnEnd（**Phase B 唯一时机**，解决"闸门作答不一定有用户消息"的时序问题）
- 组合根装配：链（H1..H5 + warn 转发）、装饰器包装 UserQuestionsAdapter、onTurnEnd → setImmediate 执行 runPending（D-17：不在派发内做会话写操作）；链按幂等键去重，与结算路径重复触发只跑一轮
- 新增 tests/gate-handlers.test.ts 9 用例（H1 三态 / H4 四种分流与降级 / H5 审计与 skip）
- 门禁核验：tsc 无新增错误（仅基线一条）；全量 vitest 与 pristine 基线 diff → 新增失败 0（14 vs 15）

### 改动文件

- `packages/pages/dsh-pmboard/src/application/gate/handlers/h1-advance.ts`
- `packages/pages/dsh-pmboard/src/application/gate/handlers/h4-resume.ts`
- `packages/pages/dsh-pmboard/src/application/gate/handlers/h5-audit.ts`
- `packages/pages/dsh-pmboard/src/application/gate/handlers/h2-compact.ts`
- `packages/pages/dsh-pmboard/src/application/gate/GatePostChain.ts`
- `packages/pages/dsh-pmboard/src/adapters/CaptureHook.ts`
- `packages/pages/dsh-pmboard/src/domain/gate/GateSpec.ts`
- `packages/pages/dsh-pmboard/src/index.ts`
- `packages/pages/dsh-pmboard/tests/gate-handlers.test.ts`

### 下一步

t8 立项 pm 专有弹框（reqboard_capture + CaptureHook 文案改指 + 两问/三问统一）——**需等 REQ-99f5fe 停手**；t9 看板通道 B（确认即推进 + 链侧投递）

---
