# t-f33035 立项 pm 专有弹框工具 reqboard_capture + 文案改造

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
立项 pm 专有弹框工具 reqboard_capture + 文案改造

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：implement
- 端侧：backend

## 得到什么结果

grep -n "ask_user_question" packages/pages/dsh-pmboard/src/application/internal/capture-section.ts → 0 命中（本卡汇报 1 已实测）；
三处「两问/三问」口径统一为三问（capture-section.ts / QueryState.ts / CreateTool/prompt.ts），全仓 src「两问」残留 0；
npx vitest run tests/capture-tool.test.ts 通过（含三问同批弹出、自定义优先、创建+绑定+推进、缺项 defaults_used、通道不可用 fallback=board 不创建、用户取消中性、名称为空响亮失败、已绑定则弹框根本没发生）。
说明（正位，2026-09-20 用户裁定）：需求级线上走查（AC-7.1「立项后不再输入任何消息即产出 requirement.md」）不在本卡承载——改由 accepting 阶段的 verification 证据采集，验收义务不变。

## 实施方案（implementation）
新增 tools/CaptureTool/* + 用例 captureCreate；capture-section.ts 文案改指；QueryState.ts:63 与 CreateTool/prompt.ts 对齐

## 上游产出摘要（dependsSummary）
- GateAwareQuestions 装饰器 + 三条 pm 弹框带 gate 声明

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-20T07:12:52.677Z，窗口 session-878da638-a076-4266-ae40-70a2390060f2）

立项不再靠「先弹宿主通用弹框、再另调一次创建」的两段式：pmboard 有了自己的立项弹框工具 reqboard_capture——一次调用弹出三问（需求名称/需求类型/提示词难度），用户作答即在同一次调用内创建需求、绑定本窗口、把状态推到需求分析；之后的「注入需求分析纪律 + 继续干活」由后置链自动接上，用户不用再发任何消息。同时把三处互相矛盾的「两问/三问」口径统一为三问，并让注入文案不再点名宿主通用弹框。

### 完成项

- 新增用例 application/use-cases/CaptureRequirement.ts（captureRequirement）：前置判定（已绑定/遗留 pending 卡直接拒绝，不白弹一次框）→ 三问弹框（声明 gate=G0，由装饰器登记后置链）→ 答案映射 → 创建即立项 → 原子推进 draft→brainstorming（用唯一迁移助手 transitionRequirement，带快照）
- 新增 application/internal/capture-mapping.ts：三问题目构造 + 答案映射（名称自定义优先、类型/难度缺项或非法回落既有默认并记 defaults_used；**名称没有默认值，空则响亮失败**）。单独成模块是为让编排与表单契约正交，也避免输出契约静态扫描把内部映射对象误判成未声明的响应字段
- 新增 tools/CaptureTool/{CaptureTool.ts,prompt.ts,index.ts}（三段式）并注册到 tools/index.ts 与组合根（工具数 11→12，注册清单与 info 日志同步）
- 口径统一为三问：capture-section.ts（引导段 + 针对性提示，ask_user_question 命中数 1→0）、QueryState.ts 的自查提示、CreateTool/prompt.ts（改为指向 reqboard_capture，reqboard_create 降为「已明确取值/弹框不可用」的手工路径）；CaptureHook/index/protocol/CreateTool schema 的注释与描述同步（全仓 src「两问」残留 0）
- AC-7.2 新测 tests/capture-tool.test.ts（9 例）：三问同批弹出、自定义优先、创建+绑定+推进、缺项 defaults_used、通道不可用 fallback=board 且不创建、用户取消中性失败、名称为空响亮失败、已绑定则**弹框根本没发生**
- 修复因新工具暴露的门禁：output-contract 的 RESPONSE_SOURCES 补 Capture 映射；tools-dispatch 目录清单 9→12 并给 REQ-327bdf 的两个非三段式目录显式留债；apply-wiring 工具清单 9→12（这两处是 REQ-327bdf 加工具后遗留的过期期望，顺带修好）
- 门禁实测：npx tsc --noEmit 仅剩基线 1 条（TaskExecuteTool/update-task-card.ts TS6133，非本需求）；npx vitest run 全量 12 失败 1383 通过——逐条核对全部落在他人需求债上（REQ-327bdf 的 TaskExecuteTool/TaskStatusTool 5 条、REQ-a8d582 的验收覆盖 5 条、typecheck 基线 1 条、size-budget 的 src/index.ts 432 行 1 条，改前即 431 行超限），**本需求新增失败 0**；pnpm build:client + verify-client 通过（bundle=232395 bytes，关键符号齐全，styles.ts 括号配对）

### 改动文件

- `packages/pages/dsh-pmboard/src/application/use-cases/CaptureRequirement.ts`
- `packages/pages/dsh-pmboard/src/application/internal/capture-mapping.ts`
- `packages/pages/dsh-pmboard/src/application/internal/capture-section.ts`
- `packages/pages/dsh-pmboard/src/application/query/QueryState.ts`
- `packages/pages/dsh-pmboard/src/tools/CaptureTool/CaptureTool.ts`
- `packages/pages/dsh-pmboard/src/tools/CaptureTool/prompt.ts`
- `packages/pages/dsh-pmboard/src/tools/CaptureTool/index.ts`
- `packages/pages/dsh-pmboard/src/tools/index.ts`
- `packages/pages/dsh-pmboard/src/tools/CreateTool/prompt.ts`
- `packages/pages/dsh-pmboard/src/tools/CreateTool/CreateTool.ts`
- `packages/pages/dsh-pmboard/src/adapters/CaptureHook.ts`
- `packages/pages/dsh-pmboard/src/shared/protocol.ts`
- `packages/pages/dsh-pmboard/src/index.ts`
- `packages/pages/dsh-pmboard/tests/capture-tool.test.ts`
- `packages/pages/dsh-pmboard/tests/capture.test.ts`
- `packages/pages/dsh-pmboard/tests/tools-dispatch.test.ts`
- `packages/pages/dsh-pmboard/tests/apply-wiring.test.ts`
- `packages/pages/dsh-pmboard/tests/output-contract.test.ts`
- `packages/pages/dsh-pmboard/tests/helpers/tool-deps.ts`

### 下一步

联调：真机走一次立项（:13080 需重启加载新代码，与 t10 的 AC-11.2 共用同一次走查）——验收标准第 2 条「真实走一次立项后不再输入任何消息即产出 requirement.md」在重启后验证。另有 bookkeeping 待用户在看板取消 REQ-99f5fe（其范围已按 D7② 并入本需求）。

---
