# t-44ae54 H2 压缩接线：接入 isolateNodeContext（含自足判定与开关）

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
H2 压缩接线：接入 isolateNodeContext（含自足判定与开关）

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：implement
- 端侧：backend

## 得到什么结果
npx vitest run tests/h2-compact.test.ts（新增）断言 status=replaced 且 range 自首个非 system 节点起、artifactSeq < replacementSeq；文档不存在→skip、idle=false→skip、触达不到→fallback

## 实施方案（implementation）
新增 application/gate/handlers/h2-compact.ts；调用既有 isolateNodeContext；开关沿用 NODE_ISOLATION

## 上游产出摘要（dependsSummary）
- 后置链框架：H1..H5 + 两相执行 + 幂等/降级

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-20T04:02:13.929Z，窗口 session-878da638-a076-4266-ae40-70a2390060f2）

H2 压缩接线完成：新增 application/gate/handlers/h2-compact.ts，把既有 isolateNodeContext 接进闸门后置链——压缩算法一行未改，本层只做条件判定、端口构造与四态映射（replaced→continue / skipped→skip / rejected 与 fallback→degraded），并新增"输入包自足"这道闸（需求文档未落盘就不压，这正是 G0 不压缩的依据）。

### 完成项

- 新增 src/application/gate/handlers/h2-compact.ts（name=h2-compact，契约上永不抛）
- 条件判定：非肯定项→skip(negative_verdict)；to 非可注入阶段→skip(not_prompt_stage)；无归属需求→skip(no_requirement)；需求文档未落盘→skip(doc_not_ready)；idle=false→skip(agent_busy)
- 失败态映射：触达不到→degraded(isolation_unreachable)；边界不平衡→degraded(boundary_unbalanced)；用例抛错→degraded(h2_threw)；docs.read 抛错按未落盘处理
- 新增 tests/h2-compact.test.ts 11 用例：真跑 replaced（假隔离端口 + 真实 isolateNodeContext）断言 replace 区间 start=1（首个非 system 节点）/ end=2 / shadowed=[1,2]，且 persistArtifacts 取到的台账 revision(0) < 替换 seq(100)，即"先落盘再遗弃"可观测；另 10 条覆盖各 skip/degraded 分支与请求形状
- 门禁核验：tsc 无新增错误（仅剩基线 update-task-card.ts 一条）；全量 vitest 与 pristine HEAD 基线逐条 diff → 新增失败 0（14 vs 15）

### 改动文件

- `packages/pages/dsh-pmboard/src/application/gate/handlers/h2-compact.ts`
- `packages/pages/dsh-pmboard/tests/h2-compact.test.ts`

### 下一步

t6 H3 注入器 + 难度取词映射（promptDifficulty → light/heavy，冲突取重），挂到同一条链

---
