# t-0f54a6 审计全仓投递形状并归档验证证据

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
审计全仓投递形状并归档验证证据

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：review
- 端侧：doc

## 得到什么结果
grep -rn 'followup(' packages/*/src 中每个调用点的实参均为对象信封（或 createUserMessage），无字符串；docs/requirements/REQ-9494f9/verification.md 存在且 AC-1..AC-6 每条都有命令/输出或证据路径。

## 实施方案（implementation）
grep -rn 'followup(' packages/*/src 逐点核对形状；产出 docs/requirements/REQ-9494f9/verification.md；decision_audit(record) + memory_write(experience)。

## 上游产出摘要（dependsSummary）
- 补催办消息形状回归测试
- 修复 session-6faac762 脏 inbox 并核验控制流恢复

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-20T18:51:22.941Z，窗口 session-f8006463-e476-454d-8262-b86351a7b8bb）

投递形状审计做完了（AC-3 通过：全仓 followup 调用点无一传字符串），并把 AC-1..AC-6 的逐条状态与交接 runbook 落成 verification.md；但 AC-4/5/6 依赖 t-f0dcf2，本卡不能结单，退回待办。

### 完成项

- 全仓 followup 调用点审计（grep packages/*/src packages/*/*/src）：lifecycle ×6 用 createUserMessage、solve-kit/target.ts:35 传调用方构造的信封、solve-kit/host.ts:147 用 buildNudgeMessage 信封、pmboard AgentDeliverer 结构复刻信封——无字符串实参
- 产出 docs/requirements/REQ-9494f9/verification.md（草稿·未完成）：AC-1/2/3 带命令与输出通过；AC-4/5/6 标待办并给出复验命令
- 汇入未引入回归的证据：solve-kit + execution + pmboard 相关 16 tests 全绿；全量套件 14 files failed 均为既有环境/其他包问题，无一与本需求相关
- 汇入 t-f0dcf2 的交接 runbook（备份路径 / --apply 命令 / 重启方式 / 回滚方式 / 残留风险）

### 改动文件

- `docs/requirements/REQ-9494f9/verification.md`

### 下一步

t-f0dcf2 另排窗口执行（--apply + 重启 + 三重复验）后，本卡再补 AC-4/5/6 证据并结单

---
## 汇报 2（2026-09-20T18:57:48.261Z，窗口 session-f8006463-e476-454d-8262-b86351a7b8bb）

投递形状审计结论已坐实并写进验证记录：全仓 followup 调用点没有一处再传字符串（要么用 createUserMessage、要么由调用方构造信封），本次修复点 host.ts:147 也在其中。验证记录里 AC-1/2/3/4 都有可复跑的命令与输出；AC-6 的原文判据被证明不成立（追加式日志），已换成等价判据并跑通；只有 AC-5 还差一次重启后的浏览器确认，所以本卡暂不结单。

### 完成项

- 重跑全仓 grep followup( 审计：lifecycle×6 用 createUserMessage、solve-kit/target.ts:35 传调用方信封、solve-kit/host.ts:147 用 buildNudgeMessage 信封、pmboard AgentDeliverer 结构复刻信封——无字符串实参（AC-3 通过）
- verification.md 重写为可复核版本：AC-1..AC-6 逐条给出命令/输出或证据路径与数据时点
- AC-4 证据补齐（追加帧输出 + 独立折叠复核 + 幂等 dry-run）
- AC-6 记为判据缺陷并给出等价判据与体检脚本；AC-5 记为待重启，附精确剩余步骤
- 汇入未引入回归的证据（solve-kit 等 16 tests 全绿）

### 改动文件

- `docs/requirements/REQ-9494f9/verification.md`

### 下一步

待 t-f0dcf2 的重启与 AC-5 浏览器确认完成后，本卡补上 AC-5 证据并结单；AC-6 判据改写需你裁决。

---
