# t-673e77 补催办消息形状回归测试

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
补催办消息形状回归测试

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：test
- 端侧：backend

## 得到什么结果
cd agent-dh && npx vitest run packages/solve-kit/tests/nudge-message.test.ts → 全绿；故障注入：把 buildNudgeMessage 临时改回返回字符串后同命令必须变红。

## 实施方案（implementation）
用 vitest（根 vitest.config.ts 已 include packages/**/tests/**/*.test.ts）；从 ../src/host.js 导入 buildNudgeMessage；严格双重身内联在测试中，不引框架包。

## 上游产出摘要（dependsSummary）
- 修 buildNudgeMessage 信封并让调用点传 plugin

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-20T18:48:18.726Z，窗口 session-f8006463-e476-454d-8262-b86351a7b8bb）

把「催办消息必须是 UserMessage 信封、不能是字符串」这条复现路径固化成一个独立回归门禁：修复前必红、修复后必绿，并用一条反例断言防止双重身退化成恒真。

### 完成项

- 新增 packages/solve-kit/tests/nudge-message.test.ts（4 个用例）
- A1 形状：对象 + id 为 UUID 形态字符串 + role='user' + content[0].type='text'（含「收单催办」）+ source.kind='plugin' 且 plugin 透传
- A2 唯一性：连续两次调用 id 不相等（对治 message "undefined" is already pending）
- A3 严格双重身：逐条复刻框架两个真实读取点（inbox.mutate 读 message.id / queueItemsFromInbox 读 message.source.kind）→ 不抛错
- A4 反例锁：同一双重身对字符串投递必须抛错——证明 A3 不是恒真
- 红绿对比实测：故障注入（把 buildNudgeMessage 临时改回 return text）→ 3 failed | 1 passed；还原后 → 4 passed。残留检查：FAULT-INJECTION 行数=0
- 未引入回归：solve-kit + solve-predispatch-guard + agent-deliverer 共 16 tests 全绿；全量套件已在后台跑（bash-1）

### 改动文件

- `packages/solve-kit/tests/nudge-message.test.ts`

### 下一步

t-f0dcf2 存量脏 inbox 修复与线上核验

---
