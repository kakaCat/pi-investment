# t-90d985 修 buildNudgeMessage 信封并让调用点传 plugin

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
修 buildNudgeMessage 信封并让调用点传 plugin

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：implement
- 端侧：backend

## 得到什么结果
cd agent-dh && npx tsx -e "import {buildNudgeMessage} from './packages/solve-kit/src/host.ts'; const m:any=buildNudgeMessage({eventId:'x',title:'t',attempt:1,total:3,actorWindow:'w-a',panel:'执行看板',plugin:'dashboard-execution'}); console.log(typeof m.id, m.role, m.source.kind, m.content[0].type)" → 输出 string user plugin text

## 实施方案（implementation）
编辑 packages/solve-kit/src/host.ts:106-114（返回值改对象信封，参数表加 plugin）与 :134（补 plugin: opts.plugin）；randomUUID 已在文件头 import；不引入新依赖；不顺手重构（不改走 deliverMessage）。

## 上游产出摘要（dependsSummary）
- （无依赖）

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-20T18:47:34.291Z，窗口 session-f8006463-e476-454d-8262-b86351a7b8bb）

催办消息从「纯字符串」改为合法的 UserMessage 信封，并让调用点带上 plugin 署名——投递物不再是坏形状，`message.id`/`message.source` 不再是 undefined。

### 完成项

- buildNudgeMessage 返回 {id: randomUUID(), role:'user', content:[{type:'text',text}], source:{kind:'plugin',plugin}}，与同文件 buildSolveMessage 逐字段同形
- 参数表新增 plugin；watchResolution 调用点（host.ts:134）传 plugin: opts.plugin
- 导出该纯函数供回归测试直接断言（仅导出，语义不变）
- 补了形状纪律注释：说明传字符串会同时触发 inbox 去重拒绝与 Host 级 control stream 崩溃
- 自测：npx tsx 一行式验收命令 → 输出 `string user plugin text`
- typecheck 复核：tsc --noEmit 在 solve-kit 下仅剩 client/index.ts 的既有 DOM lib 报错，host.ts 无新增错误

### 改动文件

- `packages/solve-kit/src/host.ts`

### 下一步

t-673e77 独立回归测试卡：把这条复现路径固化成先红后绿的门禁

---
