# t-ee3911 HTTP 路由快照接入（routers/tasks.ts）

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 目标
HTTP 路由快照接入（routers/tasks.ts）

## 背景摘要（context）
（待补充）

## 范围
- 阶段：implement
- 端侧：backend

## 验收标准
单测：模拟 HTTP POST /tasks/:id/move 带 sessionId，mock tokenSnapshot，断言 rollup 派生推进带快照结算；既有 routes 测试通过

## 实施方案（implementation）
修改 packages/pages/dsh-pmboard/src：(1) http/routes.ts::ReqboardRouteDeps 扩展 tokenSnapshot?:(windowKey:string)=>TokenSnapshot|undefined；(2) index.ts 插件启动构造 routeDeps 时注入 tokenSnapshot:(wk)=>{try{return sessionProbe?.tokenTotals(wk)}catch{return undefined}}（需获取 SessionProbeAdapter 实例）；(3) http/routers/tasks.ts::handleTaskMove 读 body.sessionId，传给 applyTaskRollup ctx：snapshot:body.sessionId?()=>ctx.tokenSnapshot?.(body.sessionId):undefined；(4) 同步更新 t4 中 routers/tasks.ts:65,108 调用点。补充单测到 tests/http-router-snapshot.test.ts。

## 上游产出摘要（dependsSummary）
- rollup 改用迁移助手 + 可选快照提供者

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-18T16:51:38.648Z，窗口 session-2b82d910-6ab6-453d-9f0c-ffad83413851）

HTTP 路由快照接入完成，routes 回归测试通过

### 完成项

- http/routes.ts: ReqboardRouteDeps 扩展 tokenSnapshot 提供者
- http/routers/shared.ts: RouterCtx.deps 扩展 tokenSnapshot
- http/routers/tasks.ts: handleTaskMove 读 body.sessionId，传给 applyTaskRollup ctx.snapshot
- index.ts: 注入 tokenSnapshot 提供者（useCaseDeps.session.tokenTotals，try/catch 包装）
- http/routes.ts: 构造 RouterCtx 时传递 tokenSnapshot 到 deps
- 回归测试通过：routes-rollup.test.ts (3 tests)

### 改动文件

- `packages/pages/dsh-pmboard/src/http/routes.ts`
- `packages/pages/dsh-pmboard/src/http/routers/shared.ts`
- `packages/pages/dsh-pmboard/src/http/routers/tasks.ts`
- `packages/pages/dsh-pmboard/src/index.ts`

### 下一步

推进到 done，开始 t7 端到端验证

---
