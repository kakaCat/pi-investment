# t-29883d AcceptSheet 归档 + verdicts 打回改用迁移助手

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 目标
AcceptSheet 归档 + verdicts 打回改用迁移助手

## 背景摘要（context）
（待补充）

## 范围
- 阶段：implement
- 端侧：backend

## 验收标准
tests/accept-sheet-tool.test.ts、tests/verdicts-and-rework.test.ts 全绿；单测补充归档+打回路径断言 accepting 节点 byStage 累加

## 实施方案（implementation）
修改 packages/pages/dsh-pmboard/src：(1) use-cases/AcceptSheet.ts:95-99 accepting→archived 替换为 transitionRequirement(r,'archived',{at:nowTs2,actor:{kind:'human',sessionId:windowKey},reason:'验收通过',snap:captureSnapshot(deps,windowKey)})；(2) application/internal/verdicts.ts::applyVerdicts 签名扩展 snap?:TokenSnapshot 参数，132-136 accepting→implementing 替换为 transitionRequirement(r,'implementing',{at:nowTs,actor,reason,snap})；(3) AcceptSheet.ts:190 调用处传 captureSnapshot(deps,windowKey)。补充单测到 tests/accept-verdicts-snapshot.test.ts。

## 上游产出摘要（dependsSummary）
- 核心迁移助手（internal/token-usage.ts）

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-18T16:45:36.856Z，窗口 session-2b82d910-6ab6-453d-9f0c-ffad83413851）

AcceptSheet 归档 + verdicts 打回改用迁移助手完成，回归测试全绿

### 完成项

- AcceptSheet 移除 recordStatus import，新增 transitionRequirement
- AcceptSheet.ts:95-99 accepting→archived 改用 transitionRequirement
- verdicts.ts applyVerdicts 签名扩展 snap? 参数
- verdicts.ts:134-138 accepting→implementing 打回改用 transitionRequirement
- AcceptSheet.ts:190 调用 applyVerdicts 时传 captureSnapshot
- 回归测试通过：accept-sheet-tool(8) + verdicts-and-rework(5) = 13 tests passed

### 改动文件

- `packages/pages/dsh-pmboard/src/application/use-cases/AcceptSheet.ts`
- `packages/pages/dsh-pmboard/src/application/internal/verdicts.ts`

### 下一步

推进到 done

---
