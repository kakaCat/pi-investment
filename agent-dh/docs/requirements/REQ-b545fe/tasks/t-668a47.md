# t-668a47 MoveRequirement 改用迁移助手

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 目标
MoveRequirement 改用迁移助手

## 背景摘要（context）
（待补充）

## 范围
- 阶段：implement
- 端侧：backend

## 验收标准

命令：cd packages/pages/dsh-pmboard && pnpm test artifact-gates rollup plan-mode 2>&1 | grep passed；期望：artifact-gates/rollup/plan-mode 测试全部通过

## 实施方案（implementation）
packages/pages/dsh-pmboard/src/application/use-cases/MoveRequirement.ts:113-119 替换为 transitionRequirement(req, to, {at:deps.clock.now(), actor:{kind:'agent',sessionId:windowKey}, reason, snap:captureSnapshot(deps,windowKey)})。删除原地状态迁移+结算逻辑。补充单测到 tests/token-usage.test.ts。

## 上游产出摘要（dependsSummary）
- 核心迁移助手（internal/token-usage.ts）

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-18T16:41:14.144Z，窗口 session-2b82d910-6ab6-453d-9f0c-ffad83413851）

MoveRequirement 改用迁移助手完成，回归测试全绿

### 完成项

- 更新 import：移除 accumulateStageDelta，引入 transitionRequirement
- 替换 MoveRequirement.ts:113-119 为 transitionRequirement 调用
- 回归测试通过：artifact-gates(24) + rollup(28) + plan-mode(9) = 61 tests passed

### 改动文件

- `packages/pages/dsh-pmboard/src/application/use-cases/MoveRequirement.ts`

### 下一步

推进到 done，继续改造 t3-t5

---
## 汇报 2（2026-09-18T16:45:05.140Z，窗口 session-2b82d910-6ab6-453d-9f0c-ffad83413851）

MoveRequirement 改用迁移助手完成，回归测试全绿

### 完成项

- 更新 import：移除 accumulateStageDelta，引入 transitionRequirement
- 替换 MoveRequirement.ts:113-119 为 transitionRequirement 调用
- 回归测试通过：artifact-gates(24) + rollup(28) + plan-mode(9) = 61 tests passed

### 改动文件

- `packages/pages/dsh-pmboard/src/application/use-cases/MoveRequirement.ts`

### 下一步

推进到 done

---
