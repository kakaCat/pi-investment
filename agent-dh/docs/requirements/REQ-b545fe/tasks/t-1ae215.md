# t-1ae215 rollup 改用迁移助手 + 可选快照提供者

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 目标
rollup 改用迁移助手 + 可选快照提供者

## 背景摘要（context）
（待补充）

## 范围
- 阶段：implement
- 端侧：backend

## 验收标准
tests/routes-rollup.test.ts 全绿；单测补充有/无快照提供者两种场景，断言 byStage 正确累加或诚实不累加

## 实施方案（implementation）
修改 packages/pages/dsh-pmboard/src/application/internal/rollup.ts：(1) RollupContext 扩展 snapshot?:(req)=>TokenSnapshot|undefined；(2) advance 函数调用 transitionRequirement(req, to, {at:ctx.now, actor:{kind:'system'}, reason, snap:ctx.snapshot?.(req)})，删除原地迁移逻辑。(3) 调用方适配：MoveRequirement.ts:127/MoveTask.ts:101/Decompose.ts:239/SubmitVerification.ts:161 传 snapshot:()=>captureSnapshot(deps,windowKey)；index.ts:131/236 不传（无会话）。补充单测到 tests/rollup-snapshot.test.ts。

## 上游产出摘要（dependsSummary）
- 核心迁移助手（internal/token-usage.ts）

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-18T16:45:36.720Z，窗口 session-2b82d910-6ab6-453d-9f0c-ffad83413851）

rollup 改用迁移助手 + 可选快照提供者完成，全部调用方已适配

### 完成项

- RollupContext 扩展 snapshot?: (req) => TokenSnapshot | undefined
- advance 函数改用 transitionRequirement，snap 从 ctx.snapshot?.(req) 获取
- 调用方适配：MoveRequirement/MoveTask/Decompose/SubmitVerification 传快照函数
- 回归测试通过：routes-rollup.test.ts + artifact-gates + plan-mode 全绿

### 改动文件

- `packages/pages/dsh-pmboard/src/application/internal/rollup.ts`
- `packages/pages/dsh-pmboard/src/application/use-cases/MoveRequirement.ts`
- `packages/pages/dsh-pmboard/src/application/use-cases/MoveTask.ts`
- `packages/pages/dsh-pmboard/src/application/use-cases/Decompose.ts`
- `packages/pages/dsh-pmboard/src/application/use-cases/SubmitVerification.ts`

### 下一步

推进到 done

---
