# t-0553af 实现设计文档拆分内容硬门禁与成组确认

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
实现设计文档拆分内容硬门禁与成组确认

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：implement
- 端侧：backend

## 得到什么结果
运行 `npx vitest run tests/decomposition-detect.test.ts tests/confirm-group.test.ts`；预期全绿。断言：depends_on 表头命中、代码块示例不命中、中文散文不命中；三通道均抛 design_contains_decomposition 且不落章不推进；确认 kind=design 后全部 design 产物都有 confirmedAt。

## 实施方案（implementation）
修改 src/application/internal/content-gates.ts：新增 detectDecompositionFeatures，识别 depends_on 表头、acceptance+implementation 表头、H2+「拆分计划/任务 DAG」标题，并剥离围栏代码块。修改 content-gate-wiring.ts：新增 checkDesignDecompositionGate。把扫描接到 ConfirmArtifact.ts（文字证据）、AskConfirm.ts（弹框）、http/routers/requirements.ts handleArtifactConfirm（看板一键）落章前；kind=design 确认改为全部 design 产物一次性落章。新增 tests/decomposition-detect.test.ts 与 tests/confirm-group.test.ts。

## 上游产出摘要（dependsSummary）
- 定稿设计文档策略与闸门错误契约

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-21T05:16:52.022Z，窗口 session-a3d998e1-5a3b-4729-970c-9263dc64e132）

设计文档里再混入任务表/拆分计划，三条确认通道都会在落章前拦下：扫描 design/*.md 发现 depends_on 表头、acceptance+implementation 表头或「拆分计划/任务 DAG」标题即拒，并指引把内容挪到拆分阶段（代码块里的示例与中文散文提及不误伤）。同时「确认设计文档」从一次只落一份章改为成组落章——人在弹框、会话文字、看板一键任意一个通道确认一次，全部设计文档一次性盖章，不会再出现「只确认了第一份、其余几份没人看就放行」。

### 完成项

- content-gates.ts：detectDecompositionFeatures 纯判定（特征集 v1 宁漏勿冤：depends_on 表头 / acceptance+implementation 表头 / H2+ 拆分计划·任务 DAG 标题；围栏代码块由 parseDocument 剥离）
- design-gates.ts：checkDesignDecompositionGate 装配（扫 design/ 目录全部落盘文档，命中即 design_contains_decomposition 带文件+特征清单）
- 三通道落章前接线：AskConfirm 弹框（用户作答后落章前）/ ConfirmArtifact 文字证据 / handleArtifactConfirm 看板一键（非存量且 docs 未装配 fail-closed）
- 成组落章：artifact-gates.ts 新增 artifactsToConfirm（kind=design 全部、其余 kind 首份），三通道共用；留痕写明「成组确认 N 份」
- http 适配层 'design' 字面量改走 domain 助手 isDesignArtifactKind（过 layer-boundary）；拼接消息 fmt 化（message-hygiene 棘轮净降）
- 新增 tests/decomposition-detect.test.ts（10 用例：表头/标题命中、代码块与散文不命中、三通道拒且不落章不推进）与 tests/confirm-group.test.ts（5 用例：三通道全部落章、非 design 首份语义、缺产物报 missing、UC-4 成组后 G2 自动推进放行）
- 验收命令两文件 15 用例全绿；全量回归 133 文件 1626 用例全绿；tsc 0 错误

### 改动文件

- `packages/pages/dsh-pmboard/src/application/internal/content-gates.ts`
- `packages/pages/dsh-pmboard/src/application/internal/design-gates.ts`
- `packages/pages/dsh-pmboard/src/application/internal/artifact-gates.ts`
- `packages/pages/dsh-pmboard/src/application/internal/content-gate-wiring.ts`
- `packages/pages/dsh-pmboard/src/application/use-cases/ConfirmArtifact.ts`
- `packages/pages/dsh-pmboard/src/application/use-cases/AskConfirm.ts`
- `packages/pages/dsh-pmboard/src/http/routers/requirements.ts`
- `packages/pages/dsh-pmboard/src/domain/artifact/ArtifactSpec.ts`
- `packages/pages/dsh-pmboard/tests/decomposition-detect.test.ts`
- `packages/pages/dsh-pmboard/tests/confirm-group.test.ts`
- `packages/pages/dsh-pmboard/lib/client.js`

### 下一步

T-4（t-a19c66）：产物登记可打开性校验（submit 两入口 + verification/archive + ArtifactSync）

---
