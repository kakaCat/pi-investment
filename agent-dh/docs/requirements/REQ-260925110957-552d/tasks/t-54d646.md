# t-54d646 台账迁移

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
台账迁移

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：implement
- 端侧：backend

## 得到什么结果

1. Schema版本检查: `grep "REQBOARD_SCHEMA_VERSION = 8" packages/web/dsh-pmboard/src/shared/protocol.ts` 预期找到该行
2. 单元测试通过: `cd packages/web/dsh-pmboard && npx vitest run tests/application/repository.test.ts 2>&1 | grep schemaVersion` 预期包含 "toBe(8)"

## 实施方案（implementation）
1. 修改 repositories/RequirementRepository.ts：SCHEMA_VERSION 递增到 8
2. 读取时容错处理：filesPlanned/runId 等字段缺省时按旧行为
3. 写入时始终包含新字段（即使为 undefined）
4. 新增 tests/unit/migration-v8.test.ts 验证兼容性

## 上游产出摘要（dependsSummary）
- 领域类型定义

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-25T05:23:55.562Z，窗口 session-c954a261-4edb-4445-b1ff-56a53a9b5431）

完成台账迁移：SCHEMA_VERSION 递增到 8，兼容 v7 旧台账读取

### 完成项

- 修改 shared/protocol.ts：REQBOARD_SCHEMA_VERSION 从 7 递增到 8
- 确认读取逻辑兼容：新字段（filesPlanned, runId, currentSubtaskId, stepIndex, heartbeatAt）都是可选类型，旧台账加载时自动为 undefined
- 确认写入逻辑保持：新字段可为 undefined，不会破坏台账结构
- 新增 tests/unit/migration-v8.test.ts：覆盖 v7→v8 迁移场景，5个测试全部通过

### 改动文件

- `packages/web/dsh-pmboard/src/shared/protocol.ts`
- `packages/web/dsh-pmboard/tests/unit/migration-v8.test.ts`

### 下一步

台账迁移完成，下一步：t3 DSH jobs 适配器（封装 ctx.jobs.start/get）

---
