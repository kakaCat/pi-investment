# t-7f957a 仓储层扩展

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
仓储层扩展

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：implement
- 端侧：backend

## 得到什么结果
tsc 通过；新方法可被用例层调用；单测覆盖 checkpoint 读写

## 实施方案（implementation）
1. 修改 repositories/RequirementRepository.ts：
   - 新增 updateRunState(reqId, {runId, stepIndex, currentSubtaskId, heartbeatAt})
   - 新增 readCheckpoint(reqId) → Checkpoint | null
   - 新增 clearCheckpoint(reqId)
2. 修改 repositories/TaskRepository.ts：
   - 新增 getOrphans(reqId, thresholdMs) → SubtaskRecord[]
   - 新增 updateHeartbeat(subtaskId, timestamp)

## 上游产出摘要（dependsSummary）
- 领域类型定义

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-25T07:09:14.456Z，窗口 session-c954a261-4edb-4445-b1ff-56a53a9b5431）

完成仓储层扩展：新增 checkpoint 读写、批量查询、孤儿检测

### 完成项

- 新增 repositories/RequirementRepository.ts：需求仓储接口和内存实现
- 实现 updateRunState()：更新运行状态（runId/stepIndex/currentSubtaskId/heartbeatAt）
- 实现 readCheckpoint()：读取 checkpoint
- 实现 clearCheckpoint()：清理 checkpoint 字段
- 新增 repositories/TaskRepository.ts：任务仓储接口和内存实现
- 实现 getTasksByRequirement()：批量查询任务
- 实现 getOrphans()：孤儿任务检测
- 实现 updateHeartbeat()：更新任务心跳
- 新增 tests/unit/repository-extensions.test.ts：14个测试全部通过
- 测试覆盖：checkpoint CRUD、批量查询、孤儿检测、心跳更新、异常处理

### 改动文件

- `packages/web/dsh-pmboard/src/repositories/RequirementRepository.ts`
- `packages/web/dsh-pmboard/src/repositories/TaskRepository.ts`
- `packages/web/dsh-pmboard/tests/unit/repository-extensions.test.ts`

### 下一步

仓储层扩展完成，批次4全部完成！下一步：批次5既有用例改造

---

### 验证方法（可执行命令）
1. 单元测试通过: `cd packages/web/dsh-pmboard && npx vitest run tests/unit/repository-extensions.test.ts` 预期输出包含 "14 tests" 和 "passed"
