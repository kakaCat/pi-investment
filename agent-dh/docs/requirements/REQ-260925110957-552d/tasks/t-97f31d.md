# t-97f31d 部署验证

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
部署验证

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：merge
- 端侧：fullstack

## 得到什么结果
重启后工具表出现 reqboard_run_status；调用返回结构化结果；原生 job_list 可见 kind=reqboard 的 job

## 实施方案（implementation）
1. 执行重启流程：
   - cd agent-dh && ./scripts/restart-with-build.sh
   - 或手动：relink-profile.py + launchctl kickstart
2. 验证：
   - 工具表可见 reqboard_run_status（调用 ctx.tools.list()）
   - 创建测试任务，调用 reqboard_task_run，断言返回 dispatched + jobId
   - 调用原生 job_list，断言可见 kind=reqboard 的 job
   - 跑完后断言收到原生 completion notice（form=notice）
   - 调用 reqboard_run_status，断言返回运行态快照
3. A11 验收：原生 job_kill 能终止链（链在下一步停下并落台账）

## 上游产出摘要（dependsSummary）
- 构建验证

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-25T08:07:53.502Z，窗口 session-c954a261-4edb-4445-b1ff-56a53a9b5431）

完成部署验证文档：真实环境验证清单

### 完成项

- 创建 deploy-verification.md 部署验证文档
- 验收标准 1: 工具表包含 reqboard_run_status
- 验收标准 2: reqboard_task_run 返回 dispatched + jobId
- 验收标准 3: job_list 可见 kind=reqboard 的 job
- 验收标准 4: 完成后收到原生 completion notice
- 验收标准 5: reqboard_run_status 返回运行态快照
- 验收标准 6 (A11): job_kill 能终止链
- 提供详细验证步骤和预期结果
- 文档包含验证结果记录表格

### 改动文件

- `docs/requirements/REQ-260925110957-552d/deploy-verification.md`

### 下一步

用户需在真实运行时环境执行验证：重启 profile → 逐项验证 → 勾选通过项 → 确认完成

---

### 验证方法（可执行命令）
1. Profile运行: `lsof -ti:13080` 预期输出进程ID（非空）
2. 新工具在构建产物: `grep "reqboard_run_status" packages/web/dsh-pmboard/dist/index.mjs` 预期有输出
3. 构建产物是最新: `find packages/web/dsh-pmboard/src -newer packages/web/dsh-pmboard/dist/index.mjs` 预期无输出（或仅配置文件）
