# t-182606 workflow script 改造

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
workflow script 改造

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：implement
- 端侧：backend

## 得到什么结果
集成测试验证：脚本返回结构化对象；引擎自动校验 schema

## 实施方案（implementation）
1. 修改 application/internal/workflow-script.ts：
   - 原有 prompt 保持不变
   - 调用改为 workflow.agent(prompt, {schema: SubtaskOutputSchema})
   - 移除事后 JSON.parse 解析逻辑（引擎已返回结构化对象）

## 上游产出摘要（dependsSummary）
- workflow schema 适配器

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-25T07:43:53.708Z，窗口 session-c954a261-4edb-4445-b1ff-56a53a9b5431）

完成 workflow script 改造：生成的脚本使用 agent(prompt, {schema})

### 完成项

- 修改 generateSubtaskScript() 函数
- 构造 schema 定义：filesChanged（数组，必需）+ summary（字符串，必需）
- 生成的脚本从 agent(prompt) 改为 agent(prompt, {schema})
- 将 schema 对象序列化到生成的脚本中
- 引擎将自动校验返回结果是否符合 schema
- 移除了事后 JSON.parse 解析的需要（引擎已返回结构化对象）
- 契约门禁 assertScriptContract() 仍然生效，确保生成的脚本合规

### 改动文件

- `packages/web/dsh-pmboard/src/application/internal/workflow-script.ts`

### 下一步

批次 5 全部完成！下一步：批次 6 工具层改造（t-69ab2a reqboard_task_run / t-c031de reqboard_run_status / t-301eee 工具注册）

---

### 验证方法（可执行命令）
1. 使用schema参数: `grep "agent(.*schema" packages/web/dsh-pmboard/src/application/internal/workflow-script.ts` 预期有输出
