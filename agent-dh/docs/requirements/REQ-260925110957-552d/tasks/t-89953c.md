# t-89953c 后台执行器

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
后台执行器

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：implement
- 端侧：backend

## 得到什么结果
集成测试验证：循环推进直到完成/暂停；中断后 checkpoint 正确；心跳更新

## 实施方案（implementation）
1. 新增 application/internal/background-runner.ts
2. 实现 runChain(reqId, exec, signal) 循环：
   - 调用批调度器获取下一批 ready 子卡
   - 批内用 Promise.all 并发执行
   - 每步写 checkpoint
   - 更新心跳（每30秒）
   - 监听 signal.aborted 退出
3. 持有独立 AbortController，不依赖调用方栈

## 上游产出摘要（dependsSummary）
- 领域类型定义
- 写集冲突检测
- 批调度器
- 孤儿回收
- checkpoint 管理器

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-25T07:01:15.941Z，窗口 session-c954a261-4edb-4445-b1ff-56a53a9b5431）

完成后台执行器：实现循环推进、checkpoint、心跳更新

### 完成项

- 新增 application/internal/background-runner.ts：BackgroundRunner 类
- 实现 runChain()：主循环逻辑，选择子卡→批调度→并行执行→写checkpoint
- 实现依赖检查：findReadyTasks() 确保依赖已完成才执行
- 实现心跳更新：定时器每30秒更新心跳
- 支持取消信号：监听 AbortSignal 中断执行
- 实现状态判断：completed/paused/cancelled/failed
- 新增 tests/unit/background-runner.test.ts：7个测试全部通过
- 测试覆盖：全部完成、单任务执行、依赖关系、取消信号、checkpoint、暂停、错误处理

### 改动文件

- `packages/web/dsh-pmboard/src/application/internal/background-runner.ts`
- `packages/web/dsh-pmboard/tests/unit/background-runner.test.ts`

### 下一步

后台执行器完成，下一步：t10 投递用例

---

### 验证方法（可执行命令）
1. 单元测试通过: `cd packages/web/dsh-pmboard && npx vitest run tests/unit/background-runner.test.ts` 预期输出包含 "7 tests" 和 "passed"
2. backgroundRunner存在: `grep "export.*backgroundRunner" packages/web/dsh-pmboard/src/application/internal/background-runner.ts` 预期有输出
