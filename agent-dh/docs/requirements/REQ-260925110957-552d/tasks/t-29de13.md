# t-29de13 批调度器

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
批调度器

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：implement
- 端侧：backend

## 得到什么结果
单测验证：写集不交的卡分到同批；有冲突的卡分到不同批；批次顺序符合依赖关系

## 实施方案（implementation）
1. 新增 application/internal/batch-scheduler.ts
2. 实现 scheduleBatches(readySubtasks: SubtaskRecord[]) → Batch[]
3. 算法：贪心分组，每批内任意两卡写集不交；考虑 depends_on 约束
4. 新增 tests/unit/batch-scheduler.test.ts 覆盖并行/串行/混合场景

## 上游产出摘要（dependsSummary）
- 领域类型定义
- 写集冲突检测

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-25T06:50:58.562Z，窗口 session-c954a261-4edb-4445-b1ff-56a53a9b5431）

完成批调度器：按写集分批，批内并发、批间串行

### 完成项

- 新增 application/internal/batch-scheduler.ts：批调度算法实现
- 实现 scheduleBatches()：贪心分组，批内写集不交
- 实现拓扑排序：确保依赖任务优先调度（入度低的先调度）
- 实现 validateBatch()：验证批次有效性
- 新增 tests/unit/batch-scheduler.test.ts：13个测试全部通过
- 测试覆盖：无冲突并行、有冲突串行、同文件冲突、目录前缀冲突、混合场景、依赖关系、空写集、大规模任务（50个）

### 改动文件

- `packages/web/dsh-pmboard/src/application/internal/batch-scheduler.ts`
- `packages/web/dsh-pmboard/tests/unit/batch-scheduler.test.ts`

### 下一步

批调度器完成，下一步：t7 孤儿回收

---

### 验证方法（可执行命令）
1. 单元测试通过: `cd packages/web/dsh-pmboard && npx vitest run tests/unit/batch-scheduler.test.ts` 预期输出包含 "13 tests" 和 "passed"
2. scheduleBatches存在: `grep "export function scheduleBatches" packages/web/dsh-pmboard/src/application/internal/batch-scheduler.ts` 预期有输出
