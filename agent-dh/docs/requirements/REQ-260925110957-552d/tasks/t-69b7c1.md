# t-69b7c1 workflow schema 适配器

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
workflow schema 适配器

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：implement
- 端侧：backend

## 得到什么结果

1. 单元测试通过: `cd packages/web/dsh-pmboard && npx vitest run tests/unit/workflow-schema-adapter.test.ts` 预期输出包含 "14 tests" 和 "passed"
2. executeWithSchema存在: `grep "executeWithSchema" packages/web/dsh-pmboard/src/adapters/WorkflowSchemaAdapter.ts` 预期有输出

## 实施方案（implementation）
1. 新增 infrastructure/workflow-schema-adapter.ts
2. 定义 SubtaskOutputSchema（filesChanged: string[], summary: string）
3. 导出 executeWithSchema(workflow, prompt, schema) 方法
4. 新增 tests/unit/workflow-schema-adapter.test.ts 用 mock workflow 验证

## 上游产出摘要（dependsSummary）
- 领域类型定义

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-25T05:28:38.996Z，窗口 session-c954a261-4edb-4445-b1ff-56a53a9b5431）

完成 workflow schema 适配器：封装引擎 agent(prompt, {schema}) 调用

### 完成项

- 新增 adapters/WorkflowSchemaAdapter.ts：封装引擎 schema 调用
- 定义 SubtaskOutputSchema：filesChanged（写集）+ summary
- 实现 executeWithSchema()：传递 schema 给引擎，返回结构化输出
- 实现降级逻辑：引擎拒绝或返回 null 时降级为结构化空值
- 提供辅助函数：isValidSubtaskOutput、createEmptySubtaskOutput
- 新增 tests/unit/workflow-schema-adapter.test.ts：14个测试全部通过

### 改动文件

- `packages/web/dsh-pmboard/src/adapters/WorkflowSchemaAdapter.ts`
- `packages/web/dsh-pmboard/tests/unit/workflow-schema-adapter.test.ts`

### 下一步

Workflow schema 适配器完成，批次2完成。下一步：批次3核心算法（t5-t8）

---

### 验证方法（可执行命令）
1. 单元测试通过: `cd packages/web/dsh-pmboard && npx vitest run tests/unit/workflow-schema-adapter.test.ts` 预期输出包含 "14 tests" 和 "passed"
2. executeWithSchema存在: `grep "executeWithSchema" packages/web/dsh-pmboard/src/adapters/WorkflowSchemaAdapter.ts` 预期有输出
