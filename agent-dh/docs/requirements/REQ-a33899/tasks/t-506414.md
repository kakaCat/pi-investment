# t-506414 读路径装配与 HTTP（需求/节点/任务）

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 目标
读路径装配与 HTTP（需求/节点/任务）

## 背景摘要（context）
（待补充）

## 范围
- 阶段：implement
- 端侧：backend

## 验收标准
端到端 curl token 接口返回 byStage+executions；不存在 404；有 unavailable 快照时 degraded=true 且无编造数字；progress/state 扩展字段可见。

## 实施方案（implementation）
新增 src/application/query/QueryRequirementToken.ts，改 QueryStageDetail.ts、http/routers/stages.ts、http/routes.ts；测试 tests/token-endpoint.test.ts。验证：pnpm test + curl。

## 上游产出摘要（dependsSummary）
- 台账 v6 + 写路径快照与迁移

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-17T21:52:00.675Z，窗口 session-b11b0a40-5c65-4c8d-88c7-23988b4979b0）

读路径装配与 HTTP：新增 application/query/QueryRequirementToken.ts（byStage+executions+degraded，缺失节点 buckets 为 undefined 而非 0）；StageDetailBase 增 tokens（会话内节点面板/进度条数据源）；stages 路由扩 /state 的 tokenTotals、/session/:sid/progress 的 nodes[].tokens，并新增 GET /requirements/:id/token（404 走 not_found）；routes.ts 注册分派。

### 完成项

- 新增 QueryRequirementToken（assembleRequirementToken / requirementTotalTokens）
- StageDetailBase.tokens + QueryStageDetail 装配器填充
- GET /requirements/:id/token 新增（200/404）；/state 增 tokenTotals；/session/:id/progress 增 nodes[].tokens
- 新增 tests/token-endpoint.test.ts 6 条全绿；typecheck 通过
- 修掉一处新增拼接式消息（改用 domain/text/fmt），message-hygiene 棘轮恢复

### 改动文件

- `agent-dh/packages/pages/dsh-pmboard/src/application/query/QueryRequirementToken.ts`
- `agent-dh/packages/pages/dsh-pmboard/src/shared/protocol.ts`
- `agent-dh/packages/pages/dsh-pmboard/src/application/query/QueryStageDetail.ts`
- `agent-dh/packages/pages/dsh-pmboard/src/http/routers/stages.ts`
- `agent-dh/packages/pages/dsh-pmboard/src/http/routes.ts`
- `agent-dh/packages/pages/dsh-pmboard/tests/token-endpoint.test.ts`

### 下一步

t5：提示词成本读路径（固定系统提示词 + 注入提示词）。

---
