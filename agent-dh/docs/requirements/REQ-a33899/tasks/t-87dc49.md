# t-87dc49 立 Token 契约与纯函数

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 目标
立 Token 契约与纯函数

## 背景摘要（context）
（待补充）

## 范围
- 阶段：implement
- 端侧：fullstack

## 验收标准
tests/token-usage.test.ts：四桶加减正确、负分量截断、totalTokens=四桶之和、估算单调非负、fmtTokens/fmtCny 边界（<1000/k/M/undefined→—）；pnpm test 与 typecheck 全绿。

## 实施方案（implementation）
改 src/shared/protocol.ts；测试 tests/token-usage.test.ts。验证：pnpm -C packages/pages/dsh-pmboard test && typecheck。

## 上游产出摘要（dependsSummary）
- （无依赖）

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-17T21:47:05.858Z，窗口 session-b11b0a40-5c65-4c8d-88c7-23988b4979b0）

落地 Token 契约与纯函数：protocol.ts 新增四桶 TokenBuckets、TokenSnapshot、RequirementTokenUsage、ExecutionTokenUsage、PromptPartCost/SystemPromptCost/InjectionItem/InjectionCost 类型，以及 emptyBuckets/addBuckets/subBuckets（负分量截断为 0）/totalTokens/estimateTokensFromChars/TOKENS_PER_CHAR(0.25,对齐 DSH)/fmtTokens/fmtCny 纯函数；新增 11 条单测。

### 完成项

- protocol.ts 新增 token 契约类型与纯函数（零依赖，host/client 共用）
- TOKENS_PER_CHAR=0.25 与 DSH token-meter 固定密度一致（估算口径单点）
- 新增 tests/token-usage.test.ts 11 条用例（四桶加减/负分量截断/合计/估算单调非负/格式边界）
- pnpm typecheck 通过；pnpm test 新用例全绿

### 改动文件

- `agent-dh/packages/pages/dsh-pmboard/src/shared/protocol.ts`
- `agent-dh/packages/pages/dsh-pmboard/tests/token-usage.test.ts`

### 下一步

t2：会话 Token 读取端口与降级（SessionProbe.tokenTotals + SessionProbeAdapter 实现）。

---
