# t-77067a 提示词成本读路径（固定系统提示词 + 注入提示词）

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 目标
提示词成本读路径（固定系统提示词 + 注入提示词）

## 背景摘要（context）
（待补充）

## 范围
- 阶段：implement
- 端侧：backend

## 验收标准
单测：systemPrompt 可用→sections 逐段 chars 等于文本长度且每段 text 随响应返回、perTurn=sections+contexts+tools；不可用→source=unavailable 不猜；injections 聚合 chars 等于留痕 charCount 之和、按阶段分组正确、明细可用。

## 实施方案（implementation）
新增 src/adapters/SystemPromptCostAdapter.ts，改 src/application/ports.ts、src/application/internal/injection-log.ts、src/http/routes.ts（注入 systemPrompt 只读端口）、routers/stages.ts；测试 tests/prompt-cost.test.ts。验证：pnpm test。

## 上游产出摘要（dependsSummary）
- 读路径装配与 HTTP（需求/节点/任务）

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-17T21:53:11.781Z，窗口 session-b11b0a40-5c65-4c8d-88c7-23988b4979b0）

提示词成本读路径：新增 application/internal/prompt-cost.ts（summarizeSystemPrompt 按段折算 chars/估算token，空段与空 tools 不计；summarizeInjections 按窗口归属聚合注入留痕，无匹配窗口不归因；injectionWindowsOf 生成 sessionId∪窗口码集合）；路由 deps 增 systemPrompt 提供者（index.ts 装配注入），GET /requirements/:id/token 返回 systemPrompt + injections(+sharePct)；服务不可得一律 source=unavailable 不猜。

### 完成项

- prompt-cost.ts：系统提示词逐段折算 + 注入留痕聚合（缺失不补 0、不张冠李戴）
- SystemPromptCost.cumulativeEstTokens 改可选：回合数不可得时不给累计
- RouterCtx/routes deps 增 systemPrompt 提供者；index.ts 捕获 systemPrompt 服务并注入
- /requirements/:id/token 返回 systemPrompt 与 injections（含量级 sharePct）
- 新增 tests/prompt-cost.test.ts 8 条全绿；typecheck 通过；全量仅基线红

### 改动文件

- `agent-dh/packages/pages/dsh-pmboard/src/application/internal/prompt-cost.ts`
- `agent-dh/packages/pages/dsh-pmboard/src/application/query/QueryRequirementToken.ts`
- `agent-dh/packages/pages/dsh-pmboard/src/shared/protocol.ts`
- `agent-dh/packages/pages/dsh-pmboard/src/http/routers/shared.ts`
- `agent-dh/packages/pages/dsh-pmboard/src/http/routers/stages.ts`
- `agent-dh/packages/pages/dsh-pmboard/src/http/routes.ts`
- `agent-dh/packages/pages/dsh-pmboard/src/index.ts`
- `agent-dh/packages/pages/dsh-pmboard/tests/prompt-cost.test.ts`

### 下一步

t6：详情页「🪙 Token」tab（前端折叠分块 + 具体提示词内容）。

---
