# t-1616b2 端到端自证与文档更新

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 目标
端到端自证与文档更新

## 背景摘要（context）
（待补充）

## 范围
- 阶段：test
- 端侧：doc

## 验收标准
verification.md 含 curl 输出摘要 + tokenUsage.totals 核对 + 系统提示词段字符对照 + 三条命令通过输出；文档含 token 契约与口径说明。

## 实施方案（implementation）
写 docs/requirements/REQ-a33899/verification.md；更新 agent-dh architecture 文档页。验证：命令输出粘贴。

## 上游产出摘要（dependsSummary）
- 台账 v6 + 写路径快照与迁移
- 读路径装配与 HTTP（需求/节点/任务）
- 提示词成本读路径（固定系统提示词 + 注入提示词）
- 详情页「🪙 Token」tab（汇总卡 + 四个折叠块）
- 会话顶部每节点 Token（同行）+ 看板卡面

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-17T21:58:29.415Z，窗口 session-b11b0a40-5c65-4c8d-88c7-23988b4979b0）

端到端自证与文档更新：对线上台账副本跑链式迁移（dry-run/apply/verify，白名单外差异 0）；用真实台账副本 + 真实注入留痕走 HTTP 全链路验证 token 接口（200；injections 180 条/207,442 字符按阶段聚合；过程快照因未发版如实 degraded）；三条命令（typecheck / vitest / build:client）全绿（仅 1 例改动前即红的基线失败）；新增 L2 文档 docs/architecture/reqboard-token-usage.md 并挂进 README 卷 7 与 INDEX.md；写 verification.md 列证据与缺口。

### 完成项

- 迁移证据：线上台账副本 v5→v6，差异路径 2 条全在白名单，--verify 通过
- 端到端证据：真实台账副本 + 真实 injection-log 跑通 /requirements/:id/token（200；injections 真实聚合）
- 命令证据：pnpm typecheck / npx vitest run / pnpm build:client（含 verify-client-build）
- 文档：新增 architecture/reqboard-token-usage.md + README 卷 7 链接 + INDEX.md 重生成
- verification.md 含覆盖矩阵、已知缺口（未发版/费用单价未接入/回合数不可得/subagent 未并入）与回滚路径

### 改动文件

- `agent-dh/docs/architecture/reqboard-token-usage.md`
- `agent-dh/docs/README.md`
- `agent-dh/docs/INDEX.md`
- `agent-dh/docs/requirements/REQ-a33899/verification.md`

### 下一步

提交验收材料（reqboard_submit kind=verification）等待人工逐项验收；发版（restart-with-build）待用户放行。

---
