# t-2ba947 固化拒绝路径复现：加三条失败用例（红）

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
固化拒绝路径复现：加三条失败用例（红）

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：test
- 端侧：backend

## 得到什么结果
跑 npx vitest run packages/web/dsh-pmboard/tests/capture-tool.test.ts packages/web/dsh-pmboard/tests/gate-aware-questions.test.ts packages/web/dsh-pmboard/tests/gate-handlers.test.ts —— 新增断言失败（至少一条红），且失败原因与 BUG-1/BUG-2 现象一致；未改动任何 src/ 文件（git diff --stat 只含 tests/）。

## 实施方案（implementation）
复用既有测试桩（fake questions / fake chain / fake delivery）。先读 tests/capture-tool.test.ts 与 tests/gate-aware-questions.test.ts 的既有构造方式，按同风格新增用例；跑命令确认新断言在未修复代码上失败（红），并把红项原文摘录进任务汇报。

## 上游产出摘要（dependsSummary）
- （无依赖）

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-23T16:38:13.882Z，窗口 session-4799e386-677d-431b-bb36-c752ad82640b）

拒绝路径复现已固化成三条失败用例（红），跑在未修复代码上全红且失败原因与 BUG-1/BUG-2 现象逐条对应——"选了不立项还继续追问"和"系统回头发闸门未通过"这两件事现在都有机器判据，修复后这三条即为回归防线。

### 完成项

- tests/capture-tool.test.ts：假弹框服务改为逐段记录 calls + 跨段累计 questions；新增「拒绝即终端」用例——拒绝路径 calls=1 且题目 id 只有 ['name']（类型/难度/文档位置从未下发）
- tests/gate-aware-questions.test.ts：新增「立项拒绝路径不进链」用例——拒绝路径 chain.stats().enqueued=0、pending.size()=0，且 runPending 后投递 spy 为空
- tests/gate-handlers.test.ts：新增「无 from 的闸门（G0）负分支不编节点仍在」用例——文案含「待改进」但不含「节点仍在」
- 跑 npx vitest run packages/web/dsh-pmboard/tests/{capture-tool,gate-aware-questions,gate-handlers}.test.ts：3 failed | 32 passed，红项 = ①题目 id 收到 4 个 ②enqueued=1（期望 0）③文案含「节点仍在 brainstorming」

### 改动文件

- `agent-dh/packages/web/dsh-pmboard/tests/capture-tool.test.ts`
- `agent-dh/packages/web/dsh-pmboard/tests/gate-aware-questions.test.ts`
- `agent-dh/packages/web/dsh-pmboard/tests/gate-handlers.test.ts`

### 下一步

t-0ec5ae 修复卡：题目拆两段 + 拒绝短路 + G0 挪段 + H4 无 from 不编现状，让这三条转绿。

---
