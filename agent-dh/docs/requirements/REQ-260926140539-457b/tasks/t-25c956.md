# t-25c956 实现 FR/serves/implements/covers 标注解析

> 需求：REQ-260926140539-457b RTM YAML 追溯基础设施 - Dive 模式自动化的数据底座

## 在做什么
实现 FR/serves/implements/covers 标注解析

## 解决什么问题
创建 src/rtm/parser.ts，实现 parseFRList / parseServesAnnotations / parseImplementsAnnotations / parseCoversAnnotations，用正则提取标注并返回结构化数据。

## 得到什么结果
运行 `pnpm test rtm-parser.test.ts` 通过；解析 "**FR-1: 功能点" 返回 [{id:"FR-1",...}]；解析 "serves: FR-1, FR-2" 返回 ["FR-1","FR-2"]；空文档返回空数组

---
## 汇报 1（2026-09-26T10:32:48.363Z，窗口 session-c5ea210f-afc3-4735-a4d1-f9058b378363）

这一步做完，文档里写的追溯标注机器能读懂了：需求里的 FR、设计章节的 serves、测试用例的 covers 都能被解析成结构化数据（解析不到就留空，由覆盖度把缺口暴露出来）。

### 完成项

- parseRequirementFRs / parseDesignSections / parseTestCovers / parseDecompositionServes
- 跳过代码围栏内的示例，避免把文档里的例子当真实标注
- 边界情况（空文档/无标注/重复编号）单测覆盖

### 改动文件

- `packages/tools/reqboard/src/rtm/parser.ts`
- `packages/tools/reqboard/tests/rtm/parser.test.ts`

---
