# t-6019b3 在 reqboard_submit 各类型触发对应 RTM 生成

> 需求：REQ-260926140539-457b RTM YAML 追溯基础设施 - Dive 模式自动化的数据底座

## 在做什么
在 reqboard_submit 各类型触发对应 RTM 生成

## 解决什么问题
修改 src/tools/reqboard-submit.ts，在门禁通过后按 kind 分派：requirement→generateBrainstormingRTM、design→generateDesignRTM、plan 批准→generateDecomposingRTM + generateImplementingRTM、verification→generateAcceptingRTM，均 try-catch 记录日志。

## 得到什么结果
运行集成测试 `pnpm test reqboard-submit-integration.test.ts` 通过；kind=requirement 后 rtm-brainstorming.yml 存在；kind=design 后 rtm-design.yml 存在；kind=plan 批准后 rtm-decomposing.yml 与 rtm-implementing.yml 都存在；kind=verification 后 rtm-accepting.yml 存在

---
## 汇报 1（2026-09-26T10:32:49.222Z，窗口 session-c5ea210f-afc3-4735-a4d1-f9058b378363）

这一步做完，提交需求/设计/验收这三类文档后，对应的追溯快照会自动更新，不需要人工补。

### 完成项

- submitRequirementArtifact → submit:requirement
- SubmitDesignArtifacts → submit:design（仅在登记成功时）
- SubmitVerification → submit:verification

### 改动文件

- `packages/tools/reqboard/src/rtm/generator.ts`
- `packages/web/dsh-pmboard/src/application/use-cases/SubmitArtifact.ts`
- `packages/web/dsh-pmboard/src/application/use-cases/SubmitDesignArtifacts.ts`
- `packages/web/dsh-pmboard/src/application/use-cases/SubmitVerification.ts`

---
