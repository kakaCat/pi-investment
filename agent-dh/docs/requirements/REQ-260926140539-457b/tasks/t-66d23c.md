# t-66d23c 增强 assembleStageOverview 合并 RTM 追溯数据

> 需求：REQ-260926140539-457b RTM YAML 追溯基础设施 - Dive 模式自动化的数据底座

## 在做什么
增强 assembleStageOverview 合并 RTM 追溯数据

## 解决什么问题
修改 src/stage-overview.ts，在 assembleStageOverview() 中读取 lifecycle 与 stage RTM，存在时合并 traceability/coverage 到 stages[stage].body，保持向后兼容。

## 得到什么结果
运行集成测试 `pnpm test stage-overview-rtm-integration.test.ts` 通过；assembleStageOverview 返回对象含 stages[stage].traceability；traceability.fr_to_design["FR-1"] 含设计章节引用；coverage.design.rate 为 0-1 数字

---
## 汇报 1（2026-09-26T10:32:49.617Z，窗口 session-c5ea210f-afc3-4735-a4d1-f9058b378363）

这一步做完，节点详情能一次拿到「追溯链 + 三层覆盖度」：FR → 设计 → 任务 → 测试，以及每层覆盖率；快照丢了会就地重建再读。

### 完成项

- assembleStageOverviewRTM 合并追溯与覆盖度
- 快照缺失时用 regenerator 实时重建（降级）
- 无 regenerator 且缺失时返回空块不抛异常

### 改动文件

- `packages/tools/reqboard/src/stage-overview/assembler.ts`

---
