# t-e57e00 实现 RTM 文件读写与版本控制

> 需求：REQ-260926140539-457b RTM YAML 追溯基础设施 - Dive 模式自动化的数据底座

## 在做什么
实现 RTM 文件读写与版本控制

## 解决什么问题
创建 src/rtm/file-io.ts，实现 readRTM / writeRTM / getRTMPath / ensureRTMDir，使用 js-yaml，原子写入（临时文件 + rename），文件不存在返回 null。

## 得到什么结果
运行 `pnpm test file-io.test.ts` 通过；测试写入后读取内容一致；测试连续写入 2 次 version 从 1 递增到 2；测试读取不存在文件返回 null

---
## 汇报 1（2026-09-26T10:32:48.282Z，窗口 session-c5ea210f-afc3-4735-a4d1-f9058b378363）

这一步做完，RTM 文件能安全地写进磁盘了：写入是原子的（不会留半截文件），文件不存在读到的是「没有」而不是崩溃，每次写入版本号自动 +1——下游据此判断数据新旧。

### 完成项

- readRTM / writeRTM / getRTMPath / ensureRTMDir / readRTMStrict
- 原子写入（临时文件 + rename）
- YAML 坏掉时宽读返回 null 并留因、严格读抛结构化 RTMError
- 8 条单测全绿

### 改动文件

- `packages/tools/reqboard/src/rtm/file-io.ts`
- `packages/tools/reqboard/tests/rtm/file-io.test.ts`

---
