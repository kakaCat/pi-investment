# t-a30145 实现拆分节点 RTM 生成

> 需求：REQ-260926140539-457b RTM YAML 追溯基础设施 - Dive 模式自动化的数据底座

## 在做什么
实现拆分节点 RTM 生成

## 解决什么问题
创建 src/rtm/decomposing-generator.ts，实现 generateDecomposingRTM(reqId)，从任务台账读取并提取 design_serves，调用 buildDesignToTasks 与 calculateImplementationCoverage 后落盘。

## 得到什么结果
运行 `pnpm test decomposing-generator.test.ts` 通过；5 个任务调用生成器后 `yq .outputs.tasks | length` 返回 5；`yq .traceability.design_to_tasks."design#1.1"[0]` 含 "t-"；`yq .coverage.implementation.rate` 返回 1.0

---
## 汇报 1（2026-09-26T10:32:48.918Z，窗口 session-c5ea210f-afc3-4735-a4d1-f9058b378363）

这一步做完，拆分计划批准后能看到「每个设计章节有没有任务接」——没有任务接的章节会被列出来。

### 完成项

- generateDecomposingRTM 从台账取任务并合 decomposition.md 的 serves 绑定
- design_to_tasks / fr_to_tasks + 实施覆盖度

### 改动文件

- `packages/tools/reqboard/src/rtm/decomposing-generator.ts`

---
