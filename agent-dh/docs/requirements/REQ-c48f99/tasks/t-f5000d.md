# t-f5000d renderSmart 人话首行：13 个 pmboard 工具逐配 summarize

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
renderSmart 人话首行：13 个 pmboard 工具逐配 summarize

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：implement
- 端侧：backend

## 得到什么结果
重启后调用 reqboard_status，工具结果文本首行为中文摘要（非 '{' 开头）；grep packages/pages/dsh-pmboard/src/tools 中 renderJson 剩余引用数 ≤ 1（shared.ts 定义处）

## 实施方案（implementation）
逐工具编辑 render 引用，不动批量替换；每个 summarize 一行函数写在各自工具文件

## 上游产出摘要（dependsSummary）
- 契约卡：toolviews 数据契约 + 纯函数骨架 + renderSmart 签名

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-21T03:24:36.880Z，窗口 session-85447f15-ee57-44ef-8fd0-d8526111c0b3）

renderSmart 人话首行落地：13 个 pmboard 工具全部迁移（renderJson 剩余引用=1 即 shared.ts 定义处）；render-summaries.ts 13 个摘要函数，单测 20 用例绿（正常/错误/畸形三档 + 端到端形态）

### 完成项

- src/tools/render-summaries.ts：13 个 summarize（单行 ≤120 字符契约）
- 13 个工具 render 逐换 renderSmart(xxxSummary)
- renderJson 保留兼容（其余插件 39 处不受影响）
- tests/render-summaries.test.ts：20 用例绿

### 改动文件

- `packages/pages/dsh-pmboard/src/tools/render-summaries.ts`
- `packages/pages/dsh-pmboard/src/tools/shared.ts`
- `packages/pages/dsh-pmboard/src/tools/（13 个工具文件换 render）`
- `packages/pages/dsh-pmboard/tests/render-summaries.test.ts`

### 下一步

重启 profile 后实测 reqboard_status 首行中文（live 验收）

---
