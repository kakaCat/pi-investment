# t-26dc91 迁移与兼容验证：renderJson 保留 + 旧会话重渲染 + 未注册工具零回归

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
迁移与兼容验证：renderJson 保留 + 旧会话重渲染 + 未注册工具零回归

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：test
- 端侧：fullstack

## 得到什么结果
grep 确认其余插件 renderJson 类 render 零改动；旧会话 REQ-4842fe 中 task_move 节点显示新卡片；新会话 bash/read/edit 各一次调用显示终端卡/读取卡/diff 卡不变

## 实施方案（implementation）
只读验证卡：grep + 打开旧会话人工比对 + 新会话实测，不写新代码

## 上游产出摘要（dependsSummary）
- 骨架接线：bizToolviews 注册入口 + BizRow 布局 + task_move 示范卡
- 其余 8 张业务卡片（submit/ask_confirm/status/capture/memory_write/decision_audit/portfolio_trade/watch_manage）
- renderSmart 人话首行：13 个 pmboard 工具逐配 summarize

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-21T03:40:20.284Z，窗口 session-85447f15-ee57-44ef-8fd0-d8526111c0b3）

兼容验证完成：①其余插件 renderJson 类 render 零改动（git 维度非 pmboard 包 0 处改动）；②ui-tool 框架 bundle 未触碰（mtime 9-14）；③用户目检确认 bash/read/edit 与改前一致；④旧会话重渲染=同一渲染管线（新会话已实测生效、事件格式一致），用户未单独目检，降级标注

### 完成项

- grep/git 确认其余插件零改动
- ui-tool 框架包 mtime 未变（零回归前提）
- 用户确认 bash/read/edit 显示一致（E3）
- 旧会话重渲染：机制同管线，用户未实测，诚实降级标注

### 下一步

t6 E2E 验收

---
