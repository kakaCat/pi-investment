# t-e0110f 任务文件上浮 + evidence 存在性 + 归档漏登警告（W4）

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 目标
任务文件上浮 + evidence 存在性 + 归档漏登警告（W4）

## 背景摘要（context）
（待补充）

## 范围
- 阶段：implement
- 端侧：backend

## 验收标准
files_changed 进需求级产物清单；伪造 evidence 路径被拒；archive 漏登返回警告清单

## 上游产出摘要（dependsSummary）
- REQ 目录产物自动发现（W4）

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-17T10:05:44.453Z，窗口 session-41e7e4cd-4038-476c-9307-d93622a411e2）

W4 后半三件落地：①task_report 的 files_changed 自动上浮为需求级 task_output 产物（覆盖 REQ 目录外源码，需求详情页可见"一共动了哪些文件"）②verify_submit 校验 evidence 引用的工作区路径真实存在（防编造证据路径，REQBOARD_EVIDENCE_MISSING）③archive_submit 返回 unlisted_files + warning（目录内有未列入归档清单的文件）

### 完成项

- ArtifactKind 增 task_output（工具登记类，不进 stage 闸门）
- task_report mutate 内自动登记 files_changed 为 task_output 产物（幂等：同 path 不重复）
- workspacePathCandidates()：从 evidence 提取工作区路径候选（根前缀+扩展名白名单，扩展名按长度降序防 json→js 截断）
- verify_submit 存在性校验 + archive_submit 漏登扫描（walk 目录对比清单）
- 4 个新用例（task-output-and-evidence.test.ts）：上浮/编造路径拒/真实路径过/漏登警告
- 踩坑两处：正则 js 抢先匹配 json（已按长度降序修）；工具按 cwd 解析路径而 vitest cwd=包目录（测试改用包内真实路径）

### 改动文件

- `packages/pages/dsh-pmboard/src/shared/protocol.ts`
- `packages/pages/dsh-pmboard/src/host/agent-tools.ts`
- `packages/pages/dsh-pmboard/tests/task-output-and-evidence.test.ts`

### 下一步

t13 验收单数据模型 + verify_submit 生成逐项验收单（依赖 t04 已就绪）

---
