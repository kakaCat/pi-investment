# t-70911c 验收单数据模型 + verify_submit 生成逐项验收单（W6）

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 目标
验收单数据模型 + verify_submit 生成逐项验收单（W6）

## 背景摘要（context）
（待补充）

## 范围
- 阶段：implement
- 端侧：backend

## 验收标准
验收单含每任务验收标准+需求级标准逐项（含 evidence）；版本化存储；挂起/续验状态持久化（pending/passed/failed 逐项）

## 上游产出摘要（dependsSummary）
- decompose 薄卡拒落 + 开工任务卡送达（W5）

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-17T10:06:29.893Z，窗口 session-41e7e4cd-4038-476c-9307-d93622a411e2）

验收单数据模型 + 逐项生成落地（W6 前半）：VerificationSheet/VerificationItem 进 protocol（status: pending/passed/failed 逐项持久化 = 挂起/续验基础），verify_submit 生成 v1（每任务验收标准 + 需求级标准，逐项带证据）；返工时 v2 只含上一版未过项（rework_only 标记），历史进 sheetHistory 版本化留痕

### 完成项

- protocol.ts：VerificationItem（id/source/criterion/evidence/status/opinion/decidedAt）+ VerificationSheet（version/items/reworkOnly）+ VerificationRecord 扩展 sheet/sheetHistory
- verify_submit mutate 内生成验收单：全量 v1 或返工 v2（只含未过项、重置为 pending）
- 返回体加 sheet_version/sheet_items/rework_only + schema 补字段
- 2 个新用例：v1 逐项结构（任务+需求级、证据、pending）；返工续验（未过项裁决后 v2 只含该项、历史留痕）

### 改动文件

- `packages/pages/dsh-pmboard/src/shared/protocol.ts`
- `packages/pages/dsh-pmboard/src/host/agent-tools.ts`
- `packages/pages/dsh-pmboard/tests/verification-sheet.test.ts`

### 下一步

t14 验收单逐项确认 UI + 返工回路（依赖 t13 已就绪）

---
