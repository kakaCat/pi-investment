# t-6f3a71 验收单逐项确认 + 断点续验 + 返工回路（W6）

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 目标
验收单逐项确认 + 断点续验 + 返工回路（W6）

## 背景摘要（context）
（待补充）

## 范围
- 阶段：implement
- 端侧：frontend

## 验收标准
ask_user_question 逐项弹框验收；发现问题可挂起（小修当场改留痕/大修打回生成返工任务）；修复后从断点继续且已过项不重弹；看板勾选并行通道

## 上游产出摘要（dependsSummary）
- 验收单数据模型 + verify_submit 生成逐项验收单（W6）

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-17T10:12:01.581Z，窗口 session-41e7e4cd-4038-476c-9307-d93622a411e2）

验收单逐项确认 + 返工回路落地（W6 后半）：①后端 POST /req/verdicts 逐项裁决路由（版本匹配防并发错版、不通过必填意见）②有不通过项 → 需求自动打回 implementing + 为每个未过项生成关联返工任务（承接原任务 phase/side/scope + 验收意见进 implementation/context）③前端 stage-panel 渲染验收单逐项 UI（通过/不通过单选 + 意见输入 + 提交裁决）+ board-mount 事件委派收集提交 ④构建产物验证（client.js 含新符号）

### 完成项

- routes.ts handleVerdicts：逐项裁决 + 版本匹配 + 返工回路（打回 + 自动生成返工任务 + 评论留痕）
- client/types.ts：VerificationSheet/VerificationItem 类型
- client/api.ts：submitVerdicts
- stage-panel.ts renderVerificationSheet：逐项徽标(⬜/✅/❌)+单选+意见+提交按钮（返工续验标注明示）
- board-mount.ts：submit-verdicts 事件委派（DOM 收集 → api.submitVerdicts → 刷新）
- 5 个后端用例（verdicts-and-rework.test.ts）：全通过/未过项返工+关联/挂起/版本不匹配/缺意见
- build:client + verify-client-build OK；bundle 含 dsh-pm-vsheet/submit-verdicts/submitVerdicts 符号
- 踩坑：多个测试文件共用 cwd 相对路径 REQ-abc123 互相污染（并行跑失败、单独跑通过）→ t12 测试改用独立 id 隔离

### 改动文件

- `packages/pages/dsh-pmboard/src/host/routes.ts`
- `packages/pages/dsh-pmboard/src/client/types.ts`
- `packages/pages/dsh-pmboard/src/client/api.ts`
- `packages/pages/dsh-pmboard/src/client/stage-panel.ts`
- `packages/pages/dsh-pmboard/src/client/board-mount.ts`
- `packages/pages/dsh-pmboard/tests/verdicts-and-rework.test.ts`

### 下一步

t17 W7 阶段产物边界（依赖 t03/t04 已就绪）

---
