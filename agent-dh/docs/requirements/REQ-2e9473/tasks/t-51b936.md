# t-51b936 PlanTask.implementation 字段 + plan_submit 校验（W5，含事故 G DAG 校验）

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 目标
PlanTask.implementation 字段 + plan_submit 校验（W5，含事故 G DAG 校验）

## 背景摘要（context）
（待补充）

## 范围
- 阶段：implement
- 端侧：backend

## 验收标准
缺 implementation 或 acceptance 空话的计划提交被拒；任务表 DAG 提交时校验（前向引用/不存在依赖打回）；单测覆盖

## 上游产出摘要（dependsSummary）
- （无依赖）

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-17T06:17:10.032Z，窗口 session-41e7e4cd-4038-476c-9307-d93622a411e2）

PlanTask.implementation 字段 + plan_submit 三重校验落地：①implementation 必填（拆分卡≠实施卡）②验收标准可证伪（空话黑名单 + 锚点检测）③DAG 提交时校验（前向引用/不存在依赖打回，事故 G）。两遍校验设计保持旧报错优先级兼容，6 个新用例全绿

### 完成项

- protocol.ts PlanTask 加 implementation 字段（含设计注释：REQ-6f39b5 事故 F 教训）
- normalizePlanTasks 重写为两遍校验：结构（key 唯一/标题）→ 依赖（悬空/自依赖/前向引用）→ 内容（acceptance 可证伪 + implementation 必填）
- VACUOUS_ACCEPTANCE 空话黑名单 + VERIFIABLE_ANCHOR 锚点检测（路径/命令/断言关键词）
- 事故 G 修复：前向引用提交时打回并提示"把被依赖任务排在前面"，不再留到 decompose 时炸 invalid_dag
- 6 个新 vitest 用例：缺 implementation/空话/缺锚点/前向引用/依赖悬空/合法通过且 implementation 落库
- 存量夹具同步（TWO_TASKS/PLAN_TASKS/solo 补 implementation）

### 改动文件

- `packages/pages/dsh-pmboard/src/shared/protocol.ts`
- `packages/pages/dsh-pmboard/src/host/agent-tools.ts`
- `packages/pages/dsh-pmboard/tests/decompose-tools.test.ts`
- `packages/pages/dsh-pmboard/tests/plan-mode.test.ts`

### 下一步

t04（decompose 薄卡拒落 + 开工任务卡送达）依赖本任务，可开工

---
## 汇报 2（2026-09-17T06:21:01.746Z，窗口 session-41e7e4cd-4038-476c-9307-d93622a411e2）

纠错补录：首次关闭时只跑了 2 个相关测试文件（27/27），全量回归发现 21 个失败——新校验生效导致 5 个存量测试文件的夹具缺 implementation/锚点，已全部修复后回到 402/403（仅剩 1 个已隔离的存量失败）。教训：关任务前必须跑全量回归

### 完成项

- 修复 5 个存量测试文件夹具（artifact-gates/handoff/routes-rollup/task-report/acceptance-criteria）：补 implementation 字段、无锚点 acceptance（"ok"）改为可验证表述
- 全量回归恢复 402/403（唯一失败为 stash 隔离验证过的存量问题）

### 改动文件

- `packages/pages/dsh-pmboard/tests/artifact-gates.test.ts`
- `packages/pages/dsh-pmboard/tests/handoff.test.ts`
- `packages/pages/dsh-pmboard/tests/routes-rollup.test.ts`
- `packages/pages/dsh-pmboard/tests/task-report.test.ts`
- `packages/pages/dsh-pmboard/tests/acceptance-criteria.test.ts`

### 下一步

t04（decompose 薄卡拒落 + 开工任务卡送达）开工

---
