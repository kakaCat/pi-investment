# t-23ca69 rollup 阻塞 blockers 字段（W3）

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 目标
rollup 阻塞 blockers 字段（W3）

## 背景摘要（context）
（待补充）

## 范围
- 阶段：implement
- 端侧：backend

## 验收标准
有未完成任务的 verify_submit/task_move 返回含 blockers 任务清单；单测覆盖

## 上游产出摘要（dependsSummary）
- （无依赖）

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-17T06:03:22.884Z，窗口 session-41e7e4cd-4038-476c-9307-d93622a411e2）

rollup 阻塞显式化：新增 rollupBlockersOf 共享 helper，task_move/verify_submit 返回体在需求停 implementing 且有未完成任务时带 blockers 清单 + warning 文案（verify_submit 连 note 都改写），3 个故障注入测试全绿

### 完成项

- 新增 rollupBlockersOf()：implementing 且存在非 done 未取消任务时返回阻塞清单，否则 undefined
- task_move 返回体加 blockers + warning（"需求未进验收：N 个任务未完成"）
- verify_submit 返回体加 blockers + warning（含幽灵任务处置提示），有阻塞时 note 改写为"停在 implementing——见 warning/blockers"
- 3 个 vitest 用例：幽灵场景 task_move 告警 / verify_submit 阻塞态 status+note 断言 / 全 done 无 blockers 正常进 accepting

### 改动文件

- `packages/pages/dsh-pmboard/src/host/agent-tools.ts`
- `packages/pages/dsh-pmboard/tests/decompose-tools.test.ts`

### 下一步

无联调对象，转 testing 验证后关闭

---
