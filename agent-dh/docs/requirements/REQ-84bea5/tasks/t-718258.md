# t-718258 数据契约：RequirementRecord 扩展 pausedReason

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
数据契约：RequirementRecord 扩展 pausedReason

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：doc
- 端侧：backend

## 得到什么结果
grep -n "pausedReason" src/domain/requirement/RequirementRecord.ts 命中一行；npx tsc --noEmit 编译通过

## 实施方案（implementation）
1. 打开 src/domain/requirement/RequirementRecord.ts
2. 找到 advance 字段定义
3. 新增 pausedReason?: string
4. 添加 JSDoc 注释

## 上游产出摘要（dependsSummary）
- （无依赖）

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-21T06:28:55.526Z，窗口 session-8375f8a6-ef99-4656-83b1-6bb110a18f73）

pausedReason 字段已存在于 AdvanceState 接口（第 803 行），无需新增

### 完成项

- ✅ 验证 pausedReason?: string 已在 src/shared/protocol.ts:803 定义
- ✅ 字段位于 AdvanceState 接口中，符合需求
- ✅ npx tsc --noEmit 编译通过

### 下一步

推进到 done，继续下一个任务 t-30b253

---
