# t-30b253 覆盖门禁双源合并实现

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
覆盖门禁双源合并实现

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：implement
- 端侧：backend

## 得到什么结果
代码中 taskRefsFromDecomposition 被调用；npx tsc --noEmit 编译通过；手工测试：requirement.md 含 FR-1 + decomposition.md RTM 覆盖 + plan 无 refs → assertClauseCoverageGate 不抛 requirement_uncovered

## 实施方案（implementation）
1. 打开 src/application/internal/content-gate-wiring.ts
2. 定位 assertClauseCoverageGate 函数
3. 导入 taskRefsFromDecomposition
4. 改为 new Set([...tasks.flatMap(t => t.requirement_refs || []), ...taskRefsFromDecomposition(requirementDir)])
5. 确保 RTM 解析失败时返回空数组

## 上游产出摘要（dependsSummary）
- （无依赖）

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-21T06:31:19.034Z，窗口 session-8375f8a6-ef99-4656-83b1-6bb110a18f73）

覆盖门禁已改为双源合并：任务对象 ∪ decomposition.md RTM

### 完成项

- ✅ 在 content-gate-wiring.ts:32 添加 taskRefsFromDecomposition 导入
- ✅ 在 assertClauseCoverageGate 函数中实现双源合并逻辑（第 85-91 行）
- ✅ 从任务对象获取 refs（refsFromTasks）
- ✅ 从 decomposition.md RTM 获取 refs（refsFromRTM，文件不存在时返回空数组）
- ✅ 合并并去重（covered = [...new Set([...refsFromTasks, ...refsFromRTM])]）
- ✅ npx tsc --noEmit 编译通过

### 改动文件

- `src/application/internal/content-gate-wiring.ts`

### 下一步

推进到 testing，验证功能正确性

---
