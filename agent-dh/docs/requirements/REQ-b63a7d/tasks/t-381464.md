# t-381464 /reqboard/file 接口：allowlist 放宽到工作区根 + 越界改 404

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 目标
/reqboard/file 接口：allowlist 放宽到工作区根 + 越界改 404

## 背景摘要（context）
（待补充）

## 范围
- 阶段：implement
- 端侧：backend

## 验收标准
curl 断言三条：?path=agent-dh/docs/architecture/documentation-standard.md → 200；?path=docs/architecture/documentation-standard.md → 200；?path=quantsys-v2/main.py → 404 且 code=outside_workspace。?path=../../etc/passwd 与 ?path=/etc/passwd 仍 403。新增 tests/http/file-route.test.ts 覆盖上述分支。

## 实施方案（implementation）
改 packages/pages/dsh-pmboard/src/http/routers/artifacts.ts（第 22-40 行）+ 复用 t1 归一层；新增 tests/http/file-route.test.ts（用真实临时目录，不 mock fs）。验证：npx vitest run tests/http/file-route.test.ts + 三条 curl。

## 上游产出摘要（dependsSummary）
- 新增产物路径归一层（domain 纯函数）+ 单测

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-18T15:42:39.544Z，窗口 session-e7129862-11b3-4e7d-ab11-53f6a6db9923）

t3：/reqboard/file 白名单放宽到工作区根 + 归一层兜底 + 越界语义分档 + 新增批量解析端点

### 完成项

- src/http/routers/artifacts.ts 重写：classify() 成为路径判定的唯一处（单文件接口与批量端点共用）；allowlist 由 <cwd>/docs 放宽到 <cwd>；接入 domain 归一层（兼容 agent-dh/ 前缀与工作区内绝对路径）；反斜杠/.. /工作区外绝对路径仍 403；兄弟仓库路径 404 outside_workspace；伪路径 404 not_a_file；工作区缺失 404 not_found
- 新增 POST /dashboard/api/reqboard/docs/resolve（host 单点判定 form/exists/openable/reason，恒 200），routes.ts 注册
- tests/http/file-route.test.ts（13 例）：A1 两种前缀同结果 + 源码产物 200 + 工作区内绝对路径 200；A2 ../../etc/passwd 与 /etc/passwd 仍 403；A3 跨仓 404 outside_workspace / 缺失 404 not_found；A4 伪路径 404 not_a_file；目录与尾斜杠按"不是文件"；A5 批量端点含原因 + 空数组诚实空
- 实测：curl 断言 A1（agent-dh/docs/... 与 docs/... 均 200）、A3（quantsys-v2/main.py 404 outside_workspace）——见 t7 线上核验

### 改动文件

- `packages/pages/dsh-pmboard/src/http/routers/artifacts.ts`
- `packages/pages/dsh-pmboard/src/http/routes.ts`
- `packages/pages/dsh-pmboard/tests/http/file-route.test.ts`

### 下一步

t4 前端消费批量端点

---
