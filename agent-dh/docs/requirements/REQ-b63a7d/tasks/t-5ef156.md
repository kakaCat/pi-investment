# t-5ef156 构建 client + 重启 + 线上核验

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 目标
构建 client + 重启 + 线上核验

## 背景摘要（context）
（待补充）

## 范围
- 阶段：merge
- 端侧：fullstack

## 验收标准
pnpm --filter <dsh-pmboard 包名> build:client 退出码 0 且 verify-client-build.mjs 通过（WRAP_SENTINEL 未命中）；重启后 curl A1/A2/A3 三条符合预期；详情页控制台无 403、源码类产物无「缺失」标；重启前后台账无回退（schemaVersion 仍 6）。

## 实施方案（implementation）
在 packages/pages/dsh-pmboard 下构建 client；写 pending-resume 后续跑消息；launchctl kickstart -k gui/$(id -u)/com.pi-investment.dsh；重启后 curl + 页面核验并留 decision_audit/memory。验证：构建门退出码 0 + 三条 curl + 台账复核。

## 上游产出摘要（dependsSummary）
- 回归与故障注入测试

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-18T15:52:54.566Z，窗口 session-e7129862-11b3-4e7d-ab11-53f6a6db9923）

t7 完成：client 构建 + self_restart 重启 :13080 + 线上 A1–A4 全部符合预期

### 完成项

- 构建：pnpm build:client + verify-client-build OK（bundle=227289 bytes，WRAP_SENTINEL 未命中）；relink-profile --check 25/25 symlink-ok
- 重启：self_restart(preserve_context=true) → 新进程 pid 32032（23:51:52 起，旧 2266），检查点 agent-self/20260918-235136
- A1 实测：agent-dh/docs/... 200（用户报的 403 那条）、docs/... 200、packages/... 200
- A2 实测：../../etc/passwd 403、/etc/passwd 403（穿越防护未放宽）
- A3 实测：quantsys-v2/CLAUDE.md → 404 outside_workspace；quantsys-v2/main.py（确不存在）→ 404 not_found
- A4 实测：quantsys-v2/tests/{a.py,b.py} → 404 not_a_file；POST /docs/resolve → 200 且逐条返回 normalized/form/exists/openable
- 台账清理跨重启存活：--verify 复跑仍 0 条
- 验收材料：docs/requirements/REQ-b63a7d/verification.md（含未覆盖项：浏览器控制台需人硬刷新确认）

### 改动文件

- `docs/requirements/REQ-b63a7d/verification.md`
- `packages/pages/dsh-pmboard/lib/client.js`

### 下一步

提交验收材料 → 请人审核；通过后 self_finalize(merge) 合并检查点分支。

---
