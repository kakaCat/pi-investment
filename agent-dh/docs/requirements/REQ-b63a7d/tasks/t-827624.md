# t-827624 回归与故障注入测试

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 目标
回归与故障注入测试

## 背景摘要（context）
（待补充）

## 范围
- 阶段：test
- 端侧：backend

## 验收标准
npx vitest run 全绿（含新增用例）；故障注入用例逐条命名可追溯到形态 A/B/C/D；穿越防护用例仍拒绝。

## 实施方案（implementation）
新增 packages/pages/dsh-pmboard/tests/fault-injection-paths.test.ts（聚拢 t1–t5 的边界），跑全量并修净。验证：npx vitest run。

## 上游产出摘要（dependsSummary）
- 登记侧接入归一（ReportTask 等登记点）
- /reqboard/file 接口：allowlist 放宽到工作区根 + 越界改 404
- openable 由 host 单点判定并下发；前端只对 openable 预检

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-18T15:42:39.607Z，窗口 session-e7129862-11b3-4e7d-ab11-53f6a6db9923）

t6：路径口径故障注入回归（四形态矩阵 + 真实台账扫描）

### 完成项

- tests/fault-injection-paths.test.ts（10 例）：形态矩阵 A/B/C/D + 穿越，逐条标形态名；并断言「会被判 403 的只有绝对路径非 workspace 与含 .. / 反斜杠两类」
- 真实台账回归扫描：读取 .dsh-data/dsh-reqboard.json，遍历 artifacts / archive.docs / archive.mergedInto / plan.path / docLinks(含 extras[]) 全部路径，断言不存在会被判 403 的写法（文件不存在则跳过，不伪造绿灯）
- 实测：npx vitest run tests/fault-injection-paths.test.ts → 10 passed（含真实台账扫描 0 命中）

### 改动文件

- `packages/pages/dsh-pmboard/tests/fault-injection-paths.test.ts`

### 下一步

t5 存量清理 / t7 重启核验

---
