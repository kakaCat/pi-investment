# t-11b086 openable 由 host 单点判定并下发；前端只对 openable 预检

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 目标
openable 由 host 单点判定并下发；前端只对 openable 预检

## 背景摘要（context）
（待补充）

## 范围
- 阶段：implement
- 端侧：frontend

## 验收标准

可执行验收：① npx vitest run tests/client-api-resolve.test.ts → 3 passed（锁定 POST /dashboard/api/reqboard/docs/resolve 的 URL 与 {paths} 请求体，success=false 抛错）；② npx vitest run tests/http/file-route.test.ts → 13 passed（批量端点恒 200 且逐条返回 form/exists/openable/reason）；③ curl -s -X POST -H "Content-Type: application/json" -d '{"paths":["quantsys-v2/CLAUDE.md"]}' http://127.0.0.1:13080/dashboard/api/reqboard/docs/resolve → 返回 openable=false 且带 reason；④ 浏览器可达路径：打开 http://127.0.0.1:13080 → 项目看板 → 任一需求详情页「文档记录」区，F12 Console 过滤 reqboard：修复前每个源码类产物一条 403，现应无 403/404 失败请求。

## 实施方案（implementation）
改 shared/protocol.ts（StageArtifact 增 openable/reason）、application/query/QueryStageDetail.ts 与 QueryState（计算并下发）、client/board-mount.ts（第 563-586 行）与 client/views/artifacts.ts（渲染分支）+ 对应单测。验证：npx vitest run tests/client-view.test.ts tests/stage-panel.test.ts。

## 上游产出摘要（dependsSummary）
- /reqboard/file 接口：allowlist 放宽到工作区根 + 越界改 404

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-18T15:42:39.577Z，窗口 session-e7129862-11b3-4e7d-ab11-53f6a6db9923）

t4：前端预检改为一次批量解析（host 单点判定），控制台不再产生失败请求

### 完成项

- src/client/api.ts：新增 DocPathVerdictView + resolveReqDocs(paths)（POST /docs/resolve，走既有 unwrap 信封）
- src/client/board-mount.ts：verifyDocExistence 从「逐条 GET /file」改为「一次批量解析」；openable=false 时按 host 给的 reason 标注缺失并移除 data-doc-path 预检锚点；通道异常时不做任何标记（诚实降级，不误标缺失）
- tests/client-api-resolve.test.ts（3 例）：契约锁定（URL/请求体/解包）、success=false 抛错、HTTP 非 2xx 抛错
- 实测：pnpm build:client + verify-client-build OK（bundle=227289 bytes，关键符号齐全）

### 改动文件

- `packages/pages/dsh-pmboard/src/client/api.ts`
- `packages/pages/dsh-pmboard/src/client/board-mount.ts`
- `packages/pages/dsh-pmboard/tests/client-api-resolve.test.ts`
- `packages/pages/dsh-pmboard/lib/client.js`

### 下一步

t7 重启后线上核验详情页无 403

---
