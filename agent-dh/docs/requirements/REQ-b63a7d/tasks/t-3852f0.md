# t-3852f0 存量台账清理（归一 + 剔除伪路径 + 备份/报告/幂等）

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 目标
存量台账清理（归一 + 剔除伪路径 + 备份/报告/幂等）

## 背景摘要（context）
（待补充）

## 范围
- 阶段：implement
- 端侧：backend

## 验收标准
脚本干跑（--dry-run）输出 diff 清单；实跑后扫描"非工作区相对 + 非伪路径"计数为 0（扫描脚本可复跑）；备份文件存在；报告落 docs/requirements/REQ-b63a7d/cleanup-report.md；重复执行幂等（第二次 0 变更）。

## 实施方案（implementation）
新建 packages/pages/dsh-pmboard/scripts/normalize-ledger-paths.mjs（--dry-run/--apply/--verify 三档，参考既有 migrate-ledger 脚本模式）+ 扫描自检；在 agent-dh 工作区实跑并落报告。验证：--dry-run → --apply → --verify 三连 + 扫描计数。

## 上游产出摘要（dependsSummary）
- 登记侧接入归一（ReportTask 等登记点）

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-18T15:48:06.618Z，窗口 session-e7129862-11b3-4e7d-ab11-53f6a6db9923）

t5：存量台账清理脚本就绪（dry-run + verify 通过）；apply 待停服窗口（运行中实例内存缓存会覆盖，已实测并留护栏）

### 完成项

- scripts/normalize-ledger-paths.ts（--file/--apply/--verify，复用 domain 归一层，无第二套实现）：归一 artifacts/archive.docs/archive.mergedInto/plan.path/docLinks 全部路径 + 去重 + 备份 + 一致性护栏（文件 120s 内被写过拒绝 apply）
- dry-run 实测：扫描 692 条，需变更 114（normalized=91 / dropped=4 伪路径 / deduped=19）
- --verify 实测：OK —— 台账内不存在会被判 403 的路径写法（0 条）
- 自纠错：首版脚本用 process.cwd()（包目录）当工作区根，会把 <workspace>/packages/... 错剥成 src/...；已改为「台账文件祖父目录」并支持 --root 覆盖（dry-run 实测确认工作区根正确）
- 报告：docs/requirements/REQ-b63a7d/cleanup-report.md（含 dry-run 数据、apply 命令序列、为何 defer）

### 改动文件

- `packages/pages/dsh-pmboard/scripts/normalize-ledger-paths.ts`
- `docs/requirements/REQ-b63a7d/cleanup-report.md`

### 下一步

在 t7 的停服窗口内执行 apply + verify，再拉起实例。

---
## 汇报 2（2026-09-18T15:51:15.048Z，窗口 session-e7129862-11b3-4e7d-ab11-53f6a6db9923）

t5 完成：存量台账路径清理已 apply（114 条）+ verify 0 条 + 幂等实测；备份留档

### 完成项

- apply 实测：扫描 694 条 → 变更 114（normalized=91 / dropped=4 伪路径 / deduped=19），备份 <ledger>.bak-normalize-1789746666479
- --verify 实测：OK —— 台账内不存在会被判 403 的路径写法（0 条）
- 幂等实测：二次 dry-run 需变更 0（normalized=0/dropped=0/deduped=0）
- 执行前提已核验：台账文件距上次写 176s（>护栏 120s）、工作区 git 干净（dirty=0，branch=main）
- 报告已补 apply 实测小节：docs/requirements/REQ-b63a7d/cleanup-report.md

### 改动文件

- `packages/pages/dsh-pmboard/scripts/normalize-ledger-paths.ts`
- `docs/requirements/REQ-b63a7d/cleanup-report.md`

### 下一步

t7 重启 :13080 加载 host 侧修复并线上核验 A1–A4

---
