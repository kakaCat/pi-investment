# t-0619c1 登记侧接入归一（ReportTask 等登记点）

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 目标
登记侧接入归一（ReportTask 等登记点）

## 背景摘要（context）
（待补充）

## 范围
- 阶段：implement
- 端侧：backend

## 验收标准
npx vitest run tests/application/report-task-path.test.ts 全绿；用例断言 `agent-dh/docs/x` → 产物 path=`docs/x`、绝对路径 → 相对、`{a,b}` → 不进 artifacts；既有 report-task 用例语义不变。

## 实施方案（implementation）
改 packages/pages/dsh-pmboard/src/application/use-cases/ReportTask.ts（第 134-143 行）+ 复用 t1 的归一层；新增 tests/application/report-task-path.test.ts。验证：npx vitest run tests/application tests/task-report.test.ts。

## 上游产出摘要（dependsSummary）
- 新增产物路径归一层（domain 纯函数）+ 单测

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-18T15:41:06.839Z，窗口 session-e7129862-11b3-4e7d-ab11-53f6a6db9923）

t2：reqboard_task_report 登记侧接入归一层（去掉 403 刷屏的登记侧根因）

### 完成项

- src/application/use-cases/ReportTask.ts：files_changed 上浮前过 normalizeArtifactPath(deps.docs.workspaceRoot())；pseudo/空路径不入 artifacts；同一归一路径去重
- tests/application/report-task-path.test.ts（5 例）：agent-dh/docs/x → docs/x、工作区内绝对路径 → 相对、brace/通配伪路径不入清单、合规路径原样+./ 清洗+去重、跨仓路径保持原样
- 实测：npx vitest run tests/application/report-task-path.test.ts → 5 passed

### 改动文件

- `packages/pages/dsh-pmboard/src/application/use-cases/ReportTask.ts`
- `packages/pages/dsh-pmboard/tests/application/report-task-path.test.ts`

### 下一步

t5 存量清理可直接复用同一归一层

---
## 汇报 2（2026-09-18T15:41:56.602Z，窗口 session-e7129862-11b3-4e7d-ab11-53f6a6db9923）

t2：登记侧接入归一层（并补：工作区外绝对路径不入产物清单）

### 完成项

- src/application/use-cases/ReportTask.ts：files_changed 上浮前过 normalizeArtifactPath(deps.docs.workspaceRoot())；pseudo/空路径不入 artifacts；工作区外绝对路径（/etc/…）不入清单；同一归一路径去重
- tests/application/report-task-path.test.ts（6 例）：agent-dh/docs/x → docs/x、工作区内绝对路径 → 相对、brace/通配伪路径不入、/etc 绝对路径不入、合规路径原样+./ 清洗+去重、跨仓路径保持原样
- 实测：npx vitest run tests/application/report-task-path.test.ts tests/http/file-route.test.ts → 全绿

### 改动文件

- `packages/pages/dsh-pmboard/src/application/use-cases/ReportTask.ts`
- `packages/pages/dsh-pmboard/tests/application/report-task-path.test.ts`

### 下一步

t5 存量清理复用同一归一层

---
