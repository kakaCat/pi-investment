# t-d5263f 设计文档种类建模与旧条目回填

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 目标
设计文档种类建模与旧条目回填

## 背景摘要（context）
（待补充）

## 范围
- 阶段：implement
- 端侧：backend

## 验收标准
`npx vitest run tests/domain/artifact.test.ts tests/sync-artifacts.test.ts` 全绿；用例包含：ALL_ARTIFACT_KINDS 含 design 而 STAGE_ARTIFACT_REQUIREMENTS/ARTIFACT_CONFIRM_GATES 均不含；二次同步返回 0 且不再改写。

## 实施方案（implementation）
改 src/domain/artifact/ArtifactSpec.ts（ArtifactKind 加 design、规则 /^design\/.+\.md$/ ）、src/adapters/ArtifactSync.ts（stageForKind design、staleAutoKind 仅 autoDiscovered）、tests/domain/artifact.test.ts、tests/sync-artifacts.test.ts。

## 上游产出摘要（dependsSummary）
- （无依赖）

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-19T09:10:50.307Z，窗口 session-45748eec-92e1-49c6-a414-5b43164229d9）

设计文档成为独立产物种类：design/*.md 登记为 kind=design、stage=design，旧 autoDiscovered 的 notes 条目在同步时被回填；design 不进任何必备产物与确认门，人工登记的条目不被改写。

### 完成项

- ArtifactSpec 增加 design 种类与路径规则 /^design\/.+\.md$/（非 .md 如 token-ui.html 仍归 notes）
- ArtifactSync 的 stageForKind 支持 design；staleAutoKind 仅重分类 autoDiscovered === true 的条目
- 用例锁定：ALL_ARTIFACT_KINDS 含 design，STAGE_ARTIFACT_REQUIREMENTS / ARTIFACT_CONFIRM_GATES 均不含
- 二次同步返回 0 且不再改写台账（幂等）；人工登记条目 kind 保持 notes

### 改动文件

- `packages/pages/dsh-pmboard/src/domain/artifact/ArtifactSpec.ts`
- `packages/pages/dsh-pmboard/src/adapters/ArtifactSync.ts`
- `packages/pages/dsh-pmboard/tests/domain/artifact.test.ts`
- `packages/pages/dsh-pmboard/tests/sync-artifacts.test.ts`

### 下一步

t3 设计节点逐份交付状态展示

---
