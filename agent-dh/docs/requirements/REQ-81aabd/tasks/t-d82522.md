# t-d82522 台账 v6→v7 迁移脚本与实迁执行

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 目标
台账 v6→v7 迁移脚本与实迁执行

## 背景摘要（context）
（待补充）

## 范围
- 阶段：implement
- 端侧：backend

## 验收标准
`npx vitest run tests/migration.test.ts` 全绿；`node --import tsx/esm scripts/migrate-ledger.ts --file .dsh-data/dsh-reqboard.json --dry-run` 退出码 0 且白名单外差异为 0；`--apply` 后 `--verify` 输出 0 问题项且 schemaVersion=7；正文历史 planning 措辞仍在。

## 实施方案（implementation）
改 src/domain/legacy/LegacyStatus.ts（LEGACY_REQ_STATUS_ALIASES、migrateArtifactStageNames）、scripts/migrate-ledger.ts（V7、C11、WHITELIST 数组下标、--verify 残留检查）、tests/migration.test.ts；执行 node --import tsx/esm scripts/migrate-ledger.ts --file .dsh-data/dsh-reqboard.json --dry-run 后再 --apply。

## 上游产出摘要（dependsSummary）
- 节点键改名 planning → design（状态机/协议/提示词分片/全仓字面量）
- 设计文档种类建模与旧条目回填

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-19T09:18:16.776Z，窗口 session-45748eec-92e1-49c6-a414-5b43164229d9）

台账 v6→v7 迁移脚本落地并对线上台账实迁：脚本支持 --dry-run/--apply/--verify 三条路径与白名单核对，v7 起 schemaVersion=7、需求状态 planning→design、artifacts[].stage planning→design 一并固化。实迁一次通过（dry-run 白名单外差异 0 条、apply 备份 .bak-req47939a-*、verify 结构自洽）。

### 完成项

- npx vitest run tests/migration.test.ts → Test Files 1 passed (1)，Tests 9 passed (9)（含白名单数组下标分支、幂等、结构自洽）
- dry-run：before schemaVersion=6 revision=1581 requirements=42 tasks=176 → after schemaVersion=7（revision/条数不变），差异路径 81 条全部在白名单内、白名单外 0 条，exit 0
- C3_legacy_status_renamed: 26（planning→design 等旧状态名改写）、C11_artifact_stage_renamed: 24（artifacts[].stage 回填）
- --apply 原子写入（tmp+fsync+rename）+ 备份 .dsh-data/dsh-reqboard.json.bak-req47939a-<epoch>；--verify 输出 0 问题项且 schemaVersion=7
- 正文历史措辞保留：迁移只改 status / statusHistory[].status / artifacts[].stage 三类字段，散文里的历史 planning 字样原样保留（190 处，属保留而非失败）
- 实迁时机修正：迁移必须在「进程已停、新进程未起」的窗口内落盘——运行中的旧进程把台账整体驻留内存（load-once + 每次 mutate 全量重写），在旧进程存活期间迁移会被其内存快照静默覆盖（本次实测复现）；已把 migrate --apply/--verify 编排进本次发版的重启脚本

### 改动文件

- `packages/pages/dsh-pmboard/scripts/migrate-ledger.ts`
- `packages/pages/dsh-pmboard/tests/migration.test.ts`
- `packages/pages/dsh-pmboard/src/shared/protocol.ts`

### 下一步

进程停止窗口内对线上台账实迁（随本次重启脚本执行），重启后 --verify 复核 schemaVersion=7 与 0 问题项。

---
