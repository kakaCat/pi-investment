# t-9fc198 新增 reqboard_confirm_artifact 并扩展确认模型（via/evidence）

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 目标
新增 reqboard_confirm_artifact 并扩展确认模型（via/evidence）

## 背景摘要（context）
（待补充）

## 范围
- 阶段：implement
- 端侧：backend

## 验收标准
会话确认落库 confirmedBy.via=session 且 evidence/sessionId 与传入一致；看板确认写 via=board；老记录读取不报错；单测覆盖三类来源。

## 上游产出摘要（dependsSummary）
- （无依赖）

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-16T16:22:56.337Z，窗口 session-24ded829-8d22-4e59-9318-bad027351335）

新增 reqboard_confirm_artifact（会话确认落章）+ 确认模型扩展 via/evidence

### 完成项

- agent-tools.ts 新增 reqboard_confirm_artifact：target=artifact（配 kind）/ target=plan（批准计划），落库 confirmedBy={kind:human,sessionId} + via=session + evidence
- protocol.ts：StageArtifact 增 confirmedVia/confirmedEvidence；PlanRecord 增 approvedVia/approvedEvidence（可选，老记录兼容为 board）
- routes.ts：看板「确认产物」写 confirmedVia=board、「批准计划」写 approvedVia=board
- index.ts 注册新工具
- 实测：decomposition 确认落章成功，台账 via=session + evidence 与答复原文一致

### 改动文件

- `packages/pages/dsh-pmboard/src/host/agent-tools.ts`
- `packages/pages/dsh-pmboard/src/shared/protocol.ts`
- `packages/pages/dsh-pmboard/src/host/routes.ts`
- `packages/pages/dsh-pmboard/src/index.ts`

### 下一步

t2 已完成，等 t1 补测与 t4 汇总

---
