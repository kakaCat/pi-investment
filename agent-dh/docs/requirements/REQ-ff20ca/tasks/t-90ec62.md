# t-90ec62 确认门全链路单测与 13080 实测

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 目标
确认门全链路单测与 13080 实测

## 背景摘要（context）
（待补充）

## 范围
- 阶段：test
- 端侧：fullstack

## 验收标准
13080 上全链路走通（提交→确认→推进）；看板「确认产物」按钮返回 200 并写入 confirmedAt；vitest 全绿。

## 上游产出摘要（dependsSummary）
- 新增 reqboard_requirement_submit 登记需求文档产物
- 新增 reqboard_confirm_artifact 并扩展确认模型（via/evidence）
- 改造 reqboard_move 门禁：产物已确认即放行

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-16T16:32:15.038Z，窗口 session-24ded829-8d22-4e59-9318-bad027351335）

确认门全链路验证：门禁两态实测 + 全量单测 398 通过

### 完成项

- 实测 1（未确认拒绝）：move decomposing→implementing 被拒，提示两条确认通道 ✓
- 实测 2（会话确认落章）：reqboard_confirm_artifact 写入 via=session + evidence + sessionId ✓
- 实测 3（门开启放行）：确认后同转移成功推进 ✓
- t1 验证：工具已注册（调用返回状态纪律消息）；首次登记写入路径受状态限制未直接实测（当前需求已过 brainstorming），如实记录
- vitest 全量：23 文件 / 398 用例通过（含新增工具与门禁用例）
- 构建门禁：pnpm build:client + verify sentinel 通过

### 下一步

t7 的 stage-prompts 改动需重启生效；随后提交验收

---
