# t-447c4a 改造 reqboard_move 门禁：产物已确认即放行

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 目标
改造 reqboard_move 门禁：产物已确认即放行

## 背景摘要（context）
（待补充）

## 范围
- 阶段：implement
- 端侧：backend

## 验收标准
未确认时 move 被拒且提示含 ask_user_question；已确认（board 或 session 来源）时放行；推进留痕 by=agent + 依据说明；单测覆盖两态。

## 上游产出摘要（dependsSummary）
- 新增 reqboard_confirm_artifact 并扩展确认模型（via/evidence）

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-16T16:22:56.400Z，窗口 session-24ded829-8d22-4e59-9318-bad027351335）

改造 reqboard_move 门禁：产物已确认即放行 + agent 侧补产物闸门

### 完成项

- 门判定从"谁调用"改为"产物是否已确认"（确认来源不限：board/session）
- agent 侧补上 assertArtifactGates（此前只有看板 API 有，agent 可绕过）
- 未确认时拒绝并明确两条确认通道；取消/验收/归档类仍硬拒
- 实测：未确认时 decomposing→implementing 被拒（提示两通道）；确认后同转移放行成功

### 改动文件

- `packages/pages/dsh-pmboard/src/host/agent-tools.ts`

### 下一步

已完成

---
