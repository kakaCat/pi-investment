# t-9f7594 验收态按钮常显 + 「验收通过」二次确认弹框

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
验收态按钮常显 + 「验收通过」二次确认弹框

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：implement
- 端侧：frontend

## 得到什么结果
cd agent-dh/packages/pages/dsh-pmboard && npx vitest run tests/board-info-fixes.test.ts tests/client-view.test.ts 全绿；其中含断言"accepting 且无 verification 时操作条包含 data-action=verify-pass 且不再包含旧提示文案"；手工在 :13080 看板点「验收通过」，弹框可见、选取消后无任何请求。

## 实施方案（implementation）
改 packages/pages/dsh-pmboard/src/client/views/stage-detail.ts（renderActionBar：accepting 分支删掉"未交材料只给提示"、按钮只判阶段）、src/client/board-mount.ts（case verify-pass 装配"通过/不通过/未裁决"计数文案 + window.confirm，取消即 return，确认带 confirm_override）、src/client/api.ts（verifyPass 增可选 confirm_override）、src/client/types.ts（verification 增可选 override）。

## 上游产出摘要（dependsSummary）
- （无依赖）

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-19T16:35:15.767Z，窗口 session-054044c2-507e-48ec-96ff-8f582605b2e2）

这一步做完：需求一进验收阶段，「验收通过」按钮就一直在；点下去会先把"不通过/未裁决"的条数摆到人眼前，人点取消则一个请求都不发。看板不再出现"只给一行提示、没有按钮"或"点下去毫无提示"两种状态。

### 完成项

- 操作条：accepting 分支删掉"未交材料只给提示"，按钮显示条件只看阶段（stage-detail.ts renderActionBar）
- 新增纯函数 verifyConfirmCopy：装配三种确认文案（无材料 / 全过 / 有不合格）+ 覆盖说明（board-mount.ts）
- verify-pass 分支：先 window.confirm，取消即 return（零请求），确认才带 confirm_override 发请求
- api.verifyPass 支持 confirm_override；client types 增 AcceptanceOverride
- 既有断言反转 + 新增 3 条确认文案用例（board-info-fixes.test.ts）
- 重建 client 产物：pnpm build:client 退出码 0，verify-client 报「关键符号齐全」

### 改动文件

- `packages/pages/dsh-pmboard/src/client/views/stage-detail.ts`
- `packages/pages/dsh-pmboard/src/client/board-mount.ts`
- `packages/pages/dsh-pmboard/src/client/api.ts`
- `packages/pages/dsh-pmboard/src/client/types.ts`
- `packages/pages/dsh-pmboard/tests/board-info-fixes.test.ts`
- `packages/pages/dsh-pmboard/lib/client.js`
- `packages/pages/dsh-pmboard/lib/client.cjs`

### 下一步

服务端语义配套（t2/t3/t4）已完成；t5 端到端与全量回归见其卡内阻塞说明。

---
