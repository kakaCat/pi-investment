# t-1aa789 Dive 服务接线：订阅收归服务、requirement-moved 不再直接续跑

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
Dive 服务接线：订阅收归服务、requirement-moved 不再直接续跑

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：implement
- 端侧：backend

## 得到什么结果
npx vitest run tests/dive-manager-wiring.test.ts 全绿（7 条订阅全部成立；requirement-moved 后 deliverMessage 未被调用、agent 空闲后才投递一次；teardown() 后触发不再投递）；grep -c followup src/application/dive/ReqboardDiveManager.ts 输出 0。

## 实施方案（implementation）
重写 src/application/dive/ReqboardDiveManager.ts：constructor(ctx, store)；构造内订阅 agent/pre-step、agent/inbox/inserted|claimed|discarded、agent/error、agent/disposed、reqboard/requirement-moved（委托 round 半，成立/失败均留痕）；requirement-moved 只置 needsCheckpoint + requestDrive；暴露 roundDriver()/teardown()；删除 shouldContinue/triggerContinuation/incrementRound/checkAndContinue。改 src/index.ts（new ReqboardDiveManager(ctx, store)、构造 ctx 端口、登记 teardown disposer）与 src/wiring/pm-capture-root.ts（注入 round 端口 + 订阅缺失响亮 warn）。新增 tests/dive-manager-wiring.test.ts。

## 上游产出摘要（dependsSummary）
- 投递适配：回合消息带机器可识别的来源标识
- 会话驱动器组合：一拍内的定序与原采集行为不变（含向后兼容）

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-26T14:47:07.775Z，窗口 session-c5ea210f-afc3-4735-a4d1-f9058b378363）

续跑的「方向盘」交回了 Dive 服务：它现在只负责盯着七类宿主事件，把活转给回合状态机；需求状态一变不再直接往窗口塞消息，而是先记一笔「该复查了」，等窗口真正空闲才起轮。服务卸载时也会先关门、再收尾，不留半开的续跑。

### 完成项

- 新增 application/dive/round-subscriptions.ts：七路订阅接线（pre-step / inbox inserted·claimed·discarded / error / disposed / requirement-moved），订阅失败逐条响亮告警，返回合并解绑
- 重写 ReqboardDiveManager：构造注入 DiveRoundPorts，持有七路订阅并完全委托 round 半；暴露 roundDriver()/teardown()；删除 shouldContinue/triggerContinuation/incrementRound/checkAndContinue（全仓 0 处 followup）
- wiring/pm-capture-root.ts 增可选 deps.round 并透传给 session-driver
- index.ts：把 Dive 服务实例化下移到 createCaptureRuntime 之后；构造 round 端口（repo/agents/fiberActive/delivery=deliverer/cancel/whenIdle/checkpoint=store.read/renderRoundText/now/logger）；登记 teardown disposer；装配时注入 round: diveManager.roundDriver()
- 新增 tests/dive-manager-wiring.test.ts：6 条断言（七路订阅齐全、总线缺失时七条告警、忙时不投递/空闲起 1 轮、pre-step 转发拒伪造、teardown 后不投递、manager 内 0 处 followup）

### 改动文件

- `packages/web/dsh-pmboard/src/application/dive/round-subscriptions.ts`
- `packages/web/dsh-pmboard/src/application/dive/ReqboardDiveManager.ts`
- `packages/web/dsh-pmboard/src/wiring/pm-capture-root.ts`
- `packages/web/dsh-pmboard/src/index.ts`
- `packages/web/dsh-pmboard/tests/dive-manager-wiring.test.ts`

### 下一步

T-6 对齐验收单测（11 FR）→ T-7 迁移兼容 + 重建 dist + 重启回归

---
