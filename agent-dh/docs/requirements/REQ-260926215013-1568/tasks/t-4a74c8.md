# t-4a74c8 投递适配：回合消息带机器可识别的来源标识

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
投递适配：回合消息带机器可识别的来源标识

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：implement
- 端侧：backend

## 得到什么结果
npx vitest run tests/agent-deliverer.test.ts 全绿；断言 createRoundMessage 返回的 message.source 与 {kind:'dive',requirementId,revision,round} 结构相等；deliverMessage 在 agents 不可得时返回 delivered:false 且不抛。

## 实施方案（implementation）
src/adapters/AgentDeliverer.ts 增 createRoundMessage（结构复刻 {id,role:'user',content:[{type:'text',text}],source:{kind:'dive',requirementId,revision,round}}，不 import @deepseek-ai/dsh-llm）与 deliverMessage（复用 agents 不可得/窗口离线/无 followup 三态守卫，永不抛）；扩展 tests/agent-deliverer.test.ts。

## 上游产出摘要（dependsSummary）
- 契约定死：回合来源、相位值域与驱动状态纯判定

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-26T14:39:57.467Z，窗口 session-c5ea210f-afc3-4735-a4d1-f9058b378363）

自动续跑的「回合消息」现在带上了机器可识别的出身证明（哪条需求、哪个版本、第几回合），并且构造与投递拆成两步——这样发出去之前就能登记好身份，避免旧版本或被篡改的消息混进执行。

### 完成项

- AgentDeliverer 改为实现 DiveRoundDeliveryPort（在既有 deliver 之上）
- 新增 createRoundMessage：结构复刻 UserMessage，source={kind:'dive',requirementId,revision,round}，返回 {message,messageId}（不投递）
- 新增 deliverMessage：原样投递已构造消息，复用三态守卫（agents 不可得 / 离线 / 无 followup / 抛错），永不抛
- deliver/deliverMessage 共用私有 publish（形状纪律单点）
- 扩展 tests/agent-deliverer.test.ts：新增 2 条断言（source 字段完整、原样投递与离线不抛），9/9 通过

### 改动文件

- `packages/web/dsh-pmboard/src/adapters/AgentDeliverer.ts`
- `packages/web/dsh-pmboard/tests/agent-deliverer.test.ts`

### 下一步

T-4 会话驱动器组合（依赖 T-2 已完成）；之后 T-5 接线、T-6 对齐验收

---
