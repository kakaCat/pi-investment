# t-2b3e57 迁移与兼容核查 + 回归发版

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
迁移与兼容核查 + 回归发版

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：test
- 端侧：backend

## 得到什么结果
python3 scripts/relink-profile.py --check 退出码 0；npx vitest run tests/capture-hook.test.ts tests/dive-session-driver-wiring.test.ts tests/isolate-node-context.test.ts 全绿且 npx tsc --noEmit -p tsconfig.json 错误数不高于基线；curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:13080/ 输出 200。

## 实施方案（implementation）
核查旧台账读容错（缺 phase/activation 不驱动）、旧调用方兼容（createDiveSessionDriver 不传 round 仍可用）、回滚路径（revert + relink + kickstart）；记录 tsc 基线错误数并对比；python3 scripts/relink-profile.py 与重启 :13080 后复跑三个回归套件。

## 上游产出摘要（dependsSummary）
- 对齐验收：11 个 FR 的可证伪单测

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-26T14:51:14.639Z，窗口 session-c5ea210f-afc3-4735-a4d1-f9058b378363）

这次改动真正上线了：插件重建后重启，服务正常服务请求；旧台账里的老数据不会被误当成「可自动续跑」；三件套回归与新增的 52 条对齐断言全绿，类型错误数还比改动前少了 5 条。

### 完成项

- 迁移/兼容核查：旧数据用阶段名当 phase（老写法）或没有 dive 字段 → isDrivableRequirement 判否，不会被驱动；未知阶段上限回落 10（新增 tests/t7-legacy-tolerance.test.ts 3/3 锁定）
- 向后兼容：session-driver 的 round 依赖可选，不注入时行为与改动前逐字一致（旧调用方/脚本不受影响）
- 回滚路径：git revert + relink + launchctl kickstart（已在设计文档写明）
- python3 scripts/relink-profile.py --check → 退出码 0（5 个条目均符号链接、无副本漂移）
- npx vitest run tests/capture-hook.test.ts tests/dive-session-driver-wiring.test.ts tests/isolate-node-context.test.ts → 3 文件 63/63 通过
- npx vitest run（6 个对齐/回归文件）→ 52/52 通过
- npx tsc --noEmit -p tsconfig.json → 140 个错误（改动前基线 145，净减 5）
- 重建 dist（pnpm build，22:48）并重启 :13080；进程存活（PID 13808）、看板 API 正常响应

### 改动文件

- `packages/web/dsh-pmboard/tests/t7-legacy-tolerance.test.ts`
- `packages/web/dsh-pmboard/dist/index.mjs`

### 下一步

全部 7 张卡 done → 提交验收材料（reqboard_submit kind=verification）

---
