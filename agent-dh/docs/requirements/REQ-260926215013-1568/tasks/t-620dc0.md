# t-620dc0 对齐验收：11 个 FR 的可证伪单测

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
对齐验收：11 个 FR 的可证伪单测

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：test
- 端侧：backend

## 得到什么结果
npx vitest run tests/dive-manager-alignment.test.ts 输出 Tests 数不少于 11、failed 0；每条 FR-1…FR-11 至少一条断言（含 FR-11 六条路径留痕断言）。

## 实施方案（implementation）
新增 tests/dive-manager-alignment.test.ts（文件头 // serves: FR-1 … FR-11），按设计 test-cases.md 的 TC-01…TC-11 逐条断言；fake ReqboardRepository + fake agents（可切 status）+ 可编排 checkpoint promise + 记录型 logger/deliverMessage。

## 上游产出摘要（dependsSummary）
- 回合状态机：空闲才起轮、准入才计数、上限写终态
- 投递适配：回合消息带机器可识别的来源标识
- 会话驱动器组合：一拍内的定序与原采集行为不变（含向后兼容）
- Dive 服务接线：订阅收归服务、requirement-moved 不再直接续跑

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
