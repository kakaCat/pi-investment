# t-2a7c20 工具实现 - 覆盖度检查器

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
工具实现 - 覆盖度检查器

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：implement
- 端侧：backend

## 得到什么结果
执行：`npx vitest run packages/tools/reqboard/tests/rtm/coverage-checker.test.ts`
期望：
1. checkCoverage: 输入 3 个 FR（task_coverage 只覆盖 2 个），返回 unreceived_clauses = [FR-3], coverage_rate = 67%
2. validateCoverage: 覆盖度 < 100% 时抛出错误，错误信息包含 "未覆盖的 FR: FR-3"

## 实施方案（implementation）
1. 创建 `packages/tools/reqboard/src/rtm/coverage-checker.ts`
2. 实现 CoverageChecker 类：checkCoverage / validateCoverage
3. 编写单元测试覆盖 2 个场景

## 上游产出摘要（dependsSummary）
- 数据模型 - RTM 类型定义
- 工具实现 - RTM 管理器（核心）

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-25T12:10:37.278Z，窗口 session-18ecbbd5-0d45-4816-a90b-3040ed8a1a0c）

已完成覆盖度检查器，实现了 checkCoverage 和 validateCoverage 两个方法，所有测试通过（4/4）

### 完成项

- 创建 packages/tools/reqboard/src/rtm/coverage-checker.ts
- checkCoverage: 检查 FR 覆盖度，返回覆盖率和未覆盖的 FR 列表
- - 计算 total_frs、covered_frs、unreceived_clauses、coverage_rate
- - 支持部分覆盖和完全覆盖两种场景
- validateCoverage: 验证 100% 覆盖，否则抛出错误
- - 错误信息包含未覆盖的 FR 列表
- - 覆盖度 = 100% 时不抛出错误
- 创建单元测试 packages/tools/reqboard/tests/rtm/coverage-checker.test.ts
- 所有测试通过（4/4）

### 改动文件

- `packages/tools/reqboard/src/rtm/coverage-checker.ts`
- `packages/tools/reqboard/tests/rtm/coverage-checker.test.ts`

### 下一步

下一步：t-627cc8（验收门禁），并行执行；之后可以开始工具集成任务

---
