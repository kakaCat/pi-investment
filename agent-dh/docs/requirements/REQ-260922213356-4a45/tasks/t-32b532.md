# t-32b532 定义模板地址映射表与守护单测

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
定义模板地址映射表与守护单测

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：implement
- 端侧：backend

## 得到什么结果
npx vitest run tests/template-address.test.ts 全绿；临时移走 templates/design/architecture.md 后该测试变红（exit≠0）；pnpm typecheck 绿。

## 实施方案（implementation）
新增 packages/web/dsh-pmboard/src/domain/template/types.ts（TemplateRef/DocRef/AddressSectionInput/UpstreamSource，domain 局部结构类型，不 import shared）与 packages/web/dsh-pmboard/src/domain/template/registry.ts（NODE_TEMPLATES：只登记 stageEnabledFor=true 的节点×类型组合；design(feature)=architecture/data-model/interfaces/test-cases/use-cases，design(refactor)=architecture/migration，design(bug)=空）。新增 packages/web/dsh-pmboard/tests/template-address.test.ts：①每条 relPath existsSync；②与 effectiveDesignDocs(category, []) 双向一致且禁用组合无表项；③templates/ 下未引用文件必须在显式 allowlist（README.md/examples/**/common/notes.md/design/prototype.html 等，逐条带理由）；④relPath 拒绝 ..、绝对路径、空串；⑤id 唯一。

## 上游产出摘要（dependsSummary）
- （无依赖）

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-22T15:09:26.066Z，窗口 session-2c955dd9-15b6-4cc5-8a0b-67ff978455aa）

模板地址映射表落地：窗口进入任一节点时，代码里已有一份「节点×类型 → 模板文件」的静态事实源（TemplateRef + NODE_TEMPLATES），改名/新增模板忘同步会被守护单测直接拦下——这是「节点注入带模板地址」的第一块地基。

### 完成项

- 新增 src/domain/template/types.ts：TemplateRef（id/stage/category/relPath/title/purpose）与表容器类型，domain 局部结构类型，零 import 零 I/O
- 新增 src/domain/template/registry.ts：NODE_TEMPLATES 静态表（6 节点 × 启用类型，共 27 条），只登记 stageEnabledFor=true 的组合；feature design=5 份、refactor=架构+迁移、bug=空
- 新增 tests/template-address.test.ts（12 用例）：①每条 relPath existsSync；②与 effectiveDesignDocs(category,[]) 双向一致 + 禁用组合无表项；③templates/ 未引用文件必须在带理由的 allowlist；④relPath 形态（防 .. 穿越）；⑤id 与三元组唯一；⑥字段齐备
- 故障注入实测：临时移走 templates/design/architecture.md → 单测 exit=1 且报出两条对应条目，随后已还原（RESTORED）

### 改动文件

- `packages/web/dsh-pmboard/src/domain/template/types.ts`
- `packages/web/dsh-pmboard/src/domain/template/registry.ts`
- `packages/web/dsh-pmboard/tests/template-address.test.ts`

### 下一步

T-2（t-3e89da）实现 resolveNodeTemplates / resolveUpstreamDocs / renderAddressSection / augmentResolvedPrompt，依赖本卡。另需处置：本实例 workflow engine 不可用导致父卡自动链暂停（engine_unavailable），T-2 起需手动推进。

---
