# t-868e3f 增三条确认纪律并加同产物防重弹守卫

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
增三条确认纪律并加同产物防重弹守卫

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：implement
- 端侧：backend

## 得到什么结果
node scripts/check-prompt-fragments.mjs 退出 0；npx vitest run tests/ask-confirm.test.ts 绿；iron-rules 文本含三条纪律关键词（先答后确认/文字确认走 evidence/同产物不重复弹框）；对已确认产物再次 reqboard_ask_confirm → 弹框端口调用 0 次且返回 note 含「已确认」。

## 实施方案（implementation）
改 packages/web/dsh-pmboard/src/domain/prompt/fragments/common/iron-rules.md 增三条纪律（先答后确认；文字确认走 evidence 路径；同一产物不重复弹框）；跑 packages/web/dsh-pmboard/scripts/inline-prompt-fragments.mjs 重生成 src/domain/prompt/generated/fragments.ts，跑 packages/web/dsh-pmboard/scripts/dump-stage-prompts.mjs 更新 P1 基线；改 packages/web/dsh-pmboard/src/application/use-cases/AskConfirm.ts 增守卫：目标产物已确认（或计划已批准）→ 不调用弹框通道，直接返回既有字段 + note。补 packages/web/dsh-pmboard/tests/ask-confirm.test.ts 的守卫与纪律措辞断言。

## 上游产出摘要（dependsSummary）
- 定义模板地址映射表与守护单测

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-22T15:27:32.863Z，窗口 session-2c955dd9-15b6-4cc5-8a0b-67ff978455aa）

确认门回到对话：节点纪律里写明了"先答后确认 / 文字确认走 evidence / 同一产物不重复弹框"；代码层面，目标产物已确认（或计划已批准）时直接返回，不再弹第二个框打扰用户。

### 完成项

- 改 common/iron-rules.md：增三条确认纪律（确认门对话优先 / 文字确认走 evidence / 同一产物不重复弹框）
- 重跑 inline-prompt-fragments.mjs 与 dump-stage-prompts.mjs：生成物与 P1 基线同步；check-prompt-fragments 退出 0
- 改 AskConfirm.ts：目标产物已确认（或计划已批准）→ 不调用弹框通道，直接返回既有字段 + note（无新输出字段，schema 不变）
- 补 tests/ask-confirm.test.ts：同一产物再次发起时弹框端口调用 0 次且 note 含「已确认」；iron-rules 文本含三条纪律
- 修复域层消息卫生（resolve/render 报错改用 fmt），并把模板根解析收进 adapters/TemplateRoot.ts 以少动组合根
- typecheck 绿；ask-confirm/prompt-baseline/prompt-cost/prompt-gates/prompt-tiers/message-hygiene 共 92 例绿

### 改动文件

- `packages/web/dsh-pmboard/src/domain/prompt/fragments/common/iron-rules.md`
- `packages/web/dsh-pmboard/src/domain/prompt/generated/fragments.ts`
- `packages/web/dsh-pmboard/tests/fixtures/stage-prompts-baseline-p1.json`
- `packages/web/dsh-pmboard/src/application/use-cases/AskConfirm.ts`
- `packages/web/dsh-pmboard/tests/ask-confirm.test.ts`
- `packages/web/dsh-pmboard/src/domain/template/resolve.ts`
- `packages/web/dsh-pmboard/src/domain/template/render.ts`
- `packages/web/dsh-pmboard/src/adapters/TemplateRoot.ts`
- `packages/web/dsh-pmboard/src/index.ts`

### 下一步

T-8（t-7fb8e4）矩阵对照断言 + 设计落点校准；依赖 T-6。注：src/index.ts 现 426 行（>400 门禁；改动前已被其它窗口改到 415 行，非本卡引入）。

---
