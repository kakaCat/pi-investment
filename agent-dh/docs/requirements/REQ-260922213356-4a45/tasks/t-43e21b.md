# t-43e21b 实现非肯定项分流（H3 verdict / H4 不附纪律）

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
实现非肯定项分流（H3 verdict / H4 不附纪律）

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：implement
- 端侧：backend

## 得到什么结果
npx vitest run tests/template-address-injection.test.ts -t TC-10 与 npx vitest run tests/h3-inject.test.ts 绿；verdict=negative 或 undefined 时 scratch.promptText 未写、H4 投递文本不含「按以下阶段纪律继续」及下一节点特征串；affirmative 分支输出与改造前逐字一致。

## 实施方案（implementation）
改 packages/web/dsh-pmboard/src/application/gate/handlers/h3-inject.ts：新增 resolveStagePromptForGate(ctx,requirement)——verdict 为 negative 或 undefined（H1 未跑到）→ { kind:skip, code:negative_verdict }，不调用 resolve、不写 scratch.promptText、不留痕；affirmative 时取词后调 augmentResolvedPrompt 再写 scratch 与留痕。改 packages/web/dsh-pmboard/src/application/gate/handlers/h4-resume.ts：非肯定分支不附任何纪律块（只发作答摘要 + 用户意见），肯定分支 D5 分流不变。补 packages/web/dsh-pmboard/tests/template-address-injection.test.ts 的 TC-10。

## 上游产出摘要（dependsSummary）
- 接线四个注入点并解析模板根

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-22T15:16:55.842Z，窗口 session-2c955dd9-15b6-4cc5-8a0b-67ff978455aa）

修掉"说不改反而被推着走"：用户选"需修改"时，窗口不再收到下一节点的推进纪律，只看到作答摘要和用户的修改意见；肯定答复的行为不变。

### 完成项

- 改 h3-inject.ts：取词前按 ctx.verdict 分流——非肯定（或 verdict 缺省）→ skip(negative_verdict)，不取词、不写 scratch、不留痕
- 改 h4-resume.ts：非肯定分支不附任何纪律块（只发作答摘要 + 用户意见）；肯定分支维持原样（防修反）
- 扩展 tests/template-address-injection.test.ts 的 TC-10：negative 与 verdict 缺省两例断言 skip + scratch 未写 + 无留痕；H4 negative 文本不含纪律特征串、含用户意见；H4 affirmative 仍附纪律全文
- typecheck 绿；注入测试 7 例绿；h3-inject / gate-handlers 回归绿

### 改动文件

- `packages/web/dsh-pmboard/src/application/gate/handlers/h3-inject.ts`
- `packages/web/dsh-pmboard/src/application/gate/handlers/h4-resume.ts`
- `packages/web/dsh-pmboard/tests/template-address-injection.test.ts`

### 下一步

T-5（t-25f7fd）节点输入包追加「本节点文档」小节并锁压缩两路径一致；依赖本卡。

---
