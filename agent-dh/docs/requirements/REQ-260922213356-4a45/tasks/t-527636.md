# t-527636 补空集兼容回归与回退开关

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
补空集兼容回归与回退开关

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：implement
- 端侧：backend

## 得到什么结果
npx vitest run tests/template-address.test.ts tests/prompt-baseline.test.ts tests/stage-prompts.test.ts 全绿；空集场景注入文本 sha256 与改造前一致；addressSectionEnabled=false 时注入文本与空集逐字节相同。

## 实施方案（implementation）
在 packages/web/dsh-pmboard/tests/template-address.test.ts 补 TC-13（空集场景与既有基线逐字节/哈希一致；类型无模板、未启用节点两分支）与回退开关断言（addressSectionEnabled=false 时 augmentResolvedPrompt 返回原引用）。不改 fragments 源，故 tests/fixtures/stage-prompts-baseline-p1.json 保持不变。

## 上游产出摘要（dependsSummary）
- 补节点输入包地址节并锁两路径一致

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-22T15:26:26.231Z，窗口 session-2c955dd9-15b6-4cc5-8a0b-67ff978455aa）

证明"没有地址可给时什么都不会变"：无模板且无上游的节点，注入文本与改造前逐字节相同；开关关掉时同样回到改造前行为。

### 完成项

- 在 tests/template-address.test.ts 增 T-6 两例：枚举全部 (节点×类型) 组合，凡无表项且无上游者 renderAddressSection 必为空串；空集时 augmentResolvedPrompt 与入参同引用（文本逐字节不变的可测形态）
- 跑 template-address + prompt-baseline + stage-prompts 三个文件，共 76 例全绿（P1 基线快照未被扰动）

### 改动文件

- `packages/web/dsh-pmboard/tests/template-address.test.ts`

### 下一步

T-7（t-868e3f）注入纪律三条 + 同产物防重弹守卫；依赖 T-1。

---
