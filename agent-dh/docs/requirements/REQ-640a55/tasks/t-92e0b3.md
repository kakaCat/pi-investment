# t-92e0b3 修复编号门禁的两处判据（跳号与重复）

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
修复编号门禁的两处判据（跳号与重复）

## 解决什么问题
（待补充）

## 范围
- 阶段：implement
- 端侧：backend

## 得到什么结果
npx vitest run tests/clause-numbering.test.ts 全绿：FR-1+FR-3 返回 [FR-2]、两个 FR-1 定义返回「FR-1（出现2次）」、多前缀各自连续与不可解析 id 各返回 []。反向验证：把跳号正则改回 d+ 后该文件 2 条变红；把重复判定改喂去重后的清单后 1 条变红。

## 实施方案（implementation）
改 packages/pages/dsh-pmboard/src/application/internal/content-gates.ts:278 为 /^([A-Z]+)-(\d+)$/；同文件新增 extractClauseDefinitionOccurrences（与 extractClauseDefinitions 同源解析但不去重）；改 content-gate-wiring.ts:184 的重复判定改喂新函数；新增 tests/clause-numbering.test.ts（8 条单元用例）。

## 上游产出摘要（dependsSummary）
- （无依赖）

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-19T11:16:40.142Z，窗口 session-98f2b4b4-cfff-4a90-baa9-214fc15d99d2）

编号跳号与重复两处死门禁已修复并锁上回归：跳号正则 (d+) → (\d+)（此前匹配恒为 null）；判重改吃新加的不去重来源 extractClauseDefinitionOccurrences（此前吃去重清单、计数恒 ≤1）。新增 tests/clause-numbering.test.ts 13 条用例；反向验证证明改坏即变红。

### 完成项

- FR-3 跳号可检出：content-gates.ts 跳号正则修正为 /^([A-Z]+)-(\d+)$/；FR-1+FR-3 返回 [FR-2]、FR-1+FR-5 逐个报 FR-2/FR-3/FR-4
- FR-4 重复可检出：新增 extractClauseDefinitionOccurrences（与 extractClauseDefinitions 同源解析、不去重）；content-gate-wiring 判重改喂它，两个 FR-1 定义 → FR-1（出现2次）
- 端到端：npx vitest run tests/clause-numbering.test.ts 13 条全绿，含 checkRequirementDocFormatGate 的跳号被拒 / 重复被拒 / 连号放行 / 存量需求不拦
- 反向验证一：跳号正则改回 (d+) → 3 条用例变红（跳号 2 条 + 端到端 1 条）
- 反向验证二：判重改喂去重清单 → 1 条用例变红（端到端重复）
- 全量回归：npx vitest run 98 文件 / 1263 条全绿（基线 97 文件 / 1250 条），typecheck.test.ts 含 tsc --noEmit 零错误

### 改动文件

- `packages/pages/dsh-pmboard/src/application/internal/content-gates.ts`
- `packages/pages/dsh-pmboard/src/application/internal/content-gate-wiring.ts`
- `packages/pages/dsh-pmboard/tests/clause-numbering.test.ts`

### 下一步

t-c8fb87：让拆分骨架与兜底骨架头直出三要素节（接线的前置）。

---
## 汇报 2（2026-09-19T11:17:36.626Z，窗口 session-98f2b4b4-cfff-4a90-baa9-214fc15d99d2）

补记第二段：新增 2 条用例覆盖「标题式定义位」（### FR-1:）——它走的是新函数里 ** + 标题 那条分支，原 13 条只覆盖了加粗式；现 15 条全绿。

### 完成项

- FR-3/FR-4 定义位两种写法均计入：### FR-1: 标题式与 **FR-1 ...** 加粗式都算定义位，混用不漏计
- npx vitest run tests/clause-numbering.test.ts → 15 条全绿（13 + 2）

### 改动文件

- `packages/pages/dsh-pmboard/tests/clause-numbering.test.ts`

### 下一步

t-c8fb87：拆分骨架直出三要素节。

---
