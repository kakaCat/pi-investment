# t-ad7826 端到端回归与反向验证

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
端到端回归与反向验证

## 解决什么问题
（待补充）

## 范围
- 阶段：test
- 端侧：backend

## 得到什么结果
cd packages/pages/dsh-pmboard && npx vitest run 全绿（基线 97 文件 / 1250 测试 + 新增）；三处反向验证各自变红（骨架改回 ## 目标 → 出口拦截命中；重复判定喂去重清单 → 重复用例红；跳号正则改回 d+ → 跳号用例红）；npx tsc --noEmit 无错误。

## 实施方案（implementation）
新增 tests/e2e-triad-gate.test.ts 走「需求文档 → 计划 → 拆分 → 出口」全链路（骨架产出的卡全部通过；人为删掉某卡一节后出口被拦）；逐条跑反向验证三连（改坏 → 确认变红 → 还原）；跑 tsc --noEmit 与全量 vitest 并记录输出摘要。

## 上游产出摘要（dependsSummary）
- 把三要素门禁接上执行链（出口与结单）
- 冻结存量兼容与改卡通道双标题

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-19T11:33:43.508Z，窗口 session-98f2b4b4-cfff-4a90-baa9-214fc15d99d2）

全链路 E2E 证明了本次修复的两侧：新骨架产出的卡能直接过自己新接的出口门禁；卡被改坏一节后确实被拦。最终反向验证三连与全量回归一并跑完。

### 完成项

- 全链路用例 tests/e2e-triad-gate.test.ts（2 条）：需求文档（含 FR-1）→ 提交计划 → 人批准 → 拆分落库（带 requirement_refs）→ 确认拆分产物 → 推进 implementing
- ① 骨架产出的卡三节齐备且正文非空 → 出口门禁放行；② 把卡上一节改回 ## 验收标准 → 出口门禁拦下 code=task_card_incomplete
- 顺带验证 feature 类型必填设计文档集：缺 design/*.md 时 reqboard_plan_submit 确实被拒（REQBOARD_MISSING_REQUIRED_DOC）
- 最终反向验证三连（改坏 → 确认变红 → 还原）：骨架节名改回 ## 目标 → 4 条红；判重改喂去重清单 → 1 条红；跳号正则改回 (d+) → 3 条红
- 最终全量回归：npx vitest run → 100 文件 / 1282 条全绿（基线 97 文件 / 1250 条）；npx tsc --noEmit 零错误
- 分支产出：feat/pmboard-triad 上 6 个提交（2cf2b57e / 4792fccd / 6e4a2224 / 4f4af257 / f7c8696d / d5195b70），相对快照共 18 个文件、+653/-34

### 改动文件

- `packages/pages/dsh-pmboard/tests/e2e-triad-gate.test.ts`

### 下一步

提交验收材料（reqboard_submit kind=verification）等人工审核。

---
