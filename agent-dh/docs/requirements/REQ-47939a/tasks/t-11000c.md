# t-11000c 工具壳拆分与 13→9 收敛

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 目标
工具壳拆分与 13→9 收敛

## 背景摘要（context）
（待补充）

## 范围
- 阶段：implement
- 端侧：backend

## 验收标准
npx vitest run tests/tools-schema.test.ts tests/output-contract.test.ts 全绿且 agent-dh/tests/plugin-schema.smoke.test.ts 通过；src/tools/ 下存在 9 个工具目录；断言 reqboard_submit 按 kind 表驱动分派（分支体内仅一行用例调用，grep 可证）。

## 实施方案（implementation）
src/host/agent-tools.ts → src/tools/<Name>Tool/{NameTool.ts,prompt.ts,index.ts}（基于 @pi-investment/core-tool 的 BaseTool）；requirement/plan/verify/archive 四个 submit 合并为 reqboard_submit(kind)，用分派表 + 4 个独立用例（禁止大 if）；confirm_artifact 并入 reqboard_ask_confirm；src/index.ts 注册 9 个工具；扩展 tests/output-contract.test.ts 覆盖 9 个工具。验证：npx vitest run tests/tools-schema.test.ts tests/output-contract.test.ts 与 cd agent-dh && npx vitest run tests/plugin-schema.smoke.test.ts。

## 上游产出摘要（dependsSummary）
- 用例层落地（12 用例 + 3 查询投影）

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-17T12:23:03.211Z，窗口 session-41e7e4cd-4038-476c-9307-d93622a411e2）

t8 复核通过：工具 13→9 收敛完成，src/tools/ 恰好 9 个三段式目录，reqboard_submit 表驱动分派；契约扫描器改为遍历 tools/ 且经故障注入证明覆盖 9 个新工具。BaseTool 偏离经核实理由成立（会丢错误码 code）。

### 完成项

- 发起窗口独立复核：tools-schema + output-contract + tools-dispatch → 22 passed；根 plugin-schema.smoke → 19 passed
- 形状核验：ls -d src/tools/*/ = 9，每个目录含 XxxTool.ts + prompt.ts + index.ts
- 契约门禁故障注入：删掉 StatusTool 里被返回的 bound 声明 → 红（报 defineStatusTool 未声明 bound）→ 证明 t8 改造后的多文件扫描 + RESPONSE_SOURCES 映射确实覆盖新工具，不是空转；还原后 24 passed
- BaseTool 偏离核实：BaseTool.toDSHToolDefinition().execute 失败时 throw new Error(issue)，裸 Error 丢 .code，与 Object.assign(new Error(msg),{code}) 契约冲突 → 接受"保留 defineTool + 三段式文件形状"的偏离，并把"让 BaseTool 透传 code"记为后续项
- 客户端构建门：pnpm build:client + verify-client-build 哨兵通过（本批触及 src/，按 done 凭证门的构建新鲜度要求重建）
- 全量：611 passed / 1 failed（唯一失败为基线项）

### 改动文件

- `docs/requirements/REQ-47939a/verification.md`
- `packages/pages/dsh-pmboard/src/tools/index.ts`
- `packages/pages/dsh-pmboard/src/tools/StatusTool/StatusTool.ts`
- `packages/pages/dsh-pmboard/tests/output-contract.test.ts`
- `packages/pages/dsh-pmboard/tests/tools-dispatch.test.ts`

### 下一步

t9：删 host/ 并存实现 + 尺寸门禁 + 全量回归。

---
