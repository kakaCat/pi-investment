# t-5e153b 客户端 view.ts 按视图区拆分

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 目标
客户端 view.ts 按视图区拆分

## 背景摘要（context）
（待补充）

## 范围
- 阶段：implement
- 端侧：frontend

## 验收标准
npx vitest run tests/client-view.test.ts tests/stage-panel.test.ts tests/card-face.test.ts 全绿；src/client/views/board.ts 等 6 个分片文件存在；src/client/view.ts 已不存在或降为汇总再导出。

## 实施方案（implementation）
src/client/view.ts → src/client/views/{board,stage-detail,stage-panel,verification,artifacts,timeline}.ts + src/client/render/dom-utils.ts；不改 DOM 结构、类名与订阅方式；import 路径随分层调整。验证：npx vitest run tests/client-view.test.ts tests/stage-panel.test.ts tests/card-face.test.ts。

## 上游产出摘要（dependsSummary）
- 旧文件删除 + 尺寸/边界门禁 + 全量回归

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
