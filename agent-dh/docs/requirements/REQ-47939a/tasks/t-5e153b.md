# t-5e153b 客户端 view.ts 按视图区拆分

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 目标
客户端 view.ts 按视图区拆分

## 背景摘要（context）
（待补充）

## 范围
- 阶段：implement
- 端侧：frontend

## 验收标准
npx vitest run tests/client-view.test.ts tests/stage-panel.test.ts tests/card-face.test.ts 全绿；src/client/views/board.ts 等 6 个分片文件存在；src/client/view.ts 已不存在或降为汇总再导出。

## 实施方案（implementation）
src/client/view.ts → src/client/views/{board,stage-detail,stage-panel,verification,artifacts,timeline}.ts + src/client/render/dom-utils.ts；不改 DOM 结构、类名与订阅方式；import 路径随分层调整。验证：npx vitest run tests/client-view.test.ts tests/stage-panel.test.ts tests/card-face.test.ts。

## 上游产出摘要（dependsSummary）
- 旧文件删除 + 尺寸/边界门禁 + 全量回归

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-17T12:50:29.773Z，窗口 session-41e7e4cd-4038-476c-9307-d93622a411e2）

t11 复核通过：view.ts 2579 行按视图区拆为 barrel(16 行) + 8 个 views 分片 + render/dom-utils，导出集合守恒（缺失 0/0），客户端三件 113 passed，故障注入可红。

### 完成项

- 独立核验导出守恒：脚本比对 HEAD view.ts 的 14 value + 4 type 导出 vs 新 barrel+views+render 并集 → 缺失 0 / 0
- 尺寸：无任何分片 >400（最大 364）；view.ts 降为 16 行 barrel
- 客户端三件测试：tests/client-view + stage-panel + card-face = 113 passed（54/44/15）
- 子代理故障注入（删 views/board.ts 的 buildTriage 导出 → client-view 7 failed；还原后 54/54 绿）——门禁可红的证据在其汇报中，我复核了导出守恒作为等价性主证据
- size-budget 白名单：client/view.ts 条目已按门禁要求删除（该门禁要求条目必须仍超限，拆完不删即红）
- 偏离（已接受）：多出 2 个分片（views/stage-nodes-quality.ts、views/stage-nodes-doc.ts）——节点专属渲染器约 950 行，在 ≤400 行门禁下 7 个文件装不下；卡面点名的 6 个文件名全部保留
- 它声称"3 个历史死 import 未搬"经我独立核对属实（renderStagePanel/getStageLabel 各仅 1 次=import 行；STAGE_LABELS 第 2 处是注释）

### 改动文件

- `docs/requirements/REQ-47939a/verification.md`
- `packages/pages/dsh-pmboard/src/client/view.ts`
- `packages/pages/dsh-pmboard/src/client/views/board.ts`
- `packages/pages/dsh-pmboard/src/client/views/stage-detail.ts`
- `packages/pages/dsh-pmboard/src/client/render/dom-utils.ts`
- `packages/pages/dsh-pmboard/tests/size-budget.test.ts`

### 下一步

t12 复核（同批交付）；随后 t13 收尾。

---
