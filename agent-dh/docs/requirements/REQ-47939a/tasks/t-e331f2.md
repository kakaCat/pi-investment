# t-e331f2 领域搬迁A：状态机 + 可证伪验收 + 产物规则

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 目标
领域搬迁A：状态机 + 可证伪验收 + 产物规则

## 背景摘要（context）
（待补充）

## 范围
- 阶段：implement
- 端侧：backend

## 验收标准
npx vitest run tests/domain 与 tests/reqboard.test.ts 全绿；src/domain/requirement/RequirementStatus.ts 存在且 src/shared/protocol.ts 为再导出；断言遍历 REQ_TRANSITIONS 表逐项与 canReqTransition 结果一致（表驱动，新增状态不漏测）。

## 实施方案（implementation）
src/shared/protocol.ts 的 REQ_TRANSITIONS/HUMAN_ONLY_REQ_TRANSITIONS/SYSTEM_REQ_TRANSITIONS/TASK_TRANSITIONS/HUMAN_ONLY_TASK_TRANSITIONS/agentNextActions → src/domain/requirement/RequirementStatus.ts 与 src/domain/task/TaskStatus.ts；src/host/agent-tools.ts 的 VACUOUS_ACCEPTANCE/VERIFIABLE_ANCHOR/前向引用检查 → src/domain/task/Acceptability.ts；STAGE_ARTIFACT_REQUIREMENTS/ARTIFACT_CONFIRM_GATES/ARCHIVE_DOC_RULES/kindForRelPath → src/domain/artifact/ArtifactSpec.ts；protocol.ts 从 domain 再导出以保持 20 个引用方 import 路径不变；注释随代码搬。新增 tests/domain/{requirement-status,task-status,acceptability,artifact}.test.ts。验证：npx vitest run tests/domain tests/reqboard.test.ts。

## 上游产出摘要（dependsSummary）
- 立层边界门禁与端口骨架

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
