# t-e331f2 领域搬迁A：状态机 + 可证伪验收 + 产物规则

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 目标
领域搬迁A：状态机 + 可证伪验收 + 产物规则

## 背景摘要（context）
（待补充）

## 范围
- 阶段：implement
- 端侧：backend

## 验收标准
npx vitest run tests/domain 与 tests/reqboard.test.ts 全绿；src/domain/requirement/RequirementStatus.ts 存在且 src/shared/protocol.ts 为再导出；断言遍历 REQ_TRANSITIONS 表逐项与 canReqTransition 结果一致（表驱动，新增状态不漏测）。

## 实施方案（implementation）
src/shared/protocol.ts 的 REQ_TRANSITIONS/HUMAN_ONLY_REQ_TRANSITIONS/SYSTEM_REQ_TRANSITIONS/TASK_TRANSITIONS/HUMAN_ONLY_TASK_TRANSITIONS/agentNextActions → src/domain/requirement/RequirementStatus.ts 与 src/domain/task/TaskStatus.ts；src/host/agent-tools.ts 的 VACUOUS_ACCEPTANCE/VERIFIABLE_ANCHOR/前向引用检查 → src/domain/task/Acceptability.ts；STAGE_ARTIFACT_REQUIREMENTS/ARTIFACT_CONFIRM_GATES/ARCHIVE_DOC_RULES/kindForRelPath → src/domain/artifact/ArtifactSpec.ts；protocol.ts 从 domain 再导出以保持 20 个引用方 import 路径不变；注释随代码搬。新增 tests/domain/{requirement-status,task-status,acceptability,artifact}.test.ts。验证：npx vitest run tests/domain tests/reqboard.test.ts。

## 上游产出摘要（dependsSummary）
- 立层边界门禁与端口骨架

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-17T11:57:55.316Z，窗口 session-41e7e4cd-4038-476c-9307-d93622a411e2）

t2 复核通过：状态机（REQ_TRANSITIONS/HUMAN_ONLY_*/SYSTEM_*）+ 可证伪验收（VACUOUS_ACCEPTANCE/VERIFIABLE_ANCHOR/前向引用）+ 产物规则（STAGE_ARTIFACT_REQUIREMENTS/ARTIFACT_CONFIRM_GATES/ARCHIVE_DOC_RULES/kindForRelPath）已搬入 domain 并改为唯一实现；protocol.ts 改为再导出，20 个引用方 import 路径不变。

### 完成项

- 新增 domain 模块 6 个：actor.ts（纯类型基座）、requirement/RequirementStatus.ts、requirement/Requirement.ts、task/TaskStatus.ts、task/Acceptability.ts、artifact/ArtifactSpec.ts
- 新增 tests/domain/{requirement-status,task-status,acceptability,artifact}.test.ts：表驱动遍历 REQ_TRANSITIONS/TASK_TRANSITIONS 逐项断言 canReqTransition 结果（新增状态不会漏测）
- 发窗口独立复核（不采信 subagent 自述）：grep 证明 REQ_TRANSITIONS / TASK_TRANSITIONS / HUMAN_ONLY_REQ_TRANSITIONS / ARCHIVE_DOC_RULES / STAGE_ARTIFACT_REQUIREMENTS 各只有 1 处定义且都在 domain —— INV-2「同一规则只有一处实现」对这批常量已成立
- 复核命令 npx vitest run tests/domain tests/reqboard.test.ts → 全绿；layer-boundary.test.ts 9/9 仍绿（domain 未越界）
- 偏离（已接受）：卡面把待搬符号定位在 host/agent-tools.ts，实际在 shared/protocol.ts 的 normalizePlanTasks 内 → 按内容搬迁；domain 不许 import shared，故把 domain 所需类型一并迁入 domain（新增 actor.ts）；REQ_TRANSITIONS 等保留现名（design 草案里的 AGENT_TRANSITIONS 改名会破坏零行为变更）

### 改动文件

- `packages/pages/dsh-pmboard/src/domain/actor.ts`
- `packages/pages/dsh-pmboard/src/domain/requirement/RequirementStatus.ts`
- `packages/pages/dsh-pmboard/src/domain/requirement/Requirement.ts`
- `packages/pages/dsh-pmboard/src/domain/task/TaskStatus.ts`
- `packages/pages/dsh-pmboard/src/domain/task/Acceptability.ts`
- `packages/pages/dsh-pmboard/src/domain/artifact/ArtifactSpec.ts`
- `packages/pages/dsh-pmboard/src/shared/protocol.ts`
- `packages/pages/dsh-pmboard/tests/domain/requirement-status.test.ts`
- `packages/pages/dsh-pmboard/tests/domain/task-status.test.ts`
- `packages/pages/dsh-pmboard/tests/domain/acceptability.test.ts`
- `packages/pages/dsh-pmboard/tests/domain/artifact.test.ts`

### 下一步

t3/t4 复核（同批交付），随后 t6 用例层；t9 删 host/ 后 protocol.ts 的再导出可进一步收敛。

---
