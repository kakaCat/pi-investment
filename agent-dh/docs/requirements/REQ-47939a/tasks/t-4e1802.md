# t-4e1802 账本 v4→v5 无损迁移（脚本 + 报告 + 测试）

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 目标
账本 v4→v5 无损迁移（脚本 + 报告 + 测试）

## 背景摘要（context）
（待补充）

## 范围
- 阶段：implement
- 端侧：backend

## 验收标准
node scripts/migrate-ledger.mjs --file <ledger> --dry-run 退出码 0 且报告白名单外 diff 数为 0；npx vitest run tests/migration.test.ts 全绿；docs/requirements/REQ-47939a/migration-report.md 存在且含 34 需求 / 84 任务复核行与逐条白名单差异计数。

## 实施方案（implementation）
新增 scripts/migrate-ledger.mjs（--dry-run/--apply/--verify，幂等；5 步：备份→dry-run→白名单校验→原子替换→复核）；实现 C1-C11（含删 projectId/parentId、statusHistory 必填、source 判别联合、dependsOn 去悬空、artifacts 去重）；statusHistory 补齐复用既有 backfill 算法；新增 tests/migration.test.ts 与 tests/fixtures/ledger-v4-sample.json；删除 LEGACY_REQ_STATUS_ALIASES 及其 5 处引用与 backfill 读时兜底；实际执行迁移并产出 docs/requirements/REQ-47939a/migration-report.md。验证：node scripts/migrate-ledger.mjs --dry-run 与 npx vitest run tests/migration.test.ts。

## 上游产出摘要（dependsSummary）
- 旧文件删除 + 尺寸/边界门禁 + 全量回归

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-17T12:40:20.889Z，窗口 session-41e7e4cd-4038-476c-9307-d93622a411e2）

t10 复核通过：运行时读路径已零 legacy 兼容（别名表/ backfill 兜底移出契约层），迁移脚本行为未改，真实台账未被触碰（schemaVersion 仍 4），副本端到端 dry-run/apply/verify/幂等/损坏保护/回滚全部实测通过，产出 migration-report.md。

### 完成项

- 发起窗口独立复核：真实台账 .dsh-data/dsh-reqboard.json schemaVersion=4、34/97 未变 → 未迁移 ✓（md5 与我先前测得不同是我自己 move 任务把 revision 926→927 所致）
- REQBOARD_SCHEMA_VERSION 4→5 的必要性经独立确认：JsonLedgerRepository.load():103 用该常量重建解析结果的 schemaVersion——不升则 v5 文件被报成 4、下次写盘回退，迁移成果被静默抹掉
- 脚本行为未改：scripts/migrate-ledger.ts 仅换 import（迁移专属算法移至 domain/legacy/LegacyStatus.ts）；tests/migration.test.ts 9 passed
- 副本端到端实测（82 万字节真实台账副本）：dry-run 白名单外 0 条（22 条白名单内，C7=20）；apply 备份+原子替换；verify 通过；二次 apply 幂等无操作；截断 JSON → exit 2 且原文件字节不变；备份还原后回到 v4 且 --verify 正确报 2 项未达 v5（证明校验非空转）
- 🟡 发起窗口处置（同 t9）：删除迁移变换的第二份实现 application/use-cases/MigrateLedger.ts；修正 t6 用例计数 12→11
- 任务数口径修正已确认：卡面写 34/84，事实 34/97/2（84 是需求期口径；design/migration.md §1.1 已有记录），报告按事实记 97

### 改动文件

- `docs/requirements/REQ-47939a/verification.md`
- `docs/requirements/REQ-47939a/migration-report.md`
- `packages/pages/dsh-pmboard/src/domain/legacy/LegacyStatus.ts`
- `packages/pages/dsh-pmboard/src/adapters/JsonLedgerRepository.ts`
- `packages/pages/dsh-pmboard/src/shared/protocol.ts`

### 下一步

t11/t12 客户端机械拆分（P1），随后 t13 文档收尾与本窗口执行真实台账迁移 + 重启。

---
