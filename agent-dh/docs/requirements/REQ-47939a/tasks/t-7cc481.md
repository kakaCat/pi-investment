# t-7cc481 领域搬迁C：验收单规约（生成/裁决/返工）

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 目标
领域搬迁C：验收单规约（生成/裁决/返工）

## 背景摘要（context）
（待补充）

## 范围
- 阶段：implement
- 端侧：backend

## 验收标准
npx vitest run tests/domain tests/verification-sheet.test.ts tests/verdicts-and-rework.test.ts 全绿；src/domain/workflow/AcceptanceSheetSpec.ts 存在；断言 failed 无 opinion 抛 invalid_input、续版只含 pending+failed 两项。

## 实施方案（implementation）
src/host/verdicts.ts 与 agent-tools.ts 的 accept_sheet 逻辑 → src/domain/workflow/AcceptanceSheetSpec.ts（buildSheet/applyVerdicts/isAllPassed/返工任务生成）；VerificationItem.source 由字符串改为 {kind:"requirement"} | {kind:"task",taskId}。新增 tests/domain/acceptance-sheet.test.ts。验证：npx vitest run tests/domain tests/verification-sheet.test.ts tests/verdicts-and-rework.test.ts。

## 上游产出摘要（dependsSummary）
- 立层边界门禁与端口骨架

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-17T11:58:58.924Z，窗口 session-41e7e4cd-4038-476c-9307-d93622a411e2）

t4 复核通过：验收单规约（buildSheet/applyVerdicts/isAllPassed + 返工规格）进入 domain，host 只做台账校验与落库副作用；source 改判别联合后既有断言改写属纯机械未削弱。上浮一处真实语义分叉待裁决（续版丢弃 pending 项）。

### 完成项

- 发起窗口独立复核全部通过（见 docs/requirements/REQ-47939a/verification.md 的 t4 段）
- 卡面验收命令 → 68 passed；buildSheet 单点定义；applyVerdicts 第二处经查为适配层包装（规则仍单点）
- 既有测试断言改写审查：纯机械、未削弱、条数未减少（git diff 逐行核对）
- 上浮待裁决项：续版只含 failed、丢弃 pending → 存在"带着从未裁决项归档"的极端路径（已写入 verification.md，等用户裁决）
- 补报本窗口产出的验收证据文档

### 改动文件

- `docs/requirements/REQ-47939a/verification.md`
- `packages/pages/dsh-pmboard/src/domain/workflow/AcceptanceSheetSpec.ts`
- `packages/pages/dsh-pmboard/src/shared/protocol.ts`
- `packages/pages/dsh-pmboard/src/host/verdicts.ts`
- `packages/pages/dsh-pmboard/src/client/stage-panel.ts`

### 下一步

t5 复核记录已入 verification.md 并关闭；随后进入 t6（用例层），续版语义待用户裁决后再定是否另开修复。

---
