# t-69ea73 返工：立层边界门禁与端口骨架

> 需求：REQ-47939a reqboard 分层重构：DDD 用例层拆分 + 领域规则收敛
> 验收标准：npx vitest run tests/layer-boundary.test.ts 通过；src/domain/errors.ts 与 src/application/ports.ts 存在且被该测试引用；测试内含"扫描器命中文件数"下限断言（防扫描器失效假绿）。

---
## 汇报 1（2026-09-17T13:05:05.181Z，窗口 session-41e7e4cd-4038-476c-9307-d93622a411e2）

返工完成：按验收意见引入三件抽象（fmt 解决拼接 / LIMITS 解决魔数 / labels 解决 host-client 文案漂移），规则层拼接从 29 降到 0，工具超时与批次改具名常量，新增 message-hygiene 门禁（含棘轮）并三次故障注入实测可红。

### 完成项

- 新增 domain/text/fmt.ts（具名占位符 + 缺变量抛错，替代字符串拼接）
- 新增 domain/limits.ts（标题/文本/证据/批次/节流/提醒/证据窗/三类超时具名化）
- 新增 domain/text/labels.ts（弹框选项、徽章、终确认、缺省确认选项）并经 shared/protocol 再导出给 client —— 解决用户点到的"host 与 client 各写一份文案"
- 规则层迁移：src/domain 拼接式消息 29 → 0（7 个文件逐条改 fmt，消息逐字不变，由断言消息内容的既有测试守住）
- 魔数迁移：9 个工具 timeoutMs → LIMITS.*；AcceptSheet 批次 → LIMITS.sheetBatchDefault/Max；protocol 标题/文本上限 → LIMITS.titleMax/textMax
- 文案接线：AcceptSheet 用 FINAL_PASS_LABEL/FINAL_DECLINE_LABEL/ACCEPT_ITEM_OPTIONS；AskConfirm 用 DEFAULT_CONFIRM_OPTIONS；client stage-panel 徽章用 ITEM_STATUS_BADGE
- 新增 tests/message-hygiene.test.ts（5 例）：domain 拼接硬零 + 其余层棘轮 + 工具超时禁裸数字 + 批次禁裸数字 + 六条文案单点
- 故障注入实测三条全红：domain 拼接探针 / client 文案探针 / tools 裸超时探针；探针已清理，全量 621 passed / 1 failed（基线项）
- 诚实边界：上层仍有 322 处拼接未逐条迁移（一次性改 300+ 处消息回归面过大），已用棘轮门禁钉住"只降不升"，并在 verification.md 显式写明未完成范围

### 改动文件

- `docs/requirements/REQ-47939a/verification.md`
- `packages/pages/dsh-pmboard/src/domain/text/fmt.ts`
- `packages/pages/dsh-pmboard/src/domain/text/labels.ts`
- `packages/pages/dsh-pmboard/src/domain/limits.ts`
- `packages/pages/dsh-pmboard/src/domain/task/Acceptability.ts`
- `packages/pages/dsh-pmboard/src/domain/workflow/AcceptanceSheetSpec.ts`
- `packages/pages/dsh-pmboard/src/domain/workflow/DoneEvidenceSpec.ts`
- `packages/pages/dsh-pmboard/src/application/use-cases/AcceptSheet.ts`
- `packages/pages/dsh-pmboard/src/client/stage-panel.ts`
- `packages/pages/dsh-pmboard/tests/message-hygiene.test.ts`

### 下一步

关闭返工卡后需求应重新 rollup 进验收；届时重交验收材料（v2 只含未过项）并继续逐项验收。

---
