# t-172e64 用例层落地（12 用例 + 3 查询投影）

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 目标
用例层落地（12 用例 + 3 查询投影）

## 背景摘要（context）
（待补充）

## 范围
- 阶段：implement
- 端侧：backend

## 验收标准
npx vitest run tests/application 全绿；src/application/use-cases/ 下 12 个用例文件均存在；断言用例测试不落盘（测试文件内无 mkdtempSync 调用，全部用内存端口实现）。

## 实施方案（implementation）
新增 src/application/use-cases/{CreateRequirement,MoveRequirement,SubmitArtifact,ConfirmArtifact,AskConfirm,Decompose,MoveTask,ReportTask,AcceptSheet,SubmitVerification,SubmitArchive,MigrateLedger}.ts 与 src/application/query/{QueryState,QueryStageDetail,QueryStageOverview}.ts；构造注入端口，不含 HTTP/工具 schema 细节；新增 tests/application/*.test.ts 用内存仓储与假 DocRepository。验证：npx vitest run tests/application。

## 上游产出摘要（dependsSummary）
- 领域搬迁A：状态机 + 可证伪验收 + 产物规则
- 领域搬迁B：rollup + done 凭证门 + 拆分幂等 + 文档同步 + 里程碑
- 领域搬迁C：验收单规约（生成/裁决/返工）
- 适配器落地（仓储/文档/时钟/ID/会话/弹框）

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-17T12:23:03.060Z，窗口 session-41e7e4cd-4038-476c-9307-d93622a411e2）

t6 复核通过：12 个用例 + 3 个查询投影 + 5 个 application/internal 落地；用例层基本走注入端口（28 处 deps.ids），残留全局 ID 2 处 + Date.now() 1 处（列入 t9 收口）。

### 完成项

- 发起窗口独立复核：tests/application → 36 passed；全量 611 passed / 1 failed（基线项）
- 用例测试零落盘：tests/application/harness.ts + use-cases.test.ts 内 grep mkdtempSync = 0
- 端口注入实测计数：deps.ids 28 处；残留 newCommentId()/newTaskId() 2 处、Date.now() 1 处（已在 architecture.md 按实测改正措辞并列入 t9）
- 层边界硬门在迁移中保持全绿（28 passed），证明 12 个新用例 + 5 个 internal 模块未破坏分层

### 改动文件

- `docs/requirements/REQ-47939a/verification.md`
- `packages/pages/dsh-pmboard/src/application/use-cases/MoveRequirement.ts`
- `packages/pages/dsh-pmboard/src/application/ports.ts`
- `packages/pages/dsh-pmboard/tests/application/use-cases.test.ts`

### 下一步

t7/t8 复核（同批交付），随后 t9 收口。

---
