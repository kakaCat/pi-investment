# t-a68851 领域搬迁B：rollup + done 凭证门 + 拆分幂等 + 文档同步 + 里程碑

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 目标
领域搬迁B：rollup + done 凭证门 + 拆分幂等 + 文档同步 + 里程碑

## 背景摘要（context）
（待补充）

## 范围
- 阶段：implement
- 端侧：backend

## 验收标准
npx vitest run tests/domain tests/rollup.test.ts tests/task-output-and-evidence.test.ts 全绿；src/domain/workflow/DoneEvidenceSpec.ts 存在；Object.freeze 入参后调用 rollup 不抛（证明纯函数无副作用）。

## 实施方案（implementation）
src/host/rollup.ts → src/domain/workflow/RollupSpec.ts（planRollup/planPickupReconcile 纯函数，入参 Object.freeze 不改）；agent-tools.ts 的 assertDoneEvidence → src/domain/workflow/DoneEvidenceSpec.ts；decompose 幂等守卫 → src/domain/workflow/DecomposeSpec.ts；docSyncPending 规则 → src/domain/workflow/DocSyncSpec.ts；capture-hook 的 30min 提醒判定 → src/domain/workflow/MilestoneSpec.ts。新增 tests/domain/{rollup,done-evidence,decompose-idempotency,doc-sync}.test.ts。验证：npx vitest run tests/domain tests/rollup.test.ts tests/task-output-and-evidence.test.ts。

## 上游产出摘要（dependsSummary）
- 立层边界门禁与端口骨架

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-17T11:58:03.518Z，窗口 session-41e7e4cd-4038-476c-9307-d93622a411e2）

t3 复核通过：rollup 三规则改为纯决策函数（domain 出决策、host 落副作用）+ done 凭证门 + 拆分幂等 + 文档同步 + 里程碑提醒四处规则进入 domain，拒绝码与消息逐字保留。

### 完成项

- 新增 domain/workflow/{RollupSpec,DecomposeSpec,DoneEvidenceSpec,DocSyncSpec,MilestoneSpec}.ts（5 个规约模块）
- 新增 tests/domain/{rollup,done-evidence,decompose-idempotency,doc-sync}.test.ts，含 Object.freeze 入参不抛 + 入参内容不变的纯度断言
- 发窗口独立复核：t3 验收命令全绿；domain 目录内 node:fs / Date.now() / Math.random() 出现次数 = 0（层门禁 + 纯度双重成立）
- 偏离（已接受）：done 凭证门的 fs statSync 与工具痕迹统计仍留 host（domain 只接收宿主算好的布尔/数值）——符合"domain 不碰 I/O"的分层，拒绝码与消息逐字保留；rollup 采用"domain 决策 + host 落副作用"，planRollup 命名对齐卡面

### 改动文件

- `packages/pages/dsh-pmboard/src/domain/workflow/RollupSpec.ts`
- `packages/pages/dsh-pmboard/src/domain/workflow/DecomposeSpec.ts`
- `packages/pages/dsh-pmboard/src/domain/workflow/DoneEvidenceSpec.ts`
- `packages/pages/dsh-pmboard/src/domain/workflow/DocSyncSpec.ts`
- `packages/pages/dsh-pmboard/src/domain/workflow/MilestoneSpec.ts`
- `packages/pages/dsh-pmboard/src/host/rollup.ts`
- `packages/pages/dsh-pmboard/src/host/capture-hook.ts`
- `packages/pages/dsh-pmboard/tests/domain/rollup.test.ts`
- `packages/pages/dsh-pmboard/tests/domain/done-evidence.test.ts`
- `packages/pages/dsh-pmboard/tests/domain/decompose-idempotency.test.ts`
- `packages/pages/dsh-pmboard/tests/domain/doc-sync.test.ts`

### 下一步

t4 复核（含续版语义待裁决项），然后 t6。

---
## 汇报 2（2026-09-17T11:58:32.399Z，窗口 session-41e7e4cd-4038-476c-9307-d93622a411e2）

t3 复核通过（发起窗口独立验收）：验收命令 81 passed；domain 零副作用独立核验；四处规约各只有 1 处定义。补报验收证据文档 verification.md。

### 完成项

- 发起窗口独立复核全部通过（见 docs/requirements/REQ-47939a/verification.md 的 t3 段）
- 卡面验收命令 npx vitest run tests/domain tests/rollup.test.ts tests/task-output-and-evidence.test.ts → 81 passed
- domain 纯度独立核验：node:fs / Date.now() / Math.random() 在 src/domain/ 下出现 0 次
- 纯度断言：tests/domain/rollup.test.ts 含 3 处 Object.freeze 入参冻结断言
- 单点实现：checkDoneEvidence / checkDecomposeIdempotency / applyDocSync / shouldRemindConfirm 各 1 处定义且都在 domain
- 补报本窗口产出的验收证据文档（发起窗口自己撰写的独立复核记录，非执行者自述）

### 改动文件

- `docs/requirements/REQ-47939a/verification.md`
- `packages/pages/dsh-pmboard/src/domain/workflow/RollupSpec.ts`
- `packages/pages/dsh-pmboard/src/domain/workflow/DoneEvidenceSpec.ts`
- `packages/pages/dsh-pmboard/src/domain/workflow/DecomposeSpec.ts`
- `packages/pages/dsh-pmboard/src/domain/workflow/DocSyncSpec.ts`
- `packages/pages/dsh-pmboard/src/domain/workflow/MilestoneSpec.ts`

### 下一步

t4/t5 复核记录追加到 verification.md 后逐卡关闭；注意凭证门要求"汇报的文件 mtime ≥ 开工时间"，故每卡复核必须由发起窗口留下真实产物（复核记录即产物）。

---
