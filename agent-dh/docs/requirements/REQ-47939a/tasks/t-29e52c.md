# t-29e52c 客户端 styles.ts 分层 + 构建门

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 目标
客户端 styles.ts 分层 + 构建门

## 背景摘要（context）
（待补充）

## 范围
- 阶段：implement
- 端侧：frontend

## 验收标准
pnpm build:client 退出码 0 且 node scripts/verify-client-build.mjs 通过（WRAP_SENTINEL 未命中）；src/client/styles/base.ts 等 5 个分片存在；src/client/styles.ts 单文件 ≤400 行。

## 实施方案（implementation）
src/client/styles.ts → src/client/styles/{base,board,detail,panel,files}.ts，由 styles.ts 汇总导出（注入接口与 CSS 内容不变）；跑客户端构建与构建校验。验证：pnpm build:client && node scripts/verify-client-build.mjs。

## 上游产出摘要（dependsSummary）
- 客户端 view.ts 按视图区拆分

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-17T12:50:29.849Z，窗口 session-41e7e4cd-4038-476c-9307-d93622a411e2）

t12 复核通过：styles.ts 1886 行按连续区段分为 5 个分片 + 26 行 barrel，独立提取比对确认 CSS 逐字节一致（89,677 字符），构建门与哨兵通过。

### 完成项

- **独立验证 CSS 逐字节不变**：从 /tmp/req47939a-t12-backup/styles.ts 提取原 CSS 模板（89,677 字符）vs 5 分片按 barrel 顺序（base→detail→files→board→panel）拼接 → 完全一致
- 构建门：pnpm build:client exit 0 + verify-client-build 哨兵 OK；bundle 216528 bytes 与 t11 一致（佐证 CSS 未变）
- 尺寸：5 分片全部 ≤400（base 398 最大）；styles.ts 降为 26 行 barrel，CSS_TAG 与 injectStyles 逐字节保留
- size-budget 白名单：client/styles.ts 条目已删除
- 子代理故障注入（哨兵行前加空格 → 构建失败 EXIT=1；还原后 OK）——构建门可红的证据在其汇报中
- 取向复核：分片取"连续区段"而非按主题重排——这是正确的（重排会改变 CSS 级联行为）
- 全量：616 passed / 1 failed（基线项）

### 改动文件

- `docs/requirements/REQ-47939a/verification.md`
- `packages/pages/dsh-pmboard/src/client/styles.ts`
- `packages/pages/dsh-pmboard/src/client/styles/base.ts`
- `packages/pages/dsh-pmboard/src/client/styles/detail.ts`
- `packages/pages/dsh-pmboard/src/client/styles/panel.ts`

### 下一步

t13 文档演进与归档材料（文档部分已由发起窗口完成，见 verification.md t13 段）。

---
