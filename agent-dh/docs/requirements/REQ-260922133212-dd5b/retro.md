# 复盘 · REQ-260922133212-dd5b（bug）

## 根因教训
id 格式升级只改了生成端（RandomIdFactory/CHANGELOG），忘了校验端（REQUIREMENT_DIR_PATTERN）。
**格式/协议变更必须双向检查：谁生产、谁消费、谁校验，三处一起列进变更清单。**

## 过程教训
1. 正则首版凭印象写（\d{12,14}-[0-9a-f]{6}），测试红即暴露，按生成器源码校准——校验规则先查生产端真身。
2. 会话工具注册表在配置热更时丢失约 1 小时（bundle 化切换副作用），代码工作未停但流水线记账断档；恢复后补账未伪造顺序。
3. 门禁分层设计经受了考验：计划期锚点 + 验收期"怎么验" + 9 类文档清单各自拦下了该拦的。

## 后续
- 存量 91 条主干红灯（chdir 环境 + diag-log 层边界）建议立项治理。
- triage 遗留路径删除是下一条独立需求。
