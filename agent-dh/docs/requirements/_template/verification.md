# 验收材料（REQ-xxxxxx）

**交付结论**：做了什么、验了什么（一句话）

**证据**（可复核，禁止"功能正常"这类空话）：
- 命令 + 输出摘要（如 `pnpm vitest run → 178 passed / 14 files`）
- 报告/截图路径
- 关键数据（对比前后：延迟、错误率、覆盖率）

**未覆盖/已知限制**：诚实列出

**人工审核**：通过 / 退回返工（意见）
