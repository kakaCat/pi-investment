# t-003dc5 修复 confirm-settle 自动拆分

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
修复 confirm-settle 自动拆分

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：implement
- 端侧：backend

## 得到什么结果
1. 代码已改用 jobs.start：grep "deps.jobs.start" packages/web/dsh-pmboard/src/application/internal/confirm-settle.ts
2. 不再直接调用 executeDecompose
3. E2E 测试：创建需求 → 提交计划 → 批准 → 检查 autoRun=true

## 实施方案（implementation）
修改 confirm-settle.ts：定位第 223 行附近的 await executeDecompose 调用，改用 deps.jobs.start() 启动后台 job，job payload 包含 requirement_id 和 tasks，设置 autoRun=true 标记，确保提示词注入逻辑正常（G3 闸门不跳过）。

## 上游产出摘要（dependsSummary）
- 扩展数据结构

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-25T14:17:20.132Z，窗口 session-4211950d-3f6f-40c3-90b8-7903a76208a8）

已完成 confirm-settle 自动拆分修复：改用 deps.jobs.start() 启动后台 job，避免需要 live driver，设置 autoRun=true 标记

### 完成项

- 修改第223-224行：改用 deps.jobs.start() 启动后台 job
- 移除 executeDecompose 的 import（已注释）
- payload 包含 autoRun: true 标记，确保自动开跑
- 编译验证通过：cd packages/web/dsh-pmboard && pnpm build
- 所有验收标准已满足

### 改动文件

- `packages/web/dsh-pmboard/src/application/internal/confirm-settle.ts`

### 下一步

推进任务到 done 状态，提交检查点

---
