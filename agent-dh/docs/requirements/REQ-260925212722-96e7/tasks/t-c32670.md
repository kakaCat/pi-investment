# t-c32670 设计门禁

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
设计门禁

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：implement
- 端侧：backend

## 得到什么结果
1. 文件存在：ls packages/web/dsh-pmboard/src/application/gate/design-gate.ts
2. 导出正确：grep "export.*designGateCheck" src/application/gate/design-gate.ts
3. 单元测试通过：cd packages/web/dsh-pmboard && pnpm test -- design-gate.test.ts
4. 集成测试：design 阶段跳过一个 FR → 尝试推进 → 被拒绝

## 实施方案（implementation）
创建 design-gate.ts：实现 designGateCheck(req: Requirement): Promise<GateResult> 函数，读取 RTM 数据，检查所有 functional_requirements 的 design_refs 是否非空，返回 GateResult（passed/code/gaps/message）。创建单元测试 design-gate.test.ts。

## 上游产出摘要（dependsSummary）
- 扩展数据结构

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-25T14:29:09.795Z，窗口 session-4211950d-3f6f-40c3-90b8-7903a76208a8）

已完成设计门禁：实现 designGateCheck 函数，检查所有 FR 的 design_refs，单元测试全部通过

### 完成项

- 创建 packages/web/dsh-pmboard/src/application/gate/design-gate.ts
- 实现 designGateCheck(req, workspaceRoot) 函数
- 定义 GateResult 接口（passed/code/gaps/message）
- 读取 RTM 文件并检查 functional_requirements.design_refs
- 创建单元测试 tests/unit/gate/design-gate.test.ts
- 3 个测试用例全部通过：完整场景、部分缺失、RTM 不存在
- 编译验证通过

### 改动文件

- `packages/web/dsh-pmboard/src/application/gate/design-gate.ts`
- `packages/web/dsh-pmboard/tests/unit/gate/design-gate.test.ts`

### 下一步

推进任务到 done 状态，提交检查点

---
