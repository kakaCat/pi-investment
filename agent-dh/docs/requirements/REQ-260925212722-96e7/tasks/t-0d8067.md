# t-0d8067 注册 DiveManager 到插件

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
注册 DiveManager 到插件

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：implement
- 端侧：backend

## 得到什么结果
1. 导入语句存在：grep "import.*ReqboardDiveManager" packages/web/dsh-pmboard/src/index.ts
2. 注册调用存在：grep "ctx.plugin(ReqboardDiveManager"
3. 插件启动成功：cd agent-dh && ./scripts/start.sh --check

## 实施方案（implementation）
修改 dsh-pmboard/src/index.ts：导入 ReqboardDiveManager，在插件构造函数中注册服务：ctx.plugin(ReqboardDiveManager, config)，确保 DiveManager 在合适的时机被调用（回合结束时）。

## 上游产出摘要（dependsSummary）
- 实现 ReqboardDiveManager

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-25T14:57:45.249Z，窗口 session-4211950d-3f6f-40c3-90b8-7903a76208a8）

已完成注册 DiveManager 到插件：在 dsh-pmboard 入口文件中导入并实例化 ReqboardDiveManager

### 完成项

- 在 packages/web/dsh-pmboard/src/index.ts 添加 ReqboardDiveManager 导入
- 在 apply 函数中实例化 DiveManager：const diveManager = new ReqboardDiveManager(ctx)
- 添加初始化日志：logger.info("ReqboardDiveManager initialized")
- 编译验证通过

### 改动文件

- `packages/web/dsh-pmboard/src/index.ts`

### 下一步

推进任务到 done 状态，提交检查点。注意：完整启动测试需要在运行环境中进行

---
