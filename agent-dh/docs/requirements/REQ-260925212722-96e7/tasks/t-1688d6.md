# t-1688d6 MoveTask 工具增加父子卡检查

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
MoveTask 工具增加父子卡检查

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：implement
- 端侧：backend

## 得到什么结果
1. 代码包含 armed + parentId 检查
2. 错误提示正确：grep "reqboard_task_run"
3. E2E 测试：armed + 子任务时手动推进 → 被拒绝

## 实施方案（implementation）
修改 MoveTask.ts：检查任务的 parentId（父子卡模式），检查需求的 dive?.activation === 'armed'，如果 armed 且是子任务则抛出错误：父子卡模式下不允许手动推进子任务。提示使用 reqboard_task_run 在父卡上执行。

## 上游产出摘要（dependsSummary）
- 扩展数据结构

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-25T15:04:08.055Z，窗口 session-4211950d-3f6f-40c3-90b8-7903a76208a8）

已完成 MoveTask 工具增加父子卡检查：Dive 模式 armed 且任务是子卡时拒绝手动推进

### 完成项

- 在 MoveTask.ts 添加 armed + parentId 检查
- 检查条件：requirement?.dive?.activation === 'armed' && task.parentId
- 获取需求对象并检查 dive.activation
- 检查任务是否为子任务（parentId 存在）
- 错误提示：请在父卡上使用 reqboard_task_run() 自动执行
- 错误码：REQBOARD_DIVE_ARMED
- 编译验证通过

### 改动文件

- `packages/web/dsh-pmboard/src/application/use-cases/MoveTask.ts`

### 下一步

推进任务到 done 状态，提交检查点

---
