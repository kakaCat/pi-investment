---
requirement_refs: [FR-1, FR-2, FR-3, FR-4, FR-5, FR-6, FR-7, FR-8]
---

# 交付核对：命令与输出（收口窗口实跑）

> 本文件是**人复核用**的记录：每条都是收口窗口在本机真跑过的命令与结果，不是子代理的转述。
> 逐子卡的测试记录（链内产出）见同目录其余 `t-*-test.md`。

## 1. 验收三件套（t-145cb0 口径）

```
cd packages/web/dsh-pmboard
npx vitest run e2e-design-handoff migration consistency zero-arg-binding contract-shapes
```

结果：`Test Files 5 passed (5) / Tests 37 passed (37)`，exit 0。

## 2. 类型门禁

```
cd packages/web/dsh-pmboard && npx tsc --noEmit -p tsconfig.json
```

结果：23 条错误。**改动前基线同为 23 条**（用 `git stash` 临时回退到 9e5ebf60 实测），
即本次交付未新增类型错误；全部新增/改动文件 0 报错。

## 3. 全量回归

```
cd packages/web/dsh-pmboard && npx vitest run
```

| | 改动前（9e5ebf60） | 交付后（ec69a758） |
|---|---|---|
| 失败文件 | 7 | 5 |
| 失败用例 | 9 | 6 |
| 通过用例 | 1769 | 1857 |

失败集合 ⊆ 基线、无新增失败。链另修好 2 条既有失败：`design-completeness-gate`（×2，FR-2 文案分化）、
`size-budget`（×1，组合根瘦身）。剩余 6 条为既有失败（client-view / template-address-injection ×2 /
layer-boundary / repository / typecheck），与本次改动无关。

## 4. 零参工具实调（FR-4 / A4）

在 run_code 里对 `tools.reqboard_status` **不带任何参数**调用 → 正常返回看板状态，
未出现 `binding arguments must be lossless JSON`。

## 5. 收口窗口修正的两处链引入回归

| 回归 | 现象 | 修正 |
|---|---|---|
| 轻档设计片段锚点 | `language-layer` 断言 `每节必须标注服务哪条功能点` 失败 | 片段恢复该短语（保留链新增的登记命令条目），并重跑 `inline-prompt-fragments` / `dump-stage-prompts` |
| 新测试类型 | `tests/ask-confirm-pending.test.ts(55,36) TS2322`（deliver 签名不符 AgentDeliveryPort） | 改用具名端口类型 `AgentDeliveryPort` |

修正后：tsc 回到 23，`language-layer` / `ask-confirm-pending` / `prompt-baseline` / `gate-aware-questions` 全绿（43 用例）。
