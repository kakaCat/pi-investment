# t-800d53 组合根瘦身使尺寸门禁转绿

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
组合根瘦身使尺寸门禁转绿

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：implement
- 端侧：backend

## 得到什么结果
npx vitest run tests/size-budget.test.ts tests/isolate-node-context.test.ts tests/capture-hook.test.ts 全绿；wc -l packages/web/dsh-pmboard/src/index.ts ≤ 400；nodeIsolationEnabled 仍可从 src/index.ts import 且行为不变

## 实施方案（implementation）
把配置/路径纯函数与捕获根装配从 packages/web/dsh-pmboard/src/index.ts 抽到新增 packages/web/dsh-pmboard/src/plugin-config.ts 与 packages/web/dsh-pmboard/src/wiring/pm-capture-root.ts；index.ts 再导出 dshHomePath/nodeIsolationEnabled 保兼容。

## 上游产出摘要（dependsSummary）
- 实现断点常驻与续跑输入包

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
