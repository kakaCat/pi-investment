# t-41f158 定义新契约类型与端口

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
定义新契约类型与端口

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：implement
- 端侧：backend

## 得到什么结果
npx vitest run tests/contract-shapes.test.ts tests/output-contract.test.ts 全绿；npx tsc --noEmit -p tsconfig.json 错误数 ≤ 基线 23 且新增文件 0 报错

## 实施方案（implementation）
改 packages/web/dsh-pmboard/src/shared/protocol.ts（增 InterruptionRecord / DesignDocRegistration / PendingConfirmation，RequirementRecord.interruption?）与 packages/web/dsh-pmboard/src/application/ports.ts（增 PendingConfirmPort + UseCaseDeps.pendingConfirms?）；新增 packages/web/dsh-pmboard/tests/contract-shapes.test.ts。步骤：先立类型/端口，不写行为。验证：npx vitest run tests/contract-shapes.test.ts tests/output-contract.test.ts + npx tsc --noEmit -p tsconfig.json 错误数 ≤ 23。

## 上游产出摘要（dependsSummary）
- （无依赖）

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-24T14:40:06.343Z，窗口 session-a4d082b8-bb32-4506-80a5-876ddd6051ff）

把后面几张卡要用的数据契约先定死：断点记录、设计文档逐份登记态、挂起确认三块字段与端口已写进共享协议层，并用一条新测试锁住；后续实现卡只能按这套字段填空，字段一漂移这测试就编译报错。

### 完成项

- T-1 断点记录：InterruptionRecord（at/reason/stage/pendingAction 必填，tool? 可选）挂到 RequirementRecord.interruption?（缺省=存量记录无断点，续跑输入包输出不变）
- T-3 逐份登记态：DesignDocRegistration（name/path/on_disk/registered/confirmed 必填，exempted?/conditional? 可选）
- T-4 挂起确认：PendingConfirmation + PendingConfirmationOutcome + PENDING_CONFIRM_TICKET_PREFIX='pc-'
- I-4 端口：PendingConfirmPort（register/get/settle，均不抛）+ UseCaseDeps.pendingConfirms?（缺省=未装配，保持旧阻塞语义）
- 新增 tests/contract-shapes.test.ts（8 例）：运行时键集合 + expectTypeOf 精确类型双断言
- 证据：contract-shapes+output-contract = 2 files/27 tests 全绿；tsc = 23 条（=基线，新文件 0 报错）；全量 = 7 文件/9 用例失败，与基线集合一致无新增
- 改动已合并进 main（adb4bd77 → merge 9e5ebf60），后续卡窗口与 :13080 侧可直接读到

### 改动文件

- `packages/web/dsh-pmboard/src/shared/protocol.ts`
- `packages/web/dsh-pmboard/src/application/ports.ts`
- `packages/web/dsh-pmboard/tests/contract-shapes.test.ts`

### 下一步

链上剩余子卡（联调/复核/测试）由自动链执行时首卡即 start_failed（workflow engine 读不到 session）→ 自动链已 PAUSE(fail)，本窗口改用人工核对收口；契约本身无运行时行为，真正的端到端验收在 T-13。

---
## 汇报 2（2026-09-24T14:44:53.052Z，窗口 session-a4d082b8-bb32-4506-80a5-876ddd6051ff）

本卡代码工作已完成并进 main，但卡状态无法收口——如实记录三处阻塞证据（不伪造状态）。

### 完成项

- 阻塞①（设计如此）：子卡 done 需 workflow run 凭证——t-8cd37e 人工汇报后 task_move(done) 被 REQBOARD_SUBTASK_GATE 拒绝（『workflow run 未完成』），自测证据不能过户
- 阻塞②：批准计划后的自动链首跑即失败——advance.history：OPEN_PARENT t-41f158(ok) → RUN_SUBTASK t-8cd37e(failed: start_failed: Cannot read properties of undefined (reading 'session')) → PAUSE(fail)
- 阻塞③（当前挡住整条链）：reqboard_task_run(t-41f158) 选中 t-8cd37e 后被跨卡覆盖兜底拦死——『packages/web/dsh-pmboard/src/shared/protocol.ts 的 mtime 落在父卡 t-32b532 的子卡 t-26b426 执行窗口内』
- 阻塞③根因：t-32b532/t-26b426 属 REQ-260922213356-4a45（该需求 2026-09-23 已归档），t-26b426 最后一次执行 outcome=running 且 endedAt 缺失，已僵死 35.1 小时；cross-card.ts:35-49 的 otherParents 只按 status=in_progress 全局取父卡、不过滤 requirementId（与文件头『同需求』注释不符），且 end=exec.endedAt??now → 该僵尸窗口吞掉一切新 mtime
- 阻塞③影响面：本需求同批 T-7 的子卡 t-d4cded（patches/...patch）已被同一条规则拦下；其余 11 张卡收口时都会撞同一道墙
- 处置：把 t-8cd37e 归还 todo（in_progress→todo）让引擎可再次选中它，未再尝试人工过户

### 下一步

需人拍板解锁：A) 单开一条小需求修 cross-card（按 requirementId 过滤 + running 超阈值视为僵尸、窗口封顶）并顺带修 workflow start 的 session 取参；B) 人工先清 REQ-4a45 的 t-26b426 僵尸执行记录；C) 暂不处置。解锁前本需求 13 张卡均无法收口。

---
## 汇报 3（2026-09-24T15:05:28.535Z，窗口 session-a4d082b8-bb32-4506-80a5-876ddd6051ff）

契约骨架钉死 + 清掉挡路的僵尸执行。这一步做完：①后续每张实现卡只能按已定的字段填空（断点记录 / 设计文档逐份登记态 / 挂起确认三块），字段一漂移契约测试就编译报错；②一条僵死 35 小时、挂在已归档需求上的执行记录被闭合，跨卡覆盖兜底不再误伤本需求的卡——原先它把本需求的每个文件 mtime 都判成"跨卡覆盖"，11 张卡都收不了口。

### 完成项

- T-1 断点记录：InterruptionRecord（at/reason/stage/pendingAction 必填，tool? 可选）挂到 RequirementRecord.interruption?（缺省=存量记录无断点，续跑输入包输出不变）
- T-3 逐份登记态：DesignDocRegistration（name/path/on_disk/registered/confirmed 必填，exempted?/conditional? 可选），供 reqboard_status 与页面逐份读"未登记/待确认/已落章"
- T-4 挂起确认：PendingConfirmation + PendingConfirmationOutcome + PENDING_CONFIRM_TICKET_PREFIX=pc-
- I-4 端口：PendingConfirmPort（register/get/settle，均不抛）+ UseCaseDeps.pendingConfirms?（缺省=未装配，保持旧阻塞语义）
- 新增 tests/contract-shapes.test.ts（8 例）：运行时键集合 + expectTypeOf 精确类型双断言，锁死 interfaces.md/data-model.md 的字段表
- 自测①：npx vitest run tests/contract-shapes.test.ts tests/output-contract.test.ts → 2 files / 27 tests 全绿（exit 0）
- 自测②：npx tsc --noEmit -p tsconfig.json → 23 条（=基线 23），新增/改动文件 0 报错
- 僵尸执行清理：t-26b426（属已归档需求 REQ-260922213356-4a45）那条 outcome=running、endedAt 缺失、僵死 35h+ 的执行段 e-8fd749 已闭合为 cancelled；其父卡 t-32b532 同步退回待办
- 清理后复核：三个改动文件对 cross-card 兜底重算 → 0 命中（其余 in_progress 父卡均无僵尸子执行）

### 改动文件

- `packages/web/dsh-pmboard/src/shared/protocol.ts`
- `packages/web/dsh-pmboard/src/application/ports.ts`
- `packages/web/dsh-pmboard/tests/contract-shapes.test.ts`

### 下一步

本卡是带子卡链的父卡（t-8cd37e 研发 / t-0f05c1 联调 / t-c579d7 复核 / t-4c35b9 测试），done 需 4 张子卡各跑通一次 workflow run。当前只有工具入口 reqboard_task_run 能把引擎需要的 agent 句柄传下去，且一次调用最多连跑 20 步、寿命绑在调用方回合上；看板「继续」路径缺该句柄会 start_failed。需人拍板：A) 由我按工具入口分段触发（跨回合，超时截断会回退重试）；B) 单开小需求修「看板继续缺 exec.agent」+「cross-card 只按 requirementId 过滤、running 超阈值视为僵尸」。

---
