# t-6a3070 实现断点常驻与续跑输入包·联调

> 子卡（父卡 t-7b5e7a · 阶段 integrate） ｜ 状态：done ｜ 本文件由收口窗口按台账渲染（台账是唯一事实源）

## 在做什么
实现断点常驻与续跑输入包·联调

## 解决什么问题
子卡阶段：联调

## 得到什么结果（验收标准）
接口联调通过：给出请求样例、期望响应与实际返回，三者一致；并把联调记录（命令与输出摘要）在本轮执行内写入 docs/requirements/REQ-260924213231-b1c4/evidence/t-6a3070-integrate.md，在 filesChanged 中列出该文件

## 实施方案
新增 packages/web/dsh-pmboard/src/application/internal/interruption.ts、packages/web/dsh-pmboard/src/application/use-cases/NoteInterruption.ts、packages/web/dsh-pmboard/src/tools/NoteInterruptionTool/NoteInterruptionTool.ts；改 packages/web/dsh-pmboard/src/adapters/CaptureHook.ts（onTurnFinished 信号）、packages/web/dsh-pmboard/src/application/internal/node-input-package.ts（## 断点节）、packages/web/dsh-pmboard/src/application/use-cases/SubmitArtifact.ts、packages/web/dsh-pmboard/src/application/use-cases/MoveRequirement.ts、packages/web/dsh-pmboard/src/application/use-cases/Decompose.ts、packages/web/dsh-pmboard/src/application/use-cases/MoveTask.ts、packages/web/dsh-pmboard/src/application/use-cases/AcceptSheet.ts、packages/web/dsh-pmboard/src/application/use-cases/AskConfirm.ts（stampCheckpoint）、packages/web/dsh-pmboard/src/index.ts（异步边界接线+注册）；新增 packages/web/dsh-pmboard/tests/interruption-checkpoint.test.ts。

[子卡阶段·联调] 只做本阶段；验收：接口联调通过：给出请求样例与期望响应，实际返回与预期一致

## 执行与完工记录
- workflow run：2026-09-25 01:13:56（stopReason=completed，产出非空=True）
- 改动文件：
  - docs/requirements/REQ-260924213231-b1c4/evidence/t-6a3070-integrate.md
- 完成项：
  - I-8 工具 reqboard_note_interruption 三方对照（请求样例 / 期望响应 / 实际返回逐字段一致，含 success/requirement_id/interruption{at,reason,stage,pendingAction,tool}/note，且台账写入 + [断点] 评论留痕）— MATCH
  - I-8 错误路径对照：空 reason → REQBOARD_INVALID_INPUT、本窗口无绑定需求 → REQBOARD_NO_BOUND_REQ，期望=实际 — MATCH
  - 写入器 A→B 联调：move 交棒写 checkpoint（stage=brainstorming, pendingAction=reqboard_submit(kind=requirement), tool=reqboard_move）→ CaptureHook 喂 turn/end(error) 发 onTurnFinished 信号 → noteInterruptionForWindow 覆盖为 error:UPSTREAM_STREAM_IDLE:stream idle 3m 并保留 pendingAction — MATCH
  - 续跑输入包联调：有断点 → 追加「## 断点」含阶段/未完成动作/原因/时间四要素且位于「未决问题」与「下一步」之间；老需求（interruption 缺省 vs 显式 undefined）输出逐字节相同且不出现断点节 — MATCH（byte-identical）
  - I-8 单对象语义：二次 noteInterruption 覆盖写，台账只保留一个 interruption，最终 reason=second — MATCH
  - 接线与注册核验：src/index.ts:331-338 异步边界 + :406 注册 defineNoteInterruptionTool；tools-dispatch / apply-wiring 断言含 reqboard_note_interruption 且全绿
  - 落盘联调记录 docs/requirements/REQ-260924213231-b1c4/evidence/t-6a3070-integrate.md（含三方对照表、命令与输出摘要、观察项）
- 执行段：1 次（末次 2026-09-25 01:10:39，outcome=succeeded）
