# t-3d5f80 迁移兼容卡与 E2E 复跑·研发

> 子卡（父卡 t-145cb0 · 阶段 dev） ｜ 状态：done ｜ 本文件由收口窗口按台账渲染（台账是唯一事实源）

## 在做什么
迁移兼容卡与 E2E 复跑·研发

## 解决什么问题
子卡阶段：研发

## 得到什么结果（验收标准）

本卡为该父卡的研发子卡，交付物 = 父卡验收口径：npx vitest run tests/e2e-design-handoff.test.ts tests/migration.test.ts tests/consistency.test.ts 全绿；E2E 一次通过且全程无 REQBOARD_MISSING_ARTIFACT；legacy（artifacts 空）需求仍放行；npx vitest run 失败集合 ⊆ 基线且不新增失败；本卡的改动清单与自测输出见 docs/requirements/REQ-260924213231-b1c4/tasks/t-3d5f80.md

## 实施方案
新增 packages/web/dsh-pmboard/tests/e2e-design-handoff.test.ts（复跑设计阶段：落盘 5 份→submit(design)→ask_confirm→move）并回归 packages/web/dsh-pmboard/tests/migration.test.ts（legacy 放行、老需求无 interruption 逐字节兼容）。

[子卡阶段·研发] 只做本阶段；验收：改动已落盘，相关测试或命令跑通并附输出摘要

## 执行与完工记录
- workflow run：2026-09-25 02:10:13（stopReason=completed，产出非空=True）
- 改动文件：
  - packages/web/dsh-pmboard/tests/e2e-design-handoff.test.ts
- 完成项：
  - 新增 E2E 复跑卡 tests/e2e-design-handoff.test.ts（头部带 serves: FR-1，满足孤儿用例门禁），走真实工具 defineSubmitTool/defineAskConfirmTool/defineMoveTool + 真实适配器落临时目录，断言可观察终态而非‘函数被调用过’
  - TC-19 主用例（A1 一次通过）：落盘 5 份 design/*.md → reqboard_submit(kind=design)（registered_count=5、逐份 registered=true/confirmed=false）→ reqboard_ask_confirm(target=artifact,kind=design)（confirmed=true、advanced=true、to=decomposing）→ 台账状态 decomposing，全程无 REQBOARD_MISSING_ARTIFACT
  - 字面三步链用例（卡面顺序）：submit(design) → ask_confirm(advance:false) 落章（advanced=false、状态仍 design、5 份 confirmedAt 全写）→ move(decomposing) 一次通过
  - 两态闸门反例（证明闸门未放松、E2E 非空转）：未登记时 move 报 REQBOARD_MISSING_ARTIFACT；已登记未确认时报 REQBOARD_ARTIFACT_NOT_CONFIRMED
  - 迁移兼容用例：legacy（artifacts 空/undefined）存量需求 design→decomposing 仍放行，且产物簿保持为空（不凭空造产物）
  - 回归 tests/migration.test.ts 未改动（git status 仅新增该文件），与 tests/consistency.test.ts 同批跑绿；老需求无 interruption 逐字节兼容由同批回归的 tests/interruption-checkpoint.test.ts（TC-16）覆盖并跑绿
- 执行段：2 次（末次 2026-09-25 02:05:38，outcome=succeeded）
