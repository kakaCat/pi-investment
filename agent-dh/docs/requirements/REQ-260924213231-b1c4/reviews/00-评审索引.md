---
requirement_refs: [FR-1, FR-2, FR-3, FR-4, FR-5, FR-6, FR-7, FR-8]
---

# 评审索引（收口窗口）

> 链内每张父卡的 `review` 子卡各落一份复核记录（同目录 `t-*-review.md`，13 份）；
> 本文件是收口窗口对**交付整体**的复核，并记录自查发现的两处回归及其处置。

## 1. 复核口径

- 卡片交付面：13 张父卡全部 `done`，需求由 ROLLUP 自动滚进 `accepting`
  （逐事件留痕见 [advance-log.md](../advance-log.md)）。
- 契约面：`packages/web/dsh-pmboard/src/shared/protocol.ts`（InterruptionRecord / DesignDocRegistration /
  PendingConfirmation）与 `packages/web/dsh-pmboard/src/application/ports.ts`（PendingConfirmPort）是设计文档
  `design/interfaces.md` / `design/data-model.md` 字段表的逐字落地，由
  `packages/web/dsh-pmboard/tests/contract-shapes.test.ts` 双闸锁定。
- 兼容面：「旧数据 / 旧调用方仍走通」由 `packages/web/dsh-pmboard/tests/migration.test.ts`、
  `packages/web/dsh-pmboard/tests/consistency.test.ts`、`packages/web/dsh-pmboard/tests/e2e-design-handoff.test.ts` 覆盖。

## 2. 自查发现的偏离（已修正）

1. **设计轻档提示词锚点漂移**：链为补 FR-5「登记命令」重写了覆盖条目，把守卫断言的锚点短语
   `每节必须标注服务哪条功能点` 改成不带「必须」的措辞 → `language-layer` 转红。
   处置：恢复原锚点短语，**保留**链新增的登记命令条目（FR-5 不丢）。
2. **新增测试自身的类型错误**：`tests/ask-confirm-pending.test.ts` 的桩 deliver 签名与
   `AgentDeliveryPort` 不符（TS2322，属新增错误，基线 23 → 24）。处置：改用具名端口类型。

## 3. 未决 / 需人判断

- `:13080` 进程仍是改动前代码（tsx 直载源码），**人工核验 A1/A2/A3/A6 前需重启 profile**。
- 链尾 `reqboard_task_run` 返回体在 `next_ready` / `blocked` 为 `null` 时违反自身 output schema
  （报 `"value.next_ready" must be an object`）——本次未改，建议单独立卡。
