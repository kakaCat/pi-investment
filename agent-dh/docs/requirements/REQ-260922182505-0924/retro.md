---
requirement_refs: BUG-1, BUG-2
---

# 复盘 · REQ-260922182505-0924 删除 triage 遗留兼容路径

> 窗口：w-9faaac35（investor）｜周期：2026-09-24（单会话完成 design → 归档前）

## 交付结论

旧立项流程（triage）的兼容层前后台全删：路由 4 端点、前端"待归类"面板、
立项前置检查（REQBOARD_PENDING_TRIAGE）、has_pending 状态字段、3 个 .bak 垃圾文件。
台账 2 条 resolved 历史记录与读取管道冻结保留（删行为留数据）。
验证：tsc 0 错误；vitest 失败集修前 86 = 修后 86（差集为空，存量红灯不动）；
新代码实例 4 端点全部 404；migration v4 fixture 9/9 绿。

## 根因回顾

M2 自动分类流程退役（2026-09）时只删了实现本体，兼容层以"处理遗留卡"名义保留；
遗留卡清零后无人触发删除动作——**退役流程没有"兼容层寿命"这个概念**。
本次靠立项链路走查（台账实证 pending=0）才发现尸体。

## 过程数据

- 三卡制执行：t1 复现基线 → t2 删除（22 文件：删 4、摘 14、冻结 4、注释 3）→ t3 独立验证
- 基线差集法：修前取 86 条失败清单，修后 comm 双向 diff 为空——比"全绿"口径
  诚实（存量红灯属其他治理线，不夹带修复）
- 部署验证走独立实例 :13081（现役 :13080 跑本会话，避免自杀式重启）；
  pidfile 被调试实例覆盖的问题当场发现并恢复

## 教训（供未来窗口）

1. **机制退役 = 实现 + 兼容层 + 判定检查一起列删除清单**：本次残留的三类
   （路由/面板/前置检查）正是一个完整兼容层的标准组成，退役时按这个清单核。
2. **"永远对着尸体站岗"的探测法**：前置检查类代码，先查它检查的对象还在不在生产
   （台账实证 pending=0），对象死了检查就是死代码。
3. **多实例机器验证铁律再次生效**：调试实例会覆盖共享 state/server.pid——
   起之前记旧值，停之后恢复（本次已照做）。
4. **删除即文档同步**：日志串、schema 描述、prompt 文案里的死概念描述要与代码同删，
   否则下一个走查的人会以为功能还在（本次同步了 4 处）。
