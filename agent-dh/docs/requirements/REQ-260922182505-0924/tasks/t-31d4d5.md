# t-31d4d5 复现死代码现状并建立回归基线

> 任务卡骨架（reqboard_decompose 自动生成）；汇报经 reqboard_task_report 追加到本文件

## 在做什么
复现死代码现状并建立回归基线

## 解决什么问题
（未填写——开工前补充这张卡要解决的业务问题）

## 范围
- 阶段：analysis
- 端侧：backend

## 得到什么结果
docs/requirements/REQ-260922182505-0924/ 下存在 baseline 记录（grep 清单 + vitest 失败集），且记录了 triage 路由当前在线可达的证据（curl 状态码）。

## 实施方案（implementation）
在 dsh-pmboard 包目录执行 grep -rn "triage" src/ --include="*.ts" 与 npx vitest run，把失败测试名清单写入 baseline 文件；curl 线上 :13080 的 triage 路由记录状态码。

## 上游产出摘要（dependsSummary）
- （无依赖）

## 执行方式提示（executorHint）
优先新窗口或 subagent 执行；按本卡自足执行，不读会话历史
## 汇报 1（2026-09-22T12:12:02.034Z，窗口 session-9faaac35-2641-473c-a5dc-ea6e6d11efbf）

复现成立并建立回归基线：triage 路由在线上仍返回 200（含 2 条 resolved 历史记录），src/ 有 127 行 triage 引用分布 22 个文件，3 个 .bak 文件存在；全量测试基线 = 10 个文件/91 条用例失败（均为存量红灯，已落盘为差集对比基准）。

### 完成项

- grep 基线：127 行命中 / 22 文件 → baseline-grep.txt
- 线上路由证据：GET /dashboard/api/reqboard/triage 返回 200 → baseline-curl.txt
- vitest 全量基线：10 failed files / 91 failed tests（1571 passed）→ baseline-test-failures.txt
- 基线汇总说明 → baseline.md

### 改动文件

- `docs/requirements/REQ-260922182505-0924/baseline.md`
- `docs/requirements/REQ-260922182505-0924/baseline-grep.txt`
- `docs/requirements/REQ-260922182505-0924/baseline-curl.txt`
- `docs/requirements/REQ-260922182505-0924/baseline-test-failures.txt`

### 下一步

t2（t-8d1d4f）：按设计处置表删除 triage 兼容路径与 .bak 文件

---
