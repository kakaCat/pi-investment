# 盈利引擎·线 A 工单包（不等地基，本周可推）

| 字段 | 值 |
|---|---|
| 日期 | 2026-08-27（agent-dh k3，审计+文档角色编制） |
| 上游 | [RFC 005 工单包 M5/M6/M7](../../rfcs/005-profit-engine-work-tickets.md)、[推进方案三波路线](m1-market-perception-handover.md) §审计结论 |
| 实施方 | 待领工（建议 w-98f9a35c 或指定窗口） |
| 复核方 | agent-dh k3（每票完成后按"复核点"验收） |

**背景**：盈利引擎整体 ~55-62%。地基（K线同步管线、M1 快照时序、Agent OS 稳定性）由线 B 处理；本包是**不依赖地基、本周即可推**的 5 张票。

---

## A-1：M5-1 滑点追踪收口

**现状**：wip 已提交（`aaa27865`：portfolio_trade 下单前抓决策时价、成交后方向归一滑点落库 `trade:slippage`、`slippage_report` 工具），未做端到端验证。

| 项 | 内容 |
|---|---|
| 任务 | ①确认 wip 分支已合入基线且 DSH 已重启加载（pnpm file: 硬拷贝教训：`pnpm install` 刷新快照再重启）；②模拟盘做一笔小额交易触发滑点落库；③`slippage_report` 出报告 |
| 验收命令 | `slippage_report`（工具直调） |
| 验收标准 | 成交笔数≥1，滑点字段方向归一正确（正=买贵/卖便宜）；wip 分支无残留 |
| 复核点 | 抽查落库记录的 decision_price vs fill_price 是否符合方向归一定义 |
| 注意 | ⚠️ 滑点落库走 Agent OS osMemory——Agent OS 宕机时落库会失败，需确认降级行为（不阻塞交易、有日志），Agent OS 在线时验证 |

## A-2：M5-2 trade_verify 例行化

| 项 | 内容 |
|---|---|
| 任务 | 每日收盘后自动 `trade_verify`（对账），异常时飞书告警。挂载到**运行中的调度体系**（quantsys-v2 scheduler 或 Agent OS 提醒链，二选一，挂完必须验证次日真实触发） |
| 验收命令 | 次日查调度执行记录 + `trade_verify`（手动补验当日） |
| 验收标准 | 连续 2 个交易日自动触发（created_at 时间戳为证）；有异常时产生处理记录 |
| 复核点 | 检查调度任务的最近触发时间戳，不接受"已挂载"口头声明 |
| 注意 | M1 调度前车之鉴：**挂载≠生效**，且 Agent OS 近期不稳定（白天在线半夜宕），优先挂 quantsys-v2 scheduler |

## A-3：M6-1 决策前检索合规抽查（R-008）

**现状**：规则已在基因组生效（R-008：下单前必须 memory_search 且 reason 注明检索结论），从未验证执行合规性。

| 项 | 内容 |
|---|---|
| 任务 | 抽查最近 10 笔 portfolio_trade 的 reason 字段：是否含「已检索」痕迹（R-008 格式）；learning 自动追踪的 context 是否有对应 memory_search 调用 |
| 验收命令 | 查交易记录 reason 字段 + memory 检索日志交叉比对 |
| 验收标准 | 合规率统计出报告；不合规的标注窗口编码归因 |
| 复核点 | 报告需区分"无历史教训但留了检索痕迹"（合规）与"完全没检索"（违规） |
| 产出 | `docs/work-logs/2026-08/r008-compliance-audit.md` |

## A-4：M7-1 opponent_behavior 数据源诊断

**现状**：S2 已知 bug——数据不可信（600737 涨停日返回 panic_bottom 式矛盾结论）。

| 项 | 内容 |
|---|---|
| 任务 | ①复现：对 600737 近期涨停日调 `opponent_behavior`；②逐指标溯源（散户/机构/游资各自的数据源与计算路径）；③定位是数据源缺失、字段映射错、还是方向逻辑反了；④出修复规格（不改代码也行，规格给实施线） |
| 验收命令 | `opponent_behavior(symbol="600737")` + 数据源逐层日志 |
| 验收标准 | 输出诊断报告：每个指标的取值来源、故障点定位、修复建议；如修复，复核标准为"结论与当日盘面方向一致" |
| 复核点 | 重点查"方向一致性"（涨停日不得出恐慌底结论） |
| 产出 | `docs/work-logs/2026-08/opponent-behavior-diagnosis.md` |

## A-5：M6-2 归因分析启动（依赖 A-1 收口）

| 项 | 内容 |
|---|---|
| 任务 | 定义归因标签体系（选股/择时/板块/滑点四类）；对近期平仓标的逐只打归因标签；产出第一份周报归因段落 |
| 验收命令 | 查归因标签落库 + 周报试跑（`weekly_report` 或 `weekly_report_push`） |
| 验收标准 | 每只平仓标的有归因标签；周报含归因段落 |
| 复核点 | 滑点归因的数据必须来自 A-1 的真实落库，不接受估算值 |
| 依赖 | **A-1 收口后启动** |

---

## 排序与并行

```
A-1（收口，半天）──→ A-5（启动，1天）
A-2（挂载+2日观察）      A-3（抽查，半天）      A-4（诊断，1天）
```

A-1/A-2/A-3/A-4 四票可并行，A-5 等 A-1。**全部完成后我做统一复核**，并在 RFC 005 勾选 M5-1/M5-2/M6-1/M7-1。

## 全局警戒（复用血泪教训）

1. 挂载调度后必须验证真实触发时间戳（M1 教训：挂了 4 天没生效）
2. pnpm file: 硬拷贝——改 agent-dh 包后 `pnpm install` + 重启才生效
3. Agent OS 不稳：落库类验证挑其在线时段，或先确认降级行为
4. 多会话并行：开工先 `git branch --show-current`；出现不属于自己的改动 = 停手
