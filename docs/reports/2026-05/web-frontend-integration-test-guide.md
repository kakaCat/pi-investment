# Web-Frontend 风控检查页面 - 集成测试指南

**测试日期**: 2026-05-24  
**测试范围**: 前后端集成功能验证

---

## 前置条件

### 1. 启动后端服务

```bash
cd /Users/mac/Documents/ai/pi-investment/quantsys-v2
python api/server.py
```

**验证**: 访问 http://127.0.0.1:5001/api/health 应返回成功

### 2. 启动前端服务

```bash
cd /Users/mac/Documents/ai/pi-investment/web-frontend
npm run dev
```

**验证**: 访问 http://127.0.0.1:3001 应显示首页

### 3. 准备测试数据

确保数据库中有持仓数据：
- 至少 2-3 个持仓
- 持仓有对应的 K线数据
- 持仓有对应的风险指标数据（可选）

---

## 测试用例

### 测试 1: 风险检查基本功能 ✅

**步骤**:
1. 访问 http://127.0.0.1:3001/risk-check
2. 在"账户总值"输入框输入 `1000000`
3. 点击"执行检查"按钮
4. 等待加载完成

**预期结果**:
- ✅ 显示成功提示："风险检查完成"
- ✅ 风险概览卡片显示数据（总体风险等级、预警数量、VaR、组合波动率）
- ✅ 风险指标区域显示 6 个指标的进度条
- ✅ 持仓风险明细表格显示所有持仓

**验证点**:
```
风险概览:
  - 总体风险等级: 显示 "低风险" / "中风险" / "高风险"
  - 预警数量: 显示数字（可能为 0）
  - VaR (95%): 显示百分比或金额
  - 组合波动率: 显示百分比

风险指标:
  - 持仓集中度: 进度条 + 百分比 + 状态标签
  - 单票风险敞口: 进度条 + 百分比 + 状态标签
  - 行业集中度: 进度条 + 百分比 + 状态标签
  - 最大回撤: 进度条 + 百分比 + 状态标签
  - 波动率: 进度条 + 百分比 + 状态标签
  - VaR风险价值: 进度条 + 百分比 + 状态标签
```

---

### 测试 2: 持仓风险明细数据显示 ✅

**步骤**:
1. 执行风险检查后
2. 查看"持仓风险明细"表格

**预期结果**:
- ✅ 表格显示所有持仓
- ✅ 每行显示：代码、名称、持仓市值、占比、VaR 95%、波动率、最大回撤、检查项、状态

**关键验证点** (修复重点):
```
VaR 95% 列:
  ❌ 修复前: 显示 "0.0%" 或空白
  ✅ 修复后: 显示真实数据，如 "-6.8%"（负数，红色）

波动率列:
  ❌ 修复前: 显示 "0.0%" 或空白
  ✅ 修复后: 显示真实数据，如 "25.0%"

最大回撤列:
  ❌ 修复前: 显示 "0.0%" 或空白
  ✅ 修复后: 显示真实数据，如 "-15.0%"（负数，红色）

检查项列:
  显示格式: "3/5" (通过数/总数)
```

**截图位置**: 
- 保存表格截图到 `/tmp/risk-check-table.png`

---

### 测试 3: 风险预警列表 ✅

**步骤**:
1. 执行风险检查后
2. 向下滚动查看"风险预警"卡片

**预期结果**:
- ✅ 如果有风险预警，显示预警列表
- ✅ 如果无预警，不显示该卡片

**验证预警类型** (修复重点):
```
持仓集中度预警:
  - 风险类型: "持仓集中度"
  - 等级: "高" (红色标签)
  - 描述: "600519 仓位集中度 50.0% > 30%"

行业集中度预警 (新增):
  ❌ 修复前: 不显示
  ✅ 修复后: 显示 "行业集中度"
  - 风险类型: "行业集中度"
  - 等级: "高" (红色标签)
  - 描述: "600519 所属行业 \"白酒\" 集中度 65.3% > 50%"

VaR风险预警:
  - 风险类型: "VaR风险"
  - 等级: "中" (橙色标签)
  - 描述: "600519 VaR 95% = -0.068"
```

**操作验证**:
- ✅ 点击"标记已处理"按钮，状态变为"已处理"

---

### 测试 4: 设置止损规则 ✅

**步骤**:
1. 在持仓风险明细表格中，点击某个持仓的"设置止损"按钮
2. 弹出"设置止损规则"对话框

**预期结果**:
- ✅ 对话框显示
- ✅ 股票代码自动填充
- ✅ 股票名称自动填充
- ✅ **当前价格显示真实价格**（修复重点）

**关键验证点** (修复重点):
```
当前价格:
  ❌ 修复前: 显示 "¥0.00"
  ✅ 修复后: 显示真实价格，如 "¥1,850.50"
```

**测试止损类型**:

#### 4.1 固定价格止损
1. 选择"固定价格"
2. 输入止损价格: `1750`
3. 验证显示跌幅: "-5.43%"
4. 点击"保存"

**预期结果**:
- ✅ 显示成功提示："止损规则已创建"
- ✅ 对话框关闭
- ✅ 止损规则列表显示新规则

#### 4.2 百分比止损 (修复重点)
1. 选择"百分比"
2. 输入止损比例: `5`
3. 验证显示触发价格
4. 点击"保存"

**预期结果**:
- ✅ 显示成功提示："止损规则已创建"
- ✅ 后端正确接收 `triggerPercent: 5`
- ✅ 后端正确映射为 `stopLossPercent: 5`
- ✅ 规则类型正确映射为 `fixed_percent`

**验证方法**:
```bash
# 查看止损规则文件
cat ~/.pi-invest/stop_loss_rules.json | jq

# 验证字段
{
  "rules": [
    {
      "id": "...",
      "symbol": "600519",
      "type": "fixed_percent",  // ✅ 应为 fixed_percent，不是 percent
      "stopLossPercent": 5,     // ✅ 应存在
      "triggerPercent": 5,      // ✅ 应存在
      "status": "active"
    }
  ]
}
```

#### 4.3 追踪止损
1. 选择"追踪止损"
2. 输入追踪比例: `10`
3. 验证显示说明文字
4. 点击"保存"

**预期结果**:
- ✅ 显示成功提示："止损规则已创建"
- ✅ 规则类型正确映射为 `trailing_stop`

---

### 测试 5: 批量设置止损 ✅

**步骤**:
1. 点击"批量设置止损"按钮
2. 弹出"批量设置止损"对话框
3. 选择止损类型: "百分比"
4. 输入止损比例: `5`
5. 勾选 2-3 个股票
6. 点击"批量设置"

**预期结果**:
- ✅ 显示成功提示："已为 N 个股票设置止损规则"
- ✅ 对话框关闭
- ✅ 止损规则列表显示所有新规则

**验证**:
```bash
# 查看止损规则文件
cat ~/.pi-invest/stop_loss_rules.json | jq '.rules | length'

# 应该显示规则数量增加
```

---

### 测试 6: 止损规则列表 ✅

**步骤**:
1. 向下滚动查看"止损规则"卡片
2. 查看规则列表

**预期结果**:
- ✅ 显示所有止损规则
- ✅ 每行显示：代码、名称、类型、触发条件、状态、创建时间、操作

**验证类型显示**:
```
类型标签:
  - "固定价格" (type: fixed_price)
  - "百分比" (type: fixed_percent)
  - "追踪止损" (type: trailing_stop)

触发条件:
  - 固定价格: "¥1,750.00"
  - 百分比: "-5%"
  - 追踪止损: "回撤 10%"

状态:
  - "生效中" (绿色标签)
  - "已触发" (橙色标签)
  - "已取消" (灰色标签)
```

**操作验证**:
1. 点击"编辑"按钮
   - ✅ 弹出编辑对话框
   - ✅ 数据预填充
   - ✅ 修改后保存成功

2. 点击"删除"按钮
   - ✅ 弹出确认对话框
   - ✅ 确认后删除成功
   - ✅ 列表更新

---

### 测试 7: 边界情况测试 ⚠️

#### 7.1 无持仓数据
**步骤**:
1. 清空持仓数据
2. 执行风险检查

**预期结果**:
- ✅ 不报错
- ✅ 显示"无持仓数据"或空表格
- ✅ 风险等级显示"低风险"

#### 7.2 账户总值为 0
**步骤**:
1. 输入账户总值: `0`
2. 执行风险检查

**预期结果**:
- ✅ 不报错
- ✅ 占比计算不出现 NaN
- ✅ 集中度检查跳过

#### 7.3 无 K线数据
**步骤**:
1. 持仓中有股票但无 K线数据
2. 执行风险检查

**预期结果**:
- ✅ 不报错
- ✅ 当前价格显示 0 或 "N/A"
- ✅ 其他数据正常显示

#### 7.4 无风险指标数据
**步骤**:
1. 持仓中有股票但无风险指标数据
2. 执行风险检查

**预期结果**:
- ✅ 不报错
- ✅ VaR、波动率、最大回撤显示 0
- ✅ 不影响其他功能

---

## 性能测试

### 测试 8: 大量持仓性能 ⚡

**步骤**:
1. 准备 20+ 个持仓
2. 执行风险检查
3. 记录响应时间

**预期结果**:
- ✅ 响应时间 < 3 秒
- ✅ 页面不卡顿
- ✅ 表格渲染流畅

**性能指标**:
```
持仓数量    响应时间    可接受性
5 个        < 1s       ✅ 优秀
10 个       < 2s       ✅ 良好
20 个       < 3s       ✅ 可接受
50 个       < 5s       ⚠️ 需优化
```

---

## 浏览器兼容性测试

### 测试 9: 多浏览器测试 🌐

**测试浏览器**:
- [ ] Chrome 90+
- [ ] Firefox 88+
- [ ] Safari 14+
- [ ] Edge 90+

**验证点**:
- ✅ 页面布局正常
- ✅ 表格显示正常
- ✅ 对话框显示正常
- ✅ 交互功能正常

---

## 回归测试

### 测试 10: 其他页面功能 🔄

**验证其他页面未受影响**:
- [ ] 首页 (Dashboard)
- [ ] 持仓管理
- [ ] 交易记录
- [ ] 策略中心
- [ ] 数据管理

**验证点**:
- ✅ 页面正常加载
- ✅ 数据正常显示
- ✅ 交互功能正常

---

## 测试结果记录

### 测试环境

```
操作系统: macOS / Linux / Windows
浏览器: Chrome 120.0.0.0
Node.js: v22.x.x
Python: 3.9+
后端版本: quantsys-v2
前端版本: web-frontend
```

### 测试结果汇总

| 测试用例 | 状态 | 备注 |
|---------|------|------|
| 测试 1: 风险检查基本功能 | ⏳ 待测试 | |
| 测试 2: 持仓风险明细数据 | ⏳ 待测试 | |
| 测试 3: 风险预警列表 | ⏳ 待测试 | |
| 测试 4: 设置止损规则 | ⏳ 待测试 | |
| 测试 5: 批量设置止损 | ⏳ 待测试 | |
| 测试 6: 止损规则列表 | ⏳ 待测试 | |
| 测试 7: 边界情况测试 | ⏳ 待测试 | |
| 测试 8: 大量持仓性能 | ⏳ 待测试 | |
| 测试 9: 多浏览器测试 | ⏳ 待测试 | |
| 测试 10: 回归测试 | ⏳ 待测试 | |

### 发现的问题

| 问题编号 | 严重程度 | 描述 | 状态 |
|---------|---------|------|------|
| - | - | - | - |

---

## 快速测试命令

### 后端 API 测试

```bash
# 运行自动化测试脚本
chmod +x test-risk-check-api.sh
./test-risk-check-api.sh

# 手动测试风险检查接口
curl -X POST http://127.0.0.1:5001/api/risk/check \
  -H "Content-Type: application/json" \
  -d '{"accountValue": 1000000}' | jq

# 手动测试止损规则创建
curl -X POST http://127.0.0.1:5001/api/risk/stop-loss/rules \
  -H "Content-Type: application/json" \
  -d '{"symbol": "600519", "type": "percent", "triggerPercent": 5}' | jq

# 查看止损规则
curl http://127.0.0.1:5001/api/risk/stop-loss/rules | jq
```

### 前端开发者工具检查

```javascript
// 在浏览器控制台执行

// 1. 检查 API 响应
fetch('http://127.0.0.1:5001/api/risk/check', {
  method: 'POST',
  headers: {'Content-Type': 'application/json'},
  body: JSON.stringify({accountValue: 1000000})
})
.then(r => r.json())
.then(d => console.table(d.checks))

// 2. 检查 Vue 组件状态
$vm0.positionRisks  // 查看持仓风险数据
$vm0.warnings       // 查看预警列表
$vm0.stopLossRules  // 查看止损规则

// 3. 检查网络请求
// Network 标签 -> 筛选 XHR -> 查看请求/响应
```

---

## 测试通过标准

### 必须通过 (P0)

- ✅ 风险检查接口正常返回数据
- ✅ 持仓风险明细显示真实的 VaR、波动率、最大回撤
- ✅ 当前价格显示正确（不是 0）
- ✅ 止损规则创建成功（字段映射正确）
- ✅ 止损类型映射正确（percent → fixed_percent）

### 应该通过 (P1)

- ✅ 行业集中度预警正常显示
- ✅ 批量设置止损功能正常
- ✅ 止损规则编辑/删除功能正常
- ✅ 边界情况不报错

### 可以优化 (P2)

- ⚠️ 大量持仓时性能优化
- ⚠️ 加载状态优化
- ⚠️ 错误提示优化

---

## 测试完成后

### 1. 更新测试结果

将测试结果填入"测试结果汇总"表格

### 2. 提交问题报告

如果发现问题，创建问题报告：
- 问题描述
- 复现步骤
- 预期结果 vs 实际结果
- 截图/日志

### 3. 更新文档

如果测试通过，更新以下文档：
- README.md - 添加风控检查功能说明
- CHANGELOG.md - 记录修复内容

---

**测试负责人**: _________  
**测试日期**: _________  
**测试结果**: ⏳ 待测试 / ✅ 通过 / ❌ 失败
