# RFC 005: 盈利引擎工单包（Work Tickets）

| 字段 | 值 |
|---|---|
| 状态 | 🟡 待领工 |
| 创建 | 2026-08-20 |
| 上游 | [RFC 004 模块设计](004-profit-engine-design.md)（评审通过即按本单施工） |

**工单规则**：每个工单可独立验收；验收命令是可执行的工具调用/SQL；「负责方」= 挣钱线（agent-dh 本会话）或 基建线（其他 agent）；跨线工单只约定接口契约。

---

## M0 数据地基 〔基建线〕

| ID | 工单 | 依赖 | 验收命令 | 验收标准 |
|---|---|---|---|---|
| M0-1 | API 过滤死因子：`/api/stock/{symbol}/factors` 剔除冻结大写因子或标注 `stale_days`/`deprecated` | 无 | `curl localhost:5001/api/stock/600737/factors` | 返回因子全部 ≤3 交易日，或陈旧因子带明确标记 |
| M0-2 | FactorStage 全市场历史回填（250 日，逐日保存） | M0-1 | `psql -c "SELECT factor_name, MIN(factor_date), MAX(factor_date) FROM quant.factor_values WHERE factor_name='ma5' GROUP BY factor_name"` | 小写因子历史 ≥120 交易日、覆盖 ≥5000 股 |
| M0-3 | 大写死数据归档至 `quant.factor_values_legacy` | M0-1 | `psql -c "SELECT COUNT(*) FROM quant.factor_values WHERE factor_name='RSI12'"` | 主表无冻结大写因子 |
| M0-4 | 因子新鲜度门禁入 `data_quality_report` | 无 | 调 `data_quality_report` | 因子陈旧 >5 交易日出现在异常列表 |

## M1 市场感知 〔挣钱线〕

| ID | 工单 | 依赖 | 验收命令 | 验收标准 |
|---|---|---|---|---|
| M1-1 | 每日 regime 落库（趋势/震荡/恐慌/狂热 + 判定指标值） | 无 | 查 regime 存储最近 5 日记录 | 每个交易日 1 条，含判定依据字段 |
| M1-2 | 每日主线识别器：涨停聚类+板块资金流+催化剂关联，输出 Top3 主线 | M1-1 | 输入 2026-08-18 → 输出应含「农业/粮食安全（厄尔尼诺）」 | 与人工复盘一致率 ≥70%（首周样本） |
| M1-3 | 情绪时间序列落库（涨跌家数/新高新低/量能） | 无 | 查最近 5 日情绪记录 | 每日 1 条且字段完整 |

## M2 标的工厂 〔挣钱线〕

| ID | 工单 | 依赖 | 验收命令 | 验收标准 |
|---|---|---|---|---|
| M2-1 | 主线→标的映射器（如 粮食安全→糖/种业/米业） | M1-2 | 输入「粮食安全」 | 输出 ≥2 只候选 + 入选理由 + 风险标注 |
| M2-2 | 排雷清单：`manipulation_detect` 实测校准 + 减持/商誉/ST 过滤 | 无 | 对 3 只已知问题股跑 `manipulation_detect` | 嫌疑评分有区分度（问题股 > 正常股） |
| M2-3 | `pool_battlefield` 实测与评分校准 | 无 | 对 pool 27/35 调 `pool_battlefield` | 输出综合评分+排名且理由可解释 |

## M3 信号与择时 〔挣钱线〕

| ID | 工单 | 依赖 | 验收命令 | 验收标准 |
|---|---|---|---|---|
| M3-1 | 信号分级制文档化：A 级=多维度共振（标准仓）、B 级=半仓、C 级=只观察 | 无 | 文档合入 docs/ | 分级定义+仓位映射+至少 3 个历史信号回溯分级示例 |
| M3-2 | 候选策略跨环境回测（5 策略 × 牛/熊/震荡 3 区间） | 无 | `strategy_execute(mode=backtest)` | 输出收益/回撤/夏普矩阵，选出 ≥3 个样本外夏普 >1 |
| M3-3 | 信号质量追踪：每个买入信号后续 5/10/20 日表现落库 | M1-1 | 查信号追踪表 | 每个信号有后续表现记录，可算胜率 |

## M4 仓位与风控 〔挣钱线〕

| ID | 工单 | 依赖 | 验收命令 | 验收标准 |
|---|---|---|---|---|
| M4-1 | regime 仓位映射表（恐慌≤100%/震荡≤60%/狂热≤30%）嵌入决策流程 | M1-1 | 任意一笔交易决策上下文 | 决策记录中引用当日 regime 与仓位上限 |
| M4-2 | 组合回撤熔断：账户回撤 >8% 强制减仓一半 | 无 | 模拟回撤场景触发一次 | 熔断触发有记录且执行减仓 |
| M4-3 | `risk_controller`/`risk_barra_decomposition` 实测校准 | 无 | 对当前账户调两工具 | 输出合理（与 risk_metrics 交叉一致） |

## M5 交易执行 〔挣钱线〕

| ID | 工单 | 依赖 | 验收命令 | 验收标准 |
|---|---|---|---|---|
| M5-1 | 滑点建模：成交价 vs 决策时价差逐笔落库 | 无 | 查最近成交滑点记录 | 每笔成交有滑点字段 |
| M5-2 | 每日 `trade_verify` 例行化 | 无 | 当日 `trade_verify` | 无异常或有异常处理记录 |

## M6 学习飞轮 〔挣钱线·护城河〕

| ID | 工单 | 依赖 | 验收命令 | 验收标准 |
|---|---|---|---|---|
| M6-1 | 决策前强制检索：下单前自动检索 memory/experience（标的+场景），结论写入决策上下文 | 无 | 一笔交易决策记录 | 含「已检索历史教训：…」引用（无教训也要留检索痕迹） |
| M6-2 | 归因分析：周度盈亏拆解（选股/择时/板块/滑点） | M3-3、M5-1 | 第一份周报归因段落 | 每只平仓标的有归因标签 |
| M6-3 | 周报自动生成（胜率/盈亏比/夏普/回撤/归因/下周计划） | M6-2 | 连续 2 周周报 | 无中断，指标齐全 |
| M6-4 | evolution 常态化：每周 `evolution_run` 全策略 | M3-2 | `evolution_leaderboard` | ≥10 条有效记录且有 fitness 区分度 |

## M7 对手博弈情报 〔联合：基建线供数，挣钱线定义指标〕

| ID | 工单 | 依赖 | 验收命令 | 验收标准 |
|---|---|---|---|---|
| M7-1 | `opponent_behavior` 数据源诊断与真实数据接入（龙虎榜/北向/大单资金流） | 基建线 | 对 600737 调 `opponent_behavior` | 结论与当日盘面方向一致（涨停日不得返回 panic_bottom） |
| M7-2 | 散户恐慌代理指标：放量长阴+超跌优质标的识别 | M1-1、M0-2 | 输入一个历史恐慌日 | 输出当日被错杀标的清单及后续 5 日胜率 |
| M7-3 | 操纵周期识别：拉高→出货→崩盘→抄底窗口 | M0-2 | 对一只已知操纵股回溯 | 阶段划分与走势吻合 |

## M8 预测引擎 〔基建线为主，后置〕

| ID | 工单 | 依赖 | 验收命令 | 验收标准 |
|---|---|---|---|---|
| M8-1 | `model_predict` 恒等输出根因排查 | 无 | 对两只不同股票调 `model_predict` | 输出有区分度，或定位到具体故障点并出报告 |
| M8-2 | 重训练 + 上线门禁（AUC>0.55 且 IR>0） | M0-2 | `model_evaluate(recent_3m)` | 达标才接入决策加权；不达标记录原因不接入 |

---

## 启动波次

| 波次 | 工单 | 说明 |
|---|---|---|
| 🌊 立即 | M0-1~4（基建线）、M1-1、M4-1、M6-1、M3-1 | 不依赖回填，今天就能开工 |
| 🌊 M0-2 完成后 | M1-2、M2-1、M3-2、M7-2 | 需要因子/历史数据 |
| 🌊 历史 ≥120 日后 | M8-2、M6-4 | ML 相关 |

## 变更日志

| 日期 | 内容 |
|---|---|
| 2026-08-20 | 创建。8 模块 26 个工单，三波启动顺序 |
