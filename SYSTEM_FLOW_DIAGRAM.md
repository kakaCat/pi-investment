# 博弈智能系统流程图

生成时间：2026-06-26 16:00

---

## 🎯 博弈智能整体架构

```
┌─────────────────────────────────────────────────────────────────┐
│                         博弈智能系统                              │
│                  (PI Investment System)                          │
└─────────────────────────────────────────────────────────────────┘
                                │
                ┌───────────────┼───────────────┐
                │               │               │
                ▼               ▼               ▼
        ┌──────────┐    ┌──────────┐    ┌──────────┐
        │ Agent-ts │    │quantsys  │    │  Web     │
        │ AI决策层 │    │  -v2     │    │ Frontend │
        │          │    │ 后端服务  │    │ 监控面板  │
        └──────────┘    └──────────┘    └──────────┘
```

---

## 📋 每日完整流程

### 时间线视图

```
时间轴 ═══════════════════════════════════════════════════════════

00:00 ─────────────────────────────────────────────────────────────
       │
03:00  │  ⚙️  (某些后台任务)
       │
       │
08:00 ─┼──────────────────────────────────────────────────────────
       │
       │  📊 数据更新？(可能)
       │
09:00 ─┼──────────────────────────────────────────────────────────
       │
       │  🤖 Agent AI: 早盘分析
       │     ├─ 对手行为分析
       │     ├─ 市场操纵检测
       │     ├─ 博弈预警检查
       │     └─ 制定今日策略
       │
       │     输出: 早盘分析报告 📊
       │           飞书通知 📱
       │
09:25 ─┼──────────────────────────────────────────────────────────
       │
       │  ⚙️  后端: 开盘前扫描
       │     ├─ 扫描20个池
       │     ├─ 使用20个策略
       │     └─ 生成买卖信号
       │
       │     输出: 买入机会 💰
       │           持有信号 📊
       │           飞书通知 📱
       │
09:30 ─┼──────────────────────────────────────────────────────────
       │  (开盘)
       │
       │  🤖 Agent AI: 盘中监控 #1
       │     └─ 快速检查
       │
10:00 ─┼──────────────────────────────────────────────────────────
       │
       │  🤖 Agent AI: 盘中监控 #2
       │
10:30 ─┼──────────────────────────────────────────────────────────
       │
       │  🤖 Agent AI: 盘中监控 #3
       │
11:00 ─┼──────────────────────────────────────────────────────────
       │
       │  🤖 Agent AI: 盘中监控 #4
       │
11:30 ─┼──────────────────────────────────────────────────────────
       │  (午间收盘)
       │
       │  [午休时间]
       │
13:00 ─┼──────────────────────────────────────────────────────────
       │  (下午开盘)
       │
       │  🤖 Agent AI: 盘中监控 #5
       │
13:30 ─┼──────────────────────────────────────────────────────────
       │
       │  🤖 Agent AI: 盘中监控 #6
       │
14:00 ─┼──────────────────────────────────────────────────────────
       │
       │  🤖 Agent AI: 盘中监控 #7
       │
14:30 ─┼──────────────────────────────────────────────────────────
       │
       │  🤖 Agent AI: 盘中监控 #8
       │     (尾盘重点监控)
       │
15:00 ─┼──────────────────────────────────────────────────────────
       │  (收盘)
       │
15:30 ─┼──────────────────────────────────────────────────────────
       │
       │  ⚙️  后端: 数据更新
       │     ├─ 更新K线(190只股票)
       │     ├─ 更新财务数据
       │     └─ 数据质量检查
       │
       │     输出: 数据更新报告 📊
       │
16:00 ─┼──────────────────────────────────────────────────────────
       │
16:30 ─┼──────────────────────────────────────────────────────────
       │
       │  ⚙️  后端: 信号生成
       │     ├─ 全面扫描所有池
       │     ├─ 策略回测验证
       │     └─ 信号评分
       │
       │     输出: 每日信号报告 📈
       │
18:00 ─┼──────────────────────────────────────────────────────────
       │
       │  🤖 Agent AI: 每日复盘
       │     ├─ 分析今日决策
       │     ├─ 成功/失败案例
       │     ├─ 提取经验教训
       │     ├─ 更新知识库
       │     └─ 制定改进计划
       │
       │     输出: 复盘报告 📚
       │           飞书日报 📱
       │
23:59 ─┴──────────────────────────────────────────────────────────
```

---

## 🔄 详细执行流程

### 流程1: 早盘分析 (09:00)

```
Agent-ts (AI决策)
       │
       │ 1. 定时触发
       │    cron: '0 9 * * 1-5'
       │
       ▼
   调度器启动
       │
       │ 2. Agent AI被唤醒
       │    执行 agent_turn
       │
       ▼
   Agent开始工作
       │
       ├─► 调用工具: opponent_behavior
       │   │
       │   └─► quantsys-v2 API
       │       GET /api/game/opponent-behavior
       │       │
       │       └─► 返回: 散户/机构/游资分析
       │
       ├─► 调用工具: manipulation_detect
       │   │
       │   └─► quantsys-v2 API
       │       GET /api/game/manipulation-detect
       │       │
       │       └─► 返回: 操纵信号
       │
       ├─► 调用工具: alert_check
       │   │
       │   └─► quantsys-v2 API
       │       GET /api/alerts
       │       │
       │       └─► 返回: 预警列表
       │
       ├─► 调用工具: pool_battlefield
       │   │
       │   └─► quantsys-v2 API
       │       GET /api/pools/{id}/battlefield
       │       │
       │       └─► 返回: 战场评估
       │
       ▼
   Agent AI思考
       │
       │ 综合所有信息
       │ 制定今日策略
       │
       ▼
   输出报告
       │
       ├─► 早盘分析报告 (文本)
       │
       └─► 调用工具: feishu_notify
           │
           └─► 发送飞书消息到你的手机 📱
```

### 流程2: 开盘前扫描 (09:25)

```
quantsys-v2 (后端调度)
       │
       │ 1. 定时触发
       │    cron: '25 9 * * 1-5'
       │
       ▼
   调度器执行
       │
       ├─► 步骤1: 获取所有池
       │   GET /api/pools
       │   返回: [池21, 池23, 池33, ...]
       │
       ├─► 步骤2: 获取所有策略
       │   GET /api/strategies
       │   返回: [策略430, 策略431, ...]
       │
       ├─► 步骤3: 循环扫描
       │   │
       │   FOR EACH 池 in 池列表:
       │     │
       │     POST /api/pools/{id}/scan
       │     Body: {
       │       strategy_ids: [430, 431, ...],
       │       min_score: 60
       │     }
       │     │
       │     ▼
       │   策略执行引擎
       │     │
       │     FOR EACH 股票 in 池:
       │       │
       │       ├─► 获取K线数据
       │       │   SELECT * FROM quant.daily_klines
       │       │   WHERE symbol = '600809'
       │       │
       │       ├─► FOR EACH 策略:
       │       │   │
       │       │   ├─► 计算技术指标
       │       │   │   (MA, MACD, RSI, etc.)
       │       │   │
       │       │   ├─► 执行策略代码
       │       │   │   (Python/Indicator)
       │       │   │
       │       │   └─► 生成信号
       │       │       buy/hold/sell
       │       │
       │       └─► 信号评分
       │           技术面 + 基本面 + 博弈面
       │
       ▼
   汇总结果
       │
       ├─► 按评分排序
       │
       ├─► 过滤低质量信号
       │
       ▼
   存储到数据库
       │
       INSERT INTO signals (
         symbol, signal_type, score,
         strategy_id, created_at
       )
       │
       ▼
   发送通知 (如有高分信号)
       │
       └─► 飞书消息 📱
```

### 流程3: 盘中监控 (每30分钟)

```
Agent-ts (AI决策)
       │
       │ 定时触发
       │ cron: '*/30 9-14 * * 1-5'
       │
       ▼
   Agent快速检查
       │
       ├─► 检查预警
       │   GET /api/alerts?level=critical
       │   │
       │   └─► 如有critical预警
       │       │
       │       ▼
       │   立即深入分析
       │       ├─► manipulation_detect
       │       ├─► 评估影响
       │       └─► 决策：止盈/止损/持有
       │
       ├─► 监控持仓
       │   GET /api/portfolio/positions
       │   │
       │   └─► 检查盈亏
       │       如触发止损/止盈
       │       │
       │       └─► 生成卖出信号
       │           发送通知 📱
       │
       └─► 快速扫描重点池
           GET /api/pools/21/quick-scan
           │
           └─► 如发现新机会
               └─► 发送通知 📱
```

### 流程4: 数据更新 (15:30)

```
quantsys-v2 (后端调度)
       │
       │ 定时触发
       │ cron: '30 15 * * 1-5'
       │
       ▼
   数据更新任务
       │
       ├─► 步骤1: 获取股票列表
       │   SELECT DISTINCT symbol
       │   FROM quant.daily_klines
       │   UNION
       │   SELECT symbol FROM pools_members
       │
       ├─► 步骤2: 循环下载K线
       │   │
       │   FOR EACH 股票:
       │     │
       │     ├─► 调用akshare
       │     │   ak.stock_zh_a_hist(
       │     │     symbol='600809',
       │     │     period='daily',
       │     │     start_date='2026-06-26'
       │     │   )
       │     │
       │     ├─► 数据清洗
       │     │   - 重命名列
       │     │   - 数据类型转换
       │     │   - 异常值处理
       │     │
       │     └─► 写入数据库
       │         INSERT INTO quant.daily_klines
       │         ON CONFLICT (symbol, trade_date)
       │         DO UPDATE
       │
       ├─► 步骤3: 更新财务数据
       │   (如有新财报)
       │
       ├─► 步骤4: 数据质量检查
       │   - 完整性检查
       │   - 最新日期验证
       │   - 异常值检测
       │
       ▼
   生成报告
       │
       └─► 数据更新日志
```

### 流程5: 信号生成 (16:30)

```
quantsys-v2 (后端调度)
       │
       │ 定时触发
       │ cron: '30 16 * * 1-5'
       │
       ▼
   信号生成任务
       │
       │ (类似开盘前扫描，但更全面)
       │
       ├─► 扫描所有池
       ├─► 使用所有策略
       ├─► 基于最新K线数据
       ├─► 策略回测验证
       └─► 信号评分排序
       │
       ▼
   更新信号状态
       │
       ├─► 标记过期信号
       │   UPDATE signals
       │   SET status = 'expired'
       │   WHERE signal_date < today
       │
       └─► 插入新信号
           INSERT INTO signals ...
```

### 流程6: 每日复盘 (18:00)

```
Agent-ts (AI学习)
       │
       │ 定时触发
       │ cron: '0 18 * * *'
       │
       ▼
   Agent AI复盘
       │
       ├─► 获取今日数据
       │   │
       │   ├─► GET /api/scheduler/runs?date=today
       │   │   (今日任务执行记录)
       │   │
       │   ├─► GET /api/signals?date=today
       │   │   (今日信号)
       │   │
       │   └─► GET /api/trades?date=today
       │       (今日交易，如有)
       │
       ├─► AI分析
       │   │
       │   ├─► 成功案例
       │   │   为什么成功？
       │   │   可复用的模式？
       │   │
       │   └─► 失败案例
       │       为什么失败？
       │       如何避免？
       │
       ├─► 更新知识库
       │   │
       │   └─► 调用工具: knowledge_record
       │       POST /api/knowledge
       │       Body: {
       │         pattern: "institutional_buying",
       │         success_rate: 0.75,
       │         examples: ["688111-20260626"]
       │       }
       │
       ├─► 制定改进计划
       │   - 明日策略调整
       │   - 风控优化
       │   - 仓位管理
       │
       ▼
   生成复盘报告
       │
       └─► 发送飞书日报 📱
```

---

## 🎯 关键数据流

### 数据流向

```
数据源 ────► quantsys-v2 ────► Agent-ts ────► 决策输出
  │              │                  │              │
  │              │                  │              │
akshare      PostgreSQL         AI模型        飞书通知
东方财富      quant schema      100工具        交易信号
新浪财经      daily_klines      博弈分析       分析报告
              strategy_configs
              signals
              knowledge
```

### 控制流向

```
时间触发 ────► 调度器 ────► 任务执行 ────► 结果存储
  │              │              │              │
  │              │              │              │
cron表达式   Scheduler      Agent AI      Database
定时任务      Service       Tools         Notifications
补偿机制      Runtime       APIs
```

---

## ❓ 你认为哪里可能有问题？

### 检查点

**1. Agent启动时是否初始化了调度任务？**
```
启动输出应该有:
🤖 初始化 Agent AI 决策任务
📋 创建 3 个 Agent AI 决策任务...
✅ Agent AI 决策任务初始化完成
```

**2. Agent的调度器是否独立运行？**
```
Agent-ts 可能有自己的调度系统
不依赖 quantsys-v2 的调度器
```

**3. 09:00任务是否真的触发了？**
```
可能:
- 触发了但无输出（无机会）
- 未触发（配置问题）
- 触发了但记录在别处
```

---

**请告诉我：这个流程对吗？哪里可能有问题？**
