/**
 * Memory Distillation Task - Weekly蒸馏任务
 * 设计：docs/superpowers/plans/2026-08-12-execution-tickets.md T2（W1.5b）
 */

/**
 * 周日蒸馏任务 Prompt
 * 调用 backend 蒸馏 API，产出记忆规则候选
 */
export const WEEKLY_MEMORY_DISTILL_PROMPT = `
📚 周记忆蒸馏任务

**目标：从本周经验中提炼可复用的记忆规则**

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
第一步：获取蒸馏输入
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

使用 runQuantV2 工具调用：
- command: 'memory_distill_inputs'
- params: { days: 7 }

这会返回过去 7 天的：
- episodes: 记忆条目（已有的经验记录）
- decisions: 决策记录（交易、分析等决策）

检查返回的数据：
- 有多少条 episodes？
- 有多少条 decisions？
- 如果数据为空，记录"本周无数据可蒸馏"并结束

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
第二步：蒸馏规则候选
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

基于输入数据，提炼可复用的规则。每条候选必须：

1. **有明确的触发条件**（什么情况下适用）
2. **有可操作的行动建议**（该做什么）
3. **有证据支持**（引用输入中的 id）

**规则格式要求**：
- title: 简短标题（≤50字）
- content: 详细规则内容（包含触发条件、行动建议、预期结果）
- evidence_ids: 引用的证据 ID 数组（必须来自输入的 episodes 或 decisions 的 id）

**蒸馏原则**：
- ✅ 提炼模式：多次出现的成功/失败模式
- ✅ 反直觉教训：与常识相悖但有效的经验
- ✅ 失败归因：明确的错误和避免方法
- ❌ 不要：陈述事实（"今天大涨了"）
- ❌ 不要：缺乏证据的推测
- ❌ 不要：过度具体无法复用（"600519今天涨5%"）

**示例候选**：
\`\`\`json
{
  "title": "放量突破前高需确认机构资金流入",
  "content": "触发条件：股价放量突破前期高点\\n行动建议：使用capital_flow确认当日机构资金净流入>5000万，否则警惕假突破\\n预期结果：过滤游资拉高出货陷阱，提高突破交易胜率",
  "evidence_ids": [15, 23, 31]
}
\`\`\`

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
第三步：提交候选
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

使用 runQuantV2 工具调用：
- command: 'memory_distill_candidates'
- params: { candidates: [ ...你蒸馏出的候选数组... ] }

**注意**：
- 如果没有蒸馏出任何高质量候选，传空数组 []
- 无证据的候选会被后端自动跳过

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
第四步：汇报结果
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

使用 decision_record 记录本轮蒸馏结果：
- 产出了 N 条候选
- 跳过了 M 条无证据条目（如有）
- 关键规则简述（前3条候选的标题）

**重要**：
这些候选会进入 testing 状态，后续由进化系统评估。
高适应度的规则会被提升为 active 并影响你的未来决策。

现在开始蒸馏！
`;
