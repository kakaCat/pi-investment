# 策略清理台账 2026-09-13（w-a9ec14d7 / investor / 投资脑）

**动作**：删除 75 条无用策略（`quant.strategy_configs` 89 → 14）。全部走应用层 `POST /api/strategies/delete/{id}`，**75 成功 / 0 失败**；抽验已删 id 返回 404；API `strategy_list` 可见数 35 → 13。

**可逆性**：删除前做过全量行级备份（含 code_content）：`strategy_configs_20260913.json`（89 行 / 217KB）。恢复＝把备份行插回 `quant.strategy_configs`（id 已释放）。

## 判废口径（证据支持才判死）

| 类别 | 条数 | 依据 |
|---|---|---|
| ① 已停用 is_active=false | 53 | 已不在服役；且经查**无任何活引用**（30 个定时任务 0 引用、`strategy_registry.json` 无 active 项、`strategy_signals`/`strategy_backtest`/`strategy_execution_log`/`strategy_performance` 四表 **0 行**） |
| ② 启用但 validation_status=invalid | 18 | 平台自身验证判无效；与 R-011「invalid 输出不可信」一致 |
| ③ 启用且 valid 但体检证伪 | 4 | 本文体检器（T+1、次日开盘成交、含成本）OOS CAGR 为负且交易 ≥100 笔 |

## 关键发现：valid ≠ 好用

`validation_status=valid` 是**结构有效**（能跑通、参数合法），**不是**"有超额收益"。实测 14 条 active+valid 策略在各自默认标的上：

OOS CAGR 区间 **−3.32% ~ +2.65%**（中位数约 −1%），OOS Sharpe −1.21 ~ +0.86；
同期同池等权基准 CAGR **+23.3%**（2024-07~2026-09，800 只 2024H1 定义的最活跃池）

⇒ **没有一条**单标的择时策略跑赢基准。这正是 635/637/648 显示 "valid 却 OOS 为负" 的原因 —— 与之前记录的 "validation_status 语义是结构而非性能" 完全吻合。

## 未删的 14 条（为什么留下）

- **162 / 164（active+invalid）、163（inactive+invalid）**：被 2026-05-31 的 `quant.strategy_stock_matching`（800 行、含 ret_162/163/164 固定列）引用；该表已陈旧 3.5 个月，未确认对应页面是否下线 → 保留待确认。
- **202（active+invalid）**：v13 线账户的策略，按 R-019 不归投资脑，不代为判死。
- **245 / 266 / 267 / 268 / 269 / 365 / 416 / 418 / 429 / 637**：体检为负或微正，但**交易数 <100 笔（5~64 笔）样本不足**，按 R-016 不判死 —— 留待样本补齐。相对最好的是 429（OOS +2.65%、SR 0.86、61 笔）、416（+1.73%、SR 0.49、85 笔）、269（+0.55%、SR 0.80、14 笔），但仍**远低于基准**。

## 被删清单（75 条）

- `178` value-macd-cross-v1（inactive，停用且无活引用）
- `179` 多因子波段策略v9 日线版（inactive，停用且无活引用）
- `183` RegimeAdaptive-v1 自适应猎手（inactive，停用且无活引用）
- `193` VBottom-v2（inactive，停用且无活引用）
- `270` 均值回归策略 v1.0（inactive，停用且无活引用）
- `271` 激进动量策略 v1.0（inactive，停用且无活引用）
- `272` 新能源动量策略 v1.0（inactive，停用且无活引用）
- `273` 宽松动量策略 v1.0（inactive，停用且无活引用）
- `278` 半导体均值回归 v1.0（inactive，停用且无活引用）
- `279` 半导体波段猎手 v1.0（inactive，停用且无活引用）
- `364` 均值回归波段 v1.0（inactive，停用且无活引用）
- `366` 放量突破 v1.0（inactive，停用且无活引用）
- `367` 多因子综合评分 v1.0（inactive，停用且无活引用）
- `368` 牧原股份-超卖反弹策略 v1.5（inactive，停用且无活引用）
- `376` 万华化学-均值回归 v6.0（inactive，停用且无活引用）
- `377` 扬农化工-均值回归 v6.0（inactive，停用且无活引用）
- `409` 牧原股份-猪周期趋势双模策略 v3.0（inactive，停用且无活引用）
- `410` 牧原股份-熊市短线策略 v1.0（inactive，停用且无活引用）
- `411` 牧原股份-猪周期宽幅交易 v1.0（inactive，停用且无活引用）
- `412` 牧原股份-熊市猎手 v2.0（inactive，停用且无活引用）
- `413` 牧原股份-多因子均值回归策略 v1.0（inactive，停用且无活引用）
- `414` 牧原MR-ADX15策略 v1.0（inactive，停用且无活引用）
- `415` 牧原双轨策略 v1.0（inactive，停用且无活引用）
- `419` 真均值回归 v1.0（inactive，停用且无活引用）
- `421` 趋势回调买入 v1.5（inactive，停用且无活引用）
- `423` 趋势回调买入 v2.0（inactive，停用且无活引用）
- `424` DISCOVERY-RSI均值回归-002714.SZ（inactive，停用且无活引用）
- `425` DISCOVERY-MA金叉死叉-002714.SZ（inactive，停用且无活引用）
- `426` DISCOVERY-布林带回归-002714.SZ（inactive，停用且无活引用）
- `427` DISCOVERY-MACD信号-002714.SZ（inactive，停用且无活引用）
- `428` DISCOVERY-量价突破-002714.SZ（inactive，停用且无活引用）
- `430` DISCOVERY-RSI均值回归-600519.SH（inactive，停用且无活引用）
- `431` DISCOVERY-RSI均值回归-000858.SZ（inactive，停用且无活引用）
- `432` DISCOVERY-MA金叉死叉-600519.SH（inactive，停用且无活引用）
- `433` DISCOVERY-MACD信号-600519.SH（inactive，停用且无活引用）
- `436` DISCOVERY-MA金叉死叉-000858.SZ（inactive，停用且无活引用）
- `437` DISCOVERY-MACD信号-000858.SZ（inactive，停用且无活引用）
- `439` DISCOVERY-布林带回归-600519.SH（inactive，停用且无活引用）
- `440` DISCOVERY-布林带回归-000858.SZ（inactive，停用且无活引用）
- `442` DISCOVERY-量价突破-600519.SH（inactive，停用且无活引用）
- `443` DISCOVERY-量价突破-000858.SZ（inactive，停用且无活引用）
- `447` ZigZag波段交易 v1.0（inactive，停用且无活引用）
- `450` ZigZag猎手 v1.0（inactive，停用且无活引用）
- `452` 波段猎手 SwingHunter v1（inactive，停用且无活引用）
- `453` 网格猎手 GridHunter v1（inactive，停用且无活引用）
- `454` 网格猎手 GridHunter v2（inactive，停用且无活引用）
- `482` 恐慌抄底策略（inactive，停用且无活引用）
- `484` 恐慌抄底v1（inactive，停用且无活引用）
- `485` 恐慌抄底v2（inactive，停用且无活引用）
- `486` 恐慌抄底v3（inactive，停用且无活引用）
- `640` macd-market-filter-v1（inactive，停用且无活引用）
- `641` macd-index-filter-v1（inactive，停用且无活引用）
- `642` macd-trailing-stop-v1（inactive，停用且无活引用）
- `166` 激进突破动量v1（active，启用但平台判 invalid）
- `171` 强势突破动量v2（active，启用但平台判 invalid）
- `172` 15分钟高频突破v4（active，启用但平台判 invalid）
- `177` breakout-pullback-v1（active，启用但平台判 invalid）
- `180` MA交叉动量 v1（active，启用但平台判 invalid）
- `181` trend-atr-v1（active，启用但平台判 invalid）
- `182` RangeHunter-v1 震荡猎手（active，启用但平台判 invalid）
- `191` SwingTrader-v8（active，启用但平台判 invalid）
- `203` GridPro-v1 自适应网格（active，启用但平台判 invalid）
- `204` GridPro-v2 极简网格（active，启用但平台判 invalid）
- `636` bollinger-breakout-v1（active，启用但平台判 invalid）
- `638` dual-ma-cross-v1（active，启用但平台判 invalid）
- `639` momentum-breakout-v1（active，启用但平台判 invalid）
- `643` macd-golden-cross-v2-market-filter（active，启用但平台判 invalid）
- `645` rsi-oversold-v2-market-filter（active，启用但平台判 invalid）
- `646` momentum-breakout-v2-market-filter（active，启用但平台判 invalid）
- `647` macd-golden-cross-v2b-ma20（active，启用但平台判 invalid）
- `649` macd-rsi-ensemble-and-v1（active，启用但平台判 invalid）
- `417` 全市场趋势跟踪 v1.0（active+valid，但体检证伪：OOS -2.98% / 138 笔）
- `420` 深度超卖反弹 v1.0（active+valid，但体检证伪：OOS -3.32% / 239 笔）
- `635` macd-golden-cross-v1（active+valid，但体检证伪：OOS -2.14% / 198 笔）
- `648` macd-rsi-ensemble-v1（active+valid，但体检证伪：OOS -0.80% / 200 笔）
## 追加（2026-09-13 15:4x，用户二次指示）

- **第二批删除 13 条**：162/163/164（其"页面"= `quant.strategy_stock_matching`，全仓**零代码引用**已确认下线）+
  245/266/267/268/269/365/416/418/429/637（样本 5~64 笔，按 R-016 本不判死，经用户明确指示清理）。
  **策略库现状：仅剩 202（v13 线账户的策略，R-019 不归投资脑）**。
- **第二批的体检证据（strategy_lab.py OOS，T+1/次日开盘/含成本）**：
  245 −2.16%｜266 +0.86%｜267 −1.94%｜268 +0.49%｜269 +0.55%｜365 −1.52%｜416 +1.73%｜418 −0.06%｜429 +2.65%｜637 −1.30%
  —— 全部远低于同池等权基准 +23.3%。
- **结论（写给未来的自己）**：本策略库**没有任何一条**单标的择时策略具备相对基准的超额。
  不要再从库里翻策略当信号源；投资脑的方向是 `core_plan.py` 那条路（等权分散 + 波动目标 + 回撤闸门，超额来自风控而非选股）。EOF
echo appended
