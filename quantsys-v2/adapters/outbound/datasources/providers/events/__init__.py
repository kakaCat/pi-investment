"""事件源出站 provider（RFC 015 §3.3 多源矩阵）

通道矩阵（§1.5.2 硬约束 1：同一数据类型的 provider 必须来自**不同上游通道**）：

个股事件（4 条通道，全部 2026-09-11 真实打样通过）：
| 优先级 | Provider (name)   | 上游通道                          | 覆盖类型                    | 打样结果 |
|--------|-------------------|-----------------------------------|-----------------------------|----------|
| 1      | eastmoney_notice  | 东财 np-anotice-stock 公告流      | 全部公司公告（聚合转载，60）| HTTP 200，600150 返回 20 条 |
| 2      | cninfo_disclosure | 巨潮资讯网 hisAnnouncement        | 法定披露（**权威 90**）     | HTTP 200，600150 返回 15 条 |
| 3      | akshare_unlock    | 东财数据中心解禁（akshare 封装）  | 解禁（结构化规模/占比，50） | 全市场窗口 37+ 行 |
| 4      | database_event    | quant.event_calendar              | 上次成功入库（**stale**，20）| 读库 OK |

政策事件（2 自动通道 + 1 人工兜底）：
| 优先级 | Provider (name)   | 上游通道                          | 打样结果 |
|--------|-------------------|-----------------------------------|----------|
| 1      | gov_policy        | gov.cn 最新政策 JSON + 发改委列表 | 24 条（2026-09-11 实测） |
| 2      | csrc_policy       | 证监会 csrc.gov.cn 政策解读       | 6 条（最新 2026-05-22，低频栏目） |
| 3      | manual_policy_seed| 人工策展（带 operator 标记）      | 3 条兜底，永远可用 |

**注册顺序即优先级**（manager 的 _try_providers 按列表顺序故障转移；
database_event 必须放最后——它是 stale 兜底，放前面会把旧数据当新鲜数据返回）。
各 provider 的打样证据与失败通道记录见各自模块 docstring。
"""
